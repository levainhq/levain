# levain — DIOGENES 2026-09-04 TRIAGE

**Generated across the 2026-09-04 session, by the levain seat, not by the reviewer.**

> ⚠ **NO HEAD SHA, DELIBERATELY** — see the same note at the top of `TRIAGE_2026-09-03.md`. A sha
> stamped here goes stale on the next commit to this file and is then precise enough to be trusted
> and wrong. Where a verdict needed a commit, the commit is named *in that verdict*.

> ⛔ **ONE BACKLOG IN THIS FILE, AND IT IS THE `STATUS` COLUMN BELOW.** Yesterday's triage published
> two — `ALL 22 CLOSED` and, twenty-nine lines later, `STILL OPEN (18)` listing the same findings,
> with the stale half LAST. That was tonight's HIGH. This file has exactly one table and no appendix
> that restates it.

## ▶ THE FINDING THE DISPATCH LED WITH IS NOT IN THE 0.4.4 CUT

**Measured, not read off the report:**

```
git show main:levain/daemon.py             | grep -c 'class SystemdUserProvider'   -> 0
git show main:levain/firing/confinement.py | grep -c '_bwrap_argv'                 -> 0
```

`main` carries **no systemd provider and no bwrap floor**. Both are K4c, which is the six unpushed
commits on `k4c-linux`. So the twelve findings split **by branch**, which nothing in the report or
the dispatch says — and five of them cannot be fixed on the frozen branch at all.

**Consequence for the day's ruled order:** "Diogenes findings → socket fix → cut" reads as though the
findings gate the tag. Six of the seven that actually gate it are markdown; one is a comment. The
frightening HIGH is real and is **not standing in front of the release**.

## THE TWELVE

| # | sev | subject | branch | status |
|---|-----|---------|--------|--------|
| 1 | HIGH | `TRIAGE_2026-09-03.md` two contradictory backlogs | main | ✅ CLOSED `f4fac94` |
| 2 | MED | `TRIAGE_2026-09-03.md` masthead sha 20 commits behind | main | ✅ CLOSED `f4fac94` |
| 3 | LOW | `TRIAGE_2026-09-03.md` doubled heading | main | ✅ CLOSED `f4fac94` |
| 4 | LOW | `TRIAGE_2026-09-03.md` denominator 36 vs 40 | main | ✅ CLOSED `f4fac94` — stated as UNRECONCILED, not bridged |
| 5 | LOW | `test_compat_integration.py` stale 0.9.6 cap comment | main | ✅ CLOSED `f439fda` |
| 6 | MED | `web_server.py` hardening duplicated into 3 copies | main | ⏸ **FILED FORWARD — see below** |
| 7 | MED | `scripts/install_hooks.sh` untracked | main | ⚖ **NOT A DEFECT — a HELD decision. See below.** |
| 8 | HIGH | `daemon.py:739` systemd ExecStart unit injection | k4c-linux | ✅ CLOSED `b16812e` |
| 9 | MED | `daemon.py:863` install rollback leaves an orphan timer | k4c-linux | ✅ CLOSED `f9977b0` |
| 10 | MED | `confinement.py:1791` deny_files under ~/.ssh gets no bwrap mount | k4c-linux | ✅ CLOSED `29eb476` |
| 11 | MED | `CHANGELOG.md:18` Ubuntu paragraph is the pre-correction draft | k4c-linux | ✅ CLOSED `ee7c6d3` |
| 12 | LOW | `daemon.py:1001` "macOS ships first" in a live raise | k4c-linux | ✅ CLOSED `ee7c6d3` |

**Plus one the reviewer did not file, found by running the gates:** `main` **does not pass ruff.**
`F401` in `doctor.py` — `_sha256_file` imported but unused, dead since `d7cee34`, which deleted its
only call site. The project's own record asserts "ruff + mypy clean" of the release; that was false
for the artifact as frozen. ✅ CLOSED `30126cf`, together with the twelve-line comment that still
explained why the dead import existed.

### ⚖ #7 IS NOT AN OVERSIGHT, AND THE FINDING'S PREMISE IS WRONG

Diogenes' *mechanism* is right — `core.hooksPath` points at a directory git does not carry, so the
gate protects exactly one clone. His *premise*, that this is an omission to be closed with
`git add`, is not: `projects/levain/next.md` carries a block headed **"⚖ AWAITING PHILL — A NINTH
GATE, BUILT AND ACTIVE BUT DELIBERATELY UNCOMMITTED"**, which says in terms *do not `git add` them
without reading this*. It is a held decision awaiting Phill. **Routed to the fan-in as an agenda
item; not acted on.**

### ⏸ #6 IS FILED FORWARD ON PURPOSE

The diagnosis is correct and the remedy — hoist the shared request preamble into one module both
servers import — is right. It is also a **refactor of two HTTP request paths inside a frozen release
window**, which is the same-day-code-shipped-same-day shape the freeze exists to prevent. The
duplication is stable and behaviourally correct today (the reviewer verified both servers behave
identically); the class is armed, not firing. **It lands after the tag, not before it.**

## ⛔ THE SOCKET FIX CORRECTED THE RULING'S MECHANISM

`spore-725` is closed on `main` at `1d7f707`, and **the fix specified in the spore does not work.**
"Add the sockets to the floor denylist" means a `deny_files` entry, and a seatbelt
`file-read* file-write*` rule does not block `connect()` to a unix socket — measured, with both
docker spellings in `deny_files` and the attack still returning the jewel. The working fix is three
denies, not one; the full measurement and the spelling rule (RESOLVED for the connect arm, BOTH for
the write arm) are in `spore-725`'s own notes and in the commit.

**macOS only, because 0.4.4 is macOS only.** No Linux host was reachable from this session, so
nothing about `bwrap` has been run and nothing about it is claimed.

## ⚠ THE COVERAGE GAP IS THE SUBJECT OF THE WINDOW

Diogenes has **no Linux host**. All 8 `linux_live` tests and both `systemd-analyze verify` tests
self-skipped, so findings 8–12 are reasoned from rendered output and from driving the pure
functions — never from a live namespace or a loaded unit. The decisive question he names and does
not answer: **does systemd 255 refuse the injected unit at load, or run the truncated `ExecStart`?**
Both are bad and one is silent. That question is not answerable by reading the renderer, and it is
the highest-value thing available on the K4c branch.

## ⚠ A RECORD CORRECTION ABOUT THE SUITE

The project record says the two live-model condenser flakes "both pass in isolation". Measured today:
`test_reanchor_reaches_model_on_recovery_turn` fails **in isolation** roughly one run in three
(2 pass / 1 fail), and passes at clean HEAD too — so it is a genuine non-deterministic flake, not a
regression, and *not* what the record claims. A gauge that is wrong one time in three, described as
reliable, is how a real regression gets waved through as "the known flake".

---

## ⚡ FINDING #8's UNDETERMINED QUESTION IS ANSWERED BY EXECUTION (2026-09-04)

Diogenes: *"NOT DETERMINED, and I am not asserting it: whether systemd 255 refuses the whole unit at
load or ignores the orphan line and runs the truncated ExecStart. Both outcomes are bad and one is
silent."* Run against **real systemd 255** (`k4c-systemd:latest`, `systemd 255 (255.4-1ubuntu8.17)`),
on a unit rendered by the **real `build_seat_spec` + `render_unit`** from `k4c-linux` at `0e42e18`:

```
/etc/systemd/system/…service:7: Unbalanced quoting, ignoring: "… --task 'review the diff"
…service: Unit configuration has fatal error, unit will not be started.
Unit …service has a bad unit file setting.        exit=1
```

**systemd REFUSES the service.** It does NOT run a truncated `ExecStart`, so an unattended agent does
NOT run stripped of `--unattended` / `--max-seconds` / `--consolidate`. Three task shapes chosen to
try to produce a *balanced* first line — plain multi-line, a quote before the newline, a leading
newline — all three fatal, exit 1. Structural reason: `shlex.quote` closes its quote at the END of
the argument and the governance flags follow it, so every line but the last sits inside an open
quote.

### ⛔ BUT THE SEVERITY COMES BACK BY A DIFFERENT DOOR, AND THIS HALF IS THE REAL ONE

`install()` enables **`<label>.timer`** for a periodic seat, never the service. Measured, same host,
same units, exit codes captured without a pipe in the way:

| verified | exit | output |
|---|---|---|
| `…seat.service` | **1** | fatal error · bad unit file setting |
| `…seat.timer`   | **0** | *prints the service's fatal error and exits 0 anyway* |

**The timer is valid; the service it points at is not; verifying the timer sees the fatal error,
prints it, and succeeds.** So a periodic `install-seat` — which is the whole point of a scheduled
seat — has nothing in its path that fails: the timer enables, install reports success, and the seat
dies at its first fire with the failure visible only in the journal.
`absence_of_signal_rendered_as_health`, and the check's subject (the timer) is not the defect's
subject (the service), which is tonight's class yet again.

↳ And the error string in `install()`'s non-rollback branch — *"the unit is KEPT at {service_path}
(it is valid)"* — is **false in exactly this case**. The unit is not valid.

⚠ **NOT EXECUTED, AND I AM NOT ASSERTING IT:** I did not run `systemctl --user enable --now` against
a live init — that needs a real host, not a container. The exit-code asymmetry above is measured; the
inference from it to "install reports success" is REASONED. It is the next thing to run on argushub.

▶ **THE FIX IS UNCHANGED AND ONE IS ADDED:** run the control-character refusal over every value
reaching a directive, not just `Environment=`; and have `install-seat` verify the **SERVICE**, not
only enable the timer.

## ⛔ THE LINUX TEST PATH EXISTS ON THIS MACHINE AND IS NOT IN THE REPO

Diogenes reported *"I HAVE NO LINUX HOST"* and self-skipped all 8 `linux_live` tests and both
`systemd-analyze verify` tests. He was right about himself and the capability was one `docker run`
away: `docker images` shows **k4c-systemd · k4c-sysdtest · k4c-bwrap · k4c-live · k4c-pytest**, built
16–17 hours ago for this exact work, and `k4c-systemd` carries systemd 255.

**None of it is committed.** No Dockerfile, no compose file, no make target, and no doc mentions
them. So the systemd tests self-skip for every reviewer, every fresh clone and CI, and the capability
dies with the images on this laptop.

⚡ **SAME SHAPE AS THE UNTRACKED PRE-PUSH HOOK — a capability that serves exactly one clone because
it was never committed — TWICE IN THIS REPO IN ONE DAY, found by two different routes.**

⚠ Honest bound: only the **systemd** half is recoverable this way. `bwrap 0.9.0` in `k4c-bwrap`
refuses — *"No permissions to create new namespace"* — as an unprivileged container user, so the 8
`linux_live` tests genuinely need a real host (argushub). No bwrap result is claimed here and none
was run.

---

## ✅ THE REAL-INSTALL `doctor` GATE IS RUN, AND IT FOUND A FOURTH SPELLING (2026-09-04)

Cut procedure step 3 — *"RE-RUN THE REAL-INSTALL `doctor` CHECK ON THE BUILT WHEEL, BOTH ADAPTERS …
Falsify against a control: tilde-wire the hooks and confirm doctor FAILS."* Run against
`levain-0.4.4.dev0-py3-none-any.whl` built with `uv build`, installed into a fresh venv under a
fully isolated `$HOME`, package importing from site-packages and not the source tree.

| case | doctor |
|---|---|
| `claude-code` real install | **exit 0** |
| `codex` real install | **exit 0** |
| control: `"python3" "~/lev/…/session_start.py"` at user level | **exit 1**, wiring reported ✅ |
| control: `"python3" "$HOME/lev/…/session_start.py"` | **exit 1**, wiring reported ✅ |
| control: `"python3" "<absolute>/…/session_start.py"` | **exit 1**, wiring reported ✅ |
| no user-level file | **exit 0** ✅ |

**The 0.4.4 dark-install fix holds and is falsified, not merely green.**

### ⛔ BUT THERE IS A FOURTH WAY TO WRITE IT, AND IT IS STILL DARK

`_user_entry_targets` iterates `tokens[1:]` — it skips the first token as the interpreter, which is
correct for the shape `levain init` writes (`"<python>" "<script>"`). A **single-token** command —
the bare hook path, with no interpreter — therefore has *no* tokens examined at all:

```
{"command": "~/lev/activation/hooks/session_start.py"}     ->  doctor exit=0, nothing reported
```

The CHANGELOG for this release says *"doctor reported green on two of the three ways to write
user-level wiring."* There is a fourth, and it is the same class one level up: the fix enumerated
spellings of the **path** and left the **command shape** unexamined.
`guard_scoped_by_symptom_misses_the_class`.

⚠ **AND IT IS A DARK INSTALL OF A DIFFERENT FLAVOUR, WHICH IS WHY IT IS WORTH WRITING DOWN
CAREFULLY.** The installed hooks carry a shebang (`#!/usr/bin/env python3`) but ship **`-rw-r--r--`,
not executable** — so a bare-path wiring does not silently run from the wrong scope, it does not run
**at all**. The operator gets an activation layer that is entirely off, and `doctor` says green. That
is the same failure the whole dark-install line of work exists to end, arriving by the one door the
fix did not enumerate.

### ⏸ NOT FIXED BEFORE THE TAG, DELIBERATELY

It is a NEW finding, not a regression: nothing that worked has stopped working. And this detector has
now turned `doctor` red for an entire operator class **three times** — the code says so in its own
docstring — so editing it inside a frozen release window, to cover a wiring that is itself
non-functional, is the trade the freeze exists to refuse. **It lands after the tag.**

↳ Second-order, and cheaper: the hooks carry a shebang while not being executable. Either the
shebang is decorative or the mode is wrong; the two disagree, and it is that disagreement which makes
the bare-path wiring look reasonable to an operator in the first place.

---

## ⛔ THE COMPAT GATE AUDITS ONE OF THE TWO ANNEALS A SINGLE INSTALL WIRES (2026-09-04)

Surfaced by running `doctor --invoke` against the real wheel install, not by reading anything.

A `levain init` install wires anneal **twice, by two different resolution rules**:

| surface | how it resolves | what it got here |
|---|---|---|
| `.mcp.json` → the memory server the harness talks to | the `anneal-memory` executable **on PATH** | `~/.local/bin/anneal-memory` — **0.9.10.dev0** |
| `.claude/settings.json` hooks → per-turn recall + capture | the **interpreter levain runs under**, importing `anneal_memory` | the venv — **0.9.9** |

One install, one store, **two anneal versions operating on it**. And `doctor`'s compat block reports
only the first: `[OK] anneal-memory CLI: /Users/…/.local/bin/anneal-memory (anneal-memory
0.9.10.dev0)`, with every downstream verdict — the AHEAD advisory, the migrate-proposal count, the
pip-floor check — computed about **the PATH binary**, and nothing at all said about the version the
hooks import.

**The check's subject is the MCP server; the install's behaviour is produced by both.** Same shape as
the `$LEVAIN_DRIVE_MODE` split-brain this codebase already fixed once, where the file editor and the
bash seatbelt would have resolved the floor from different values — *"one policy, two enforcers,
disagreeing."*

⚠ **HONEST BOUND — I MADE THIS VISIBLE AND I DID NOT MANUFACTURE IT.** My isolation was `$HOME`-only,
not `PATH`-only, so `init` found the operator's global `anneal-memory` while levain lived in a fresh
venv. That is not an exotic setup: **pipx/venv levain plus a globally installed anneal is the
ordinary way both of these get installed**, and any version skew between them is invisible to
`doctor` on the hook side. The bound worth stating is that I have not reproduced it on a machine with
only one anneal, because there the two resolve to the same thing and nothing is wrong.

⏸ **FILED FORWARD, NOT FIXED.** Pre-existing, not in the 0.4.4 diff, not a regression, and it needs a
decision rather than a patch — either `init` should wire the MCP command to the interpreter levain
runs under (making one anneal authoritative), or `doctor` should report BOTH and flag a skew. Picking
between those inside a frozen release window is exactly the trade the freeze refuses.

---

## ✅ THE `allow_container_sockets` OPT-OUT IS VERIFIED LIVE, BOTH ARMS (2026-09-04)

A gap in my own L4: the DENY was proven end-to-end and the OPT-OUT was not. An opt-out that
half-lifts the floor — restoring the connect but leaving the socket write-denied — would hand the
operator who needs containers a socket they cannot manage.

| | |
|---|---|
| jewel still denied with the opt-out ON | **DENIED** — the rest of the floor is unaffected |
| docker reachable again | **29.5.2** — connect arm lifted |
| socket renameable again | **yes** — write arm lifted |

⛔ **AND IT COST A ~40-SECOND DOCKER OUTAGE ON THE OPERATOR'S MACHINE, WHICH IS THE LESSON.** The
third arm is `mv <the real socket>`, and with the guard off it succeeds. Earlier the same day I had
deliberately stood up my *own* unix-socket server to test the rename arm without touching the
operator's daemon, and wrote that reasoning down — then pointed the identical command at the live
socket when testing the mirror case. Restored immediately (`mv` back; original inode timestamp
intact, `/var/run` symlink unaffected, `docker version` 29.5.2).

**The precaution was taken, recorded, and not carried across to the symmetric test** — a guard scoped
to the case where the symptom first appeared. ▶ Standing rule for this repo: **testing a security
opt-out means running the attack with the guard OFF, and that attack is destructive by construction
— it needs a disposable target EVERY time, not just the first time.**

---

## ⛔ 0.4.4 IS CUT-READY AND HELD FOR ONE SEAT — CODEX QUOTA EXHAUSTED 2026-09-04 ~10:05 AM

`deep_review.py --diff 1e44342 --seats complement,codex,glm` ran, then a codex-only re-run at a
larger budget. Both times codex returned nothing; the second said why:

```
[codex no output after 898s (rc=1): "You've hit your usage limit … try again at 2:15 PM."
 tokens used 189,479]
⚠ codex PRODUCED NO REVIEW — this seat contributed NOTHING to the pass above.
```

**This is `spore-744`'s measurement, on the day it was ruled.** Phill asked to run without a governor
precisely to learn IF and WHEN the limit lands. It lands on a nine-repo parallel day at ~10:05 AM,
and this repo's two runs were a large share of the spend. Both were correct under that ruling; the
cost is recorded, not apologised for.

⚠ **AN EXHAUSTED CODEX LOOKS LIKE A COMPLETED RUN** — `deep_review.py` exits 0 and the failure is a
line inside the body. A seat that reads the exit code and not the output records a review that never
happened. The script defends against this correctly: it refuses to record coverage and says *"read it
as a gap."*

### WHAT DID RUN, AND WHAT IT FOUND

| seat | verdict |
|---|---|
| complement | 1 HIGH · 1 MEDIUM · 1 LOW — triaged below |
| glm (glm-5.2, repo-read, 1 file opened) | no HIGH; 1 LOW — accepted and fixed further than proposed |
| **codex** | **NOTHING — quota. This pass is breadth-only.** |

· **HIGH refuted as filed, kept as substance.** complement said `_bwrap_argv` never consumes
  `deny_sockets`. False here: `grep -c _bwrap_argv` is **0** on main and the provider roster is
  exactly `{SeatbeltProvider}` — it inferred a Linux enforcer from the Diogenes report it was handed.
  The danger it names arrives at the **K4c merge**, so it became
  `test_every_confinement_provider_must_consume_deny_sockets`, mutation-verified.
· **MEDIUM refuted two ways.** A general `network-outbound` allow after the deny would shadow it —
  but `(allow default)` is line 6, the deny is line 22, the only later allows are `file-read*`, and
  the real docker connect is already refused end-to-end.
· **LOW fixed** — the live test's `accept()`/`close()` race.
· **glm LOW confirmed on disk** (`deny_sockets ⊆ deny_write_files` is True) and fixed further: glm
  proposed leaving a `.sock` NAME test as the discriminator; the message is now decided by
  MEMBERSHIP, in the module whose entire ssh design exists because name-based reasoning is what it
  refuses.

### ▶ THE HOLD, AND IT IS A RULING NOT A PREFERENCE

**No tag before codex runs.** `spore-725`'s own note requires targeted L3 before the tag and never
shipping the fix unreviewed; codex is the non-replaceable seat for code and state-machine work; and
this is a **crown-jewels confinement change** — the single worst thing in this repo to ship on a
breadth-only pass. Levain carries **NO CLOCK** by standing ruling, so four hours is not a trade worth
making.

**Everything else is done:** 2466 passed / 1 skipped · ruff clean on `levain/` · mypy clean but for
the 4 known template false-positives · the real-install `doctor` gate run on the built wheel for both
adapters and falsified against three controls · `## [Unreleased]` written and reconciled against
`git log v0.4.3..HEAD -- levain/`.

⚖ **The tag was always Phill's call, not the seat's.** He can lift this hold in a sentence.

---

## ⛔ A RELEASE GATE ANSWERS DIFFERENTLY DEPENDING ON WHERE YOU RUN IT (2026-09-04)

`pip_floor_verdict()` is the release gate that *"catches the manifest constant and the `pyproject`
pin drifting apart."* Measured from two checkouts of **byte-identical source** — `git diff main
k4c-linux` on `pyproject.toml`, `levain/manifest.py` and `tests/test_manifest.py` is **empty**:

| run from | `pip_floor()` | verdict |
|---|---|---|
| `~/Briefcase/levain` | **0.9.8** | `in_sync` ✅ |
| a `git worktree` of the same repo | **0.9.7** | `drift` ❌ |

**The gate compares a SOURCE constant (`KNOWN_GOOD_ANNEAL`) against a BUILD ARTIFACT.** `pip_floor()`
resolves through `importlib.metadata`, so its answer depends on `sys.path` and on which
`*.egg-info` / `*.dist-info` happens to be found — the main checkout carries a fresh
`levain.egg-info` (`anneal-memory<0.10,>=0.9.8`, matching `pyproject.toml`); the worktree has none,
because egg-info is untracked, and resolves to something stale.

⚡ **AND THE FALSE-GREEN DIRECTION IS THE ONE THAT MATTERS, ON THE EXACT OPERATION THE GATE GUARDS.**
Cut procedure **step 7** bumps `KNOWN_GOOD_ANNEAL` *and* the pip floor to 0.9.9 in lockstep. If the
`pyproject` floor moves and the metadata is not regenerated, the gate compares the constant against
the **old** floor and can report `in_sync` while the real pin disagrees. Its own docstring says it
reads *"this levain's declared dependencies"* and *"for a real pip wheel this is exactly what pip
will resolve against"* — true for a wheel, **false for the source checkout the gate actually runs
in**, which is tonight's class stated in the docstring itself.

⏸ **FILED FORWARD — NOT FIXED IN THE FROZEN WINDOW.** Pre-existing, not a regression, and the fix is
a design decision about a release gate (read `pyproject.toml` as the authority, or compare both and
fail on disagreement) rather than a patch. ⛔ **BUT THE NEXT SESSION MUST READ THIS BEFORE DOING STEP
7**, because that is precisely when the gate can lie.

### ⚡ SHARPER THAN THAT: THE FILE WROTE DOWN THE HAZARD AND THEN BUILT A GATE ON IT

Raised by `0904+3` via the fan-in and **verified verbatim in source here before recording it**.
`manifest.py`'s `declared_set()` docstring says:

> ``levain`` is the SOURCE ``__version__`` (not ``importlib.metadata`` — an editable/source install's
> dist metadata can lag the moved source; **the constant is the truth**).

Three hundred lines later `pip_floor()` reads `importlib.metadata` — **the mechanism this file
explicitly rejected** — and compares its answer against `KNOWN_GOOD_ANNEAL`, **that very constant.**
The file states the exact hazard, applies the correct rule in one function, and builds a release gate
on the mechanism it had just refused. `.gitignore:2` is `*.egg-info/`, which is why a worktree
diverges from the primary checkout at all.

↳ It surfaced only because the work happened in a **worktree** rather than the primary checkout —
the same shape as the day's other findings, where a different vantage point saw what the usual one
structurally could not. A gate that is green in the place you always run it is not thereby green.


---

## ✅ ALL TWELVE ARE CLOSED OR RULED — THE FIVE K4c ONES TOO (2026-09-04, during the codex hold)

The tag is held for codex until 2:15 PM, so step 4 (K4c) was worked instead. It is a different
branch and touches nothing in the release.

**`k4c-linux`, five commits** — `7387eec` `b16812e` `ee7c6d3` `f9977b0` `29eb476`:

| finding | closed by | how it was verified |
|---|---|---|
| **HIGH** systemd ExecStart injection | `b16812e` | 78 passed on **real systemd 255**; mutation-verified |
| **MED** install rollback orphan timer | `f9977b0` | unit-tested; ⚠ the live `enable --now` failure is NOT run |
| **MED** bwrap deny_files skip | `29eb476` | argv falsified against a control; mutation-verified |
| **MED** CHANGELOG Ubuntu paragraph | `ee7c6d3` | checked against README + the runtime remedy string |
| **LOW** "macOS ships first" raise | `ee7c6d3` | class re-swept across `levain/`, `README.md`, `docs/`, `tests/` |

Plus `tests/linux/Dockerfile`, which is what made the HIGH's answer possible at all.

⚠ **`k4c-linux` FULL SUITE: 2504 passed / 13 skipped / 1 failed** — and the one failure is
`test_pip_floor_verdict_in_sync_when_constant_matches_floor`, which is the **worktree artifact
documented above, not a branch defect**. It passes in the primary checkout on the same commit.
⛔ **Do not read this branch as red on that row**, and do not "fix" it by touching the branch — the
finding is about the gate, and it is filed forward against `main`.

⚠ **STILL OWED ON K4c AND NOT DOABLE FROM macOS:** the 8 `linux_live` bwrap tests need a real host
(a plain container refuses to create a user namespace), and the rollback fix wants one live
`systemctl --user enable --now` failure. Both are argushub work.

---

## ⚖ THE CODEX-ABSENT L3 — ACCEPTANCE CRITERION, WRITTEN BEFORE THE OUTPUT LANDED

Phill ruled 2026-09-04 that quota-blocked seats run their blocking L3 on **glm-5.3** rather than wait
for the 2:15 codex reset (`FLOW_DEV_PROTOCOL.md` `105e75cc`, read at source). codex remains
non-replaceable and first choice; this is a **fallback**, never a substitution, and the codex run
stays queued.

⛔ **THIS CRITERION IS RECORDED BEFORE THE REVIEW RETURNED, ON PURPOSE.** Deciding afterwards what
would have counted is grading a proxy chosen to fit the result — the day's dominant defect, applied
to my own hold. What follows is the bar as of the moment the command was launched.

**The hold exists because `spore-725`'s note requires *"targeted L3 before the tag, never shipped
unreviewed"* on a crown-jewels confinement change. It clears IF:**

1. The seat actually **read the code** — `files_opened > 0`. A blindfolded reviewer's silence is
   indistinguishable from a clean bill, and `files_opened == 0` is a standing model disqualifier that
   would here be a HARNESS fact, not a model one.
2. `model_used` in the footer is **`glm-5.3:cloud` on the primary rung** — not the `gpt-oss` fallback
   wearing 5.3's name. Report what was served, never what was asked for.
3. It produced a **verdict on the socket arms specifically**, not generic file commentary — the
   review was focused on whether any of the three arms can be defeated and whether the opt-out fails
   open.
4. Every finding is **resolved against disk** before being believed or dismissed, same as complement's
   and glm-5.2's were.

**It does NOT clear if:** the seat returns nothing (the `REVIEW_MAX_TOKENS` truncation shape, which
still prints `contract=OK`), or is served by the fallback model, or opens zero files. Any of those is
a **codex-absent pass with no second lineage at all**, and then the hold stands to 2:15 — because the
failure would be that no independent seat read the change, which is the exact condition the hold names.

⚠ **AND THE BAR IS NOT "IT FOUND NOTHING".** A clean pass from a seat that genuinely read the code
satisfies this; a clean pass from a seat that could not read it satisfies nothing. Those two render
identically in the output, which is why 1–3 are checked mechanically rather than inferred from the
prose.

### ⛔ RUN 1 OF THE CODEX-ABSENT L3 — NO REVIEW. THE HOLD STANDS.

```
[repo-read] primary (glm-5.3:cloud): rounds=6 files_opened=2 899.1s contract=OK ERROR=TimeoutError
[glm PRODUCED NO REVIEW — this is NOT a clean pass … Treat this dimension as UNREVIEWED.]
```

Graded against the criterion recorded above, **before** the run returned:

| # | bar | result |
|---|---|---|
| 1 | `files_opened > 0` | ✅ **2** — it was genuinely reading |
| 2 | `model_used` on the primary rung | ✅ **glm-5.3:cloud**, no `gpt-oss` masquerade |
| 3 | a verdict on the socket arms | ❌ **nothing at all** |
| 4 | findings resolved against disk | n/a |

The written clause was *"It does NOT clear if the seat returns nothing."* **THE HOLD STANDS.**
Pre-registering the bar is the only reason this is a ruling and not a rationalisation.

⚡ **AND IT IS A FOURTH FAILURE MODE, NOT ONE OF THE THREE THE PROTOCOL WARNS ABOUT.** Not the
fallback masquerade (primary rung confirmed). Not the `REVIEW_MAX_TOKENS` truncation — pinned at
65536, and that shape returns silence with `contract=OK` and *no* error, while this returned an
explicit `TimeoutError`. Not `files_opened == 0`. It ran six rounds, opened two files, and **ran out
of clock at 899.1s against a 900s budget.**

So today's L3 failures have **four distinct causes** — codex timeout, codex quota, 5.3 truncation,
5.3 clock — and **every one exits 0 and renders as "no review".**

⛔ **THE VARIABLE IS DIFF SIZE, AND "ONE FILE" IS A PROXY FOR IT THAT COMES APART HERE.** The scoped
diff is **26,562 chars for that single file** — the socket fix alone added 152 lines, and this repo's
house style is heavy inline rationale. The protocol's 5.3 success case (`peer_roster.py`, 258.3s) was
a far smaller diff. The one-file rule is what stopped the codex timeouts; it is **not sufficient** on
a comment-dense file, and the measurable quantity is diff CHARS, not file count.

▶ Re-running at `--timeout 1800`. ⚠ **A second timeout is not a verdict either** — it would mean the
scope is too large for this seat, not that the code is clean and not that 5.3 is bad. Same
discriminator the protocol makes about `files_opened == 0`: a harness fact and a model fact render
identically, and only one of them is true.

### ⛔ RUN 2 — KILLED BY THE KERNEL. THE HOLD STANDS TO 2:15, AND `uptime` NEVER SAW IT COMING.

The 1800s re-run did not time out and did not fail: **the OS killed it for low memory.**

```
task status: killed — "the system is running low on memory"
swap:        9,890 MB used of 10,240   (96.6% exhausted, 349 MB free)
free RAM:    6,029 pages ≈ 94 MB
load:        5.42 on ten cores — 0.54x, and it LOOKS FINE
```

⚡ **`spore-718`'S RULE IS "RUN `uptime` BEFORE LAUNCHING HOURS OF COMPUTE", AND `uptime` IS BLIND TO
THE THING THAT IS ACTUALLY KILLING JOBS.** It was run before every heavy step today and was never
once the number that mattered. **The check's subject is run-queue length; the defect's subject is
resident memory.** That is this session's own dominant class fired at the coordination layer — and it
is why other seats lost jobs while every load check read green.

Top resident, measured: **1,702 MB** node (video-poker) · **1,419 MB** the Docker VM · **~1,800 MB**
across five `claude bg-spare` processes · Chrome in several hundred-MB pieces · 264 MB the Diogenes
reviewer.

⚠ The Docker VM is 1.4 GB and this session is why it is busy — the systemd container work. **Not
stopped:** Docker was already running before this session began, it is the operator's environment,
and this session already took it down once by accident. Flagged upward instead. This session's use of
it is finished.

### ⚖ THE RULING, FINAL FOR THIS SESSION

**0.4.4 STAYS HELD FOR CODEX.** Not relaunching 5.3 into a box at 96.6% swap — a relaunch is killed
again and eats memory other seats need. And the scope is genuinely too large for that seat at 26,562
chars in one file. The honest read: **this change needs codex, which was the first choice all along.**

▶ **FIVE DISTINCT L3 FAILURE CAUSES IN ONE DAY, ON ONE REPO:** codex timeout · codex quota · 5.3
truncation · 5.3 clock · OS memory kill. ⛔ **FOUR OF THE FIVE EXIT 0 AND RENDER AS "no review".**
The fifth returns `[killed]` and an empty file. **A seat reading exit codes instead of output would
have recorded five clean reviews today that never happened** — which is why `deep_review.py`'s
refusal to record coverage, and its "read it as a gap" line, is the single most load-bearing thing in
that script.

### ⛔ CORRECTION — MY OWN "DIFF CHARS" RULE IS FALSIFIED. IT IS `rounds`, AND `rounds` IS THE SEAT'S CHOICE.

Two sections above, this file says *"the measurable quantity is diff CHARS, not file count."* **That
is wrong**, and it was falsified by the very next measurement. Verified independently in the episodic
store (`flow-20260904-112833-cdd2a34eae31`) rather than taken on report:

| seat | diff | files | `rounds` | `files_opened` | result |
|---|---|---|---|---|---|
| video-poker | **75,778 chars** | 12 | **4** | 13 | ✅ finished, 798.9s under a 900s cap |
| levain (this) | **26,562 chars** | 1 | **6** | 2 | ❌ timed out at 899.1s |

**The diff 2.85× LARGER finished; the smaller one did not.** So the file-count rule and its
chars-based replacement are both dead — the replacement inside twenty minutes of being issued.

⚡ **WHAT ACTUALLY DIFFERS IS `rounds`: 4 against 6.** Rounds are read-then-think cycles **the seat
itself chooses**, and 5.3 chose 50% more of them on the *smaller* input — plausibly because a dense
152-line rationale block invites re-reading in a way twelve mechanically-related files do not.
**Nothing about the input predicts the clock, because the clock is set by the seat's own search
depth.**

▶ **OPERATIONAL FORM, and it is not a threshold:** give it a generous timeout, then read `rounds` in
the footer **afterwards**. `rounds=6` means it was working hard and a re-run at the same scope will
likely time out again; `rounds=4` had headroom. **n=2 — a shape, not a law.**

⚠ **AND THIS STRENGTHENS THE HOLD RATHER THAN WEAKENING IT.** My `rounds=6` says 1800s may not have
been enough either, and this file is precisely the dense-rationale case that produced the high round
count. Three independent grounds now: the pre-registered criterion is unmet, the scope is beyond this
seat, and the box will not survive the attempt.

↳ **I WROTE THE FALSIFIED RULE INTO THIS FILE AND INTO COMMIT `ea73f13`'s MESSAGE.** Left standing
rather than rewritten, with the correction appended — the commit is the trail, and a rule that was
believed for twenty minutes and then killed by measurement is worth more visible than tidied away.
⛔ **The lesson is the one this repo keeps paying for: I derived a rule from n=1 and stated it as the
measurable quantity.** Two size rules died in twenty minutes because both were single-datum
generalisations about a system whose behaviour is chosen, not determined.

### ✅ RUN 3 DELIVERED — THE CRITERION IS MET AND **THE HOLD IS LIFTED**

```
[repo-read] primary (glm-5.3:cloud): rounds=4 files_opened=3 754.9s contract=OK
```

⚠ **The command changed and that mattered.** `b63a8ebd` shipped a real `--seats glm-5.3` seat with a
per-model budget (`glm-5.3:cloud: 131_072`), and `repo_read_review.py:259` says an explicit
`REVIEW_MAX_TOKENS` **still wins** — so pinning 65536 as `FLOW_DEV_PROTOCOL.md` trap 2 instructs would
have **halved** it. Run 3 used the native seat with no env overrides. The documented remedy was
correct at 10:00 and harmful by 11:00; nothing linked it to the fix that retired it.

⚠ `REPO_MAX_ROUNDS = 6` — six is the **CEILING**, so runs 1–2's `rounds=6` meant the seat *exhausted*
its file-request rounds, not that it "chose" more depth. Run 3's `rounds=4` converged with headroom.

| # | bar (committed at `a894605`, before run 1) | result |
|---|---|---|
| 1 | `files_opened > 0` | ✅ **3** |
| 2 | `model_used` primary rung | ✅ **glm-5.3:cloud via primary** |
| 3 | a verdict on the socket arms | ✅ explicit **no HIGH** on all three, plus a fail-open sweep |
| 4 | every finding resolved against disk | ✅ done — one refuted, three confirmed |

⚖ **THE HOLD IS LIFTED.** `spore-725`'s *"targeted L3 before the tag, never shipped unreviewed"* is
satisfied. **The tag decision is Phill's from here; it is no longer blocked by this seat.** The bar
did not move in either direction — it held twice when runs returned nothing, and it releases now that
one returned a verdict. That is the whole point of writing it down first.

**What the pass actually said, and it is the strongest independent evidence the fix is sound:** all
three arms hold against every attack it could construct — rename and unlink at both spellings,
hardlink-to-new-name, symlink-indirection connect, parent-dir relocation, and absent-at-build-time
races. The `allow_container_sockets` sweep found **no fail-open anywhere the flag is readable**, and
confirmed the flag gates all three arms together so the opt-out is total rather than partial.

⚠ **THIS REMAINS A CODEX-ABSENT PASS AND THE RECORD SAYS SO** — `deep_review.py` itself printed
*"coverage NOT recorded — codex was not among --seats, so this was not a deep pass."* The codex run
**stays queued for 2:15** as the first choice it has always been, tag or no tag.

⚠ **POST-REVIEW CHANGES, STATED SO NOBODY HAS TO DIFF FOR THEM:** `e3ca577` landed after the review —
three comment blocks and one banner string. **Zero logic.** The reviewed code and the shipping code
are behaviourally identical.

---

## ⛔⛔ STDOUT SAID "NO REVIEW"; THE STORE HELD A COMPLETE ONE WITH A SECURITY FINDING IN IT

Run 1 was graded — correctly, on the evidence available — as producing nothing. `state/verdicts.jsonl`
held `run_id 8de86ccec2bd439f`: **`ok=True`, `files_opened=2`, `role_used=primary`,
`duration_s=381.0`**, at HEAD `a894605`, with a **3,657-character review**. It had *completed*, in 381
seconds, while stdout reported a 899-second `TimeoutError` and "PRODUCED NO REVIEW".

Found because `0904+3` retracted the rule *"a backgrounded L3 produces zero verdict rows"* (its own
attribution check truncated the row at 6000 chars and could not see its subject past the cutoff), and
the fan-in asked for an **untruncated** blob test. Six rows name `confinement.py`; that one was live.

### ⛔ AND THE REVIEW WAS RIGHT — FIXED AT `91c38b0`

```
Path("~nosuchuser42/.ssh/authorized_keys").expanduser()
  -> RuntimeError: Could not determine home directory.        [MEASURED HERE]
```

`crown_jewel_reason` caught `(ValueError, OSError)` and **not `RuntimeError`** — and its `path` comes
**from the entity**. A model emitting a `~user` spelling reached a function whose docstring promises
FAIL-CLOSED and got **neither a denial nor an allow**: an exception out of the security predicate.

⛔ **The twin site is worse because it breaks its own written contract.** `declared_resources` had the
identical clause under a docstring reading **"Never raises."**, with the paragraph above it explaining
that a raise there surfaces a raw `AgentErrorEvent` and **SKIPS THE EXECUTOR**, so the floored
refusal never fires. The guard written to stop that, defeated by the spelling it did not catch.

⚡ **This release already fixed the class in `doctor.py:1132`** — comment saying *"measured, not
reasoned"* — and its CHANGELOG records that fix reintroducing an earlier defect *"by a new door"*.
**The door nobody checked was the in-process crown-jewels twin.**

Two tests: the fail-closed return, and the **premise** (that `expanduser` raises `RuntimeError`) so
the first cannot pass for the wrong reason if CPython changes. Mutation-verified.

### ▶ THE STANDING RULE THIS EARNS

**After any L3 that reports no review, grep `verdicts.jsonl` by `run_id` with an UNTRUNCATED blob
test before recording it as a gap.** `deep_review.py` captures to the store **regardless of
backgrounding**; `partnership.md`'s *"a backgrounded agent loses its results"* is **stdout-only**.

⚠ **The discriminating datum is RUN 1, not the successful run.** A run that succeeds and records
proves only that success records. Run 1 **reported a timeout and had a complete review in the
store** — so the claim is that **stdout and the store can disagree about whether a review happened at
all, and stdout is the one that lies.** Four of the day's five L3 "failures" were judged on stdout
alone.

⚖ **THE TAG STATUS IS UNCHANGED AND THE HOLD IS NOT RE-IMPOSED.** The criterion at `a894605` was met
by run 3 and stays met; this is an additional fix found afterwards, of exactly the kind a post-ship
review produces, and it makes the cut better rather than blocking it.

---

## ⚖ PHILL RULED: CUT IT, BUT NOTHING PUBLIC UNTIL CODEX CLEARS (2026-09-04 12:40)

His words, direct to the seat: *"do it please but do not actually release this thing to the public
until codex runs and gives us the all-clear please."*

**That INVERTS §①'s order and the inversion is the ruling.** The written procedure tags and publishes
and treats review as a precondition already satisfied. Here the review is the LAST gate before
anything becomes public, and it lands after the artifact is otherwise finished. Nothing is tagged and
nothing is uploaded until codex returns a verdict.

### ✅ §① STEPS 1–3 ARE DONE ON THE CURRENT ARTIFACT — RE-RUN, NOT INHERITED

The earlier install gate ran *before* the socket fix, the RuntimeError fix and the banner change.
Verifying the old build would have been checking an artifact nobody is shipping. Rebuilt with
`uv build` and re-run end to end:

| check | result |
|---|---|
| both fixes present **in the wheel**, not the source tree | 8 sockets denied · `~user` path REFUSED |
| `claude-code` real install + `doctor` | exit 0 |
| `codex` real install + `doctor` | exit 0 |
| both adapters, `doctor --invoke` (hooks actually fire) | exit 0 |
| control: user-level wiring spelled `~`, `$HOME`, absolute | **exit 1 each, wiring reported** |
| control: no user-level file | exit 0 |

**The gate is falsified, not merely green** — it can go red, on the exact configuration it exists to
catch, on the artifact being shipped.

↳ Incidental but worth knowing: the socket count is **8** here against **7** this morning. With
Docker stopped, `/var/run/docker.sock` no longer resolves into `~/.docker/run/docker.sock`, so the
two spellings stop collapsing. **More coverage, not less** — and it holds either way only because the
enumeration carries both spellings explicitly rather than relying on resolution.

### ⏱ THE CODEX SLOT IS ARMED, NOT WAITED ON

Quota resets 2:15 PM; confirmed still blocked at 12:41. A background runner polls from 2:14 every
120s (so a slightly early reset is used and a late one does not lose the slot), smoke-tests codex,
then runs `--seats codex --paths levain/firing/confinement.py --timeout 550` — the one unreviewed
file in the cut, where `91c38b0` landed.

⚠ **`--timeout 550` IS DELIBERATE AND MEASURED BY ANOTHER SEAT.** `0904+3` found codex dies on the
clock at ~600s **independently of quota** — a `signal 14` kill that renders as the same silence as a
quota block. Staying under 600 buys an attributable failure instead of an indistinguishable one.

⚠ **THE RESULT WILL BE READ FROM `verdicts.jsonl` BY `run_id`, UNTRUNCATED — NOT FROM STDOUT.** That
is this session's own rule, earned the hard way: run 1's stdout said "PRODUCED NO REVIEW" while the
store held a complete 381-second review carrying a live security finding.

### ▶ WHAT REMAINS, IN ORDER, AFTER THE ALL-CLEAR
1. Triage codex's findings against disk. Fix what is real.
2. §① step 4 — bump **both** stamps to `0.4.4` **in the tagging commit** (the gate refuses the pair at
   any other point, and the gate is now committed and verified to block).
3. §① step 5 — publish per `spore-424`: `PYPI_API_TOKEN3`, never `~/.pypirc`, twine ≥ 7 in a
   throwaway venv, both dist files named explicitly, verify on the **simple index** not the JSON API.
4. Next commit → `0.4.5.dev0`; then the anneal pins → `0.9.9`; then tell Alex.

### ⚖ THE 2:15 CODEX CRITERION — WRITTEN AT 12:50, BEFORE THE SLOT FIRES

Pre-registered for the same reason as `a894605`: **deciding afterwards what would have counted is
grading a proxy chosen to fit the result.** That method is why the earlier hold released honestly,
and it is the only defence against a bar that drifts to meet whatever the run happens to produce.

⛔ **PHILL'S BAR IS STRICTER THAN MINE WAS, AND HIS WORDING IS THE RULING:** *"do not actually release
this thing to the public until codex runs **and gives us the all-clear**."* That requires an
**AFFIRMATIVE verdict**, not merely that codex executed. **A null, a timeout, a signal-14 kill or a
second quota block is NOT an all-clear** — it is the absence of one, and the release does not go.

⚠ **THE TEMPTATION AT 2:15, NAMED NOW WHILE IT COSTS NOTHING:** if codex returns nothing, the
available rationalisation is *"5.3 already cleared it and my run-3 criterion was met, so ship."*
**That reasoning is void.** My criterion governed MY hold; Phill's ruling replaced it and asks for a
different thing. The 5.3 pass does not substitute for the all-clear he named.

**IT CLEARS AND THE TAG PROCEEDS IF, AND ONLY IF:**
1. codex **produced a review** — a verdict, not silence. Confirmed from `verdicts.jsonl` by `run_id`,
   **untruncated**, matching `metadata.repo` as a **field**. ⚠ Every value in that file is a STRING,
   including `errored: 'False'` — a truthiness test on it is true for every row, clean ones included.
2. Its findings are **resolved against disk**, each confirmed or refuted with the evidence stated.
3. **Nothing confirmed is release-blocking** — i.e. no confirmed defect in the socket floor's three
   arms, the `allow_container_sockets` opt-out, or the `crown_jewel_reason` fail-closed path.

**IT DOES NOT CLEAR IF:** codex returns nothing for any reason · or a confirmed finding lands on the
floor or the opt-out. In the first case the answer is a re-run at a tighter scope, not a lower bar.
In the second, fix it, then re-review, then tag.

▶ **AND A CONFIRMED FINDING IS NOT AUTOMATICALLY A BLOCKER.** A comment defect, a docstring that
disagrees, an adjacent-scope observation — those get fixed or filed and the tag proceeds. **The
blocking set is narrow and named above**, so that "codex found something" cannot be read as either
"ship anyway" or "hold forever" without deciding which of the two it actually is.

### ⛔ 2:15 SLOT, ATTEMPT 1 — NO ALL-CLEAR. AND A `PONG` SMOKE IS NOT A VALID QUOTA PROBE.

```
14:02:22  PONG smoke ......... 767 chars, PONG present   -> read as "quota is up"
14:02:59  the REAL review .... 435 chars = the quota fingerprint, exactly
```

**Thirty-seven seconds apart.** The trivial prompt was served; the review-sized request was refused.

⚡ **THIS IS THE DAY'S DOMINANT CLASS FIRED AT THE APPARATUS'S OWN HEALTH CHECK.**
`global/partnership.md` prescribes `echo PONG | codex exec --sandbox read-only` as *the* codex health
smoke. **The check's subject is a trivial prompt; the claim's subject is whether a real review can
run.** A PONG proves the endpoint is reachable and the credential valid, and proves **nothing** about
quota headroom for a 27,750-char diff — and the two render identically as "codex is fine".

⚠ **THE SMOKE'S 767 CHARS IS A FOURTH POPULATION THAT OVERLAPS "HEALTHY".** The fingerprint table
built today — 435/437/51 quota · 78 timeout · >1,000 real — describes *review invocations*. A smoke
response sits outside it and reads clean.

▶ **THE UNCOMFORTABLE RULE: THERE IS NO CHEAP QUOTA PROBE.** Either spend a real run to find out, or
wait past the stated reset and spend one then. Do not schedule off a PONG. ⚠ **And the error body's
own "try again at 2:15 PM" is a FLOOR TO PROBE FROM, not a start time** — I read it as a start time,
scheduled against it, and burned the slot.

↳ **I RELAYED "QUOTA IS UP, RELEASE THE OTHER TWO SEATS" ON THE SMOKE AND RETRACTED IT SEVEN MINUTES
LATER.** Two seats were queued behind that signal. The retraction was fast because the real result
landed immediately after — but the error is mine and the mechanism is worth more than the apology:
**I probed with the cheap thing and reported on the expensive thing.**

↳ Field note, corrected: `errored` on these rows is a real boolean `True`/`False`, **not** the string
`'False'` reported elsewhere. Check the type per row rather than assuming either way.

### ⚖ THE CRITERION APPLIES AS WRITTEN — NO RELEASE

`cf791e8`, pre-registered before the slot fired: *"A null, a timeout, a signal-14 kill or a second
quota block is NOT an all-clear."* codex produced nothing. **Nothing is tagged and nothing is
uploaded.** The trap named in that same entry is now the live temptation — *"5.3 already cleared it,
so ship"* — and it remains void: Phill asked for an affirmative all-clear and the fallback does not
substitute for it.

▶ **RE-ARMED CORRECTLY:** first attempt from **2:16 PM**, a **REAL review** rather than a smoke,
retrying every 5 minutes against the quota string, 2.5h ceiling. If it never serves one, that is
recorded as *no all-clear* — not as a clean pass.

---

## ⚖ THE spore-768 L3 CRITERION — WRITTEN AT 15:22, BEFORE THE RUN (2026-09-04)

Pre-registered before dispatch, per the method at `cf791e8` that held this release twice on silence
and released it honestly on a verdict. **Scope and bar are frozen here; the timeout is an
operational parameter and any change to it gets recorded with its reason.**

▶ **THE ARTIFACT:** levain `9d65e78` — `refresh_socket_denies` + `_resolve_socket_targets` +
`CrownJewelsPolicy.socket_sources` + `ConfinementProvider.spawn_shell` as a template method.
▶ **THE SCOPE:** `levain/firing/confinement.py`. One file, because the fix is one file and the
previous run on this file timed out TWICE at 550s before delivering at 466s on a 1500s clock.
▶ **THE SEATS:** `complement,codex,glm`. codex is non-replaceable for this substrate and this
release's hold is *defined* as a codex verdict. If codex is quota-blocked, the fallback is
`complement,glm-5.3,gpt-oss` (three distinct lineages) — and a fallback pass **does not lift the
hold**, because Phill's bar is an affirmative codex all-clear.

### WHAT COUNTS AS CLEARING

1. An **affirmative codex verdict on the socket floor** — it read `confinement.py` and reports no
   HIGH/CRITICAL in the connect/rename/ancestor arms or in the new spawn seam.
2. ⛔ **A null, a timeout, a signal-14 kill, an empty body, or a quota refusal is NOT an all-clear.**
   An exhausted codex EXITS 0 with the refusal inside the body — read the body, never the code.
   Fingerprints: **78** = our alarm · **435/437/51** = quota · **>1000** = a real review.
3. ⛔ **`stdout` IS THE LIAR AND THE STORE IS THE RECORD.** Today a run printed a TimeoutError while
   a complete 381s review sat in `state/verdicts.jsonl` hiding a security defect for four hours.
   Check the store before believing a failure.
4. A confirmed defect **in the three socket arms or the new refresh seam** does not clear, however
   narrow. Narrower than framed is not absent — that is exactly what the HIGH being fixed here was.

### ⛔ THE TRAP, NAMED IN ADVANCE SO IT CANNOT BE ARGUED LATER

**"The suite is green and I mutation-tested the union, so ship."** Void. Green tests are the
instrument agreeing with itself; the entire class this release keeps hitting is *a gate that passes
without exercising the thing it is named for*. My own suite passed 2483 tests while the TOCTOU was
live yesterday.

**And the second one, which is new and mine:** *"the marginal exposure was already zero, so any
verdict is cosmetic."* Also void. Exposure being zero is what made the ORIGINAL defect an honesty
defect; it says nothing about whether the FIX introduced one. `refresh_socket_denies` mutates a
security policy at spawn time and `spawn_shell` changed shape — both are new attack surface that did
not exist when the exposure was measured at zero.

### ⛔ SELF-CAUGHT WHILE THE L3 RAN — MY "STRUCTURAL" CLAIM IS STRONGER THAN MY MECHANISM

`9d65e78`'s docstring says the template method makes the socket refresh *"impossible for a provider
to skip."* **That is not true as written.** `ConfinementProvider.spawn_shell` is concrete and
`_spawn_shell_impl` is abstract, so a subclass that implements the impl gets the refresh — but a
subclass that simply **defines `spawn_shell` itself** overrides the template and loses the refresh
silently, and Python will not stop it. My test pins that `_spawn_shell_impl` is in
`__abstractmethods__`; it does not and cannot pin that nobody overrode the wrapper.

⚡ **THIS IS THE `claim > enforcement` GAP, IN THE ONE MODULE WHOSE DOCSTRINGS NAME IT AS THE THING
THEY REFUSE** — and I wrote it into the justification for the change rather than into the code. The
argument for the template method was *"a provider must not be able to forget"*; leaving the override
hole means what I actually shipped is a strong sentence plus a weak mechanism, which is the shape
`spore-658` calls a ruling that is READ but not FIRED.

▶ **THE FIX: `__init_subclass__` on `ConfinementProvider` that REFUSES a subclass overriding
`spawn_shell`** (`TypeError` at class-definition time, naming `_spawn_shell_impl` as the seam). That
turns the docstring into a fact. levain is a library, so an EXTERNAL provider is a real case, not a
hypothetical about our own two.

⏸ **DELIBERATELY NOT COMMITTED YET, AND THE REASON IS QUOTA DISCIPLINE, NOT DOUBT.** The L3 is in
flight against `9d65e78`. Editing the reviewed surface now makes the verdict chase a moving target
and costs a second codex slot on a shared, ungoverned $20 quota (`spore-744`). It gets bundled with
whatever the verdict returns into ONE fix commit and ONE re-review.
⚠ Written down rather than remembered: an intention held only in a seat's head does not survive the
seat.

---

## ⛔ THE spore-768 L3 VERDICT — NOT AN ALL-CLEAR. THE FIX HAD THE DEFECT IT WAS FIXING.

Run at 15:20 against `9d65e78`, criterion pre-registered at `4e967ca`. **2 of 3 seats produced
output; `complement` returned nothing** (`Claude CLI exit code 1`), so this was NOT the three-lineage
mesh requested and is not recorded as one — seats that return nothing corroborate nothing.

▶ **codex: 1 HIGH + 3 MED + 1 LOW in 580s. glm-5.2 (repo-read, 1 file, 141s): converged
INDEPENDENTLY on the HIGH.** Every finding resolved against disk before being believed; the HIGH was
reproduced by execution. **All five confirmed. None refuted.**

### ⛔ #1 HIGH (codex + glm, convergent) — THE REFRESH GAVE A FRESH TARGET ONE ARM OF THREE

`refresh_socket_denies` added the freshly-resolved target to `deny_sockets` and nowhere else.
Measured before fixing, on a listed-but-absent socket symlinked to an unlisted `real.sock`:

```
in deny_sockets     : True
in deny_write_files : False   <-- arm (ii)
parent write-denied : False   <-- arm (iii)
crown_jewel_reason  : None    <-- BOTH hands would allow the rename
```

So the entity could `mv real.sock moved.sock` and connect to `moved.sock` — **the exact bypass this
module already measured and wrote three arms to stop.** ⚡ **THE DAY'S DOMINANT CLASS, FIRED AT MY OWN
FIX:** I implemented the arm that is *visible in a deny list*, skipped the two that make it hold, and
then wrote a comment justifying the omission. The comment reasoned about the arms on the **listed
path** — which are genuinely unaffected — and never once about the arms on the **new target**.

### ⛔ #2 MED — "MONOTONIC" WAS TRUE FOR ONE SPAWN AND FALSE ACROSS RESPAWNS

`SandboxedBashExecutor._ensure_shell` respawns after every `exit`, always from `self._policy`. The
refresh returned a new policy that was rendered and **discarded**, so spawn 2 started from the
build-time set and spawn 1's target fell back off. The docstring's *"can only ever ADD"* — the
property I called the whole design — was false across exactly the respawns its own comment cited.
`self._policy` is now assigned the refresh's result.

### ⛔ #3 MED — the `spawn_shell` override hole. **I had self-caught this at `974a78b` 20 minutes
before codex filed it**, and held the fix to avoid moving the reviewed surface. Now closed with
`__init_subclass__`, which is what codex independently recommended. `typing.final` would not fire at
runtime.

### ⛔ #4 MED — POSITIONAL-ABI BREAK ON AN EXPORTED DATACLASS

`socket_sources` was inserted between `socket_spellings` and `own_memory_files`, shifting every later
index. Code built against the old signature passing `own_memory_files` positionally would have landed
the entity's own memory paths in `socket_sources` — resolved and *network*-denied — while **losing
their write-deny**, which is the poison-the-always-loaded-memory vector that field exists to close.
Moved to last; pinned by a test.

### ⛔ #5 LOW (codex) / MED (glm) — "THE ONE PLACE THIS DERIVATION LIVES" WAS FALSE AS WRITTEN

`build_policy` still resolved inline at two more sites, so the three arms could derive from three
different snapshots under a concurrent retarget. `_socket_arms` now resolves ONCE and returns all
three. ⚠ **glm's MECHANISM for this one is WRONG and its conclusion is right** — it cited a symlink
loop raising `OSError`, and I measured that `Path.resolve()` on a loop returns the lexical path on
CPython 3.13 rather than raising. codex reached the same defect without the false trigger.

### ⚖ THE CRITERION APPLIES AS WRITTEN — THE HOLD STANDS

`4e967ca`: *"a confirmed defect in the three socket arms or the new refresh seam does not clear."*
A HIGH in the refresh seam is exactly that. **And the trap I pre-registered is the one that fired:**
I wrote *"the marginal exposure was already zero, so any verdict is cosmetic"* into the criterion as
void — and the HIGH is precisely a case where the FIX created surface the original measurement never
covered. Writing it down before the run is what made it unarguable afterwards.

---

## ✅ ROUND 2 OF THE spore-768 REVIEW — **codex: "No HIGH findings."** All 3 seats ran.

`complement` **worked this time** (209.8s) — the fan-in measured it at a **33% silent failure rate
across every repo today** (5 of 15 runs, `exit 1` with no stderr, both clusters), so round 1's
missing seat was intermittent infrastructure, not my run. glm-5.2 337.7s, codex 569s.

▶ **codex: no HIGH, 2 MED. glm: 1 MED. complement: 1 MED/HIGH, convergent with codex's #2.**
All three confirmed against disk. All three fixed.

### ⛔ #1 (codex) — THE DOUBLE REFRESH SILENTLY LOST A TARGET
The executor refreshed, then `spawn_shell` refreshed **again**, and the executor kept the **first**
answer. Source resolves to A in the executor, is retargeted to B before the provider's call: the
shell renders A+B, the executor caches A. After `exit`, retarget to C → shell 2 gets A+C and **B is
gone**. The union was not monotonic across respawns — the exact property the design rests on,
defeated by refreshing twice and keeping the earlier result. ⚡ **My own comment asserted the double
call "costs a resolution and changes nothing"** — true only if the filesystem is identical at both
instants, which is precisely what a TOCTOU fix may not assume.
▶ Fixed: `SandboxedShell.effective_policy` is stamped by the provider seam; the executor caches
exactly what was rendered and no longer refreshes at all. **One authoritative refresh per spawn.**

### ⛔ #2 (codex + complement, convergent) — THE GUARD ON THE GUARD
`__init_subclass__` checked `cls.__dict__`, so `class P(LegacyMixin, ConfinementProvider)` passed
while its MRO resolved `spawn_shell` to the mixin's, skipping the refresh. A non-cooperative
`__init_subclass__` that never called `super()` disabled it wholesale below that point.
▶ Fixed with **`_ProviderMeta(ABCMeta)`** checking the post-MRO-resolved attribute — a metaclass
`__new__` runs at class creation whatever the class body does. ⚡ **Third time in this change that a
fix reproduced the class it was written for**, and the old guard's test passed throughout because it
only exercised the direct-override case the guard checked.

### ⛔ #3 (glm-5.2) — MY HIGH-FIX BROKE A DIFFERENT STATED INVARIANT, ONE FILE OVER
Each hand's `create` built its own policy object. Harmless while the refresh touched only
`deny_sockets` (no in-process twin) — **and my round-1 fix made it also update `deny_write_files`,
`socket_spellings` and `deny_write_dirs`, which the file editor DOES enforce.** So the bash hand
evolved a floor the file-editor hand never saw, **falsifying this module's own docstring in two
places**, including *"Both hands read the same value, so the file editor's rename-deny and the
seatbelt's connect-deny cannot disagree about whether sockets are fenced."*
⚠ **HONEST ON SEVERITY: an invariant break, not a bypass.** The rename vector is a `mv`, the file
editor has no rename primitive, and the bash hand was covered throughout. Fixed because the
invariant is load-bearing and *stated*, not because an exploit was shown.
▶ Fixed with `_SharedFloor` + a registry keyed on `(entity_dir, workspace)`; both hands read through
it. The `policy=` form still works and gets a PRIVATE floor, so no test churn and no accidental
cross-entity sharing.

### ⛔⛔ AND MY FIRST TEST FOR #3 PASSED UNDER ITS OWN MUTATION
`test_both_hands_share_one_evolving_floor` constructs both executors with an explicit shared floor —
so it grades **the holder mechanism** and never touches `create`, which is where the sharing has to
happen. Reverting `create` to separate policies left it **green**. Caught by running the mutation
rather than trusting the test. `test_create_wires_BOTH_hands_to_the_same_floor_object` drives the
real `create` path and does fail under it. ⚡ **A test that passes under the mutation it was written
for is this session's class, and it caught me one layer up from where I was looking.**

### ⚠ A GAUGE THAT CANNOT BE OBEYED AS WRITTEN
The run printed *"⛔ DECIDE NOW, BEFORE YOU SEE ROUND 2'S OUTPUT, and write the decision down"* —
**inside round 2's output**, after the findings. The instruction arrives strictly after the moment it
names, so single-pass compliance is impossible. The base-rate warning itself is right and useful
(5 of 5 cited files changed in 24h; *fixing a class does not exempt the fix from the class* — which
is exactly what happened three times here). Routed to the fan-in as a small flow-side finding.

### ⚖ THE ROUND-3 CRITERION, WRITTEN AT 16:25 BEFORE THE RUN

Artifact `b7841f1`. Scope: `levain/firing/confinement.py` + `levain/firing/openhands/tools.py`.
Seats `complement,codex,glm`, 1500s. Bar unchanged from `4e967ca`: **an affirmative codex verdict
with no HIGH/CRITICAL in the socket arms, the refresh seam, or the new shared-floor indirection.**
A null / timeout / kill / quota refusal / empty body is not an all-clear; read the BODY and the
store, never the exit code.

⛔ **THE TRAP FOR THIS ROUND, NAMED IN ADVANCE:** *"round 2 came back with no HIGH, and round 3 only
fixed round 2's MEDs, so round 3 is safe by inheritance."* **VOID.** Every round so far has found
its defects inside the PREVIOUS round's fixes — round 2's three MEDs were all in round 1's fix, and
`_SharedFloor` is the largest structural change in the whole changeset, touching how BOTH hands
obtain their policy. A clean round is evidence about the code that was reviewed, never about the
code written afterwards to satisfy it.
⚠ Second trap, and it is the one I would actually reach for: **"three rounds is enough."** Round
count is not a measurement. The bar is the verdict, and if round 3 returns a real finding then round
4 happens.

### ⛔ SELF-CAUGHT WHILE ROUND 3 RAN — TWO DEFECTS IN `_SharedFloor`, ONE OF THEM FAIL-OPEN

Same self-review pass that caught the `spawn_shell` override hole ahead of codex last round. Held,
not fixed, so the in-flight review is not chasing a moving target — bundles with round 3's findings.

**(a) THE GET-THEN-SET IS NOT ATOMIC, AND THE RACE BREAKS THE EXACT INVARIANT `_SharedFloor` EXISTS
FOR.** `floor_for_conv_state` does `_FLOORS.get(key)` → `_SharedFloor(...)` → `_FLOORS[key] = ...`.
Two concurrent `create` calls can both miss, both construct, and the second write wins — so the two
hands end up holding **different floor objects** while every test and every docstring says they share
one. The tool `create`s are not obviously concurrent today, but "not concurrent today" is a property
of callers I do not control, and the failure is silent divergence rather than an error.
▶ Fix: a module-level `threading.Lock` around the get-or-create, or `setdefault` with the policy
built first (`setdefault` is atomic and discards the loser).

**(b) A CACHED FLOOR SILENTLY DISCARDS A FRESHLY-BUILT POLICY, AND THE DIRECTION IS FAIL-OPEN.**
`floor_for_conv_state` builds `policy_for_conv_state(conv_state)` and then **throws it away** if a
floor is already cached. That policy is built with
`resolve_cred_floor(cfg.deny_standard_creds, mode=current_drive_mode())` — the drive mode is read
from the process channel at build time. If the mode changes between the two hands' `create` calls
(gated → unattended), the second hand inherits the FIRST call's cred floor. Gated is the LESS strict
of the two for the standard cred stores, so the stale direction is the permissive one.
⚠ Round 2's glm finding was that the two hands could disagree; this is the two hands agreeing on a
STALE answer, which reads as healthy and is worse to spot.
▶ Fix: keep the build-time policy as a baseline on the floor and rebuild the floor when a freshly
built policy differs from it — the spawn-time socket accumulation is legitimately discarded there,
because a changed baseline is a different floor.

⚡ Both are in code written 30 minutes ago **to fix a review finding**, which makes this the fourth
consecutive round where the fix carried the next defect. That is now the most reliable finding of
the day and it is worth stating as a rule rather than an observation: **on this changeset, the
expected number of defects introduced per fix has been ≥1, so "the fix is small" has predicted
nothing.**

---

## ⛔ ROUND 3 OF THE REVIEW — codex QUOTA-BLOCKED. THREE HIGHs FROM THE TWO SEATS THAT RAN.

codex: `You've hit your usage limit ... try again at 7:16 PM`, rc=1 in 5s, **no review**. Mesh was
**2 of 3** and is NOT recorded as a 3-lineage pass. complement 207.3s, glm-5.2 97.0s.
⛔ **THE TAG THEREFORE WAITS PAST 19:16 AT MINIMUM** — `4e967ca`'s criterion takes only an
affirmative codex all-clear, per Phill's *"nothing public until codex gives us the all-clear."* A
`glm-5.3` fallback does not substitute, and relaxing that bar is Phill's to do, not this seat's.
⚠ A stated reset time is a FLOOR, not the moment it returns (measured twice today).

### ⛔ HIGH (complement) — `register()` BYPASSED THE METACLASS COMPLETELY. CONFIRMED BY EXECUTION.
`_ProviderMeta` subclasses `ABCMeta`, so `ConfinementProvider` inherited `register()` — a virtual
subclass becomes an `isinstance`/`issubclass` match **without ever passing through `__new__`**.
Measured: a class with its own unrefreshed `spawn_shell`, registered, returned
`isinstance(...) is True` while the finality check never ran.
⚡ **AND `register()` IS THE DOCUMENTED WORKAROUND FOR EXACTLY THE THREAT MODEL MY OWN DOCSTRING
NAMES** — a third party whose provider already has an unrelated base class. A guard that closes
normal inheritance and leaves the standard idiom open is not a guard. Now refused outright: there is
nothing to validate, since a virtual subclass shares no code and its `spawn_shell` cannot be ours.

### ⛔ HIGH (complement + glm, convergent) — `_FLOORS` STALENESS IS A FAIL-OPEN ON THE CRED FLOOR
No eviction, no conversation scoping, and the freshly-built policy **discarded** on a cache hit.
`policy_for_conv_state` derives `deny_standard_creds` from `current_drive_mode()`, so in a
long-running `levain daemon` — **which K4a ships, running scheduled seats in ONE process** —
conversation 1 as an interactive REPL (creds ALLOWED) creates the floor and conversation 2 as an
UNATTENDED seat inherits it: `~/.config/gh`, `~/.aws/credentials`, `~/.netrc` readable on an
unattended seat. Precisely the fail-open the drive-mode tri-state exists to prevent.
⚠ **I had self-caught a narrower version an hour earlier and the reviewers' framing is the real
one** — I described it as a mode change *between the two hands' create calls*; the actual exposure is
*between conversations in a daemon*, which is both far likelier and a shipped configuration.
▶ Fixed by storing the BUILD-TIME policy as a `baseline` and rebuilding when a fresh build differs.
⚡ Comparing the baseline rather than the live policy is what makes it correct: the live policy
legitimately grows at each spawn (the monotonic socket union), and carrying those extra denies into a
later conversation is fail-CLOSED. **Only a changed baseline means a different floor** — so it
rebuilds on exactly the security-relevant difference and nothing else, and bounds the dict by
distinct `(entity_dir, workspace)` pairs instead of growing per conversation.

### ⛔ HIGH (complement) / MED (glm) — THE None-DEREF, AND THE ASYMMETRY IS THE DEFECT
`spawn_shell` guards `if shell is not None` with a comment saying `_spawn_shell_impl` may return a
falsy sentinel — so its own return can be `None`, and `_ensure_shell` dereferenced it one line later.
`__call__` converts `ConfinementError` into an in-band refusal, **not** `AttributeError`, so it was an
unhandled crash rather than a graceful refusal. ⚡ Anticipating a case in one function and not at its
only call site is worse than not anticipating it at all: the guard reads as though it is handled.

### ⚠ LOW (glm) — I LEFT IN THE COMMENT MY OWN COMMIT MESSAGE CALLED WRONG
Six lines above the block explaining that the double-refresh was the BUG sat the old comment:
*"Deliberately BOTH here and inside `spawn_shell` ... running it twice costs a resolution and changes
nothing."* Stale, contradictory, and positioned ABOVE the code where it reads as design rationale. I
quoted that exact sentence in `b7841f1`'s message as the false claim, and did not delete it.

### ⚠ LOW (complement) — the "genuinely final" wording overclaimed
The check fires at class creation and at `register()`; post-definition `Provider.spawn_shell = other`
is not intercepted, and that is the same reach a legitimate `monkeypatch.setattr` has. Docstring now
says **final against every way a provider can be DEFINED**, not final against arbitrary mutation.

## ⛔⛔ TWO FAILURES IN MY OWN INSTRUMENTS THIS ROUND — BOTH SELF-INFLICTED, ONE A REPEAT

**(1) `| tail -90` TRUNCATED THE REVIEW AND HID TWO OF THE THREE HIGHs.** The `register()` bypass and
the None-deref were both above the cut. I recovered them only by pulling the full 9,022-char response
from `state/verdicts.jsonl`. ⚡ Same lesson as this morning's stdout-vs-store split arriving through a
different door: **the store is the record and the terminal is the liar** — and this time the liar was
a pipe I typed myself, which silently deleted security findings from a release review.

**(2) I BUILT A GREP THAT RENDERS "no failures" AND "filter did not match" IDENTICALLY — TWICE.**
`| grep -E "^(FAILED|[0-9]+ failed)"` never matches, because pytest's summary line is ANSI-coloured.
The mutation run printed NOTHING and I read it as three clean mutations for a moment.
**I caught this exact failure an hour earlier in this same session and rebuilt it anyway.**
`absence_of_signal_rendered_as_health`, in the verification harness, self-inflicted, repeat offence.
▶ Rule: `--color=no` on any pytest output that a filter will read, and **never let a filter's empty
output mean success.**

---

## ⛔ ROUND 4 — CODEX RAN AT 19:21 AND SAID "No HIGH findings." **IT STILL DOES NOT CLEAR.**

`--seats codex` alone, 518s, 3,437-char response, store row non-errored, `metadata.repo=levain`.
⚠ **Attempt 1 at 19:16:10 was still a quota refusal** — the stated reset is a FLOOR, third confirmation
today. Attempt 2 at 19:21:15 delivered. **Retry-until-real is what got the review**; a single shot at
the floor would have reported failure.
✅ **And the rebuilt classifier earned itself in its first ten minutes:** attempt 1's refusal came
wrapped in a **1,994-byte** file. The denylist version this replaced would have called that a
completed run and produced a FALSE ALL-CLEAR at 19:16.

### ⛔⛔ "No HIGH findings" IS A HEADLINE, NOT A VERDICT — 3 MED + 2 LOW, **ALL FIVE CONFIRMED AGAINST DISK, NONE REFUTED**

**MED-1 `tools.py:425` — UNSYNCHRONIZED SHARED-FLOOR MUTATION. Execution-reproduced by codex.**
`self._lock` is a **per-executor** lock created in `__init__`. Two conversations sharing one
`_SharedFloor` each hold their OWN lock, so `self._floor.policy = ...` is unsynchronized ACROSS
executors: both read P, spawn against P+A and P+B, last writer wins. *"After the affected executor
exits and respawns, **a previously denied live container socket becomes reachable again.**"*
▶ Confirmed by reading: the lock is per-instance; nothing guards the floor itself.

**MED-2 `tools.py:231` — THE BASELINE REBUILD HANDS THE TWO HANDS DIFFERENT FLOORS.**
```
if floor is None or floor.baseline != policy:
    floor = _SharedFloor(policy); _FLOORS[key] = floor
```
Hand A builds → creates F1. Hand B builds a tightened policy → baseline differs → creates F2 and
**replaces the entry**. A still holds F1. codex's case: the editor builds before `/secret` is added
to `deny_files`, bash builds after, and *"the conversation can still `view /secret` through the
editor while bash denies it."*
⚡⚡ **THIS IS THE SHARPEST THING IN THE REVIEW: MY FIX FOR ROUND 3's FAIL-OPEN CREATED THE EXACT
DIVERGENCE `_SharedFloor` WAS BUILT TO PREVENT.** Sharing requires ONE object; freshness requires
REPLACING it. **The two requirements are in direct tension and I satisfied one at the cost of the
other, one round apart, without noticing they were the same variable.**

**MED-3 `tools.py:482` — MY `None` GUARD ONLY MOVED THE CRASH.** Confirmed: `shell =
self._ensure_shell()` at 482, `shell.run(...)` at 490. A `None` return now raises `AttributeError`
on `.run` instead of on `.effective_policy` — one line later, same unhandled class. The guard I added
in round 4 made the code *look* handled.

**LOW-4 `confinement.py:1801` — `importlib.reload` DETERMINISTICALLY BREAKS THE METACLASS.**
⚡ Verified by execution, and the error is self-incriminating:
`TypeError: ConfinementProvider overrides or shadows ConfinementProvider.spawn_shell`.
During reload the module global still names the OLD class while the new root is being created, so
the guard compares the new class against a stale global and **accuses the root class of shadowing
itself.** Discovering the root through a module global is the defect.

**LOW-5 `tools.py:275`/`393` — `policy=` IS SILENTLY IGNORED WHEN `floor=` IS ALSO PASSED**, and the
direction is fail-open: a caller supplying a strict policy plus a permissive floor gets the
permissive one with no error. Confirmed by reading both constructors.

### ⚖ THE RULING: THE HOLD STANDS, AND THE CRITERION IS WHAT DECIDES IT

`4e967ca`: *"a confirmed defect in the three socket arms or the new refresh seam does not clear,
however narrow."* **MED-1 is an execution-reproduced defect in the refresh seam that makes a denied
socket reachable.** That is the criterion's own words. ⛔ The "No HIGH" headline is precisely the
shape that invites *"codex says no HIGH, ship it"* — **grading the summary line instead of the
findings, at the last gate.**

### ⛔ NOT FIXED TONIGHT, AND THE REASON IS A DESIGN CLAIM, NOT FATIGUE

**codex's remedy for MED-1 AND MED-2 is the same one: SCOPE FLOORS PER CONVERSATION.** It is not
asking for two patches — it is saying **`(entity_dir, workspace)` IS THE WRONG IDENTITY.** A
conversation has exactly one baseline by construction, so per-conversation scoping dissolves the
share-vs-refresh tension instead of balancing it, and removes the process-global registry with it.
That is a design change on the crown-jewels floor, it needs its own review round, and **the measured
rate on this changeset is ≥1 defect introduced per fix across five consecutive rounds.** Patching
two symptoms of a wrong identity at 19:30 is how round 6 gets written.
▶ **No exposure from waiting:** nothing is pushed, tagged or released. The known fail-open lives only
in an unpushed local tree.

---

## ✅ ROUND 5 — ALL FIVE CODEX FINDINGS FIXED BY ONE DESIGN CHANGE PLUS THREE SMALL ONES

Phill: *"go ahead and handle these?"* — so the 19:30 hold on building is lifted by the operator, and
the reason it was defensible to lift is that **the diagnosis arrived from outside**: codex gave the
SAME remedy for MED-1 and MED-2, which is what turned five patches into one identity fix.

### ▶ THE DESIGN CHANGE — the floor is keyed on the CONVERSATION, not on `(entity_dir, workspace)`
`ConversationState.id` is a required UUID, so the key always exists. **A conversation has exactly one
baseline by construction**, which is why this dissolves the tension instead of balancing it:
· the **staleness fail-open** (a later conversation inheriting an earlier one's permissive cred
  floor) cannot occur, because conversations no longer share an entry;
· the **two-hands divergence** cannot occur, because there is nothing to rebuild — **`baseline` is
  DELETED, not tuned.**
⚡ **THE THING TO CARRY: sharing requires ONE object and freshness requires REPLACING it, and under an
entity key those are the same variable pulling opposite ways.** Round 3 fixed the divergence by
sharing; round 4 fixed the staleness by replacing; each broke the other, and **both were
mutation-verified and passed 2,498 tests.** The "≥1 defect per fix across five rounds" number was
never carelessness — **it was oscillation between two poles of a constraint nobody had named.**
Registry is now a `WeakValueDictionary`, so lifecycle cleanup is structural rather than a TODO.

### ▶ MED-1 — `absorb()`, not assignment
The lock now lives **with the data it protects** instead of with one of its writers, and the write is
a **union merge** rather than a wholesale replace. A per-instance lock serializes nothing when the
object is shared. Kept even though per-conversation scoping leaves one bash mutator: *a floor that is
only correct because of who happens to call it is a contract, and this file's history says contracts
drift.* Pinned by a 40-thread concurrent test.

### ▶ MED-3 — the contract was the defect, not the call site
`spawn_shell` now **refuses a non-`SandboxedShell` at the source** (`ConfinementError`) instead of
tolerating a `None` sentinel. My round-4 guard had only MOVED the crash from `.effective_policy` to
`.run`, one line later, still `AttributeError` — which `__call__` does not convert to an in-band
refusal. ⚡ **A guard that relocates an unhandled crash is worse than no guard, because the code then
READS as handled.**

### ▶ LOW-4 — the root class is recognised from its BASES, never a module global
`importlib.reload` is verified working. The old check read `globals()["ConfinementProvider"]`, which
during reload still names the OLD class while the NEW root is being created — so it **accused the
root class of shadowing itself**. Same lesson as the rest of this file: ask the object, not the cache.

### ▶ LOW-5 — `policy=` and `floor=` are now EXACTLY ONE
Accepting both and silently preferring `floor` was fail-open.

### GATES
161 targeted tests pass · **four mutations, each firing exactly its own test** (replace-not-merge ·
entity-keying · both-args-allowed · non-shell-accepted) · ruff clean on every file I touched · mypy
clean, including two real type holes closed rather than silenced (`**kwargs` into
`dataclasses.replace` defeats per-field checking on the one object where a wrong field is a security
defect; and `effective_policy` is narrowed by an explicit fail-closed raise, not a cast).

### ⛔ THIS DOES NOT LIFT THE HOLD, AND BUILDING IT WAS NEVER GOING TO
Round 5 is **unreviewed**. codex's quota went to blackjack at 19:30 by Phill's own ordering. The tag
stays held until codex reviews THIS round. **Every round so far found its defects inside the previous
round's fix, and a design change is not exempt from that** — if anything it is the round most worth
reviewing.

### ⚠ SIXTH SHELL-QUOTING INSTANCE TODAY, MINE, AND THE PATTERN IS NOW LEGIBLE
A `python3 -c "..."` edit ate a backticked `` `__weakref__` `` out of a comment I was writing ABOUT
`__weakref__`, leaving `#  is REQUIRED in __slots__` — claim intact, subject deleted. **I use quoted
heredocs for big edits and drop to `-c "..."` for small ones, and small is exactly where it bites.**
Standing rule for this seat: **`python3 - <<'PY'` for anything containing prose, with no exception
for one-liners.**

### ⚖ THE ROUND-5 CRITERION, WRITTEN AT 19:56 BEFORE THE RUN

Artifact `d1f6720`. Scope `levain/firing/confinement.py` + `levain/firing/openhands/tools.py`,
`--seats codex` alone, 1500s. Bar unchanged from `4e967ca`: **an affirmative codex verdict with no
HIGH/CRITICAL in the socket arms, the refresh seam, or the new per-conversation floor.** A null,
timeout, kill, quota refusal or empty body is not an all-clear. Read the BODY; cross-check the store
by `metadata.repo` as a field.

⛔ **THE TRAP FOR THIS ROUND, AND IT IS SHARPER THAN THE PREVIOUS FOUR BECAUSE IT IS FLATTERING:**
***"codex told me to scope floors per conversation, I did exactly that, therefore codex will approve
it."*** **VOID.** codex's remedy was ONE SENTENCE. What I built is ~300 lines: a
`WeakValueDictionary` registry, `__weakref__` in `__slots__`, a lock moved onto the floor, a
four-field union merge, a changed `spawn_shell` contract, a metaclass root-detection rewrite, and
nine rewritten tests. **A remedy being correct says nothing about whether my rendering of it is.**
The gap between "the right idea" and "the right code" is exactly where every defect today lived.

⛔ **SECOND TRAP: "the constraint is dissolved, so this round is different in kind."** Also void. That
claim is my own diagnosis of my own change — the very thing I was wrong about five times today, and
the reason I only trusted the diagnosis at all was that it came from OUTSIDE. **Trusting my read of
my own fix is the failure mode, restated in the language of the insight that fixed it.**

⛔ **THIRD: "five rounds is enough."** Round count is not a measurement. This is the LARGEST change of
the day, made LATEST in the day, on the crown-jewels floor. Every round that introduced a defect
introduced it in a fix that was mutation-verified and passed its suite — including this one, so far.
▶ **If it returns findings, that is the bar working, not the day failing.**

---

## ⛔ ROUND 5's REVIEW — THREE HIGHs, AND THE PRE-REGISTERED TRAP FIRED EXACTLY

codex 20:00, 403s, 4 findings (3 HIGH + 1 MED), **all four confirmed, none refuted, two reproduced by
me independently before fixing.** The criterion at `e4a0963` had named the trap before the run:
*"codex told me to scope floors per conversation, I did exactly that, therefore codex will approve
it."* **Every one of the four is in code I wrote in round 5.**

### ⛔⛔ HIGH-1 — **MY DESIGN PREMISE WAS FALSE, AND THIS IS THE FINDING OF THE ROUND**
Round 5 keyed the floor on `ConversationState.id` on the premise that *"a conversation has exactly
one baseline by construction."* ⛔ `ConversationState.create()` is documented as **"Create a new
conversation state OR RESUME FROM PERSISTENCE"**, and OpenHands permits a resume under a different
drive mode and even a different workspace. Start `U` interactively (creds ALLOWED), keep the old
agent alive so its entry stays live, resume `U` unattended → the resumed agent never calls
`policy_for_conv_state()` and inherits the permissive floor. **The identical fail-open as the entity
key, through a new door.**
⚡ **I HAD NOT DISSOLVED THE CONSTRAINT. I HAD MOVED IT TO A THIRD KEY THAT ALSO DOES NOT IDENTIFY A
RUNTIME BASELINE.** A UUID names the LOGICAL conversation; a runtime baseline belongs to the RUNTIME.
▶ Fixed: keyed on the `ConversationState` **INSTANCE** (a resume constructs a new object; the two
tool factories of one run receive the same one). It is weakref-able but NOT hashable, so
`WeakKeyDictionary` is unavailable — `id()` plus a `weakref.finalize` gives the same semantics, and
the finalizer is load-bearing because it evicts the entry before the `id()` can be recycled.

### ⛔ HIGH-2 — the fail-closed check was **fail-once/open-next**
`_ensure_shell` committed the shell to `self._shell` BEFORE validating `effective_policy`. The first
command was refused while the LIVE shell stayed cached; the next saw a non-closed `_shell`, skipped
the branch entirely, and executed through the unverified shell. **Worse than no check: exactly one
refusal that reads as the guard working, then silence.** Fixed by validating + absorbing a LOCAL
candidate and closing it on failure, then committing.

### ⛔ HIGH-3 — the metaclass fix had the defect it replaced. **Reproduced by me before fixing.**
Retain `OldProvider`, reload the module, subclass `OldProvider`: the old base is not an instance of
the NEW `_ProviderMeta`, `parents` came out EMPTY, the override was ACCEPTED.
⚡ **Third bypass of this guard, and all three have ONE shape: it asked a NAME for the identity of a
CLASS.** `cls.__dict__` missed the mixin; the module global missed the reload. Now `mcls` (the
executing metaclass) plus the static MRO owner — facts about the object being created.

### ⚠ MED-4 — `staticmethod(ConfinementProvider.spawn_shell)` passed a dynamic `getattr` compare and
broke binding at runtime (`TypeError`, which `__call__` does not convert to an in-band refusal).
Fixed by the static-MRO-owner check.
⚠ **AND THE DESCRIPTOR CHECK I ADDED FOR IT IS REDUNDANT — MEASURED, NOT ASSUMED.** Removing
`isinstance(raw, types.FunctionType)` leaves the suite green, because the owner test already refuses
it. I had implied it was load-bearing. Kept as a belt (free, fails closed, stops being redundant if
anyone relaxes the owner test) and the redundancy is written down beside it, same as the ssh anchor.

## ⛔⛔ MY OWN TEST BROKE TEN TESTS IN THREE OTHER FILES — AND PASSED IN ISOLATION

The HIGH-3 regression test called `importlib.reload` on the REAL `levain.firing.confinement`. A
reload rebinds the module's names to NEW class objects while every other module that did
`from ... import ConfinementError` at import time still holds the OLD ones — so `except
ConfinementError` stopped catching and `pytest.raises` stopped matching, across
`test_run.py`, `test_firing_gate.py` and `test_firing_tools.py`.
⚠ My "cleanup" was a second `importlib.reload` in a `finally`, which restores the NAME and not the
IDENTITY — it produces a THIRD set of classes matching neither. **An identity question answered with
a name, which is the exact defect the test exists for.**
▶ Confirmed by deselecting that one test: **2502 passed, 0 failed.** One test, ten casualties.

### ⚡⚡ AND WRITING IT TOOK THREE ATTEMPTS, WITH THE SAME OSCILLATION AS THE FLOOR KEY
| attempt | reproduces the defect? | pollutes? |
|---|---|---|
| reload the real module | ✅ | ⛔ 10 tests |
| two independent copies | ⛔ **no** — MUT P2 passed | 2 tests |
| one copy, `exec_module` twice, popped from `sys.modules` | ✅ | ✅ none |

**Two properties in tension — reproduce the reload mismatch vs. leave global state alone — and I
satisfied each at the cost of the other before finding the form that holds both.** Identical
signature to the floor key, in a different layer, inside the same hour. ⚡ **So the pattern is not a
fact about `_SharedFloor`: it is how a two-constraint problem gets approached — fix the pole that is
currently broken, ship, discover the other pole broke.** The tell is the same too: the clean-looking
middle attempt **passed its own mutation check**, exactly as rounds 3 and 4 each did.

### GATES
**2,503 passed / 1 skipped / ZERO failures** · four mutations each firing its own test · one mutation
that did NOT fire, recorded rather than left standing · ruff + mypy clean.

### ⚖ THE ROUND-6 CRITERION — A DEFECT HUNT, EXPLICITLY **NOT** A RELEASE GATE

Artifact `8c59a29`. Seats **`complement,glm`** — deliberately NOT codex. Phill: *"maybe instead of
codex for round 6 we try glm 5.2 or 5.3? get the viewpoint of another lineage?"*

▶ **THE DISTINCTION THIS ROUND RESTS ON, AND I HAD BEEN CONFLATING THE TWO:** *"which review finds
the most defects"* and *"which review lifts the hold"* are different questions. **Only codex lifts
the hold** — that is Phill's bar and not a seat's to relax. **Nothing requires codex to go NEXT.**
⛔ **SO THIS ROUND CANNOT CLEAR ANYTHING, BY CONSTRUCTION.** A clean `complement,glm` pass is not an
all-clear and must never be reported as one. Its only job is to find defects before codex's slot is
spent.

▶ **WHY A DIFFERENT LINEAGE NOW, and it is stronger than generic decorrelation:** codex has reviewed
this changeset FOUR times, and **rounds 4, 5 and 6 are all codex grading implementations of its own
prescriptions.** Round 5's HIGH-1 (codex catching that its own "scope per conversation" remedy had
been rendered on a false premise) was excellent — and a reviewer marking its own homework three times
running is exactly the correlated-lens problem the mesh exists to break. **A different lineage has no
stake in the prescription.**
▶ Measured on THIS file, not assumed: **complement found the `register()` bypass NOBODY else did**
(plus two more HIGHs, 9,022 chars, in the one round codex was blocked); **glm-5.2 found the two-hands
divergence in round 2 and is 4-for-4 non-errored on levain today.** ⛔ NOT `glm,glm-5.3` — one vendor
lineage, so their agreement is not corroboration.

⛔ **THE TRAP FOR THIS ROUND:** *"complement and glm came back clean, and codex passed round 5's
predecessor, so ship."* **VOID TWICE OVER** — a non-codex pass cannot clear the bar, and every round
today found its defects inside the previous round's fix.
⚠ Second trap, specific to the seat swap: **"a fresh lineage found less, so the code is better."**
A different lineage finding FEWER things may mean the code improved, or may mean it is blind where
codex is sighted. **Fewer findings from a different instrument is not a measurement of quality**, and
the only thing that settles it is codex afterwards — which is the plan regardless.

---

## ⚡ ROUND 6 — THE SEAT SWAP PAID. THREE FINDINGS, ONE CONVERGENT ACROSS LINEAGES.

Phill: *"maybe instead of codex for round 6 we try glm 5.2 or 5.3? get the viewpoint of another
lineage?"* — `complement,glm`, no codex. **complement 328.7s / glm-5.2 81.9s (repo-read, 1 file,
contract=OK), both non-errored.** All three confirmed, none refuted, two reproduced by execution.
⚖ **AND THE TRAP I PRE-REGISTERED DID NOT FIRE:** I named *"a fresh lineage found less, so the code
is better"* as void before dispatch. **It found MORE, including a HIGH.** The swap was not a softer
gate.

### ⛔ HIGH (complement) — REENTRANT-LOCK DEADLOCK VIA `weakref.finalize`. **MEASURED.**
`_FLOORS_LOCK` was a plain `Lock` and `_drop_floor` (a finalizer callback) takes it. A finalizer for
a cyclically-collected object fires SYNCHRONOUSLY on whatever thread crossed the GC threshold — which
can be the thread already inside `with _FLOORS_LOCK:`, since `policy_for_conv_state()` allocates
heavily while the lock is held. Self-deadlock, permanent, **silent**, freezing `floor_for_conv_state`
for every conversation.
▶ **Proved by timing, not by inference:** mutated to a plain `Lock` the test is killed by a 30s perl
alarm — `rc=142, elapsed=30s, 0 bytes of output`. With `RLock`: `rc=0, 4s`. ⚠ I first read "no
output" as confirmation, which is the same ambiguity this session has been catching all night;
the timing is what actually settles it.

### ⛔ CONVERGENT — complement MED + glm-5.2 HIGH, INDEPENDENT LINEAGES, SAME DEFECT
`root` derived from `parents[0]` is a POSITIONAL choice: `class SSHProvider(SomeMixin,
ConfinementProvider)`, where `SomeMixin` merely shares the metaclass, gave `root=None` while `owner`
resolved to `ConfinementProvider` — **`TypeError` at class-definition time for a class that overrides
nothing.** A false-positive fail-closed that breaks module import for a valid composition.

### ⛔ MED (complement) — a failing `candidate.close()` MASKED the fail-closed reason
An I/O error tearing down the subprocess would propagate INSTEAD of the `ConfinementError`, so a
caller matching on type — or a log printing only the outermost exception — sees "close failed"
rather than "no effective policy". The leak was still prevented; **the FIDELITY OF THE SIGNAL** was
not, which is the whole point of that rewrite. Now wrapped, with `_log.exception`.

## ⛔⛔ AND FIXING THE CONVERGENT ONE BROKE THE MIXIN CASE — FOURTH OSCILLATION IN THIS ONE GUARD

complement's recommended `cls.__mro__[1:]` **passes its own new test and re-breaks the plain-mixin
case v1 was written for**, because a shadowing mixin is itself in that MRO, so `root == owner` and it
matches. I caught it only because the earlier test was still in the suite.
▶ **THE FOUR VERSIONS, AND EVERY ONE ASKED A PROXY FOR "WHO OWNS `spawn_shell`":**
| v | mechanism | proxy | broke on |
|---|---|---|---|
| 1 | `cls.__dict__` | a NAME | a mixin shadowing it |
| 2 | the module global | a NAME | a reload |
| 3 | `parents[0]` | a POSITION | multiple inheritance (false positive, breaks import) |
| 4 | `cls.__mro__[1:]` | a WALK | the mixin AGAIN |
| **5** | **`__levain_provider_root__`, an inherited OBJECT REFERENCE** | **none** | **nothing — all 7 cases pass** |

⚡⚡ **THE METHOD CHANGE IS THE FINDING, NOT THE FIX.** Four rewrites each fixed the pole that was
broken and each passed its own new test. **A single-case test cannot see an oscillation.** So before
writing v5 I stopped and enumerated ALL SEVEN cases as a table first, then made them pass
simultaneously — and the table is now IN THE SUITE as a `parametrize`, so every future change is
graded on every case rather than on the one that prompted it.
▶ **Third instance tonight of the same meta-pattern** (the floor key across three review rounds; the
reload test across three attempts; this guard across four). **The remedy is not more care — it is
enumerate the constraints before satisfying any of them.**

### ⚖ THE ROUND-7 CODEX CRITERION, WRITTEN BEFORE THE RUN

Artifact `a6a26e5`. `--seats codex` alone, 1500s. Bar unchanged (`4e967ca`): an affirmative codex
verdict, no HIGH/CRITICAL in the socket arms, the refresh seam, the per-conversation floor, or the
provider guard. Read the BODY; cross-check the store by `metadata.repo` as a field.

⛔ **THE TRAP, AND IT IS THE STRONGEST-FLAVOURED ONE YET:** *"complement and glm just went over this
exact code and I fixed everything they found, so codex is a formality."* **VOID.** They are different
lineages with different blind spots — that is the entire reason for running them, and it cuts both
ways. codex has caught concurrency and state-machine defects on this file that neither of them
raised, four times.
⚠ Second: *"the guard now passes all seven cases, so the guard is done."* Seven cases is what I could
ENUMERATE. An eighth is exactly what an outside lineage is for.
⛔ Third, unchanged and permanent: **a clean round is evidence about the code that was reviewed,
never about code written afterwards to satisfy it.**
