# levain — DIOGENES 2026-09-04 TRIAGE

**Generated across the 2026-09-04 session, by the levain seat, not by the reviewer.**

> ⚠ **NO HEAD SHA, DELIBERATELY** — see the same note at the top of `TRIAGE_2026-09-03.md`. A sha
> stamped here goes stale on the next commit to this file and is then precise enough to be trusted
> and wrong. Where a verdict needed a commit, the commit is named *in that verdict*.

> ⛔ **ONE BACKLOG IN THIS FILE, AND IT IS THE `STATUS` COLUMN BELOW.** Yesterday's triage published
> two — `ALL 22 CLOSED` and, twenty-nine lines later, `STILL OPEN (18)` listing the same findings,
> with the stale half LAST. That was tonight's HIGH. This file has exactly one table and no appendix
> that restates it.

## ▶ THE FINDING THE DISPATCH LED WITH IS NOT IN THE 0.4.4 CUT

**Measured, not read off the report:**

```
git show main:levain/daemon.py             | grep -c 'class SystemdUserProvider'   -> 0
git show main:levain/firing/confinement.py | grep -c '_bwrap_argv'                 -> 0
```

`main` carries **no systemd provider and no bwrap floor**. Both are K4c, which is the six unpushed
commits on `k4c-linux`. So the twelve findings split **by branch**, which nothing in the report or
the dispatch says — and five of them cannot be fixed on the frozen branch at all.

**Consequence for the day's ruled order:** "Diogenes findings → socket fix → cut" reads as though the
findings gate the tag. Six of the seven that actually gate it are markdown; one is a comment. The
frightening HIGH is real and is **not standing in front of the release**.

## THE TWELVE

| # | sev | subject | branch | status |
|---|-----|---------|--------|--------|
| 1 | HIGH | `TRIAGE_2026-09-03.md` two contradictory backlogs | main | ✅ CLOSED `f4fac94` |
| 2 | MED | `TRIAGE_2026-09-03.md` masthead sha 20 commits behind | main | ✅ CLOSED `f4fac94` |
| 3 | LOW | `TRIAGE_2026-09-03.md` doubled heading | main | ✅ CLOSED `f4fac94` |
| 4 | LOW | `TRIAGE_2026-09-03.md` denominator 36 vs 40 | main | ✅ CLOSED `f4fac94` — stated as UNRECONCILED, not bridged |
| 5 | LOW | `test_compat_integration.py` stale 0.9.6 cap comment | main | ✅ CLOSED `f439fda` |
| 6 | MED | `web_server.py` hardening duplicated into 3 copies | main | ⏸ **FILED FORWARD — see below** |
| 7 | MED | `scripts/install_hooks.sh` untracked | main | ⚖ **NOT A DEFECT — a HELD decision. See below.** |
| 8 | HIGH | `daemon.py:739` systemd ExecStart unit injection | k4c-linux | ✅ CLOSED `b16812e` |
| 9 | MED | `daemon.py:863` install rollback leaves an orphan timer | k4c-linux | ✅ CLOSED `f9977b0` |
| 10 | MED | `confinement.py:1791` deny_files under ~/.ssh gets no bwrap mount | k4c-linux | ✅ CLOSED `29eb476` |
| 11 | MED | `CHANGELOG.md:18` Ubuntu paragraph is the pre-correction draft | k4c-linux | ✅ CLOSED `ee7c6d3` |
| 12 | LOW | `daemon.py:1001` "macOS ships first" in a live raise | k4c-linux | ✅ CLOSED `ee7c6d3` |

**Plus one the reviewer did not file, found by running the gates:** `main` **does not pass ruff.**
`F401` in `doctor.py` — `_sha256_file` imported but unused, dead since `d7cee34`, which deleted its
only call site. The project's own record asserts "ruff + mypy clean" of the release; that was false
for the artifact as frozen. ✅ CLOSED `30126cf`, together with the twelve-line comment that still
explained why the dead import existed.

### ⚖ #7 IS NOT AN OVERSIGHT, AND THE FINDING'S PREMISE IS WRONG

Diogenes' *mechanism* is right — `core.hooksPath` points at a directory git does not carry, so the
gate protects exactly one clone. His *premise*, that this is an omission to be closed with
`git add`, is not: `projects/levain/next.md` carries a block headed **"⚖ AWAITING PHILL — A NINTH
GATE, BUILT AND ACTIVE BUT DELIBERATELY UNCOMMITTED"**, which says in terms *do not `git add` them
without reading this*. It is a held decision awaiting Phill. **Routed to the fan-in as an agenda
item; not acted on.**

### ⏸ #6 IS FILED FORWARD ON PURPOSE

The diagnosis is correct and the remedy — hoist the shared request preamble into one module both
servers import — is right. It is also a **refactor of two HTTP request paths inside a frozen release
window**, which is the same-day-code-shipped-same-day shape the freeze exists to prevent. The
duplication is stable and behaviourally correct today (the reviewer verified both servers behave
identically); the class is armed, not firing. **It lands after the tag, not before it.**

## ⛔ THE SOCKET FIX CORRECTED THE RULING'S MECHANISM

`spore-725` is closed on `main` at `1d7f707`, and **the fix specified in the spore does not work.**
"Add the sockets to the floor denylist" means a `deny_files` entry, and a seatbelt
`file-read* file-write*` rule does not block `connect()` to a unix socket — measured, with both
docker spellings in `deny_files` and the attack still returning the jewel. The working fix is three
denies, not one; the full measurement and the spelling rule (RESOLVED for the connect arm, BOTH for
the write arm) are in `spore-725`'s own notes and in the commit.

**macOS only, because 0.4.4 is macOS only.** No Linux host was reachable from this session, so
nothing about `bwrap` has been run and nothing about it is claimed.

## ⚠ THE COVERAGE GAP IS THE SUBJECT OF THE WINDOW

Diogenes has **no Linux host**. All 8 `linux_live` tests and both `systemd-analyze verify` tests
self-skipped, so findings 8–12 are reasoned from rendered output and from driving the pure
functions — never from a live namespace or a loaded unit. The decisive question he names and does
not answer: **does systemd 255 refuse the injected unit at load, or run the truncated `ExecStart`?**
Both are bad and one is silent. That question is not answerable by reading the renderer, and it is
the highest-value thing available on the K4c branch.

## ⚠ A RECORD CORRECTION ABOUT THE SUITE

The project record says the two live-model condenser flakes "both pass in isolation". Measured today:
`test_reanchor_reaches_model_on_recovery_turn` fails **in isolation** roughly one run in three
(2 pass / 1 fail), and passes at clean HEAD too — so it is a genuine non-deterministic flake, not a
regression, and *not* what the record claims. A gauge that is wrong one time in three, described as
reliable, is how a real regression gets waved through as "the known flake".

---

## ⚡ FINDING #8's UNDETERMINED QUESTION IS ANSWERED BY EXECUTION (2026-09-04)

Diogenes: *"NOT DETERMINED, and I am not asserting it: whether systemd 255 refuses the whole unit at
load or ignores the orphan line and runs the truncated ExecStart. Both outcomes are bad and one is
silent."* Run against **real systemd 255** (`k4c-systemd:latest`, `systemd 255 (255.4-1ubuntu8.17)`),
on a unit rendered by the **real `build_seat_spec` + `render_unit`** from `k4c-linux` at `0e42e18`:

```
/etc/systemd/system/…service:7: Unbalanced quoting, ignoring: "… --task 'review the diff"
…service: Unit configuration has fatal error, unit will not be started.
Unit …service has a bad unit file setting.        exit=1
```

**systemd REFUSES the service.** It does NOT run a truncated `ExecStart`, so an unattended agent does
NOT run stripped of `--unattended` / `--max-seconds` / `--consolidate`. Three task shapes chosen to
try to produce a *balanced* first line — plain multi-line, a quote before the newline, a leading
newline — all three fatal, exit 1. Structural reason: `shlex.quote` closes its quote at the END of
the argument and the governance flags follow it, so every line but the last sits inside an open
quote.

### ⛔ BUT THE SEVERITY COMES BACK BY A DIFFERENT DOOR, AND THIS HALF IS THE REAL ONE

`install()` enables **`<label>.timer`** for a periodic seat, never the service. Measured, same host,
same units, exit codes captured without a pipe in the way:

| verified | exit | output |
|---|---|---|
| `…seat.service` | **1** | fatal error · bad unit file setting |
| `…seat.timer`   | **0** | *prints the service's fatal error and exits 0 anyway* |

**The timer is valid; the service it points at is not; verifying the timer sees the fatal error,
prints it, and succeeds.** So a periodic `install-seat` — which is the whole point of a scheduled
seat — has nothing in its path that fails: the timer enables, install reports success, and the seat
dies at its first fire with the failure visible only in the journal.
`absence_of_signal_rendered_as_health`, and the check's subject (the timer) is not the defect's
subject (the service), which is tonight's class yet again.

↳ And the error string in `install()`'s non-rollback branch — *"the unit is KEPT at {service_path}
(it is valid)"* — is **false in exactly this case**. The unit is not valid.

⚠ **NOT EXECUTED, AND I AM NOT ASSERTING IT:** I did not run `systemctl --user enable --now` against
a live init — that needs a real host, not a container. The exit-code asymmetry above is measured; the
inference from it to "install reports success" is REASONED. It is the next thing to run on argushub.

▶ **THE FIX IS UNCHANGED AND ONE IS ADDED:** run the control-character refusal over every value
reaching a directive, not just `Environment=`; and have `install-seat` verify the **SERVICE**, not
only enable the timer.

## ⛔ THE LINUX TEST PATH EXISTS ON THIS MACHINE AND IS NOT IN THE REPO

Diogenes reported *"I HAVE NO LINUX HOST"* and self-skipped all 8 `linux_live` tests and both
`systemd-analyze verify` tests. He was right about himself and the capability was one `docker run`
away: `docker images` shows **k4c-systemd · k4c-sysdtest · k4c-bwrap · k4c-live · k4c-pytest**, built
16–17 hours ago for this exact work, and `k4c-systemd` carries systemd 255.

**None of it is committed.** No Dockerfile, no compose file, no make target, and no doc mentions
them. So the systemd tests self-skip for every reviewer, every fresh clone and CI, and the capability
dies with the images on this laptop.

⚡ **SAME SHAPE AS THE UNTRACKED PRE-PUSH HOOK — a capability that serves exactly one clone because
it was never committed — TWICE IN THIS REPO IN ONE DAY, found by two different routes.**

⚠ Honest bound: only the **systemd** half is recoverable this way. `bwrap 0.9.0` in `k4c-bwrap`
refuses — *"No permissions to create new namespace"* — as an unprivileged container user, so the 8
`linux_live` tests genuinely need a real host (argushub). No bwrap result is claimed here and none
was run.

---

## ✅ THE REAL-INSTALL `doctor` GATE IS RUN, AND IT FOUND A FOURTH SPELLING (2026-09-04)

Cut procedure step 3 — *"RE-RUN THE REAL-INSTALL `doctor` CHECK ON THE BUILT WHEEL, BOTH ADAPTERS …
Falsify against a control: tilde-wire the hooks and confirm doctor FAILS."* Run against
`levain-0.4.4.dev0-py3-none-any.whl` built with `uv build`, installed into a fresh venv under a
fully isolated `$HOME`, package importing from site-packages and not the source tree.

| case | doctor |
|---|---|
| `claude-code` real install | **exit 0** |
| `codex` real install | **exit 0** |
| control: `"python3" "~/lev/…/session_start.py"` at user level | **exit 1**, wiring reported ✅ |
| control: `"python3" "$HOME/lev/…/session_start.py"` | **exit 1**, wiring reported ✅ |
| control: `"python3" "<absolute>/…/session_start.py"` | **exit 1**, wiring reported ✅ |
| no user-level file | **exit 0** ✅ |

**The 0.4.4 dark-install fix holds and is falsified, not merely green.**

### ⛔ BUT THERE IS A FOURTH WAY TO WRITE IT, AND IT IS STILL DARK

`_user_entry_targets` iterates `tokens[1:]` — it skips the first token as the interpreter, which is
correct for the shape `levain init` writes (`"<python>" "<script>"`). A **single-token** command —
the bare hook path, with no interpreter — therefore has *no* tokens examined at all:

```
{"command": "~/lev/activation/hooks/session_start.py"}     ->  doctor exit=0, nothing reported
```

The CHANGELOG for this release says *"doctor reported green on two of the three ways to write
user-level wiring."* There is a fourth, and it is the same class one level up: the fix enumerated
spellings of the **path** and left the **command shape** unexamined.
`guard_scoped_by_symptom_misses_the_class`.

⚠ **AND IT IS A DARK INSTALL OF A DIFFERENT FLAVOUR, WHICH IS WHY IT IS WORTH WRITING DOWN
CAREFULLY.** The installed hooks carry a shebang (`#!/usr/bin/env python3`) but ship **`-rw-r--r--`,
not executable** — so a bare-path wiring does not silently run from the wrong scope, it does not run
**at all**. The operator gets an activation layer that is entirely off, and `doctor` says green. That
is the same failure the whole dark-install line of work exists to end, arriving by the one door the
fix did not enumerate.

### ⏸ NOT FIXED BEFORE THE TAG, DELIBERATELY

It is a NEW finding, not a regression: nothing that worked has stopped working. And this detector has
now turned `doctor` red for an entire operator class **three times** — the code says so in its own
docstring — so editing it inside a frozen release window, to cover a wiring that is itself
non-functional, is the trade the freeze exists to refuse. **It lands after the tag.**

↳ Second-order, and cheaper: the hooks carry a shebang while not being executable. Either the
shebang is decorative or the mode is wrong; the two disagree, and it is that disagreement which makes
the bare-path wiring look reasonable to an operator in the first place.

---

## ⛔ THE COMPAT GATE AUDITS ONE OF THE TWO ANNEALS A SINGLE INSTALL WIRES (2026-09-04)

Surfaced by running `doctor --invoke` against the real wheel install, not by reading anything.

A `levain init` install wires anneal **twice, by two different resolution rules**:

| surface | how it resolves | what it got here |
|---|---|---|
| `.mcp.json` → the memory server the harness talks to | the `anneal-memory` executable **on PATH** | `~/.local/bin/anneal-memory` — **0.9.10.dev0** |
| `.claude/settings.json` hooks → per-turn recall + capture | the **interpreter levain runs under**, importing `anneal_memory` | the venv — **0.9.9** |

One install, one store, **two anneal versions operating on it**. And `doctor`'s compat block reports
only the first: `[OK] anneal-memory CLI: /Users/…/.local/bin/anneal-memory (anneal-memory
0.9.10.dev0)`, with every downstream verdict — the AHEAD advisory, the migrate-proposal count, the
pip-floor check — computed about **the PATH binary**, and nothing at all said about the version the
hooks import.

**The check's subject is the MCP server; the install's behaviour is produced by both.** Same shape as
the `$LEVAIN_DRIVE_MODE` split-brain this codebase already fixed once, where the file editor and the
bash seatbelt would have resolved the floor from different values — *"one policy, two enforcers,
disagreeing."*

⚠ **HONEST BOUND — I MADE THIS VISIBLE AND I DID NOT MANUFACTURE IT.** My isolation was `$HOME`-only,
not `PATH`-only, so `init` found the operator's global `anneal-memory` while levain lived in a fresh
venv. That is not an exotic setup: **pipx/venv levain plus a globally installed anneal is the
ordinary way both of these get installed**, and any version skew between them is invisible to
`doctor` on the hook side. The bound worth stating is that I have not reproduced it on a machine with
only one anneal, because there the two resolve to the same thing and nothing is wrong.

⏸ **FILED FORWARD, NOT FIXED.** Pre-existing, not in the 0.4.4 diff, not a regression, and it needs a
decision rather than a patch — either `init` should wire the MCP command to the interpreter levain
runs under (making one anneal authoritative), or `doctor` should report BOTH and flag a skew. Picking
between those inside a frozen release window is exactly the trade the freeze refuses.

---

## ✅ THE `allow_container_sockets` OPT-OUT IS VERIFIED LIVE, BOTH ARMS (2026-09-04)

A gap in my own L4: the DENY was proven end-to-end and the OPT-OUT was not. An opt-out that
half-lifts the floor — restoring the connect but leaving the socket write-denied — would hand the
operator who needs containers a socket they cannot manage.

| | |
|---|---|
| jewel still denied with the opt-out ON | **DENIED** — the rest of the floor is unaffected |
| docker reachable again | **29.5.2** — connect arm lifted |
| socket renameable again | **yes** — write arm lifted |

⛔ **AND IT COST A ~40-SECOND DOCKER OUTAGE ON THE OPERATOR'S MACHINE, WHICH IS THE LESSON.** The
third arm is `mv <the real socket>`, and with the guard off it succeeds. Earlier the same day I had
deliberately stood up my *own* unix-socket server to test the rename arm without touching the
operator's daemon, and wrote that reasoning down — then pointed the identical command at the live
socket when testing the mirror case. Restored immediately (`mv` back; original inode timestamp
intact, `/var/run` symlink unaffected, `docker version` 29.5.2).

**The precaution was taken, recorded, and not carried across to the symmetric test** — a guard scoped
to the case where the symptom first appeared. ▶ Standing rule for this repo: **testing a security
opt-out means running the attack with the guard OFF, and that attack is destructive by construction
— it needs a disposable target EVERY time, not just the first time.**

---

## ⛔ 0.4.4 IS CUT-READY AND HELD FOR ONE SEAT — CODEX QUOTA EXHAUSTED 2026-09-04 ~10:05 AM

`deep_review.py --diff 1e44342 --seats complement,codex,glm` ran, then a codex-only re-run at a
larger budget. Both times codex returned nothing; the second said why:

```
[codex no output after 898s (rc=1): "You've hit your usage limit … try again at 2:15 PM."
 tokens used 189,479]
⚠ codex PRODUCED NO REVIEW — this seat contributed NOTHING to the pass above.
```

**This is `spore-744`'s measurement, on the day it was ruled.** Phill asked to run without a governor
precisely to learn IF and WHEN the limit lands. It lands on a nine-repo parallel day at ~10:05 AM,
and this repo's two runs were a large share of the spend. Both were correct under that ruling; the
cost is recorded, not apologised for.

⚠ **AN EXHAUSTED CODEX LOOKS LIKE A COMPLETED RUN** — `deep_review.py` exits 0 and the failure is a
line inside the body. A seat that reads the exit code and not the output records a review that never
happened. The script defends against this correctly: it refuses to record coverage and says *"read it
as a gap."*

### WHAT DID RUN, AND WHAT IT FOUND

| seat | verdict |
|---|---|
| complement | 1 HIGH · 1 MEDIUM · 1 LOW — triaged below |
| glm (glm-5.2, repo-read, 1 file opened) | no HIGH; 1 LOW — accepted and fixed further than proposed |
| **codex** | **NOTHING — quota. This pass is breadth-only.** |

· **HIGH refuted as filed, kept as substance.** complement said `_bwrap_argv` never consumes
  `deny_sockets`. False here: `grep -c _bwrap_argv` is **0** on main and the provider roster is
  exactly `{SeatbeltProvider}` — it inferred a Linux enforcer from the Diogenes report it was handed.
  The danger it names arrives at the **K4c merge**, so it became
  `test_every_confinement_provider_must_consume_deny_sockets`, mutation-verified.
· **MEDIUM refuted two ways.** A general `network-outbound` allow after the deny would shadow it —
  but `(allow default)` is line 6, the deny is line 22, the only later allows are `file-read*`, and
  the real docker connect is already refused end-to-end.
· **LOW fixed** — the live test's `accept()`/`close()` race.
· **glm LOW confirmed on disk** (`deny_sockets ⊆ deny_write_files` is True) and fixed further: glm
  proposed leaving a `.sock` NAME test as the discriminator; the message is now decided by
  MEMBERSHIP, in the module whose entire ssh design exists because name-based reasoning is what it
  refuses.

### ▶ THE HOLD, AND IT IS A RULING NOT A PREFERENCE

**No tag before codex runs.** `spore-725`'s own note requires targeted L3 before the tag and never
shipping the fix unreviewed; codex is the non-replaceable seat for code and state-machine work; and
this is a **crown-jewels confinement change** — the single worst thing in this repo to ship on a
breadth-only pass. Levain carries **NO CLOCK** by standing ruling, so four hours is not a trade worth
making.

**Everything else is done:** 2466 passed / 1 skipped · ruff clean on `levain/` · mypy clean but for
the 4 known template false-positives · the real-install `doctor` gate run on the built wheel for both
adapters and falsified against three controls · `## [Unreleased]` written and reconciled against
`git log v0.4.3..HEAD -- levain/`.

⚖ **The tag was always Phill's call, not the seat's.** He can lift this hold in a sentence.

---

## ⛔ A RELEASE GATE ANSWERS DIFFERENTLY DEPENDING ON WHERE YOU RUN IT (2026-09-04)

`pip_floor_verdict()` is the release gate that *"catches the manifest constant and the `pyproject`
pin drifting apart."* Measured from two checkouts of **byte-identical source** — `git diff main
k4c-linux` on `pyproject.toml`, `levain/manifest.py` and `tests/test_manifest.py` is **empty**:

| run from | `pip_floor()` | verdict |
|---|---|---|
| `~/Briefcase/levain` | **0.9.8** | `in_sync` ✅ |
| a `git worktree` of the same repo | **0.9.7** | `drift` ❌ |

**The gate compares a SOURCE constant (`KNOWN_GOOD_ANNEAL`) against a BUILD ARTIFACT.** `pip_floor()`
resolves through `importlib.metadata`, so its answer depends on `sys.path` and on which
`*.egg-info` / `*.dist-info` happens to be found — the main checkout carries a fresh
`levain.egg-info` (`anneal-memory<0.10,>=0.9.8`, matching `pyproject.toml`); the worktree has none,
because egg-info is untracked, and resolves to something stale.

⚡ **AND THE FALSE-GREEN DIRECTION IS THE ONE THAT MATTERS, ON THE EXACT OPERATION THE GATE GUARDS.**
Cut procedure **step 7** bumps `KNOWN_GOOD_ANNEAL` *and* the pip floor to 0.9.9 in lockstep. If the
`pyproject` floor moves and the metadata is not regenerated, the gate compares the constant against
the **old** floor and can report `in_sync` while the real pin disagrees. Its own docstring says it
reads *"this levain's declared dependencies"* and *"for a real pip wheel this is exactly what pip
will resolve against"* — true for a wheel, **false for the source checkout the gate actually runs
in**, which is tonight's class stated in the docstring itself.

⏸ **FILED FORWARD — NOT FIXED IN THE FROZEN WINDOW.** Pre-existing, not a regression, and the fix is
a design decision about a release gate (read `pyproject.toml` as the authority, or compare both and
fail on disagreement) rather than a patch. ⛔ **BUT THE NEXT SESSION MUST READ THIS BEFORE DOING STEP
7**, because that is precisely when the gate can lie.

### ⚡ SHARPER THAN THAT: THE FILE WROTE DOWN THE HAZARD AND THEN BUILT A GATE ON IT

Raised by `0904+3` via the fan-in and **verified verbatim in source here before recording it**.
`manifest.py`'s `declared_set()` docstring says:

> ``levain`` is the SOURCE ``__version__`` (not ``importlib.metadata`` — an editable/source install's
> dist metadata can lag the moved source; **the constant is the truth**).

Three hundred lines later `pip_floor()` reads `importlib.metadata` — **the mechanism this file
explicitly rejected** — and compares its answer against `KNOWN_GOOD_ANNEAL`, **that very constant.**
The file states the exact hazard, applies the correct rule in one function, and builds a release gate
on the mechanism it had just refused. `.gitignore:2` is `*.egg-info/`, which is why a worktree
diverges from the primary checkout at all.

↳ It surfaced only because the work happened in a **worktree** rather than the primary checkout —
the same shape as the day's other findings, where a different vantage point saw what the usual one
structurally could not. A gate that is green in the place you always run it is not thereby green.


---

## ✅ ALL TWELVE ARE CLOSED OR RULED — THE FIVE K4c ONES TOO (2026-09-04, during the codex hold)

The tag is held for codex until 2:15 PM, so step 4 (K4c) was worked instead. It is a different
branch and touches nothing in the release.

**`k4c-linux`, five commits** — `7387eec` `b16812e` `ee7c6d3` `f9977b0` `29eb476`:

| finding | closed by | how it was verified |
|---|---|---|
| **HIGH** systemd ExecStart injection | `b16812e` | 78 passed on **real systemd 255**; mutation-verified |
| **MED** install rollback orphan timer | `f9977b0` | unit-tested; ⚠ the live `enable --now` failure is NOT run |
| **MED** bwrap deny_files skip | `29eb476` | argv falsified against a control; mutation-verified |
| **MED** CHANGELOG Ubuntu paragraph | `ee7c6d3` | checked against README + the runtime remedy string |
| **LOW** "macOS ships first" raise | `ee7c6d3` | class re-swept across `levain/`, `README.md`, `docs/`, `tests/` |

Plus `tests/linux/Dockerfile`, which is what made the HIGH's answer possible at all.

⚠ **`k4c-linux` FULL SUITE: 2504 passed / 13 skipped / 1 failed** — and the one failure is
`test_pip_floor_verdict_in_sync_when_constant_matches_floor`, which is the **worktree artifact
documented above, not a branch defect**. It passes in the primary checkout on the same commit.
⛔ **Do not read this branch as red on that row**, and do not "fix" it by touching the branch — the
finding is about the gate, and it is filed forward against `main`.

⚠ **STILL OWED ON K4c AND NOT DOABLE FROM macOS:** the 8 `linux_live` bwrap tests need a real host
(a plain container refuses to create a user namespace), and the rollback fix wants one live
`systemctl --user enable --now` failure. Both are argushub work.

---

## ⚖ THE CODEX-ABSENT L3 — ACCEPTANCE CRITERION, WRITTEN BEFORE THE OUTPUT LANDED

Phill ruled 2026-09-04 that quota-blocked seats run their blocking L3 on **glm-5.3** rather than wait
for the 2:15 codex reset (`FLOW_DEV_PROTOCOL.md` `105e75cc`, read at source). codex remains
non-replaceable and first choice; this is a **fallback**, never a substitution, and the codex run
stays queued.

⛔ **THIS CRITERION IS RECORDED BEFORE THE REVIEW RETURNED, ON PURPOSE.** Deciding afterwards what
would have counted is grading a proxy chosen to fit the result — the day's dominant defect, applied
to my own hold. What follows is the bar as of the moment the command was launched.

**The hold exists because `spore-725`'s note requires *"targeted L3 before the tag, never shipped
unreviewed"* on a crown-jewels confinement change. It clears IF:**

1. The seat actually **read the code** — `files_opened > 0`. A blindfolded reviewer's silence is
   indistinguishable from a clean bill, and `files_opened == 0` is a standing model disqualifier that
   would here be a HARNESS fact, not a model one.
2. `model_used` in the footer is **`glm-5.3:cloud` on the primary rung** — not the `gpt-oss` fallback
   wearing 5.3's name. Report what was served, never what was asked for.
3. It produced a **verdict on the socket arms specifically**, not generic file commentary — the
   review was focused on whether any of the three arms can be defeated and whether the opt-out fails
   open.
4. Every finding is **resolved against disk** before being believed or dismissed, same as complement's
   and glm-5.2's were.

**It does NOT clear if:** the seat returns nothing (the `REVIEW_MAX_TOKENS` truncation shape, which
still prints `contract=OK`), or is served by the fallback model, or opens zero files. Any of those is
a **codex-absent pass with no second lineage at all**, and then the hold stands to 2:15 — because the
failure would be that no independent seat read the change, which is the exact condition the hold names.

⚠ **AND THE BAR IS NOT "IT FOUND NOTHING".** A clean pass from a seat that genuinely read the code
satisfies this; a clean pass from a seat that could not read it satisfies nothing. Those two render
identically in the output, which is why 1–3 are checked mechanically rather than inferred from the
prose.

### ⛔ RUN 1 OF THE CODEX-ABSENT L3 — NO REVIEW. THE HOLD STANDS.

```
[repo-read] primary (glm-5.3:cloud): rounds=6 files_opened=2 899.1s contract=OK ERROR=TimeoutError
[glm PRODUCED NO REVIEW — this is NOT a clean pass … Treat this dimension as UNREVIEWED.]
```

Graded against the criterion recorded above, **before** the run returned:

| # | bar | result |
|---|---|---|
| 1 | `files_opened > 0` | ✅ **2** — it was genuinely reading |
| 2 | `model_used` on the primary rung | ✅ **glm-5.3:cloud**, no `gpt-oss` masquerade |
| 3 | a verdict on the socket arms | ❌ **nothing at all** |
| 4 | findings resolved against disk | n/a |

The written clause was *"It does NOT clear if the seat returns nothing."* **THE HOLD STANDS.**
Pre-registering the bar is the only reason this is a ruling and not a rationalisation.

⚡ **AND IT IS A FOURTH FAILURE MODE, NOT ONE OF THE THREE THE PROTOCOL WARNS ABOUT.** Not the
fallback masquerade (primary rung confirmed). Not the `REVIEW_MAX_TOKENS` truncation — pinned at
65536, and that shape returns silence with `contract=OK` and *no* error, while this returned an
explicit `TimeoutError`. Not `files_opened == 0`. It ran six rounds, opened two files, and **ran out
of clock at 899.1s against a 900s budget.**

So today's L3 failures have **four distinct causes** — codex timeout, codex quota, 5.3 truncation,
5.3 clock — and **every one exits 0 and renders as "no review".**

⛔ **THE VARIABLE IS DIFF SIZE, AND "ONE FILE" IS A PROXY FOR IT THAT COMES APART HERE.** The scoped
diff is **26,562 chars for that single file** — the socket fix alone added 152 lines, and this repo's
house style is heavy inline rationale. The protocol's 5.3 success case (`peer_roster.py`, 258.3s) was
a far smaller diff. The one-file rule is what stopped the codex timeouts; it is **not sufficient** on
a comment-dense file, and the measurable quantity is diff CHARS, not file count.

▶ Re-running at `--timeout 1800`. ⚠ **A second timeout is not a verdict either** — it would mean the
scope is too large for this seat, not that the code is clean and not that 5.3 is bad. Same
discriminator the protocol makes about `files_opened == 0`: a harness fact and a model fact render
identically, and only one of them is true.

### ⛔ RUN 2 — KILLED BY THE KERNEL. THE HOLD STANDS TO 2:15, AND `uptime` NEVER SAW IT COMING.

The 1800s re-run did not time out and did not fail: **the OS killed it for low memory.**

```
task status: killed — "the system is running low on memory"
swap:        9,890 MB used of 10,240   (96.6% exhausted, 349 MB free)
free RAM:    6,029 pages ≈ 94 MB
load:        5.42 on ten cores — 0.54x, and it LOOKS FINE
```

⚡ **`spore-718`'S RULE IS "RUN `uptime` BEFORE LAUNCHING HOURS OF COMPUTE", AND `uptime` IS BLIND TO
THE THING THAT IS ACTUALLY KILLING JOBS.** It was run before every heavy step today and was never
once the number that mattered. **The check's subject is run-queue length; the defect's subject is
resident memory.** That is this session's own dominant class fired at the coordination layer — and it
is why other seats lost jobs while every load check read green.

Top resident, measured: **1,702 MB** node (video-poker) · **1,419 MB** the Docker VM · **~1,800 MB**
across five `claude bg-spare` processes · Chrome in several hundred-MB pieces · 264 MB the Diogenes
reviewer.

⚠ The Docker VM is 1.4 GB and this session is why it is busy — the systemd container work. **Not
stopped:** Docker was already running before this session began, it is the operator's environment,
and this session already took it down once by accident. Flagged upward instead. This session's use of
it is finished.

### ⚖ THE RULING, FINAL FOR THIS SESSION

**0.4.4 STAYS HELD FOR CODEX.** Not relaunching 5.3 into a box at 96.6% swap — a relaunch is killed
again and eats memory other seats need. And the scope is genuinely too large for that seat at 26,562
chars in one file. The honest read: **this change needs codex, which was the first choice all along.**

▶ **FIVE DISTINCT L3 FAILURE CAUSES IN ONE DAY, ON ONE REPO:** codex timeout · codex quota · 5.3
truncation · 5.3 clock · OS memory kill. ⛔ **FOUR OF THE FIVE EXIT 0 AND RENDER AS "no review".**
The fifth returns `[killed]` and an empty file. **A seat reading exit codes instead of output would
have recorded five clean reviews today that never happened** — which is why `deep_review.py`'s
refusal to record coverage, and its "read it as a gap" line, is the single most load-bearing thing in
that script.

### ⛔ CORRECTION — MY OWN "DIFF CHARS" RULE IS FALSIFIED. IT IS `rounds`, AND `rounds` IS THE SEAT'S CHOICE.

Two sections above, this file says *"the measurable quantity is diff CHARS, not file count."* **That
is wrong**, and it was falsified by the very next measurement. Verified independently in the episodic
store (`flow-20260904-112833-cdd2a34eae31`) rather than taken on report:

| seat | diff | files | `rounds` | `files_opened` | result |
|---|---|---|---|---|---|
| video-poker | **75,778 chars** | 12 | **4** | 13 | ✅ finished, 798.9s under a 900s cap |
| levain (this) | **26,562 chars** | 1 | **6** | 2 | ❌ timed out at 899.1s |

**The diff 2.85× LARGER finished; the smaller one did not.** So the file-count rule and its
chars-based replacement are both dead — the replacement inside twenty minutes of being issued.

⚡ **WHAT ACTUALLY DIFFERS IS `rounds`: 4 against 6.** Rounds are read-then-think cycles **the seat
itself chooses**, and 5.3 chose 50% more of them on the *smaller* input — plausibly because a dense
152-line rationale block invites re-reading in a way twelve mechanically-related files do not.
**Nothing about the input predicts the clock, because the clock is set by the seat's own search
depth.**

▶ **OPERATIONAL FORM, and it is not a threshold:** give it a generous timeout, then read `rounds` in
the footer **afterwards**. `rounds=6` means it was working hard and a re-run at the same scope will
likely time out again; `rounds=4` had headroom. **n=2 — a shape, not a law.**

⚠ **AND THIS STRENGTHENS THE HOLD RATHER THAN WEAKENING IT.** My `rounds=6` says 1800s may not have
been enough either, and this file is precisely the dense-rationale case that produced the high round
count. Three independent grounds now: the pre-registered criterion is unmet, the scope is beyond this
seat, and the box will not survive the attempt.

↳ **I WROTE THE FALSIFIED RULE INTO THIS FILE AND INTO COMMIT `ea73f13`'s MESSAGE.** Left standing
rather than rewritten, with the correction appended — the commit is the trail, and a rule that was
believed for twenty minutes and then killed by measurement is worth more visible than tidied away.
⛔ **The lesson is the one this repo keeps paying for: I derived a rule from n=1 and stated it as the
measurable quantity.** Two size rules died in twenty minutes because both were single-datum
generalisations about a system whose behaviour is chosen, not determined.

### ✅ RUN 3 DELIVERED — THE CRITERION IS MET AND **THE HOLD IS LIFTED**

```
[repo-read] primary (glm-5.3:cloud): rounds=4 files_opened=3 754.9s contract=OK
```

⚠ **The command changed and that mattered.** `b63a8ebd` shipped a real `--seats glm-5.3` seat with a
per-model budget (`glm-5.3:cloud: 131_072`), and `repo_read_review.py:259` says an explicit
`REVIEW_MAX_TOKENS` **still wins** — so pinning 65536 as `FLOW_DEV_PROTOCOL.md` trap 2 instructs would
have **halved** it. Run 3 used the native seat with no env overrides. The documented remedy was
correct at 10:00 and harmful by 11:00; nothing linked it to the fix that retired it.

⚠ `REPO_MAX_ROUNDS = 6` — six is the **CEILING**, so runs 1–2's `rounds=6` meant the seat *exhausted*
its file-request rounds, not that it "chose" more depth. Run 3's `rounds=4` converged with headroom.

| # | bar (committed at `a894605`, before run 1) | result |
|---|---|---|
| 1 | `files_opened > 0` | ✅ **3** |
| 2 | `model_used` primary rung | ✅ **glm-5.3:cloud via primary** |
| 3 | a verdict on the socket arms | ✅ explicit **no HIGH** on all three, plus a fail-open sweep |
| 4 | every finding resolved against disk | ✅ done — one refuted, three confirmed |

⚖ **THE HOLD IS LIFTED.** `spore-725`'s *"targeted L3 before the tag, never shipped unreviewed"* is
satisfied. **The tag decision is Phill's from here; it is no longer blocked by this seat.** The bar
did not move in either direction — it held twice when runs returned nothing, and it releases now that
one returned a verdict. That is the whole point of writing it down first.

**What the pass actually said, and it is the strongest independent evidence the fix is sound:** all
three arms hold against every attack it could construct — rename and unlink at both spellings,
hardlink-to-new-name, symlink-indirection connect, parent-dir relocation, and absent-at-build-time
races. The `allow_container_sockets` sweep found **no fail-open anywhere the flag is readable**, and
confirmed the flag gates all three arms together so the opt-out is total rather than partial.

⚠ **THIS REMAINS A CODEX-ABSENT PASS AND THE RECORD SAYS SO** — `deep_review.py` itself printed
*"coverage NOT recorded — codex was not among --seats, so this was not a deep pass."* The codex run
**stays queued for 2:15** as the first choice it has always been, tag or no tag.

⚠ **POST-REVIEW CHANGES, STATED SO NOBODY HAS TO DIFF FOR THEM:** `e3ca577` landed after the review —
three comment blocks and one banner string. **Zero logic.** The reviewed code and the shipping code
are behaviourally identical.
