# DIOGENES OVERNIGHT — levain, 2026-08-13

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-08-13*)`
**Stated count: 4 episode(s).** Severity: COVERAGE 1 · HIGH 1 · MEDIUM 1 · STILL OPEN 1
**STILL OPEN: 22**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260813-051715-56c3588e939e
  Time: 2026-08-13T05:17:15  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: ~/Briefcase/flow/projects/levain/next.md:31 — THE CORRECTION BLOCK'S OWN LINE CITATIONS WERE INVALIDATED BY THE ACT OF INSERTING THE CORRECTION BLOCK, AND THEY NOW LABEL AS REFUTED A PARAGRAPH I VERIFIED AS TRUE THE SAME NIGHT. On 2026-08-12 I filed a HIGH and two MEDIUMs against the spore-492 doctor redesign, citing next.md:110-111 (the deletion list), :113-114 ("generalisation of a mechanism already in the repo"), :119-124 ("THE TRADE — stated, not buried") and :123-124 ("Correctness moves entirely to the suite"). The morning ritual then wrote those corrections INTO THE SAME FILE, as a warning block ABOVE the redesign section, reproducing my coordinates verbatim. MEASURED, BOTH REVISIONS FROM GIT, NOT RECALLED: `git diff --stat a9e56ca9 HEAD -- projects/levain/next.md` is 49 insertions / 4 deletions = NET +45, and every cited line below the insertion point moved by exactly 45. At a9e56ca9 (the revision I filed against) `sed -n '119p'` is [quoted: `### ⚠ THE TRADE — stated, not buried`] and `sed -n '113p'` is [quoted: `**This is generalisation of a mechanism already in the repo, not a new layer** — extend`] — my coordinates were exact. At HEAD those same sentences live at :164 and :158, and the citations still say :119-124 and :113-114. THE FOUR STALE CITATIONS ARE ALL IN THE CORRECTION BLOCK ITSELF: :31 [quoted: `> predate the package.** \`next.md:119-124\` ("THE TRADE — stated, not buried") names a real but`], :37 [quoted: `> deletion list at \`:110-111\` is too aggressive by those two entries — **but the branch SPLIT still`], :39 [quoted: `> Two supporting MEDIUMs: \`:113-114\` "generalisation of a mechanism already in the repo" is false in`], :43 [quoted: `> the check serves). And \`:123-124\` hands correctness to \`tests/test_packs.py:553\`, a pure RESOLVER`]. WHY THIS IS A HIGH AND NOT A PEDANTRY ABOUT LINE NUMBERS. The citations do not merely miss; they land on OTHER CLAIMS AND MISLABEL THEM. :43 says ":123-124 hands correctness to tests/test_packs.py:553, a pure RESOLVER unit test" — and HEAD :123-124 is [quoted: `   *is* \`hash(disk) == receipt.hash\`. And \`PackProvenance\` (\`manifest.py:787\`) already carries`] continuing "**two** maps", which is the PackProvenance claim I explicitly checked and CONFIRMED TRUE in that same night's good half ("manifest.py:786-797 defines both `files` and `rendered` with that docstring rationale verbatim"). So the correction now points at a verified-true paragraph and calls it the refuted one, while the actually-refuted sentences at :158 and :168 [quoted: `Correctness moves entirely to the suite, where \`tests/test_packs.py:553\` already proves the`] carry no marker at all. :37 is worse in kind: it says the deletion list is "too aggressive by those two entries", and HEAD :110-111 is [quoted: `**Approved by Phill 2026-08-09 after a full read of \`_hook_body\` + \`manifest.py\`.** He refused`] — an implementer told to trim the deletion list will find Phill's approval note there, and the real deletion list at :153-156 unmarked and intact. THE DESIGN SECTION IS UNCHANGED: :153-175 still asserts all three refuted sentences verbatim, and :164 still heads them [quoted: `### ⚠ THE TRADE — stated, not buried`] — a heading whose entire claim is COMPLETENESS, sitting over a trade my HIGH established is incomplete. That is `a_green_tick_over_a_superseded_paragraph_outranks_the_correction_below_it` with "stated, not buried" as the tick, and this time the tick is what the corrected reader is steered toward. BLAST RADIUS BEYOND THIS BLOCK: six of the seven next.md coordinates in the whole 2026-08-12 finding corpus now point at different text — :22 and :24 moved too (they carried my sha-pin MEDIUM and my "SEVEN of the sixteen" SELF finding); only :11 survives, by luck, because the inventory line was edited in place rather than displaced. THE MECHANISM IS STRUCTURAL, NOT CARELESSNESS: an in-file correction that PREPENDS necessarily displaces its own referents, so the citations are guaranteed wrong the instant the edit is saved. Nothing can catch it — there is no checker, and the block reads perfectly to its author because the author is looking at the pre-edit buffer. -- FIX (worked out, NOT executed; the target is a document I do not edit): cite the refuted text by QUOTED SENTENCE or by section HEADING, never by line number, whenever the correction is written into the same file as its target. A quote survives displacement and is greppable, which is the whole property a coordinate is supposed to buy. Second-best and cheaper: mark the refuted sentences AT THE SITE (:158, :168) with a one-line pointer up to the block, so the implementer working from the design section — which is the buildable artifact, and the only part of the file an implementer actually works FROM — cannot miss them without the coordinates mattering at all. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260813-051744-392202dec641
  Time: 2026-08-13T05:17:44  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: ~/Briefcase/flow/projects/levain/next.md:68 — THE ONLY STATED NON-CLOSURE PROCEDURE IN THE PICKUP FILE IS SCOPED TO ONE REPOSITORY WHILE SEVEN OF THE TWENTY-TWO FINDINGS LIVE IN ANOTHER, SO IT RETURNS "NOTHING HAS CLOSED" ON A WINDOW IN WHICH TWO THINGS CLOSED. The file states the re-derivation a levain session is meant to run, at :68 [quoted: `> \`a660bb3\`.** Non-closure verified against the diff: \`git diff --stat 3200234 HEAD -- levain/ tests/\``] concluding at :69 [quoted: `> is EMPTY — **nothing has closed.** **SEVEN OF THE SIXTEEN LIVE IN \`_check_hook_freshness\` AND ITS`]. THAT COMMAND CANNOT SEE SIX OF THE TWENTY-TWO, BY CONSTRUCTION. Partitioning my own 2026-08-12 slot (diogenes-20260812-064140-9121ada0a324) by the tree each finding's file lives in: FIFTEEN are levain-resident — items 1,2,3,6,7,8,9,10,11,12,13 in levain/doctor.py and tests/, plus 4,5,18,19 in levain's project_memory/. SIX are entirely inside ~/Briefcase/flow — item 14 (route_diogenes.py:144), 15 (route_diogenes.py:121), 16 (next.md:22-23), 17 (next.md:11-12), 21 (next.md:113-114), 22 (next.md:123-124). ONE, item 20, straddles both (levain/doctor.py:989-1193 plus next.md:108-124). 15 + 6 + 1 = 22, which reconciles. So the pinned pathspec `-- levain/ tests/` observes at most 11 of the 22, and even widening it to the whole levain tree reaches 15. RUN, NOT ARGUED — THE PROCEDURE IS LIVE-WRONG TONIGHT. At HEAD 2f98136 the command returns EMPTY, which licenses "nothing has closed". Two findings closed since I filed the 22 at 2026-08-12T06:41, both in flow, both verified by me tonight against disk. (a) Item 14, the HIGH that `extract_still_open` read a DATE and returned 08 where the truth was 16: at flow HEAD the levain 2026-08-10 window returns AMBIGUOUS — a refusal, not a wrong integer — and the mechanism is documented headline-only parsing at route_diogenes.py:275-304 plus explicit supersession resolution at :248-252. Mutation-checked rather than trusted: disabling SUPERSEDES_RE turns the live 08-12 window from '22' to 'AMBIGUOUS', so the resolution is load-bearing and not incidental. The suite is 42 passed, up from the 15 I measured on 08-12. (b) Item 17, the inventory line claiming "seven of them (08-02 -> 08-08)" where disk held eight: next.md:11-12 now reads "nine of them (08-02 -> 08-12)" and `ls -1 project_memory/diogenes_*.md | wc -l` is 9, range 20260802 to 20260812. Both closed by flow commits — ae9c7d91, 677027ed, d90c8ed3 and 3c46a28c — none of which the instructed command can observe. WHY THIS IS NOT THE SHA-PIN FINDING I ALREADY HAVE OPEN. Item 16 says the pin goes STALE (it did again: the pointer says "STILL OPEN: 22 at HEAD `2889cae`" and was written at 2026-08-12T07:23:37, twelve seconds after levain's 2f98136 landed at 07:23:25 — a third instance after eight seconds and twenty-one seconds). This is a different defect and my prescribed fix for item 16 does not touch it: I proposed replacing the pin with the RANGE `3200234..HEAD`, which has exactly the same single-tree scope and would report the same false EMPTY tonight. A pin cannot bound a population that spans two independently-moving trees no matter how fresh it is. THE CONSEQUENCE IS DIRECTIONAL AND IT IS THE BENIGN DIRECTION, WHICH IS WHY IT WILL PERSIST: the count only ever OVERSTATES debt, so a session sizes its work too large and nothing visibly breaks. Nobody chases a number that is too pessimistic, and `absence_of_signal_rendered_as_health` has no counterpart for "presence of stale signal rendered as debt". -- FIX (worked out, NOT executed; this is an edit to a document I do not write): partition the slot by TREE and give each half its own oracle — state the levain count with `git diff --stat 3200234..HEAD -- levain/ tests/` and the flow-resident count with the corresponding flow range, rather than one cardinal under one pin. That also makes the redesign's kill criterion gradeable, which the single number currently is not: when spore-492 lands, the levain half should fall by six and the flow half should not move, and today those two effects are indistinguishable inside "22". [prescription: reasoned]
  Tags: architecture, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260813-051819-797e9a6ac110
  Time: 2026-08-13T05:18:19  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: STILL OPEN (22), at HEAD 2f98136 — levain, 2026-08-13. THE NUMBER IS UNCHANGED AND THE COMPOSITION IS NOT: 22 at 08-12, minus 2 CLOSED, plus 2 NEW = 22. Read as "nothing happened" this is wrong in both directions. CLOSED (2), both verified against the mechanism tonight rather than against the symptom going quiet. (14) HIGH ~/Briefcase/flow/scripts/route_diogenes.py:144, the still-open extractor reading a DATE: at flow HEAD the levain 2026-08-10 window returns AMBIGUOUS — a refusal — where it returned 08 against a truth of 16, and the live 08-12 window returns 22 correctly. Mutation-checked, not trusted: disabling SUPERSEDES_RE flips the 08-12 window from 22 to AMBIGUOUS, so the supersession resolution at :248-252 is load-bearing; the headline-only restriction at :275-304 is what kills the date case. Suite 42 passed, up from 15. My deeper prescription (a producer-side STILL_OPEN_COUNT=N token) was NOT built; what shipped is the contract written down at :131-154 plus refusal, which closes the defect as filed. (17) MEDIUM next.md:11-12, the inventory line reading "seven of them (08-02 -> 08-08)" where disk held eight: it now reads "nine of them (08-02 -> 08-12)" and disk holds nine, 20260802 through 20260812. ⚠ CLOSED BY CORRECTION, NOT BY MECHANISM — I prescribed DELETING the count as a self-invalidating derived fact and it was updated instead, so it re-arms on the next routing. NEW TONIGHT (2). (23) HIGH ~/Briefcase/flow/projects/levain/next.md:31/:37/:39/:43 — the spore-492 correction block cites four coordinates that its own insertion displaced by exactly +45 (49 insertions, 4 deletions), so it now labels as refuted a PackProvenance paragraph I verified TRUE the same night, while the actually-refuted sentences at :158 and :168 carry no marker. (24) MEDIUM next.md:68 — the only stated non-closure procedure, `git diff --stat 3200234 HEAD -- levain/ tests/`, is scoped to one tree while 7 of the 22 findings live in ~/Briefcase/flow; it returns EMPTY tonight, licensing "nothing has closed", on a window in which two things closed. CARRIED (20). Code axis (11): `git diff --stat 3200234 HEAD -- levain/ tests/` is EMPTY, so 08-04 HIGH doctor.py:1098, 08-05 tests/test_firing_condenser.py:167 and :159, 08-05 tests/test_app_server.py:21, 08-07 HIGH doctor.py:1169, 08-07 MEDIUM doctor.py:1140, 08-07 MEDIUM tests/test_doctor_content.py:566, 08-07 MEDIUM tests/test_firing_confinement.py:1113, 08-08 HIGH doctor.py:1137+:1166, 08-08 MEDIUM doctor.py:1085 + tests/test_doctor_content.py:641 and 08-10 MEDIUM doctor.py:981-986 are structurally still open. TWELVE COORDINATES RE-READ FROM DISK ONE LINE AT A TIME TONIGHT AND ASSERTED EQUAL TO THEIR CITED TEXT: 12/12 MATCH. Record axis (4): 08-05 project_memory/diogenes_20260804.md — `grep -c 'prescription: run'` returns 6 and `grep -c 'NOT executed'` returns 6, unchanged; 08-05 diogenes_20260804.md:3 — still three `**` markers on one line, machine-scanned across all nine routed reports tonight and it remains the only header line with an odd count that is not a legitimate multi-line bold span; 08-12 MEDIUM/SELF diogenes_20260810.md:5 ("SEVEN of the sixteen" is six) — the file is unchanged AND next.md:69-71 still promotes the SEVEN; 08-12 LOW diogenes_20260810.md:27, the cross-referenced episode id routed nowhere. Flow axis (5): 08-12 MEDIUM route_diogenes.py:121, the daystamp substring filter — STILL OPEN AND MATERIALLY WORSE, re-reproduced tonight at flow HEAD where `fetch_blocks('levain','7d','2026-08-08')` returns 9 blocks of which FOUR carry a foreign Time field, up from three, and the new contaminant is my own 08-12 finding ABOUT the filter, which quotes the daystamp; 08-12 MEDIUM next.md:22-23, the sha pin — RECURRED A THIRD TIME, "STILL OPEN: 22 at HEAD `2889cae`" written at 07:23:37, twelve seconds after 2f98136 landed at 07:23:25, after eight seconds and twenty-one seconds previously; 08-12 HIGH doctor.py:989-1193 + next.md:108-124 (the redesign deletes package-moved-ahead detection); 08-12 MEDIUM next.md:113-114, the keyspace claim; 08-12 MEDIUM next.md:123-124, correctness handed to tests/test_packs.py:553 — all three OPEN because the design section at :153-175 is textually unchanged and still asserts every refuted sentence verbatim. NOT COUNTED AS OPEN, unchanged for the seventh night: the environment-dependent severity argument in the 08-04 report, and the pack-hook DELETION case I declined on 08-06, 08-07, 08-08, 08-10 and 08-12 and decline again. REPO UNTOUCHED: `git status --porcelain` empty at 2f98136 before and after; the tree was copied out with `git archive` for the sdist build, route_diogenes.py was imported read-only and never invoked with a write path, and every mutation ran in /tmp/levain_dio_20260813. [prescription: none]
  Tags: code_review, levain, still_open
```

---

```
[laptop/flow]   ID: diogenes-20260813-051845-dbeac3f35b85
  Time: 2026-08-13T05:18:45  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: levain 2026-08-13, one commit (2f98136, doc-only, +167 lines, one added file project_memory/diogenes_20260812.md). RE-DERIVED BY RUNNING, NOT CARRIED FORWARD. (1) ROUTING IS VERBATIM AND EXHAUSTIVE, machine-checked not read: opened state/episodic.db read-only (sqlite3 `file:...?mode=ro`), split the report on its fenced delimiter into 16 blocks, byte-compared each block's Content against the stored `content` column — 16/16 IDENTICAL, not one paraphrase or trim; then joined episodes to episode_tags and confirmed 109 diogenes episodes carry a 2026-08-12 timestamp, exactly 16 carry the `levain` tag, and those 16 are precisely the 16 in the file — MISSING none, EXTRA none. (2) THE HEADER HISTOGRAM IS CORRECT BY MEMBERSHIP, not merely by sum: ran route_diogenes.severity() over the live window and got HIGH 2, MEDIUM 6, LOW 1, STILL OPEN 2, SELF 3 and 2 blocks bucketing OTHER — the two GOOD HALF episodes, which bucket OTHER only because the 2026-08-12 subtractive audit deleted that label after this report was generated. Against the committed line "GOOD HALF 2 · HIGH 2 · LOW 1 · MEDIUM 6 · SELF 3 · STILL OPEN 2" that is an exact match under the vocabulary in force when it was written, and the stated count 16 equals the 16 blocks parsed. (3) THE 22 IN THE HEADER IS WHAT THE GENERATOR ACTUALLY PRODUCES: imported route_diogenes read-only at flow HEAD, `extract_still_open` on the live window returns '22', and it is the supersession resolution that earns it — MUTATION-TESTED, disabling SUPERSEDES_RE flips it to AMBIGUOUS. Also ran the 08-10 window, which returns AMBIGUOUS rather than the 08 that was my 08-12 HIGH; router suite `pytest tests/test_route_diogenes.py -q` is 42 passed. (4) TWELVE CARRIED CODE COORDINATES RE-READ FROM DISK one line at a time and asserted equal to their cited text: 12/12 match across levain/doctor.py, tests/test_doctor_content.py, tests/test_firing_confinement.py, tests/test_app_server.py, tests/test_firing_condenser.py and tests/test_packs.py. (5) THE PRODUCT-SIDE CHECK NOBODY HAD RUN, and it came back CLEAN: nine review dumps totalling 192K now live in the tracked tree, so I asked whether they ship. BUILT AN ACTUAL SDIST — `git archive HEAD | tar -x` into a scratch dir (repo never touched), scratch venv, `python -m build --sdist` — levain-0.4.1.tar.gz, 174 entries, and `grep -i 'project_memory\|diogenes'` over the member list returns NOTHING. MANIFEST.in has no graft for it and `[tool.setuptools.packages.find] include = ["levain*"]` keeps it out of the wheel. Not shipped. (6) MARKDOWN RENDERING SCANNED ACROSS ALL NINE ROUTED REPORTS for the odd-`**` class I filed on 08-05; the new file has ZERO offending header lines, so the defect did not recur in this diff. (7) THE DIFF'S OWN CLAIMS TESTED AGAINST BOTH REVISIONS OF THE PICKUP FILE via `git show a9e56ca9:` versus HEAD, which is where both of tonight's findings came from. — NOT CHECKED: I did NOT run levain's full test suite (~2081 tests) and make no claim about its state. I did NOT re-reproduce the eleven carried code findings; I am DEFERRING to the empty `git diff --stat 3200234 HEAD -- levain/ tests/` plus the 12/12 coordinate assertion, because re-running an identical repro against an identical tree produces a timestamp and no information — but that means no code finding was independently re-executed tonight. I did NOT open levain/install.py, levain/manifest.py, levain/reconcile.py or levain/update.py; the only levain source I read was doctor.py at cited coordinates. I did NOT review any part of levain outside the doctor / hook-freshness / routing axis — the firing subsystem, app_server and the pack machinery were untouched by me beyond single cited lines. I did NOT build a wheel, only an sdist. And I did NOT verify that any human has read the pickup file this pointer maintains, which is the assumption the whole routing apparatus rests on. [prescription: none]
  Tags: code_review, levain, semantic
```
