# DIOGENES OVERNIGHT — levain, 2026-08-22

**Source selector:** `episodic.py read --since 6h --tags levain  (filtered to Agent: diogenes at 2026-08-22*)`
**Stated count: 7 episode(s).** Severity: COVERAGE 1 · HIGH 1 · LOW 1 · MEDIUM 3 · STILL OPEN 1
**STILL OPEN: 44**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260822-090632-fdab81fb21f9
  Time: 2026-08-22T09:06:32  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: levain 2026-08-22, HEAD 7211bbf1f1e31c20a67405e1a35f2b3df6d6faa1 (== origin/main? yes), `git status --porcelain` <empty>, window HEAD~1..HEAD = 1 commit (7211bbf 'Deny each ssh vector at every spelling, not just the resolved one'). NOTE: ~/Documents/levain is a SYMLINK to ~/Briefcase/levain — one repo, not two targets. NOTE: this is the FIRST window in sixteen nights containing actual CODE rather than my own routed report. REVIEW STARTED, NOT FINISHED — if no COVERAGE line for levain 2026-08-22 exists in this run, THIS RUN DIED and the finding list is PARTIAL. [prescription: none]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260822-091951-47d43cdab29d
  Time: 2026-08-22T09:19:51  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: levain/firing/confinement.py:478 (+ tests/test_firing_confinement.py:775, + the 7211bbf commit message) — THE FIX IS CORRECT AND ITS PERMANENT RECORD STATES A MECHANISM THAT THE FINDING IT CITES EXPLICITLY REFUTED, AND THAT I RE-MEASURED TONIGHT AS DENIED. The comment says [quoted: `    #       only at ~/evil — while the entity writes to the LEXICAL ~/.ssh/authorized_keys, which sshd`] and the new test's docstring says [quoted: `    ``~/.ssh/authorized_keys -> ~/evil/authorized_keys`` symlink defeated the write-floor entirely.`]. Both name a DIRECT DATA WRITE at the lexical path as the bypass. IT IS NOT. MEASURED LIVE with sandbox-exec on Darwin 25.5 against the PREDECESSOR profile rendered by the real build_policy+SeatbeltProvider at HEAD~1: appending to the lexical /private/tmp/.../.ssh/authorized_keys returns 'Operation not permitted' and the real key file is untouched. ISOLATED to remove every confound — a minimal '(version 1)(allow default)(deny file-write* (literal <RESOLVED>))' profile ALSO denies the lexical write, so seatbelt CANONICALISES the requested path before matching (literal ...). The converse minimal profile denying only the LEXICAL path lets the same append through at exit=0. THE ACTUAL BYPASS IS UNLINK-THEN-RECREATE, because unlink/rename/ln operate on the LINK and seatbelt matches those lexically: against HEAD~1, 'rm -f <lexical>' exit=0 then 'echo ATTACKER > <lexical>' exit=0, and sshd would read 'ssh-ed25519 AAAA ATTACKER'; against HEAD 7211bbf both steps return 'Operation not permitted' and the file survives. ⛔ MY OWN 2026-08-21 HIGH (diogenes-20260821-045644-55b802411777) GOT THIS RIGHT AND SAID SO IN TERMS: '(A) writing THROUGH the symlink is correctly DENIED ("Operation not permitted" — content protection works); (B) rm -f ~/.ssh/config && printf ... returns BYPASS_OK'. The commit dropped step (B), which is the only step that works, and kept step (A), which the finding had already measured as safe. NO SELF-CORRECTION IS OWED — the finding was accurate; the paraphrase of it is not. WHY IT IS HIGH RATHER THAN A DOC NIT: this is the recorded threat model of the load-bearing security module, stamped 'confirmed by repro', and it is generalised into a DESIGN RULE at :485 ('DENY BOTH RATHER THAN REASONING ABOUT SEATBELT'S CANONICALISATION') that the next guard will be built from. The canonicalisation question is now ANSWERED by measurement — resolved for data access, lexical for link operations — and the file records it as unknowable. Two downstream claims already inherit the error (see the two MEDIUMs filed alongside this). — THE FIX: replace the direct-write mechanism in the :475-484 block, the test docstring at :775, and the KNOWN-OPEN preamble with the measured one (data-write already denied via canonicalisation; the closed hole is unlink/rename/re-link at the lexical path), and record the measured canonicalisation semantics in place of the 'platform detail we would then depend on being right about' framing. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260822-092011-7bd0afdcb175
  Time: 2026-08-22T09:20:11  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/firing/confinement.py:517 — THE RAW Path.home() SPELLING, THE ONE CODEX'S L3 ADDED, IS UNREACHABLE ON BOTH ENFORCING HANDS, AND IT SHIPS WITH A DEDICATED REGRESSION TEST THAT CERTIFIES IT. The line is [quoted: `        deny_write_files_l.append(ssh_home / n)               # RAW ``Path.home()`` — the true lexical`] and build_policy:502 claims [quoted: `    #           this file. The third spelling above narrows it, it does not close it.`], with tests/test_firing_confinement.py:822 repeating [quoted: `    ⛔ This NARROWS the symlinked-HOME vector; it does not close it. A *replaceable* HOME symlink is`]. MEASURED: it narrows nothing. This spelling differs from the other two only when an ANCESTOR component of the path is a symlink (a symlinked HOME), and seatbelt canonicalises ancestor components for EVERY operation, not just data access. Live on Darwin 25.5 with /tmp -> /private/tmp as the symlinked ancestor and a minimal profile whose ONLY rule is '(deny file-write* (literal "/tmp/dio_h/.ssh/authorized_keys"))': append exit=0, 'rm -f' exit=0, 'mv' exit=0 — all three allowed. The control profile naming the canonical /private/tmp spelling denies all three ('Operation not permitted'). So the entry fires for no operation. It is equally dead on the in-process twin: crown_jewel_reason:668 resolves its argument first, so a lexical entry can never match. THE TEST DOES NOT CATCH THIS BECAUSE IT ASSERTS THE WRONG LAYER — test_build_policy_denies_the_RAW_home_spelling_when_HOME_is_a_symlink checks tuple membership in policy.deny_write_files and never renders a profile or runs the sandbox, so a permanently-unreachable deny reads as a closed vector. This is the same shape the commit itself indicts in the predecessor test ('SET UP THIS EXACT SCENARIO AND LOOKED ONLY AT deny_write_dirs'), reproduced one layer over in the fix for it. — THE FIX: either delete the spelling and let KNOWN-OPEN (b) stand un-narrowed, or keep it and state that it is defence-in-depth against a canonicalisation change rather than a mitigation of (b); do not leave a claim of narrowing that measurement refutes. REMOVAL EXECUTED in a scratch copy: it turns exactly ONE test red (the membership assertion above) and nothing else — 91 passed. [prescription: run]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260822-092026-25273688516c
  Time: 2026-08-22T09:20:26  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/firing/confinement.py:518 — THE ONE SPELLING THAT ACTUALLY CLOSES THE VULNERABILITY IS THE ONE SPELLING NO TEST COVERS; DELETING IT LEAVES THE SUITE FULLY GREEN AND RE-INSTALLS THE ATTACKER KEY. The line is [quoted: `        deny_write_files_l.append(ssh_home_lexical / n)       # resolved HOME + un-deref'd .ssh`], built from [quoted: `    ssh_home_lexical = home.resolve() / ".ssh"      # resolved HOME + un-deref'd ``.ssh``, as ``ssh_anchor```]. It is the canonical-directory + lexical-final-component spelling, which is exactly what seatbelt matches for unlink/rename/re-link — i.e. it is the entry that stops the real bypass. RUN, not reasoned: I removed ONLY this line in a scratch copy of HEAD and (1) tests/test_firing_confinement.py reports 92 passed, 1 skipped — ZERO red, the same result as the unmutated tree; (2) the live rm-then-recreate attack under the profile rendered from that same mutated tree returns rm exit=0, create exit=0, and 'cat ~/.ssh/authorized_keys' prints 'ssh-ed25519 AAAA ATTACKER'. So the load-bearing line is protected by nothing and its deletion is invisible to CI. The three new tests all cover the OTHER two spellings: dropping the raw spelling reds 1 test, dropping the resolved spelling reds 2, dropping this one reds 0. The control test test_build_policy_ssh_vectors_do_not_double_up_without_symlinks cannot see it either — it asserts len(policy.deny_write_files) == 4 in the NO-symlink case, where all three spellings dedup to one, so it is satisfied by any single spelling surviving. — THE FIX: add a test that discriminates this spelling. The strongest form is the one I ran: build the policy with a real symlinked ~/.ssh/<vector>, render the profile, and assert the LEXICAL literal is present — or, gated as @live, assert sandbox-exec refuses 'rm' at the lexical path. THE DISCRIMINATING CHECK WAS EXECUTED in a scratch dir (mutation + live sandbox-exec); it is NOT written into tests/. [prescription: run]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260822-092051-b834e49dc27b
  Time: 2026-08-22T09:20:51  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/firing/confinement.py:589 — THE JUSTIFICATION FOR ssh_anchor DESCRIBES A deny_write_files_l THAT STOPPED EXISTING TWENTY LINES ABOVE IT, IN THIS COMMIT, AND THE CODE IT JUSTIFIES IS NOW UNCONDITIONALLY REDUNDANT. The comment reads [quoted: `    # anchor are different objects"). ``deny_write_files_l`` uses ``.resolve()`` for CONTENT protection`] and derives from that premise that ancestor-deny pins only the resolved target, 'leaving lexical ~/.ssh replaceable' — which is why [quoted: `    ssh_anchor = home.resolve() / ".ssh"`] exists at :595. As of 7211bbf, deny_write_files_l no longer uses .resolve() alone: :518 unconditionally appends ssh_home_lexical / n for every one of the four vectors, and ssh_home_lexical IS home.resolve() / '.ssh'. So _write_deny_ancestors, walking jewel.parents over all_jewels at :596, now yields home.resolve()/'.ssh' — byte-identical to ssh_anchor — on every build, for every ssh_mode, since _SSH_WRITE_DENIED is a non-empty module constant. The stated reason the anchor is needed is therefore false, and the anchor is now dead weight. VERIFIED TWO WAYS rather than reasoned: (1) removing ssh_anchor from the write_dirs construction in a scratch copy leaves 92 passed, 1 skipped — no test can see it; (2) directly measured across three scenarios (plain HOME, symlinked ~/.ssh, symlinked HOME) that home.resolve()/'.ssh' is in policy.deny_write_dirs AND is a parent of an entry in policy.deny_write_files in every case. ⚠ THE HARM IS NOT THE DEAD LINE, IT IS THE PREMISE: this comment is the file's own written record of what deny_write_files_l contains, sitting in the security surface, and a future reader deciding whether a spelling is safe to drop will consult it and be told the list is resolve-only. That is the identical failure the same commit names at :481 — 'THIS IS THE SAME lexical-vs-resolved ARGUMENT ALREADY MADE TWICE IN THIS FILE' — now with the argument correct and its description stale. — THE FIX: rewrite :588-594 to say ssh_anchor is retained (if it is) as an explicit belt-and-braces pin whose coverage is now also implied by the :518 spelling, or drop ssh_anchor and note the ancestor walk covers it. Do not leave the resolve-only premise on disk. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260822-092051-6180bc6d9187
  Time: 2026-08-22T09:20:51  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: levain/firing/confinement.py:521 — THE SOUNDNESS ARGUMENT FOR _dedup IS FALSIFIED BY THE FOUR LINES DIRECTLY ABOVE IT. The comment reads [quoted: `    # De-dup while preserving order (a sibling could coincide with an extra); resolved paths compare`] / [quoted: `    # exactly, so a simple seen-set is sound.`], and it is the only stated reason a plain set[Path] is acceptable here. It was true at HEAD~1, where every list handed to _dedup held resolved paths. It is false at HEAD: _dedup is applied at :534 to deny_write_files_l, two of whose three spellings per vector (:517, :518) are deliberately UNRESOLVED — that is the entire point of the change. The CONCLUSION still holds (Path.__eq__ is an exact string compare, the spellings are constructed from clean components, and we WANT the distinct spellings kept), so there is no live defect — but the premise a reader would rely on to extend or re-order this function is now wrong, and _dedup is the last gate before the rendered profile. Not the same class as the case-insensitivity handled by _canon/_ci_within at :612-633: nothing here folds case, and on a case-insensitive volume two case-variant spellings would survive dedup as separate literals. That is harmless today only because no caller generates them. — THE FIX: one sentence — say the seen-set is exact-string, that unresolved spellings are kept ON PURPOSE, and that dedup is not a canonicalisation step. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260822-092116-4f32b8febdd8
  Time: 2026-08-22T09:21:16  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (44), TOTAL AT HEAD 7211bbf — levain, 2026-08-22. Filed separately: 0 DRIFT (not in this count). ARITHMETIC, shown so it can be checked: 40 carried from the 2026-08-21 slot (diogenes-20260821-045805-6d5933517b60, at HEAD f1e6055), MINUS 1 genuinely CLOSED, PLUS 5 new = 44. Zero DRIFT because nothing was eligible — codeshape.py HEAD~1 reports 'CODE CHANGED — FINDING' (exit 1) for both levain/firing/confinement.py and tests/test_firing_confinement.py, so the mechanical discriminator resolves every description defect tonight into the count. ⭐ THE CLOSE IS REAL AND IS THE FIRST ON THIS REPO IN SIXTEEN WINDOWS: my 2026-08-21 HIGH (diogenes-20260821-045644-55b802411777, the raw-mode ssh persistence bypass) is FIXED at HEAD, verified by re-running the original repro against both sides — under the HEAD~1 profile 'rm -f <lexical vector>' exits 0, the recreate exits 0 and sshd would read a planted attacker key; under HEAD both steps return 'Operation not permitted' and the file survives. The operator-facing banner at levain/run.py:768 ('ssh authorized_keys/config/rc WRITE denied') is now TRUE in ssh_mode=raw, which it was not on 08-21. ⛔ AND THE WINDOW BROKE THE FREEZE: this is the FIRST levain window since 2026-08-05 containing actual source rather than a routed report of my own findings, so the 2026-08-21 ruling that levain's count was measuring survey completeness rather than decay does NOT apply tonight — these 5 are new defects in code that landed today, in the security module, not re-survey. THE FIVE: 1 HIGH (the recorded bug mechanism is refuted by live measurement — the fix is correct, its threat model is not, and the false model is generalised into a design rule at :485), 3 MEDIUM (the raw-Path.home() spelling is unreachable on both hands yet ships a regression test certifying it; the ONE spelling that closes the hole has zero coverage and its deletion is green AND live-exploitable; the ssh_anchor justification describes a construction this commit replaced), 1 LOW (_dedup's soundness premise falsified four lines below the code that falsified it). ⚠ THE OTHER 39 CARRIED WERE NOT RE-SURVEYED — two were incidentally re-verified as still open while I was in the file (_sbpl_regex at :1285 still has zero production callers; the build_policy docstring at :412 still describes the name-based ssh design). Repo untouched: porcelain empty on entry and exit, HEAD == origin/main at 7211bbf, every mutation and every sandbox-exec run confined to /tmp copies.
  Tags: code_review, levain, semantic
```
