# TRIAGE — 2026-09-15 · `0915+4 levain-seat`

Input: Diogenes `diogenes_20260915.md` (LOW `install.py:1758-1762`, MEDIUM `doctor.py:1662`
routed to Phill as a premise question) and Phill's C6 amendment ruling (episode
`flow-20260915-074114-4e7ca53aaca6`, relayed via `0915+1 fanin` at 07:41).

**Re-derive, do not read:**
```
grep -n "read_activation_receipt\|_read_receipt_text\|_RECEIPT_MAX_BYTES" levain/*.py
python3 -m pytest -q tests/test_receipt_and_upgrade_exit.py -k receipt
python3 -m pytest -q tests/test_hook_freshness_receipt_status.py
```

## FIXED — each reproduced first, each with a test built from that reproduction

| Finding | What the reproduction showed | Fix |
|---|---|---|
| LOW `diogenes-20260915-021123-837af5a66f91` (`install.py:1758-1762` stat-then-read still under a "Bounded" comment) | An already-oversized-at-write-time receipt was caught by the OLD stat check too — the first test written did not kill the mutation. Sharpened: `stat()` made to lie about size while the on-disk content is a genuinely oversized but syntactically valid, schema-correct receipt (caught by codex L3, `input_id` in `state/verdicts.jsonl`) | `read_activation_receipt_file` now routes through the existing `_read_receipt_text` helper (one bounded open); `ValueError` added to the `corrupt` except. One receipt reader, comment now true. |
| C6 AMENDMENT — receipt CORRUPT or EMPTY + a stale hook (`doctor.py` `_check_hook_freshness`) | Diogenes' 5-case signature reproduced exactly against `_check_hook_freshness`, before any test was written: `intact` -> (ok=False, pending=False) "changed since `levain init` wrote them"; `corrupt` -> (False, False) "unreadable (corrupt)"; `empty` -> (False, False) "unreadable (empty)"; `deleted` -> (False, True) "no install receipt covers them"; `ok_no_entry` -> (False, True) "no install receipt covers them". | A corrupt/empty receipt now short-circuits to its own `CheckResult` (exit 1, `upgrade_pending` defaults False) naming `activation_receipt_path(install)` and the `init --force` remedy, before the per-hook receipt-entry loop runs. `absent` and `ok`-but-missing-entry are UNCHANGED (still exit 6, still "no install receipt covers them" — that text is now true only for those two cases). |

## RULING PREMISE, dated (do not re-derive)

⚖ C6 amended by Phill, 2026-09-15 morning (relayed via `0915+1 fanin`, episode
`flow-20260915-074114-4e7ca53aaca6`): a legitimate install cannot produce a corrupt or
empty receipt (`_write_activation_receipt` is the sole writer, runs after the tree is in
place; a failed write reads as absent; rollback restores the prior text). Reading a
damaged receipt as "no receipt yet" would downgrade an edited/tampered hook (the L2 HIGH
class, `os.system(...)` appended, reproduced 2026-09-13) from broken (exit 1) to pending
(exit 6). doctor is a gate and fails closed here, matching the pack-provenance corrupt
refusal already in the same function. The contract stays 0/1/6 — no new exit code.

## SIBLING GREP (DONE commit)

`read_activation_receipt` (the C6 site's read) is called from exactly ONE place in
`doctor.py`: `_check_hook_freshness` (grep above). `read_pack_locks_status`
(`doctor.py:1561`) is a distinct function over a distinct lock file, already fails closed
on its own `"corrupt"` status ~90 lines above the C6 site — not a receipt-status consumer,
out of scope for this amendment.

## RESIDUAL — glm coverage incomplete twice on the C6 diff

`deep_review.py --diff aafce33 --paths levain/doctor.py tests/test_hook_freshness_receipt_status.py`
ran with `--seats complement,codex,glm`: complement and codex both completed clean, no HIGH.
glm was CUT OFF mid-read both times (first run and a solo retry) — 2 files opened, the script's
own "do not chain a commit off it" warning fired both times. The solo retry DID surface one real
LOW before being cut off (`test_corrupt_and_empty_name_the_receipt_path_and_remedy` only exercised
the "corrupt" mode despite its name/docstring promising "corrupt AND empty" — fixed by
parametrizing over both; 7 tests now pass in that file, was 6). Capped at one retry per
`0915+1 fanin`. **Coverage on this diff is 2 of 3 lineages (anthropic + openai); glm (zhipu) never
read it through.** Named here rather than silently pushed as if it were a clean 3-seat pass.

## NOT touched, per sequencing

`tests/test_receipt_and_upgrade_exit.py` and `levain/install.py` (the LOW fix) landed as a
separate commit before this ruling arrived; the C6 work above is entirely in `doctor.py`
plus a new file `tests/test_hook_freshness_receipt_status.py`, per `0915+1 fanin`'s
instruction to keep them apart until the LOW's L3 rows landed.
