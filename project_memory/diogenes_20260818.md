# DIOGENES OVERNIGHT — levain, 2026-08-18

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-08-18*)`
**Stated count: 4 episode(s).** Severity: COVERAGE 1 · LOW 1 · MEDIUM 1 · STILL OPEN 1
**STILL OPEN: 33**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260818-044354-29b1ce7bb1fb
  Time: 2026-08-18T04:43:54  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/cli.py:1071 (+ :1064) — THE SHARED WALL-CLOCK VALIDATOR HARDCODES `--max-seconds` IN ITS MESSAGE WHILE SERVING TWO DIFFERENT FLAGS, SO REJECTING `--consolidate-max-seconds` MISDIRECTS THE OPERATOR TO A FLAG THAT IS PRESENT AND CORRECT ON THE SAME COMMAND LINE. `_reject_bad_max_seconds` is called at FOUR sites (`grep -cF 'if _reject_bad_max_seconds(' levain/cli.py` returns 4): :989 and :1113 validate `--max-seconds` for `run` and `wrap`, and :992 and :1378 validate `--consolidate-max-seconds` for `run` and `daemon install-seat`. Both of its refusal strings name only the first flag: [quoted: `            f"levain {command}: --max-seconds must be >= 0, got {value:g}. "`] at :1071, and the non-finite twin at :1064. The `command` kwarg varies; the FLAG NAME does not. RUN-VERIFIED, NOT REASONED — three invocations against a scratch dir, each exiting 2. `levain run <dir> --task x --consolidate --consolidate-max-seconds -1` prints `levain run: --max-seconds must be >= 0, got -1. Use 0 to disable the wall-clock bound explicitly.` and `levain run <dir> --task x --max-seconds -1` prints the SAME LINE, byte for byte, for a different flag. The nan case behaves identically (`--consolidate-max-seconds nan` -> `--max-seconds must be a finite number of seconds, got nan`). THE FORM THAT MAKES IT A DEFECT RATHER THAN A TYPO: `levain run <dir> --task x --max-seconds 600 --consolidate --consolidate-max-seconds -1` prints `levain run: --max-seconds must be >= 0, got -1.` — the operator is told to fix `--max-seconds`, whose value is 600 and is valid, while the flag actually carrying -1 is never named. The value in the message (-1) does not match the value of the flag the message names (600), so the two halves of one sentence describe different arguments. AND THE TEST THAT GUARDS THIS PATH STRUCTURALLY CANNOT SEE IT. `tests/test_cli.py` has three rejection tests — :195 (`--max-iterations`), :209 (install-seat `--max-seconds`), :226 (run `--max-seconds`) — and all three assert the same substring: [quoted: `    assert "must be >= 0" in capsys.readouterr().err`] (`grep -cF` returns 3). The assertion deliberately excludes the flag name, so it passes whichever flag the message names. There is NO rejection test for `--consolidate-max-seconds` on either command — the flag with two call sites into the shared validator is the one flag with zero coverage of its refusal path. THE FUNCTION'S OWN DOCSTRING NAMES THE RISK IT THEN TOOK. :1046 reads [quoted: `    ONE validator for every command that takes the flag (``run``, ``wrap``, ``daemon install-seat``),`] — it enumerates the three COMMANDS and treats "the flag" as singular, which is exactly the assumption that fails: the fourth caller is a second FLAG, not a fourth command, and the shared body was written for a world with one flag name in it. `guard_scoped_by_symptom_misses_the_class`, one axis over from where the docstring is looking. WHAT IT COSTS, AT ITS REAL SIZE AND NOT INFLATED: the refusal itself is CORRECT and fails in the safe direction — exit 2, nothing runs, no unbounded consolidate is installed. This costs an operator debugging time against a message that points at the wrong argument, on the `daemon install-seat` path where the command line is composed once and then lives in a plist. It is a diagnosis defect, not a governance hole. -- FIX (worked out, NOT executed): give the validator the flag name it is validating — add a `flag: str = "--max-seconds"` kwarg and interpolate it in both strings, passing `flag="--consolidate-max-seconds"` at :992 and :1378. The acceptance test is the one the existing class is missing and it is free: assert the REJECTION MESSAGE NAMES THE FLAG THAT WAS PASSED, not merely that the run exited 2 — an assertion on `"must be >= 0"` alone cannot fail on this defect, which is why three tests over this code did not. [prescription: reasoned]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260818-044354-b940d6a6233c
  Time: 2026-08-18T04:43:54  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: docs/upstream-defects.md:80 — THE ONE PLACE THIS DOC OFFERS A GREPPABLE RECEIPT NAMES A TEST THAT HAS NEVER EXISTED IN THIS REPO, AND IT SHIPPED WRONG IN THE SAME COMMIT THAT CREATED THE TEST IT MEANS. The doc's whole thesis is that a shield's known blind spot must be PINNED rather than merely described — :79-80 reads [quoted: `implicit: `test_KNOWN_BLIND_SPOT_cjk_mojibake_passes_through_unflagged`.`]. That symbol appears in exactly one tracked file: this doc. `grep -rc` across the repo returns hits only at `docs/upstream-defects.md` and inside `.worktrees/` (gitignored — `.gitignore:21`). The real test is named [quoted: `def test_KNOWN_BLIND_SPOT_three_byte_script_mojibake_passes_through_unflagged():`] at `tests/test_firing_encoding.py:82`, and it is genuine and does the job — it asserts `not scan.repairs`, `not scan.suspect` and `scan.clean` over the corpus `("北京市", "Γειά σου", "한국어")`, i.e. the CJK/Greek/Hangul damage the doc says passes through unflagged. SO THE SUBSTANCE IS TRUE AND THE RECEIPT IS UNFOLLOWABLE, WHICH IS THE PART THAT MATTERS. A reader doing the one thing this sentence invites — grep the name to confirm the limitation is pinned by an executable test rather than by prose — finds nothing and correctly concludes the pin does not exist. The doc's own framing is what raises the cost: `A shield is not a cure` (:8) and the closing honesty paragraph at :82-84 stake the document's credibility on its claims being checkable, and this is the only checkable one it offers for the blind spot. NEVER-CORRECT, NOT ROTTED, AND I CHECKED WHICH. `git log -S` on both names over `tests/` and `docs/` returns the SAME single commit, `76d0e89` (2026-07-20, "Fix the two capture-integrity defects: turn boundary + mojibake shield") — so the doc line and the differently-named test were added together and disagreed on arrival. The draft name survives in `.worktrees/capture-integrity-0720/.pytest_cache/v/cache/nodeids`, which is the provenance: the test was renamed late inside that worktree and the prose kept the pre-rename name. `a_fix_that_removes_one_wrong_claim_ships_another_in_the_same_hunk`. It has been wrong for 29 days. THE SIBLING CITATION IN THE SAME DOC IS CORRECT, which is what makes this a slip rather than a convention: :91 cites `tests/test_turn_input.py::test_multibyte_character_split_across_read_chunks_survives` with a FILE PATH, and both halves check out (`tests/test_turn_input.py:129`). The one that broke is the one written as a bare symbol with no path — a bare name has nothing to cross-check it against, and the doc contains one of each. -- FIX (worked out, NOT executed): correct :80 to the real symbol and give it the path form its sibling at :91 already uses — `tests/test_firing_encoding.py::test_KNOWN_BLIND_SPOT_three_byte_script_mojibake_passes_through_unflagged`. Renaming the test to match the doc is the wrong direction: `three_byte_script` is the accurate name (the corpus is Greek and Hangul as well as CJK, and the mechanism is the 0xE0-0xEF lead range, not a script). I did not execute it because the durable fix is not this edit — nothing checks that a test name cited in `docs/` resolves, and this doc cites two. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260818-044434-fd7982f1d113
  Time: 2026-08-18T04:44:34  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: STILL OPEN (33), TOTAL AT HEAD a74d7a5 — levain, 2026-08-18. 32 carried, 2 new, ONE CLOSED. The window is a single doc-only commit adding project_memory/diogenes_20260817.md, the routed report of my own 2026-08-17 findings — the TWELFTH consecutive levain window whose entire content is a Diogenes report. So I stopped reviewing the router tonight and opened levain source instead, and both new findings are levain-tree. CLOSED (1), FLOW-SIDE, VERIFIED BY RUNNING RATHER THAN BY READING THE COMMIT: my 08-17 HIGH, the `--tags` filter dropped for seven of eight federated stores. Fixed at flow d76d5123 ("Honour --tags on all eight federated stores"). I re-ran my own two repro probes against the shipped code: `episodic.py read --since 24h --tags zzz_no_such_tag_zzz --limit 10` now returns `No episodes match filters.` where it returned TEN argus episodes, and the consequence in this repo's artifact is gone — a direct SQL join on episode_tags for tag=levain, agent=diogenes, timestamp LIKE '2026-08-17%' returns FIVE rows, project_memory/diogenes_20260817.md contains exactly FIVE blocks, all five ids present and all five contents verbatim (checked by substring against the store, not by eye), and zero argus weekly_drift_sweep passengers. The 08-16 report had nine blocks for six real ones. STILL OPEN AND RE-CHECKED AT THE SOURCE, NOT ASSUMED CLOSED BY THE SYMPTOM GOING QUIET: the other two 08-17 findings are untouched. route_diogenes.py `severity()` is byte-unchanged (:310-324, still `hit.start()` over word-bounded labels with no head anchor and no hyphen rejection) — tonight's header carries no phantom LOW only because the tag leak stopped admitting unlabelled argus prose, which is exactly the instance-not-class distinction my 08-17 finding pre-registered. prescription.py:116 is byte-unchanged, still [quoted: `_RETRACTION = re.compile(rf"\A\s*SELF[- ]CORRECTION\s+to\s+({_ID})", re.I)`], still reading 16 of 33. FLOW TREE: 11 - 1 = 10. LEVAIN TREE (23) — 21 carried, 2 new. The 21 are verified by GIT BYTE-IDENTITY, and the pathspec is wider than last night's on purpose: `git diff --stat 997eb56 HEAD -- levain/ tests/ pyproject.toml README.md docs/ scripts/` is EMPTY, and `git diff --numstat 997eb56 HEAD -- project_memory/` shows exactly two pure additions (diogenes_20260816.md 94/0, diogenes_20260817.md 57/0) with no existing report modified. I added `docs/` and `scripts/` to that pathspec tonight because one of my new findings lives in `docs/` and my prior pathspec would not have bounded it — my own 08-13 MEDIUM about a pathspec that cannot bound its population, fired at my own instrument. THREE CARRIED ITEMS RE-EXECUTED RATHER THAN CARRIED. (1) FULL SUITE RUN HERE: 8 failed / 2081 passed / 18 skipped in 102.76s — the same counts as my 08-15, 08-16 and 08-17 runs, and `test_live_plain_config_cannot_be_unlinked_or_relocated` FAILED rather than skipped for the twelfth night, so tests/test_firing_confinement.py:1113's [quoted: `    from levain.firing.openhands.tools import SandboxedBashExecutor  # openhands leaf, gated by @live`] is still FALSE. The other 7 failures are all tests/test_app_server.py (mcp extra absent), none from any diff. (2) README.md:164 still reads `>=0.9.6,<0.10` against pyproject.toml:74's [quoted: `    "anneal-memory>=0.9.7,<0.10",`] at version 0.4.1 — re-read tonight, unchanged, and still latent per my 08-16 self-correction; I also confirmed the release gate cannot catch it, because `pip_floor_verdict` (levain/manifest.py:571) compares KNOWN_GOOD_ANNEAL to the INSTALLED DIST METADATA and never reads README.md, so the README is a fourth pin site outside the gate that names three. (3) `scripts/check_seed_budget.py` run: clean, 38,759 B eager / 55% mechanism against limits of 40,000 B / 62%. NEW TONIGHT (2), both levain-tree, both opened by reading source rather than reading my own reports: levain/cli.py:1071 (+ :1064) — the shared `--max-seconds` validator hardcodes that flag name in both refusal strings while serving `--consolidate-max-seconds` at two of its four call sites, so a bad consolidate bound is refused with a message naming a flag that may be present and valid on the same command line; run-verified three ways, and the three tests over this code assert only the substring `"must be >= 0"`, which structurally cannot fail on it. docs/upstream-defects.md:80 — the one greppable receipt the doc offers for its own known blind spot names `test_KNOWN_BLIND_SPOT_cjk_mojibake_passes_through_unflagged`, which has never existed; the real test is `three_byte_script` at tests/test_firing_encoding.py:82, and `git log -S` puts both strings in the SAME commit 76d0e89, so the citation was wrong on arrival 29 days ago rather than rotted. HYPOTHESES OPENED AND KILLED AGAINST DISK, STATED BECAUSE A DROPPED FINDING IS EVIDENCE ABOUT THE INSTRUMENT: (a) I suspected `daemon install-seat` hand-rolled its `--max-seconds` check and omitted the non-finite guard — FALSE, cli.py:1355 has it; I had read from :1360 and missed the block above, and would have filed a fabricated HIGH. (b) I suspected the seed templates' `1x->2x->3x` enumeration went stale under AM-LEVELCAP's no-ceiling Proven tier — the commit 6281cf6's safety argument HOLDS on inspection, because levain/templates/seed/memory.md never instructs an entity past 3x, so no seed-following entity produces a line the old regex could not see. (c) A mechanical sweep of all 51 distinct `levain.*` Sphinx cross-references in levain/ found two apparent misses, BOTH false positives of my own checker (the `openhands` extra is not installed here, so the import failed) — `levain/firing/openhands/gate.py` and `entity.py:205 def build_entity_agent` both exist on disk. NOT COUNTED AS OPEN, unchanged for the twelfth night: the environment-dependent severity argument in the 08-04 report, and the pack-hook DELETION case I have declined on 08-06, 08-07, 08-08, 08-10, 08-12, 08-13, 08-14, 08-15, 08-16 and 08-17 and decline again. ARITHMETIC STATED SO THE TREND SURVIVES: 32 at afe7481 = levain 21 + flow 11; tonight flow 11 - 1 closed = 10, levain 21 + 2 new = 23; 23 + 10 = 33. REPO UNTOUCHED: `git status --porcelain` empty at a74d7a5 before and after the suite run, HEAD == origin/main; flow's tree read only; every mutation and probe ran in /tmp/levain_dio_20260818 or against read-only reads. ALL QUOTED STRINGS PRE-FLIGHTED WITH `grep -cF` AGAINST THE FILE ON DISK BEFORE FILING, and every coordinate confirmed with `grep -n` at HEAD.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260818-044505-326fa65ab642
  Time: 2026-08-18T04:45:05  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: levain 2026-08-18, HEAD a74d7a5 == origin/main, porcelain empty, window HEAD~1..HEAD — one doc-only commit adding project_memory/diogenes_20260817.md (57 lines, 5 blocks), the routed report of my own 08-17 findings. That is the TWELFTH consecutive levain window made entirely of my own prose, and last night's coverage line said the levain code axis had gone eleven days without a source file opened. So tonight deliberately inverted the split: the router got a verification pass, and the rest of the night went into levain/. RE-DERIVED BY RUNNING, NOT CARRIED FORWARD. (1) THE ARTIFACT CHECKED AGAINST THE STORE, not against the diff: a direct SQL join on episode_tags (tag=levain, agent=diogenes, timestamp LIKE '2026-08-17%') returns 5 rows; the committed file holds 5 blocks; all 5 ids present and all 5 contents verbatim by substring match; zero foreign passengers, against nine-blocks-for-six in the 08-16 file. (2) THE `--tags` FIX PROVEN BY RE-RUNNING MY OWN REPRO, not by reading flow d76d5123's commit message: the nonsense-tag probe that returned ten argus episodes now returns `No episodes match filters.` (3) THE OTHER TWO 08-17 FINDINGS RE-READ AT SOURCE and confirmed byte-unchanged (route_diogenes.py severity() :310-324, prescription.py:116) rather than inferred closed from a clean header. (4) FULL LEVAIN SUITE RUN HERE: 8 failed / 2081 passed / 18 skipped in 102.76s, all 8 enumerated — 7 mcp-absent in test_app_server.py plus test_live_plain_config_cannot_be_unlinked_or_relocated, which FAILED rather than skipped, re-executing the :1113 finding for the twelfth night. (5) `scripts/check_seed_budget.py` EXECUTED: clean, 38,759 B eager / 55% mechanism against 40,000 B / 62%. (6) A MECHANICAL SWEEP I WROTE AND THEN DISTRUSTED: all 51 distinct `levain.*` Sphinx cross-references in levain/ resolved by import; the 2 reported misses were my own checker's false positives (openhands extra absent) and both symbols exist on disk. (7) THE NEW cli.py FINDING RUN-VERIFIED THREE WAYS against a scratch dir — the bad consolidate bound alone, the nan variant, and the composite with a VALID `--max-seconds 600` alongside, which is the form that shows the message naming the wrong argument. (8) THE upstream-defects CITATION TRACED WITH `git log -S` on both symbol names over tests/ and docs/, which is what established it was never-correct rather than rotted. SOURCE OPENED IN FULL OR IN THE RELEVANT SPAN: levain/cli.py :720-750, :958-1150, :1340-1420; levain/manifest.py :440-620; levain/wrap.py :112-250, :540-610; levain/firing/deadline.py in its entirety (all 388 lines); levain/idempotency.py :1-90, :340-450; levain/writes.py :505-560; pyproject.toml :1-110; docs/upstream-defects.md (all 91 lines); levain/templates/seed/memory.md :1-60; tests/test_cli.py :195-240; tests/test_firing_encoding.py :82-99; tests/test_firing_confinement.py:1113; README.md:164. Plus the whole diff and the committed report. NOT CHECKED, AND SOME OF IT MATTERS. Twelve days of concentration show in what I have still never opened: `grep` of my own eleven levain reports cites levain/doctor.py 39 times and levain/install.py 8, while writes.py (2,129 lines), dashboard.py (1,861), web_server.py (1,411), confinement.py (1,514), session.py, interview.py, tui.py, run.py, jobs.py, verify.py and reconcile.py have never been named in a finding — I read fragments of writes.py and idempotency.py tonight and opened none of the others. I did NOT re-execute the two heavy 08-14 repros (doctor.py:1169 pack-hook false red, doctor.py:1140 corrupt-pack-lock bypass); they rest tonight on git byte-identity plus an unchanged suite result, which is a structural argument, not a re-run. I did NOT run `levain doctor` end to end, did NOT run `levain init`, did NOT exercise `daemon install-seat` past its entity-init guard (so my cli.py finding is run-verified at the `run` call site and READ-verified at the install-seat one — same function, same string, but I did not make that door emit). I did NOT patch cli.py or docs/upstream-defects.md, did NOT write the acceptance test I prescribed, and did NOT run the suite under any change. I did NOT check the public remote tonight (last night's HTTP 200 on the levainhq/levain report is carried, not re-hit), and did NOT re-verify the 5 record-axis items beyond the byte-identity argument. AND THE THING WORTH SAYING PLAINLY: the two findings I filed tonight are a misdirecting error message and a wrong test name in a doc. Both are real and both are grounded, and neither is the class that a twelve-day gap on 15,000 lines of unreviewed source would be expected to hide. An honest reading of tonight is that I finally opened the door and the shallow end of the room was clean — not that the room is. The full constellation drift sweep, the cognitive half of my dual lens, has now not run since 2026-05-24, TWELVE WEEKS. [prescription: none]
  Tags: code_review, levain, semantic
```
