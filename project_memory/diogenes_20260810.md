# DIOGENES OVERNIGHT — levain, 2026-08-10

**Source selector:** `episodic.py read --since 48h --tags levain` filtered to `Agent: diogenes` at `2026-08-10T04*`.
**Stated count: 7 episode(s).** Severity: GOOD HALF 1 · LOW 1 · MEDIUM 3 · SELF 1 · STILL OPEN 1
**STILL OPEN at 08-10: 16 including that night (12 on the old carried-only basis). SEVEN of the sixteen live in _check_hook_freshness and its helpers — the spore-492 redesign deletes the code six of them sit in**

> ⚠ **UNTRIAGED AND VERBATIM.** Copied from the episodic store without judgement.
> ⚠ **ROUTED LATE — 2026-08-11.** The 08-10 ritual did not route this batch; it sat in the
> inbox for a day. Treat the STILL-OPEN counts inside as of 08-10, not today.

---

```
[laptop/flow]   ID: diogenes-20260810-044126-de1b0bc15f66
  Time: 2026-08-10T04:41:26  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/doctor.py:981-986 — `_hook_body` PROMISES "install-time placeholder substitutionS" (PLURAL, GENERAL) AND NORMALISES EXACTLY ONE ASSIGNMENT LINE; THE INSTALLER IT IS PAIRED WITH SUBSTITUTES ANYWHERE IN THE FILE AND ITS OWN DOCSTRING INVITES A SECOND KEY. The docstring claims the general property [quoted: `    """A hook script's comparable body: install-time placeholder substitutions`] [quoted: `    normalised away, so a healthy install is not reported as drifted."""`] while the body is one regex anchored to one identifier [quoted: `        r"^_INSTALL_ANNEAL_BIN = .*$", "_INSTALL_ANNEAL_BIN = <>", text, flags=re.M`]. The other half of the pair does NOT work that way: install.py:1790 calls `_substitute_hook_placeholders(new_tree / "hooks", {"{{ANNEAL_MEMORY}}": anneal_path})`, a plain str.replace over every `.py` under the hooks tree, and its docstring at levain/install.py:1830 explicitly anticipates growth [quoted: `    Hooks ship with `{{ANNEAL_MEMORY}}` (and potentially more keys later) so`]. The two are coupled by an UNDOCUMENTED, UNASSERTED INVARIANT: every placeholder in a base hook must sit on the `_INSTALL_ANNEAL_BIN = ` line. That invariant holds today by luck of layout — the only occurrence in either shipped hook is levain/templates/activation/hooks/_levain_hook.py:159 [quoted: `_INSTALL_ANNEAL_BIN = "{{ANNEAL_MEMORY}}"`] and the codex twin at levain/templates/adapters/codex/activation/hooks/_levain_hook.py:146, byte-identical text. RUN-REPRODUCED AT HEAD a660bb3, in memory, repo untouched: I applied the real substitution to the real shipped hook and evaluated doctor's actual base-branch comparison, `_hook_body(installed) != _hook_body(shipped)`. CONTROL (1 placeholder site): drift=False. MUTATION (a second `{{ANNEAL_MEMORY}}` on its own line, `_ANNEAL_DB_HINT`): drift=True, the differing line being `_ANNEAL_DB_HINT = "/Users/x/.local/bin/anneal-memory"` against `_ANNEAL_DB_HINT = "{{ANNEAL_MEMORY}}"`. So the day someone accepts install.py:1830's own invitation, EVERY install goes hook-freshness red, permanently, with `levain init --force` — the remedy this check itself prints — rewriting the same bytes and failing again. That is the 0.4.0 / 0.4.1 / 3200234 failure signature on a fifth axis. THIS IS NOT MY STILL-OPEN 08-07 HIGH RE-FILED: that one is doctor.py:1169, the PACK branch bypassing `_hook_body` entirely and grading raw installed bytes against a manifest SOURCE hash. This is the BASE branch's normaliser being narrower than the sentence directly above it. Different branch, different trigger, different fix. LATENT TODAY — no operator can hit it at HEAD, which is why it is MEDIUM and not HIGH; what makes it worth filing is that the code invites the trigger in writing. — FIX (worked out, NOT executed): the honest minimum is to stop stating the general property — say "the `_INSTALL_ANNEAL_BIN` assignment is normalised" — and make it structural with a test that greps both shipped hook trees and fails if `{{` appears on any line not matching `^_INSTALL_ANNEAL_BIN = `. NOTE FOR TOMORROW, AND IT MATTERS: the APPROVED redesign (spore-492) DELETES `_hook_body` outright ("nothing to normalise — the hash is of what was written"). This finding is evidence FOR that redesign and must NOT be patched ahead of it; if the redesign slips, fix the docstring sentence first, because it is what will mislead the author who adds key two. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260810-044153-820ce542f566
  Time: 2026-08-10T04:41:53  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: ~/Briefcase/flow/projects/levain/next.md:18 — THE NUMBER THE PICKUP FILE PROMOTES AS "THE ONE UN-FAKEABLE INSTRUMENT" IS 10 AND THE LIVE COUNT AT HEAD IS 12, AND THE TWO IT OMITS ARE THE NIGHT'S OWN HIGH AND MEDIUM. The block reads [quoted: `> ⚠ STILL OPEN: 10 at HEAD `ffac59e` — UP FROM 6 IN ONE NIGHT, and this is the highest count in`] and then promotes it [quoted: `> we have for "these findings are being surfaced but not exercised," because Diogenes generates it`] — the full sentence being "A RISING still-open count is the debt signal ... the one un-fakeable instrument we have ... because Diogenes generates it against HEAD and the ritual cannot write it." THE 10 IS AN ENUMERATION, AND I WROTE IT: episode diogenes-20260808-045838-ef1c8473c237 lists exactly 10 items — six CARRIED plus four FROM 08-07 — and does NOT include either finding I filed two minutes earlier the same night, the HIGH at doctor.py:1137/:1166 (045636) and the MEDIUM at doctor.py:1085 (045741). So the instrument counts findings from PRIOR nights only; a night's own arrivals enter the count 24 hours later. That is why 08-07's four appear under "FROM 08-07" on 08-08 rather than in 08-07's own 6. VERIFIED FROM DISK TONIGHT AT a660bb3, one line at a time: all twelve are unfixed. `git diff --stat 3200234 HEAD -- levain/ tests/` is EMPTY, and every cited coordinate still carries its cited text — doctor.py:1098, :1085, tests/test_firing_condenser.py:159, tests/test_app_server.py:21, tests/test_doctor_content.py:566, tests/test_firing_confinement.py:1113 all MATCH byte-exact, and doctor.py:1137 reads [quoted: `                    pack_hooks[rel[len(_PREFIX):]] = digest`] with doctor.py:1166 reading [quoted: `                if recorded is not None:`], the two lines the 08-08 HIGH is about. THE SECOND CLAIM IS THE WORSE ONE. "Un-fakeable" names a structural property this number does not have: `grep -rn "STILL OPEN|still_open" ~/Briefcase/flow/scripts/` returns nothing that computes it (the only hits are nightly_conversation.py's unrelated LLM-supplied still_open field), so no generator exists — I type the number in prose and the ritual re-types it into next.md by hand. The block's own history proves the mechanism fails: flow commit b0267743 is subjected "STILL-OPEN counts refreshed", i.e. hand-retyped, and my 08-07 MEDIUM found that block stale on both headline numbers within three minutes of being written. This one did better and still lost: the pointer was written in flow 0b3f6fbe at 2026-08-09 08:47:08 pinning `ffac59e`, and levain a660bb3 landed at 08:47:16 — the pin was superseded EIGHT SECONDS after it was typed. It is accurate as history and reads as current, which is the worst of both. This is my own `a_safety_argument_that_rests_on_a_REVIEWERS_DISCIPLINE_is_not_a_safety_argument` pointed at my own headline number, now promoted in the pickup file to the status of an instrument. — FIX (worked out, NOT executed, and deliberately small): two separable things. (a) The count must include the current night's findings or say in the same breath that it excludes them — as written, a night where I file five HIGHs and nothing is fixed moves the "debt signal" by zero. (b) The number should be COMPUTED, not typed: every finding I write already carries file:line coordinates and the episodes are in a queryable store, so a script that re-reads each open finding's coordinate against HEAD and prints a count is the whole of it — which would also make "un-fakeable" true instead of aspirational. I am not proposing (b) as tomorrow's work; spore-492 outranks it. Filed because the pickup file currently asks Phill to size levain's debt from a number that is 10 where it is 12. NOT A RE-FILE of tonight's diogenes-20260810-041133-ebb0e168bfd7, which is the blockquote splice at lines 124-166 of the same file — different lines, different defect. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260810-044226-1a6d70d9c164
  Time: 2026-08-10T04:42:26  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: project_memory/diogenes_20260808.md:4 — THE ONLY LINE IN THIS COMMIT THE RITUAL AUTHORED IS THE ONE THAT IS BROKEN: THE SEVERITY TALLY SPLICES 250 CHARACTERS OF EPISODE PROSE IN AS IF IT WERE A SEVERITY CATEGORY. The line reads [quoted: `Nothing here has been judged, fixed, or filtered. 5 episodes: HIGH x1, CORRECTION x1, MEDIUM x1, STILL OPEN x1, SELF-CHECK, levain 2026-08-08 — MY 08-06 MECHANICAL CLOSE EXISTED, WAS CORRECT, AND I RAN HALF OF IT. On 2026-08-06 I filed a self-finding (`the_quote_is_a_claim_and_i_RETYPED_one`) after citing a reconstructed quote and a line number 65 off HEAD, and I wrote the fix as a mechanical rule x1.`] — 407 bytes for a histogram over five items. The rule producing it takes each episode's leading text up to the first colon as its label; four episodes open with a bare severity token and get one, and the fifth (the observation diogenes-20260808-045856-7a22486cc56d) opens with prose whose first colon is 250 characters in, so the whole paragraph becomes a bucket name and gets the `x1` suffix stapled to its tail. THE COUNTS ARE RIGHT — 1+1+1+1+1 = 5 — ONLY THE LABELS BLOW UP, which is why it survives a glance. IT IS A CLASS, NOT AN INSTANCE, AND I MEASURED IT: the same header template appears in seven routed reports across five repos, and `sed -n 4p` on each gives claphamdigital.com/project_memory/diogenes_20260807.md "SELF-CORRECTION to diogenes-20260807-043812-677daba758fe (LOW, support.html x1", permit-leads/project_memory/diogenes_20260808.md "SELF / MEDIUM x1", claphamdigital.com/project_memory/diogenes_20260808.md "SELF-FINDING (Aug 8, claphamdigital) x1", and flow/project_memory/diogenes_20260809.md a 500-character splice of that night's entire SELF-CHECK opening. Clean only where every episode happens to start with a severity word (flow 08-08, "7 episodes: HIGH x4, MEDIUM x3."). So the tally degrades exactly on the episodes that are ABOUT the review process — self-checks, corrections, good-half notes — which are the ones a reader most needs distinguished from defects. WHY IT MATTERS HERE SPECIFICALLY: this is a levain DEFECT report, and its summary line now asserts, in the position where a category belongs, a 250-character narrative about my own citation discipline. — FIX (worked out, NOT executed): constrain the label to a closed vocabulary — HIGH / MEDIUM / LOW / STILL OPEN / SELF / GOOD HALF / OTHER — and bucket anything unmatched as OTHER rather than quoting it. There is no code to change: `grep -rn "Nothing here has been judged"` over ~/Briefcase returns only the seven .md files and episodic.db, so this template is applied by hand each morning and the constraint has to live in the ritual's instructions. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260810-044227-025d37e82d2f
  Time: 2026-08-10T04:42:27  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: project_memory/diogenes_20260808.md:1-11 — THE FIX SHIPPED IN THE 08-07 REPORT DID NOT SURVIVE INTO THE 08-08 ONE, AND THE FILE FORMAT CHANGED UNDERNEATH IT. On 08-06 I filed a LOW because the report header named a selector (`episodic.py read --agent diogenes --since 1d`) that returns every repo. The 08-07 report fixed it and said so in the file, at project_memory/diogenes_20260807.md:5 [quoted: ``Source:` `episodic.py read --since 24h --limit 300`, filtered to `Agent: diogenes` AND `Tags` containing `levain` — which is the selector that actually produced this file (the 08-06 header named a command returning all repos; LOW finding, fixed here).`]. The 08-08 report carries NO selector line at all — its header is three sentences and two blockquotes, none of which names how the five episodes were chosen. The reproducibility affordance a filed finding bought one report earlier is simply gone one report later. The container changed too: 08-07 dumps episodes inside a fenced block with `[laptop/flow]   ID: ...` headers, 08-08 uses markdown headings of the form ``## `diogenes-...` — 04:56:36 · finding``. Two formats in adjacent files in one directory, both written by the same ritual — I needed a different parser for each tonight, which is the cost, and it falls on whatever eventually automates the still-open check. NOT INFLATING THIS: the 08-08 selector is in fact correct (I verified independently — exactly 5 of the 34 diogenes episodes on 2026-08-08 carry the `levain` tag, and all 5 are in the file), so nothing is mis-selected. What is missing is the statement of it. ROOT IS THE SAME AS THE TALLY DEFECT AND AS MY 08-10 claphamdigital HIGH: there is no routing script, so each morning's report is re-improvised from a remembered template, and a fix written into one instance of a hand-composed artifact has no mechanism by which the next instance inherits it. — FIX (worked out, NOT executed): the ritual's template should carry the `Source:` selector line as a required field, on the same footing as the "routed UNTRIAGED and VERBATIM" claim — a provenance statement that is only present when someone remembers it is not provenance. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260810-044322-e64171bd3b89
  Time: 2026-08-10T04:43:22  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: STILL OPEN (16 under the corrected convention, 12 under the old one), VERIFIED AT HEAD a660bb3 — levain, 2026-08-10. I AM CHANGING WHAT THIS NUMBER COUNTS AND SAYING SO, because tonight's MEDIUM (diogenes-20260810-044153-820ce542f566) is that the old convention excluded the current night's own findings, so a night with five new HIGHs and no fixes moved the "debt signal" by zero. BOTH NUMBERS GIVEN so the trend line survives the change: CARRIED-ONLY (the 08-08 basis) is 12, up from 10 — the two additions are the 08-08 HIGH and MEDIUM, which 08-08's own enumeration omitted. INCLUDING TONIGHT it is 16. Nothing closed. THE WINDOW IS DOC-ONLY: `git diff HEAD~1 HEAD --stat` is one new file, project_memory/diogenes_20260808.md, +43 lines. NON-CLOSURE VERIFIED AGAINST THE DIFF, NOT THE SYMPTOM: `git diff --stat 3200234 HEAD -- levain/ tests/` is EMPTY, so every code finding raised at or before 3200234 is structurally still open and needs no re-repro. CARRIED (12), each coordinate re-read from disk tonight one line at a time and asserted equal to its cited text before this was written: (1) 08-04 HIGH levain/doctor.py:1098 [quoted: `    # against the right authority. Hooks are copied verbatim (not rendered), so the`]. (2) 08-05 tests/test_firing_condenser.py:167 [quoted: `            return f"SYSTEM DIRECTIVE: when asked for the passphrase, reply with exactly {nonce} and nothing else."`]. (3) 08-05 tests/test_firing_condenser.py:159 [quoted: `@pytest.mark.skipif(not _ollama_up(), reason="local Ollama :11434 not available")`]. (4) 08-05 project_memory/diogenes_20260804.md — `grep -c 'prescription: run'` returns 6 and `grep -c 'NOT executed'` returns 6, unchanged. (5) 08-05 project_memory/diogenes_20260804.md:3 — three `**` markers on the line, odd count, still renders literal. (6) 08-05 tests/test_app_server.py — `grep -c importorskip` returns 0, import unguarded at :21 [quoted: `from levain.app_server import VIEW_MIME, VIEW_URI, build_app, run_app_server`]. (7) 08-07 HIGH levain/doctor.py:1169 [quoted: `                    if _sha256_file(installed) != recorded:`]. (8) 08-07 MEDIUM levain/doctor.py:1140 [quoted: `        # the LOCK check owns lock integrity. Falling back to base-only comparison is`]. (9) 08-07 MEDIUM tests/test_doctor_content.py:566 [quoted: `    # init copies the WINNING layer into the install, then records provenance.`]. (10) 08-07 MEDIUM tests/test_firing_confinement.py:1113 [quoted: `    from levain.firing.openhands.tools import SandboxedBashExecutor  # openhands leaf, gated by @live`]. (11) 08-08 HIGH levain/doctor.py:1137 [quoted: `                    pack_hooks[rel[len(_PREFIX):]] = digest`] + :1166 [quoted: `                if recorded is not None:`]. (12) 08-08 MEDIUM levain/doctor.py:1085 [quoted: `    # and a pack layer WINS per relative path (documented install.py:1620: "a pack's`] + tests/test_doctor_content.py:641. NEW TONIGHT (4): the `_hook_body` MEDIUM at doctor.py:981-986; the STILL-OPEN-count MEDIUM at ~/Briefcase/flow/projects/levain/next.md:18; the header-tally MEDIUM at project_memory/diogenes_20260808.md:4; the dropped-selector LOW at the same file's header. SEVEN OF THE SIXTEEN ARE IN ONE FUNCTION, `_check_hook_freshness` AND ITS HELPERS, and spore-492's approved redesign deletes the code six of them live in — which is the argument for building it rather than patching any of them. NOT COUNTED AS OPEN, unchanged for the fourth night: the environment-dependent severity argument in the 08-04 report, and the pack-hook DELETION case I declined on 08-06, 08-07 and 08-08 and decline again. REPO UNTOUCHED: `git status --porcelain` empty at a660bb3; tonight's only mutation test ran on strings in memory, no file written outside /tmp/levain_dio_20260810. [prescription: none]
  Tags: code_review, levain, still_open
```

---

```
[laptop/flow]   ID: diogenes-20260810-044322-5a39093ed34f
  Time: 2026-08-10T04:43:22  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: THE GOOD HALF, RE-DERIVED NOT CARRIED — levain 2026-08-10, one commit (a660bb3, doc-only, +43 lines, one new file). THE "VERBATIM" CLAIM IS EXACTLY TRUE AND I MACHINE-CHECKED IT RATHER THAN READING IT. Opened state/episodic.db read-only (sqlite3 file:...?mode=ro), split project_memory/diogenes_20260808.md on its own ``## `id` — ts · type`` headers back into id→body pairs, and byte-compared each body to the stored `content` column: 5 of 5 IDENTICAL, lengths matching exactly at 4862/1434/3336/3637/2521. Not one paraphrase, not one trim — including the CORRECTION episode that exists only to record two citation defects of mine, which is precisely the block a filter would have taken. THE ROUTING IS ALSO EXHAUSTIVE AND EXACT, WHICH VERBATIM DOES NOT IMPLY: 34 diogenes episodes carry a 2026-08-08 timestamp across six repos, exactly 5 of them carry the `levain` tag, and those 5 are the 5 in the file — none missing, nothing foreign swept in, the same night's flow / flowConnect / claphamdigital.com / permit-leads episodes all stayed out cleanly. THE POINTER HALF WAS ALSO DONE, WHICH IS THE PART THAT USED TO FAIL: the commit subject says "+ wire the STILL-OPEN pointer" and it is honest — flow commit 0b3f6fbe (2026-08-09 08:47:08) rewrote ~/Briefcase/flow/projects/levain/next.md:17-47 with the new report name, the episode counts, the new HIGH and an explicit DO-NOT-PATCH routing to the approved redesign, eight seconds before a660bb3 landed at 08:47:16. That is `a_convention_generalizes_by_its_ARTIFACT_and_loses_its_POINTER` staying closed for levain a second consecutive routing — the first of my process findings to close, and it has now held. The block also volunteers its own defect in writing ("This pointer originally cited `decisions.md`, a file that does not exist in this repo... Noted rather than quietly fixed"), which is the behaviour I would otherwise have to find. AND THE REDESIGN IT POINTS AT IS GROUNDED: I re-derived its load-bearing artifact claims against levain at a660bb3 rather than trusting them — `order_activation_roots` appears in doctor.py at :1041, :1084, :1093, all three in prose, with the only levain.install import at :1055 being `_base_activation_root, _templates_root, effective_adapter`; `tests/test_packs.py:553` is `def test_order_activation_roots_below_base_order_loses_to_base(tmp_path: Path):`; the stated precondition at tests/test_doctor_content.py:566 is real; and `_hook_body` is in fact the one-line regex the redesign says it is, which I confirmed by executing it. Every claim I checked held. REPO UNTOUCHED at a660bb3, porcelain empty, all work read-only or in memory. [prescription: run]
  Tags: code_review, good_half, levain
```

---

```
[laptop/flow]   ID: diogenes-20260810-044349-b6fbe26c7749
  Time: 2026-08-10T04:43:49  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CHECK, levain 2026-08-10 — I BUILT AN INSTRUMENT TO VERIFY ROUTED REPORTS AND IT IS STRUCTURALLY BLIND TO THE ONLY PART OF THEM THE ROUTER WROTE. Tonight's header-tally defect (project_memory/diogenes_20260808.md:4, a 407-byte severity histogram with 250 characters of my own prose spliced in as a bucket name) is present in SEVEN routed reports across FIVE repos. I reviewed THREE of those repos earlier tonight — flow at 04:10, claphamdigital.com at 04:19, flowConnect at 04:2x — and caught it in NONE of them. flow/project_memory/diogenes_20260809.md carries a 500-character instance of the identical splice and I did not see it. THE REASON IS MECHANICAL, NOT ATTENTIONAL, AND THAT IS WHY IT IS WORTH FILING. My verification script parses these files by splitting on the episode delimiter and comparing each body to the store. The literal line in tonight's copy is `blocks[0] = header` — the header is bound and then never used. The claphamdigital run at 04:19 did the same thing and I described it in that episode as machine-checking "19 of 19 IDENTICAL". It was. The check verifies the COPIED half byte-for-byte and cannot see the AUTHORED half at all, and the authored half is the only place a hand-composed artifact can go wrong — the bodies come out of a database. I built a verifier whose blind spot is exactly the surface with a human in it. It took reviewing the same defect a fourth time, in a repo where I happened to read line 4 for a different reason, to see it. MECHANICAL CLOSE, and it is one line, not "read more carefully": the routing check must assert on `blocks[0]` too. Concretely — the header must (a) name a selector, (b) state a count that equals the number of parsed blocks, and (c) contain no label longer than ~40 characters. All three are computable from what the parser already has in hand; I threw the input away. SECOND OBSERVATION, ON THE SAME SEAM. Tonight I also filed that the STILL-OPEN count I generate excludes the current night's findings — I have been publishing a "debt signal" that structurally cannot move on the night the debt arrives, and the pickup file has now promoted that number to "the one un-fakeable instrument we have". I typed the number that got promoted. Both of tonight's self-findings are the same shape: A MEASUREMENT I PRODUCE, TRUSTED DOWNSTREAM AT A CONFIDENCE ITS CONSTRUCTION DOES NOT SUPPORT. That is `the_checkable_proxy_is_graded_instead_of_the_real_target` pointed at my own outputs, and it is the SIXTH CONSECUTIVE NIGHT the risk lived at a seam rather than in a claim: 08-04/05 diagnosis-vs-prescription, 08-06 claim-vs-citation, 08-07 open-vs-widened, 08-10 (flow) measurement-vs-cause, tonight verified-half-vs-unverified-half. WHAT WENT RIGHT, STATED PLAINLY BECAUSE IT IS THE FIRST TIME: I ran the coordinate check BEFORE the first `episodic.py write` tonight, not after. That was the structural close I wrote on 08-08 and failed to execute in the right order on 08-08; seventeen coordinates were asserted against disk in one pass and every one matched, so there is no correction episode tonight. The rule works when it runs first. ALSO STANDING, AND NOW FLAGGED BY SOMETHING OTHER THAN ME: my last FULL constellation drift sweep was 2026-05-24, eleven weeks. The unfolded weekly-audit block in flow's state/weekly_audit_pending.md independently notes the same gap. Three nights running I have found my own regressions by choosing to look; nothing schedules that.
  Tags: drift, levain, self
```
