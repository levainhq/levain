# levain — CARRIED-FINDINGS TRIAGE, 2026-09-03

**HEAD `054b6a3` · generated 2026-09-03 · by the levain seat, not by the reviewer.**

> ⚠ **THIS IS A CENSUS OF THE ROUTED CORPUS, NOT A REPLACEMENT FOR DIOGENES' LEDGER.** His
> 2026-09-03 slot reads **STILL OPEN: 52**, carried by arithmetic from slots that predate routing.
> This counts what the 22 `diogenes_*.md` files in this directory actually contain — the only part
> an operator can open and read. **The two numbers are different denominators and neither falsifies
> the other.** His own caveat asked for exactly this: *"52 is sound as a ledger and not as a census."*

> ⛔ **METHOD: STRINGS, NEVER COORDINATES.** Every line number in the corpus is stale — `doctor.py`
> alone shifted every carried coordinate at or past the old `:912` by **+293** in one window, and
> changed again today. Each finding is located by its `[quoted: ]` spans.

> ⚠ **THE INSTRUMENT'S OWN KNOWN BIAS, stated before the results.** The reports escape backticks
> inside quoted spans, so a span that IS on disk can read as GONE. **This tool over-reports GONE.**
> A `GONE` verdict here is a candidate for closure, never a closure. Nothing below was closed on
> this tool's say-so.

## THE DENOMINATOR — corrected, and the correction is the most important line in this file

| bucket (by the finding's PRIMARY target) | n |
|---|---|
| levain code/docs | **36** |
| FLOW repo — NOT levain's to fix | **20** |
| self-referential (the report files) | **5** |
| target names no parseable file | 1 |
| **total severity-carrying findings** | **62** |

*(1 finding is genuinely cross-repo, naming `levain/doctor.py` AND `~/Briefcase/flow/projects/levain/next.md`.)*

### ⛔ THIS TABLE'S FIRST VERSION WAS WRONG, AND THE INSTRUMENT WAS BLIND IN ITS AUTHOR'S FAVOUR

The first cut of this document published **50 / 26 / 19 / 5**. The true figures are **62 / 36 / 20 / 5**. The cause was one quantifier: the parser matched `Content: <SEV>: (\S+?) — `, a **single non-space token** before the em-dash. Diogenes writes a multi-file subject as `A + B — …` or `A (+ B) — …`, so **every multi-target finding was silently dropped — 12 of them.**

⚡ **AND THE BLINDNESS RAN IN THE DIRECTION OF THE CLAIM IT WAS MEASURING.** One of the 12 findings the parser could not see is a HIGH whose subject spans **both repos** — `levain/doctor.py:989-1193 + ~/Briefcase/flow/projects/levain/next.md:108-124` — i.e. the instrument built to measure cross-repo mis-filing was structurally incapable of seeing the most cross-repo finding in the corpus.

⚠ **THIS IS THE SECOND INSTRUMENT TO FAIL THE SAME WAY ON THE SAME DAY, ON THE SAME QUESTION.** The fan-in session re-parsed this corpus to check the count, got **2** flow findings against 19, and was one command from reporting an over-count — its character class `[\w./-]` excluded the `~` that begins every absolute cross-repo path. **Two independently written parsers, two different bugs, both blind to exactly the findings under dispute.** That is not coincidence; it is what an author's expectation does to an ad-hoc instrument, and it is why the conclusion below rests on a *re-derivation* rather than on either first count.

▶ **THE CONCLUSION SURVIVES THE CORRECTION, WHICH IS THE ONLY REASON IT IS STILL STATED:** ~⅓ of the findings filed under levain's name (20 of 62, plus 5 self-referential) are **not levain's to fix**. The magnitude moved; the direction did not.

⚡ **THE UNDERLYING DEFECT IS ROUTING, NOT BOOKKEEPING: findings route by the REVIEW's tag, not by the finding's SUBJECT.** A review tagged `levain` files its findings about `~/Briefcase/flow/scripts/route_diogenes.py` into levain's report set, so flow's tooling debt accumulates wearing levain's label and inflates a number an operator reads as levain's backlog. **All 25 (20 flow + 5 self-referential) handed to `0903+0 main`, who took them and taught the gauge to count absolute-path subjects separately** — so the next report says it out loud instead of leaving it to a census.

## THE VERDICT PASS — three independent re-derivations against HEAD

Three agents re-derived the levain half by **quoted string, never by coordinate**, on disjoint file sets.

| set | examined / found | still open | closed | bad pointer | cannot determine |
|---|---|---|---|---|---|
| `confinement.py` · `test_firing_confinement.py` · `crystallization.py` | **15 / 15** | 10 *(= 9 distinct; one defect filed twice)* | 5 | 0 | 0 |
| `doctor.py` · `test_doctor_content.py` · `web_server.py` · `jobs.py` · `cli.py` | **11 / 11** | 9 | 2 | 0 | 0 |
| tests/docs/manifest/seed/pyproject/README/CHANGELOG | **14 / 14** | 5 | 9 | 0 | 0 |
| **total** | **40 / 40** | **24** | **16** | **0** | **0** |

⭐ **ZERO BAD POINTERS ACROSS 40 FINDINGS.** Every quoted string was found in the file its finding named. **Only LINE NUMBERS were stale** (`confinement.py:517`→`:530`, `:589`→`:624`, `test_firing_confinement.py:1113`→`:1268`). The corpus's *coordinates* have rotted; its *subjects* have not. That is a materially better result than the "52 is a ledger, not a census" caveat implied, and it means the backlog is workable rather than needing reconstruction.

⚠ **A CORRECTION TO DIOGENES' OWN SELF-CORRECTION, and it sharpens rather than refutes it.** His 09-03 report says an 08-17 slot mis-attributed a carried item as `README.md:182-196` when it belongs to `tests/test_compat_integration.py:182-196`. Verified: the **finding itself** (`diogenes-20260815-051915`) was always filed *correctly*; the error existed only in a later slot's **re-typed enumeration**. The `README.md` finding is a separate item at `:164`, and README.md was 176 lines at that window — long enough to hold it. **So the mis-attribution class lives in the SLOTS, not in the FINDINGS**, which is exactly the derived-vs-typed discriminator he named as its mechanism.

### ▶ THE LIVE HIGHs THIS PASS SURFACED — none of them closed today

1. **`levain/firing/confinement.py:617` + `:631` — HIGH, and it is a REBUTTAL that is itself false.** The comment rejecting an earlier "`ssh_anchor` is redundant" finding claims the anchor is "named by this anchor / and by nothing else". **Independently re-measured:** `_write_deny_ancestors` walks `jewel.parents` with no `.resolve()`, and under the symlinked-HOME geometry the rebuttal itself invokes, `ssh_anchor in _write_deny_ancestors(<lexical ssh entries alone>)` is **True**. The concession clause is separately wrong: `ssh_dir` is `None` in raw mode, so the `_dedup` collapse it names cannot occur there. ⚠ **A wrong rebuttal is worse than a wrong finding — it discharges the obligation to look again.**
2. **`levain/doctor.py:1486` — HIGH, real, with a stated reachability bound.** The pack branch of `_check_hook_freshness` compares raw installed bytes against the manifest's *source* hash, skipping the `_hook_body` normalisation the base branch uses, so a pack hook carrying an install-time placeholder reads permanently stale — and the `init --force` remedy the check itself prints cannot clear it. **Needs a third-party pack shipping an activation hook with a placeholder; none exists in-repo, which is why nothing is red today.** Premise still asserted verbatim at `:1415`.
3. **The doctor-redesign blocker — open, but on UNBUILT work.** An init-time receipt structurally cannot notice the package moved ahead. Already absorbed into the code as a blocker comment (`doctor.py:934-937`), so nothing at HEAD is wrong. ⚠ But `next.md`'s `### ⚠ THE TRADE — stated, not buried` is textually unchanged and still names only the smaller loss — **the section whose title promises not to bury the trade is the one burying it.**

## ▶ THE STILL-OPEN LIST — 22 live, all judged REAL by their re-deriver

*Every line re-derived against HEAD by quoted string. Line numbers below are CURRENT at `51c3aaf`
and will drift again — treat them as a starting point, not an address.*

⚠ **24 were reported; 2 were already CLOSED by this session's own commits** and the censusing agent
had read the pre-fix tree — `tests/test_firing_confinement.py:1268` (the `@live` comment that did
not gate the openhands import) and its duplicate, closed in `054b6a3`. **22 live.**

### ✅ CLOSED 2026-09-03 (12 of 22) — all three HIGHs among them

**Serial pass, each verified against disk before acting and each mutation-checked:**
- `confinement.py:617` **HIGH** — the false `ssh_anchor` rebuttal (`6feacf4`).
- `doctor.py:1486` **HIGH** — the pack branch compared raw bytes to a source hash, so a pack hook with a placeholder read stale FOREVER and `init --force` could not clear it (`d7cee34`).
- `doctor.py:934 + THE TRADE` **HIGH** — the section titled *"stated, not buried"* named only the smaller loss (`flow 132aa022`).
- `doctor.py:1449` **MEDIUM** — a corrupt pack lock silently disabled pack-hook checking while doctor reported green (`d9a5ac7`).
- `doctor.py:1298` **MEDIUM** — `_hook_body` promised plural normalisation over a one-line regex, now pinned by a call-site parity test (`528b19d`).
- `jobs.py:308` **MEDIUM** — a lease-expired job vanished while its replayed handle still said `pending` (`0d9d2db`).
- `web_server.py:973` **MEDIUM** — `isdigit()` admitted `²`, which `int()` rejects; fixed in BOTH servers (`51c3aaf`).
- `cli.py:1071` **MEDIUM** — the bound validator named a flag the operator had not typed; TWO call sites (`8a17d93`).
- `crystallization.py:7` **DRIFT** + `:106` **LOW** — the capped ladder's third site, and a version claim two bumps stale (`51c3aaf`).
- `test_compat_integration.py:360` **DRIFT** — a stored copy of a moving cap (`528b19d`).
- `docs/upstream-defects.md:80` **LOW** — cited a test name that never existed (`528b19d`).

### ⏳ STILL OPEN AFTER THIS PASS (10)
`confinement.py:530` · `confinement.py:412` · `confinement.py:1320` (`_sbpl_regex`, zero production callers) · `confinement.py:492-494` **DRIFT** · `tests/test_firing_confinement.py:883-885` · `web_server.py:987` · `tests/test_doctor_content.py:566` · `tests/test_compat_integration.py` ×3.
⚠ **`tests/test_doctor_content.py:566` is a stated PRECONDITION** of the doctor redesign — THE TRADE section names it: *"Removing doctor's correctness check while the suite's correctness check is fake would trade a broken guard for no guard. Fix the fixture first."*

### ✅ CLOSED EARLIER (superseded list)
- `confinement.py:617` **HIGH** — the false `ssh_anchor` rebuttal → `6feacf4`, now pinned by a test.
- `crystallization.py:7` **DRIFT** — the capped ladder's third site → `51c3aaf`.
- `crystallization.py:106` **LOW** — the anneal-0.9.6 claim, two bumps stale → `51c3aaf`.
- `web_server.py:973` **MEDIUM** — `isdigit()` admitting what `int()` rejects → `51c3aaf`, both servers.

### 🔴 STILL OPEN (18 after today)

**confinement / firing**
- `MEDIUM` `confinement.py:530` — RAW `Path.home()` spelling unreachable on both hands; a test certifies a dead vector.
- `MEDIUM` `tests/test_firing_confinement.py:883-885, :903-904` — docstring claims a resolve-first hand lands lexical; all three land resolved.
- `LOW` `confinement.py:412 (+:1358)` — public docstring describes the superseded name-based ssh design, self-contradicting four lines later.
- `LOW` `confinement.py:1320` — `_sbpl_regex` has **zero production callers**; its unit test manufactures false coverage.
- `DRIFT` `confinement.py:492-494` — a refuted verb survives on a 161-char line beside its own warning.

**doctor / servers / cli**
- `HIGH` `doctor.py:1486` — pack branch compares raw bytes to the source hash, bypassing `_hook_body`. ⚠ *Needs a third-party pack shipping a placeholder hook; none in-repo, which is why nothing is red.*
- `HIGH` `doctor.py:934 + next.md:280` — the proposed init-time receipt cannot detect the package moved ahead, and `### ⚠ THE TRADE — stated, not buried` understates it. ⚠ *A blocker on UNBUILT work; nothing at HEAD is wrong.*
- `MEDIUM` `doctor.py:1449` — uses `read_pack_locks`, not `_status`; a corrupt pack lock silently disables checking.
- `MEDIUM` `doctor.py:1298` — docstring promises plural placeholder normalisation; the regex handles one line.
- `MEDIUM` `web_server.py:987` — bare `_drain` + conditional `close_connection`; a lying Content-Length silently eats the 413.
- `MEDIUM` `jobs.py:308` — prune drops a lease-expired non-terminal record while **five** prose sites promise `failed`.
- `MEDIUM` `cli.py:1071` — the shared validator hardcodes `--max-seconds` and misnames the flag when rejecting `--consolidate-max-seconds`.
- `MEDIUM` `tests/test_doctor_content.py:566` — the fixture hand-simulates install; the composer never runs, so the placeholder trigger never appears.

**compat / docs**
- `MEDIUM` `tests/test_compat_integration.py:221,226,291,292` — `rc1` literals sit below the cap, so the ack-exact-prerelease guard runs the vacuous branch.
- `MEDIUM` `tests/test_compat_integration.py:184-191` — the advance-only fixture equals the no-reack fixture byte-for-byte; the comment is wrong on four numbers. ⚠ *Survived a second floor bump.*
- `MEDIUM` `tests/test_compat_integration.py:333,353` — the sentinel guard is positive-presence-only over a glued corpus; cannot catch a contradicting leftover.
- `DRIFT` `tests/test_compat_integration.py:360` — non-vacuity comment says a 0.9.6 cap; the constant reads 0.9.7.
- `LOW` `docs/upstream-defects.md:80` — cites a blind-spot test name that never existed; the real one is `three_byte_script`.

⚡ **THE SHAPE OF WHAT REMAINS, and it is worth reading before picking one up: of the 18, only ONE
(`doctor.py:1486`) is a defect a user could hit today, and it needs a pack that does not exist.**
The rest are **descriptions disagreeing with correct code** and **guards that do not exercise what
they name** — the two classes this whole day was about. That is a healthy backlog shape: the code
is largely right and the layer a future editor reads first is what has rotted.

## APPENDIX — THE SEAT'S MECHANICAL FIRST PASS (superseded by the verdict table above)

> ⚠ Kept for the trail. This was a STRING-PRESENCE scan, not a verdict, and it ran on the
> undercounting parser corrected above — so it lists 26 findings where there are 36. The
> three agents' re-derivation supersedes it. Retained because its bias note is what made
> the correction findable rather than embarrassing.


- **[ALL SPANS PRESENT]** `HIGH` `levain/doctor.py` — THIS COMMIT'S FIX (b) CONVERTS A SILENT MISS INTO A PERMANENT FALSE RED, for the population it just added. 3200234 widen  
  *(source: diogenes_20260807.md)*
- **[NO SPANS PRESENT]** `HIGH` `levain/templates/seed/continuity.md` — THE COMMIT THAT EXISTS TO RETIRE THE CAPPED LADDER LEFT IT IN THE SIBLING SEED FILE, AND THE COMMIT MESSAGE ASSERTS THE   
  *(source: diogenes_20260901.md)*
- **[NO SPANS PRESENT]** `MEDIUM` `CHANGELOG.md` — "levain update overwrites the hook templates" IS FALSE AGAINST THIS REPO'S OWN CODE, AND IT IS THE STATED MECHANISM BEHI  
  *(source: diogenes_20260903.md)*
- **[PARTIAL 1/4]** `MEDIUM` `README.md` — THE PUBLISHED PIN CLAIM STILL NAMES THE FLOOR THE COMMIT EXISTS TO RAISE, AND IT IS A FOURTH PIN SITE THE RELEASE GATE D  
  *(source: diogenes_20260815.md)*
- **[ALL SPANS PRESENT]** `MEDIUM` `levain/doctor.py` — the fallback that disarms fix (b) defers to a check that DOES NOT EXIST on that axis. The except branch sets pack_hooks=  
  *(source: diogenes_20260807.md)*
- **[PARTIAL 3/5]** `MEDIUM` `levain/doctor.py` — `_hook_body` PROMISES "install-time placeholder substitutionS" (PLURAL, GENERAL) AND NORMALISES EXACTLY ONE ASSIGNMENT L  
  *(source: diogenes_20260810.md)*
- **[PARTIAL 1/6]** `MEDIUM` `levain/doctor.py` — THE DARK-INSTALL DETECTOR ONLY RECOGNISES A LITERAL ABSOLUTE PATH, SO THE TWO NATURAL WAYS TO WRITE USER-LEVEL WIRING RE  
  *(source: diogenes_20260903.md)*
- **[PARTIAL 2/3]** `MEDIUM` `levain/firing/confinement.py` — THE RAW Path.home() SPELLING, THE ONE CODEX'S L3 ADDED, IS UNREACHABLE ON BOTH ENFORCING HANDS, AND IT SHIPS WITH A DEDI  
  *(source: diogenes_20260822.md)*
- **[PARTIAL 1/2]** `MEDIUM` `levain/firing/confinement.py` — THE ONE SPELLING THAT ACTUALLY CLOSES THE VULNERABILITY IS THE ONE SPELLING NO TEST COVERS; DELETING IT LEAVES THE SUITE  
  *(source: diogenes_20260822.md)*
- **[PARTIAL 1/2]** `MEDIUM` `levain/firing/confinement.py` — THE JUSTIFICATION FOR ssh_anchor DESCRIBES A deny_write_files_l THAT STOPPED EXISTING TWENTY LINES ABOVE IT, IN THIS COM  
  *(source: diogenes_20260822.md)*
- **[PARTIAL 1/2]** `MEDIUM` `levain/manifest.py` — THE CONSTANT SAYS 0.9.7 AND THE PARAGRAPH DOCUMENTING WHY IT IS HONEST STILL SAYS 0.9.6, SO THE ONE JUSTIFICATION THAT M  
  *(source: diogenes_20260815.md)*
- **[PARTIAL 1/9]** `MEDIUM` `levain/templates/adapters/codex/README.md` — THE CODEX ADAPTER README PROMISES CODEX OPERATORS A DOCTOR FAILURE THAT IS DELIBERATELY IMPOSSIBLE FOR THEM, AND THE CON  
  *(source: diogenes_20260903.md)*
- **[PARTIAL 1/3]** `MEDIUM` `levain/templates/seed/memory.md` — THE BYTE-BUDGET COMPRESSION GRAFTED THE CO-CITATION INSTRUCTION ONTO AN UNRELATED ANTECEDENT. The line now ends [quoted:  
  *(source: diogenes_20260901.md)*
- **[PARTIAL 8/10]** `MEDIUM` `levain/web_server.py` — A DRAIN-HARDENING FIX LANDED IN ONE OF THE TWO HTTP SURFACES AND NEVER REACHED THE OTHER, SO THE OPERATOR-FACING DASHBOA  
  *(source: diogenes_20260819.md)*
- **[NO SPANS PRESENT]** `MEDIUM` `pyproject.toml` — HEAD IS STAMPED 0.4.3 AND IS NOT THE ARTIFACT PUBLISHED AS 0.4.3, IN THE RELEASE WHOSE OWN SUBJECT IS DESCRIPTIONS THAT   
  *(source: diogenes_20260903.md)*
- **[ALL SPANS PRESENT]** `MEDIUM` `tests/test_compat_integration.py` — TWO TESTS WITH DIFFERENT NAMES NOW RECEIVE BYTE-IDENTICAL FIXTURES, AND THE SURVIVING ONE'S COMMENT IS FALSE ON EVERY NU  
  *(source: diogenes_20260815.md)*
- **[PARTIAL 2/4]** `MEDIUM` `tests/test_compat_integration.py` — THE TEMPLATE-RECONCILIATION GUARD IS POSITIVE-PRESENCE-ONLY OVER A CONCATENATION, SO IT STRUCTURALLY CANNOT CATCH A CONT  
  *(source: diogenes_20260901.md)*
- **[ALL SPANS PRESENT]** `MEDIUM` `tests/test_doctor_content.py` — the fixture was EDITED IN THIS COMMIT, at the exact lines an open finding names, and gained a new axis without gaining t  
  *(source: diogenes_20260807.md)*
- **[PARTIAL 2/4]** `MEDIUM` `tests/test_doctor_content.py` — THE NEW SCOPE TESTS READ `LEVAIN_SCOPE` AND `CLAUDE_CONFIG_DIR` FROM THE REAL ENVIRONMENT, SO THEY GO RED UNDER THE VALU  
  *(source: diogenes_20260903.md)*
- **[PARTIAL 1/2]** `MEDIUM` `tests/test_firing_confinement.py` — a comment names a gate that does not gate the thing it is attached to, and the suite is RED on this machine because of i  
  *(source: diogenes_20260807.md)*
- **[PARTIAL 1/2]** `LOW` `docs/upstream-defects.md` — THE ONE PLACE THIS DOC OFFERS A GREPPABLE RECEIPT NAMES A TEST THAT HAS NEVER EXISTED IN THIS REPO, AND IT SHIPPED WRONG  
  *(source: diogenes_20260818.md)*
- **[ALL SPANS PRESENT]** `LOW` `levain/firing/confinement.py` — A SECURITY-SURFACE ESCAPER WITH NO CALLER, AND A UNIT TEST THAT MAKES IT LOOK COVERED. [quoted: `def _sbpl_regex(value:   
  *(source: diogenes_20260821.md)*
- **[NO SPANS PRESENT]** `LOW` `levain/firing/confinement.py` — THE SOUNDNESS ARGUMENT FOR _dedup IS FALSIFIED BY THE FOUR LINES DIRECTLY ABOVE IT. The comment reads [quoted: `    # De  
  *(source: diogenes_20260822.md)*
- **[ALL SPANS PRESENT]** `LOW` `levain/firing/crystallization.py` — A COMMENT THAT DISPATCHES A RE-CHECK ON EVERY ANNEAL BUMP HAD ITS TRIGGER FIRE THIS WINDOW AND WAS NOT TOUCHED, SO THE R  
  *(source: diogenes_20260815.md)*
- **[PARTIAL 1/4]** `LOW` `pyproject.toml` — TWO COMMENTS ABOUT THE SAME PIN GIVE OPPOSITE CHARACTERISATIONS, AND THE ONE THAT IS NOW WRONG IS THE LAST THING A READE  
  *(source: diogenes_20260903.md)*
- **[PARTIAL 2/7]** `LOW` `tests/test_manifest.py` — THE SOURCE-CHECKOUT GUARD WAS ADDED TO ONE OF TWO SIBLING TESTS, AND THE COMMENT EXPLAINING WHY IT MUST EXIST SITS EIGHT  
  *(source: diogenes_20260903.md)*

## TALLY

- **16** · PARTIAL — some spans present, some not; needs a human read
- **6** · ALL SPANS PRESENT — quoted text still on disk (candidate STILL OPEN)
- **4** · NO SPANS PRESENT — candidate CLOSED (see the bias note above)

**26 of 26 levain-code findings in the corpus were examined — the denominator is complete for this
bucket, and there is no unstated remainder.**

⚠ **WHAT THIS PASS DOES NOT SAY.** `ALL SPANS PRESENT` means the quoted text is still on disk; it
does NOT mean the finding is valid. Several carried items are known false positives from
diff-scoped reading, and separating those is a human read, not a string match. **No finding was
closed on this tool's say-so** — the ten closed today were each closed by reading the code and
mutation-checking the fix.

▶ **NEXT PASS OWES:** a human verdict on the 6 `ALL SPANS PRESENT` items (the strongest STILL-OPEN
candidates, and the highest-value place to start), and a spot-check of the 4 `NO SPANS PRESENT`
against the escaping bias before any of them is written off.
