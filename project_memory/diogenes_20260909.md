# DIOGENES OVERNIGHT — levain, 2026-09-09

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-09-09*)`
**Stated count: 7 episode(s).** Severity: DRIFT 1 · HIGH 1 · LOW 1 · MEDIUM 3 — 6 of 7 episode(s) carry a severity; the other 1 are COVERAGE-OPEN 1
**STILL OPEN: NOT REPORTED BY DIOGENES THIS WINDOW.** No slot was emitted, so no count is written here. Do not infer one — an invented debt number is the defect `spore-498` exists to stop.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260909-035741-30bd0046397f
  Time: 2026-09-09T03:57:41  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: levain 2026-09-09, HEAD a8e05d80b7f9423285842c8829102425825c6b42 (== origin/main? YES — `git ls-remote origin HEAD` answered a8e05d80b7f9423285842c8829102425825c6b42 at start). `git status --porcelain` EMPTY. ⚠ `~/Documents/levain` IS a symlink to `~/Briefcase/levain`; `pwd -P` run first, tenth repo running. Window: the dispatch says 2 commits/24h and `git log --since='24 hours ago'` says ONE (a8e05d8, 2026-09-08 08:17:05 -0400); e055b9b is 2026-09-07 09:29:34, i.e. 42h back. **THE REVIEW WINDOW I AM TAKING IS `0d09703b..HEAD` = 4 COMMITS, NOT 1, BECAUSE THE 09-08 LEVAIN RUN DIED**: `diogenes-20260908-041447-42fc84b1f391` wrote a COVERAGE-OPEN at e055b9b at 08:14:47Z and the store holds NOTHING after it for this repo — no finding, no slot, no COVERAGE line. That stub is doing exactly the job Step 0 exists for; the last COMPLETE levain pool is the 09-07 one (`STILL OPEN (4)` at 0d09703b, + 2 DRIFT filed separately). REVIEW STARTED, NOT FINISHED — if no COVERAGE line for levain exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260909-041124-4c19f5e66405
  Time: 2026-09-09T04:11:24  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: levain/install.py:2212 — THE WIDENED BACKUP GUARD IS KEYED ON THE STORE PATH, SO AN OPERATOR'S CUSTOMISED `[mcp_servers.anneal_memory]` BLOCK IS STILL DESTROYED SILENTLY, WITH NO BACKUP AND NO MESSAGE, WHENEVER THE `--db` HAPPENS TO MATCH — AND BOTH A NEW TEST AND THE SHIPPED CHANGELOG ASSERT THAT CASE "TAKES NOTHING AWAY".

[quoted: `        if replacing_unknown or (old_store and new_store and old_store != new_store):`]
[quoted: levain/install.py:2211 `        replacing_unknown = old_store is None`]

⛔ REPRODUCED ON DISK, not reasoned. A `~/.codex/config.toml` holding the store levain is about to register PLUS operator customisation:

    [mcp_servers.anneal_memory]
    args = ["--db", "/home/op/same.db", "serve"]
    env = { ANNEAL_LOG = "debug", HTTP_PROXY = "http://corp:8080" }
    startup_timeout_ms = 60000

    [mcp_servers.other]
    args = ["x"]

After `_merge_codex_config(path, <levain's block for /home/op/same.db>)`:

    [mcp_servers.anneal_memory]
    args = ["--db", "/home/op/same.db", "serve"]
    [mcp_servers.other]
    args = ["x"]

`env` and `startup_timeout_ms` GONE from the machine-global config. Emitted output was the empty list. `ls config.toml.bak.*` → no matches. No backup, no notice, exit clean.

⚡ THIS IS THE WINDOW'S OWN DEFECT AT THE SIBLING DISCRIMINATOR. The commit correctly widened the guard from "both stores parse and differ" to "OR the old store is unreadable" — but the question it asks is still ONLY about the `--db` value. `_CODEX_MCP_BLOCK_RE` replaces the WHOLE block; `_codex_block_store` reads ONE field out of it. Everything else in the block is outside the guard's field of view by construction. The window's own comment states the rule it is breaking, at `_codex_block_store`'s docstring: *"a guard that does not fire on a valid input is worse than no guard, because the absence reads as approval."*

⛔ AND THE SILENCE IS NOW PINNED AND PUBLISHED, WHICH IS WHAT MAKES THIS A HIGH RATHER THAN A REPEAT. `tests/test_install.py:2256` `test_re_running_against_the_SAME_store_stays_silent_and_makes_no_backup` asserts `said == []` and `no backup`, justified at :2258 by [quoted: `    registered takes nothing away, so it must neither warn nor litter backups. Pinned because`] — a claim the reproduction above falsifies. `CHANGELOG.md:58` publishes the same sentence to operators [quoted: `case takes nothing away, and turning it into backup spam would train you to ignore the notice that`]. So a future seat that finds this defect will find a green test and a released disclosure both saying it is not one.

⚠ AND IT IS 0.4.5's HEADLINE SENTENCE ONE FILE OVER. That release exists because `levain init --force` *"deleted operator edits ... without a backup and without a word. That happened to a real operator."* The activation tree got the backup discipline; the machine-global Codex config still eats an `HTTP_PROXY` on the ordinary re-install path, and the resulting broken memory server points nowhere near levain.

▶ THE FIX IS A CHANGE OF QUESTION, NOT A WIDER SPECIAL CASE — asking "is the old store unreadable?" is the third special case bolted onto a predicate that should be asking whether ANYTHING is being taken away. Compare the parsed old block against the parsed new block (`tomllib.loads` on both, dict-equal) and back up + announce whenever they differ. That preserves the anti-spam property `test_re_running...` exists to protect: levain re-registering its own identical block still compares equal, still stays silent. It also subsumes `replacing_unknown` — an unparseable old block cannot compare equal — so the special case can be deleted rather than joined by a fourth.

[prescription: reasoned]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260909-041148-c66117c32737
  Time: 2026-09-09T04:11:48  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/install.py:746 — THE SYMLINK HALF OF THE INODE-SWAP CLASS WAS FIXED AT ONE OF THE TWO SITES THIS COMMIT TOUCHED. `_atomic_write_text` STILL REPLACES A SYMLINKED `.levain/config.json` WITH A REGULAR FILE AND LEAVES THE REAL FILE HOLDING THE OLD CONTENT.

[quoted: levain/install.py:744-746
`        if target.exists():`
`            shutil.copymode(target, tmp)`
`        os.replace(tmp, target)`]
[quoted: the fixed sibling, levain/install.py:2315 `    target = path.resolve()`]

⛔ REPRODUCED, not reasoned. `.levain/config.json` symlinked to `/tmp/.../real/config.json` (mode 0644), then `_atomic_write_text(link, '{"a":2}')`:

    before: is_symlink = True
    after : is_symlink = False
    after : content of the NAME = {"a":2}
    after : content of the REAL = {"a":1}
    after : real mode = 0o644

The write lands on a fresh regular file at the link's name; the file the operator's dotfiles manager owns is untouched and still holds the old brand/adapter config. The next `stow`/`chezmoi apply` restores it over the top — `a_true_statement_standing_where_a_thing_should_be`, which is the exact phrase this commit used to justify the fix at the OTHER site.

⚡ THE TWO HALVES OF ONE CLASS WERE SPLIT BETWEEN THE TWO SITES, AND THIS COMMIT WROTE BOTH. `_merge_codex_config` got `path.resolve()` AND `copymode`. `_atomic_write_text` got `copymode` ALONE. Its own comment at :741 argues the scope — *"FIXED IN THE HELPER, NOT AT THE CALL SITES, because the defect belongs to the inode swap and every caller inherits it — `guard_scoped_by_symptom_misses_the_class` is what fixing this at `_write_brand_config` would have been."* The symlink defect belongs to the same inode swap by the same argument, and it was scoped to the symptom the L1 finding reported (`~/.codex/config.toml`) rather than to the class the comment names.

⛔ THE TESTS RECORD THE SAME ASYMMETRY. One commit wrote `test_repointing_codex_writes_THROUGH_a_symlinked_config` and `test_atomic_write_text_PRESERVES_an_existing_files_mode`. There is no `test_atomic_write_text_writes_THROUGH_a_symlink`; `grep -c 'is_symlink' tests/test_install.py` finds the codex test's assertions and nothing for the helper.

⚠ AND THE CHANGELOG NAMES THE MECHANISM AS UNIVERSAL WHILE APPLYING ITS TWO REMEDIES AT DIFFERENT SCOPES. `CHANGELOG.md` says *"Every atomic write in Levain replaces the file rather than writing through it, so every one of them can move the mode"* — then fixes the mode in the helper and the symlink only in the codex path, without saying why the second is narrower. A reader who takes the universal sentence at face value concludes both are class-wide.

⚠ SCOPE STATED, NOT IMPLIED: `.levain/config.json` is less likely to be symlinked than `~/.codex/config.toml`, which is why this is MEDIUM and not the HIGH its sibling was. I did NOT survey the other atomic-replace sites — `git grep -n 'os.replace' levain/` returns sites in `manifest.py`, `idempotency.py`, `dashboard.py`, `jobs.py`, `writes.py`, `daemon.py` and `write_answers` at `install.py:859`, none of which I opened or reproduced. They are candidates, not findings, and are OUT of the count.

▶ One line, and the shape is already in the file 1,570 lines below: resolve first, then swap onto the resolved target — `target = target.resolve()` after the `parent.mkdir`, so `mkstemp` still lands in the real file's directory and the rename stays same-filesystem. `_merge_codex_config`'s own comment already argues why `resolve()` is safe there.

[prescription: reasoned]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260909-041212-7ee9b5769e8e
  Time: 2026-09-09T04:12:12  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/install.py:2069 — "EVERY FIX CARRIES A MUTATION CONTROL THAT FAILS AGAINST THE PRE-FIX TREE" IS FALSE FOR ONE OF THIS COMMIT'S OWN FIXES. REVERTING THE `unstaged_notes` BUFFERING TO ITS EXACT PRE-FIX FORM LEAVES THE WHOLE SUITE GREEN — 2520 PASSED, 0 FAILED.

[quoted: levain/install.py:2069-2070
`    for note in unstaged_notes:`
`        emit(note)`]
[quoted: the buffered append, levain/install.py:2008 `                        unstaged_notes.append(`]

⛔ MUTANT RUN, NOT REASONED. In a `git archive HEAD` tree I restored the pre-fix form exactly — `unstaged_notes.append(` back to `emit(`, and the deferred flush loop deleted — leaving the now-unused declaration at :1855 as the marker that the mutation applied. Printed the mutated region back to confirm the `emit(` is inside the backup loop again. Then:

    tests/test_install.py          128 passed
    full suite                    2520 passed, 3 skipped, 0 failed, 117.84s

Nothing grades it. `grep -rn 'unstaged\|could not stage a copy' tests/` returns exactly one hit — `tests/test_install.py:2038`, inside `test_a_failed_best_effort_copy_is_ANNOUNCED_not_swallowed`, which asserts the note IS emitted on a run that SUCCEEDS. That is the pre-fix property. The fix's actual claim — that the note is NOT emitted when a later file aborts the run or the swap rolls back — has no test at all.

⚡ THE CODE IS RIGHT AND ONLY THE VOUCHING IS WRONG, WHICH IS THIS WINDOW'S OWN THESIS TURNED ON THE WINDOW. I read the swap path: on the build `except BaseException` and on the swap `except BaseException` the notes are dropped and only flushed after `os.replace(new_tree, dst)` returns. The behaviour is exactly what the comment describes. The commit body's blanket sentence is the defect, and it is the same shape as the three findings the commit opens by closing — *"every number vouching for how hard the author looked wrong."*

⚠ AND THE FIX IS ABSENT FROM BOTH RECORDS' DEFECT LISTS. The commit body's "THE REAL DEFECTS" is six bullets and `project_memory/TRIAGE_2026-09-07.md`'s matching list is the same six; neither names the buffering change. The TRIAGE's parenthetical *"(each carries a mutation control that fails against the pre-fix tree)"* is scoped to its six and survives. The commit body's "Every fix" is not scoped and does not. So the only record that this glm-5.3 L3 LOW was ever fixed is the code comment itself.

⚠ FOUR OF THE SIX LISTED FIXES I DID GRADE, AND ALL FOUR INVERT — reported in the COVERAGE line, so this finding is about the one that does not, not about the practice.

▶ The control is cheap and the fixture already exists: `test_a_failed_best_effort_copy_is_ANNOUNCED_not_swallowed` builds the unwritable-`backups_root` case. Add a sibling that ALSO makes a later `copy2` raise (or makes the final `os.replace` fail), asserts `InitError`, and asserts no `could not stage a copy` reached `emit` — because `dst` is intact and the operator's edit is not gone. Pre-fix that test goes red on the emitted note.

[prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260909-041238-1f7fd03209c3
  Time: 2026-09-09T04:12:38  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/manifest.py:563 — THE RELEASE GATE THAT EXISTS TO CATCH `KNOWN_GOOD_ANNEAL` AND THE PYPROJECT PIN DRIFTING APART DOES NOT READ PYPROJECT. IT READS `levain.egg-info/`, A GITIGNORED BUILD ARTIFACT THAT IS NOT REGENERATED WHEN PYPROJECT CHANGES, SO THE DRIFT IT NAMES REPORTS `in_sync`.

[quoted: levain/manifest.py:559-563
`def _levain_requires() -> list[str]:`
`    try:`
`        import importlib.metadata as md`
`
`        return list(md.requires("levain") or [])`]
[quoted: levain/manifest.py:587 `    floor = pip_floor()`]

⛔ REPRODUCED. Archive tree of HEAD plus a copy of the repo's `levain.egg-info/`, nothing else changed:

    KNOWN_GOOD_ANNEAL = 0.9.8 · pip_floor() = 0.9.8 · in_sync   (baseline, all three agree)

Then I bumped `pyproject.toml` to `anneal-memory>=0.9.9,<0.10` and touched nothing else — the exact drift the docstring says the check exists for:

    pyproject:                anneal-memory>=0.9.9,<0.10
    levain.egg-info/PKG-INFO: Requires-Dist: anneal-memory<0.10,>=0.9.8
    KNOWN_GOOD_ANNEAL:        0.9.8
    pip_floor():              0.9.8
    VERDICT: in_sync | known-good anneal 0.9.8 matches the pip floor >=0.9.8

⚡ `absence_of_signal_rendered_as_health`, in a release gate. `pip_floor_verdict`'s own docstring says it catches *"the manifest constant and the `pyproject` pin drifting apart"* and its remediation string tells the operator to *"Reconcile KNOWN_GOOD_ANNEAL and the pyproject anneal-memory floor"* — naming a file it never opens. Bump one, don't reinstall, cut the release: green.

⛔ AND IT FIRES FALSELY IN THE OTHER DIRECTION TOO, WHICH IS HOW I FOUND IT. `tests/test_manifest.py:262` `test_pip_floor_verdict_in_sync_when_constant_matches_floor` FAILED on the first thing I ran tonight — the full suite in a clean `git archive HEAD` tree, where no `levain.egg-info/` exists and `importlib.metadata` resolves an older installed dist: `AssertionError: assert 'drift' in ('in_sync','unknown')`, detail *"known-good anneal 0.9.8 != the pip floor >=0.9.7"*. In that tree the repo's pyproject and constant agree PERFECTLY. The test's own comment at :263 states the property it believes it is checking [quoted: `    # The repo's KNOWN_GOOD_ANNEAL is kept == the pyproject floor; the gate is green.`] — and that property was true while the test was red. Copying `levain.egg-info/` in turns it green, which is the whole diagnosis in one step.

⚠ THE MECHANISM IS DOCUMENTED AND THE CONCLUSION IS NOT. `pip_floor`'s docstring is honest — *"Read from the installed distribution metadata — for a real pip wheel this is exactly what pip will resolve against."* That is right for a wheel and wrong for the gate: at release-cut time nobody is running the wheel, they are running the repo, and the artifact answering the question was built at some earlier commit.

⚠ OUT OF THE WINDOW AND IN SCOPE: `manifest.py` is untouched by `0d09703b..HEAD`. I turned it up forcing this window's own gates and I am filing it under the rule that a defect found while substantiating a claim about the diff counts.

▶ Read the floor from `pyproject.toml` when one is present beside the package (`tomllib` on `Path(__file__).parents[1]/"pyproject.toml"`, `project.dependencies`), and fall back to `importlib.metadata` only when it is absent — i.e. an installed wheel, where the dist metadata IS the truth. Repo-first, dist-fallback. The test at :262 then checks the property its comment already claims.

[prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260909-041302-b1d8edf12d30
  Time: 2026-09-09T04:13:02  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: tests/test_web_server.py:1941 — THE COMMENT THAT SAYS "MEASURED ... NOT ASSUMED" NAMES FIVE, AND THE TUPLE IT SITS ON NAMES EIGHT. THE MEASUREMENT IS EIGHT.

[quoted: tests/test_web_server.py:1939-1945
`# The methods `_InitHandler` (init_server) and `_Handler` (web_server) duplicate verbatim.`
`# MEASURED with the AST comparison below, not assumed: `_route`, `_send` and `do_POST` differ`
`# legitimately (different route sets), and these five do not.`
`_DUPLICATED_HANDLER_METHODS = (`
`    "_drain", "_host_ok", "_reject", "_send_json", "log_message", "do_GET", "do_HEAD",`
`    "version_string",`
`)`]
[quoted: tests/test_web_server.py:1958 `    CLOSED (L1, 2026-09-07). Five methods are duplicated, not two. `_host_ok` is the`]

⛔ MEASURED with the file's own `_body_ast` comparison, run against the two live modules:

    classes: _InitHandler _Handler
    shared callables: 11
    AST-IDENTICAL   : 8  ['_drain','_host_ok','_reject','_send_json','do_GET','do_HEAD','log_message','version_string']
    DIFFER          : 3  ['_route','_send','do_POST']

So the STRUCTURE of the sentence is exactly right — three differ, and they are the three it names — and the cardinality is wrong by three, twice, in a comment whose own second word is "MEASURED". The commit message and the TRIAGE both say EIGHT ("eight, not one"), so the two prose counts in the file are the only place the number is five.

⚡ IT IS THE CLASS THE PARAGRAPH IS ABOUT, WRITTEN INSIDE THE FIX FOR IT. The docstring at :1957-1958 exists to record that a first version pinned only `_drain` while claiming the class was closed — *"Pinning one member of a duplicated set is not closing the set."* A cardinality describing the author's own sweep is the one figure nobody re-derives, which is what the seatbelt-renderer comment in `confinement.py` concluded in this same commit and fixed by naming the sweep's dimensions instead of counting its cells.

⛔ AND THE PROSE INVITES THE WRONG RECONCILIATION. A maintainer who trusts "five methods are duplicated" and finds eight names in the tuple has two ways to make the file agree with itself, and the destructive one is deleting three names — including `do_GET` and `do_HEAD`, the two request entry points. `_host_ok` is called out by name in the docstring as the DNS-rebinding Host allowlist, so the security argument survives; the three unnamed ones are the ones a reconciliation would eat.

⚠ WHY THIS IS A FINDING AND NOT `DRIFT:` — `codeshape.py 0d09703b tests/test_web_server.py` exits 1. The file gained 95 lines of executable test code this window, so the code half of the discriminator fails and the ambiguous case resolves to the count, as the rule requires.

▶ Delete the count rather than correcting it to eight — `len(_DUPLICATED_HANDLER_METHODS)` is one line below the sentence and cannot disagree with itself. "…and the rest, listed below, do not." Same move the seatbelt renderer made 20 lines of diff earlier in this commit.

[prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260909-041325-c021e1a95f63
  Time: 2026-09-09T04:13:25  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: DRIFT: levain/firing/openhands/tools.py:227 — THE DERIVATION COMMAND OFFERED AS PROOF THAT ONE COMMIT WROTE `:1703` WAS MADE FALSE BY THE COMMIT THAT WROTE THE SENTENCE. IT RETURNS TWO COMMITS, AND THE SECOND IS THIS ONE.

[quoted: levain/firing/openhands/tools.py:227
`    # THAT WROTE ":1703". `git log -S"1703" -- <this file>` returns that commit alone.`]

⛔ RUN, at HEAD:

    $ git log --oneline -S"1703" -- levain/firing/openhands/tools.py
    3809ca6 Close the 09-07 findings, and the defects found in closing them
    fd7c4d4 the line that reported the fallback was the line that prevented it

Two, not one. `3809ca6` matches because the paragraph deliberately QUOTES the dead number — the sentence is its own counter-example, and the clause promising a single result was invalidated at the instant of commit by the diff containing it.

⭐ THE HISTORY IT ASSERTS IS TRUE, AND I CHECKED IT RATHER THAN THE COMMAND ALONE. `SandboxedShell.close` in `confinement.py` across the three commits the paragraph names:

    bdb4b2f -> 1694   (tools.py cited confinement.py:1694 — exact)
    23fedaf -> 1703   (six minutes later; the citation is now a blank line)
    fd7c4d4 -> 1717   (and per git log -S this is the commit that wrote ":1703")

So the sharper story the paragraph tells — the replacement coordinate was fourteen lines wrong at the instant it was committed, not stale later — reproduces exactly. Only the reproduction instruction is wrong, and it is wrong in the direction of under-stating: a reader who runs it and gets two rows cannot tell which one the sentence means.

⚠ `DRIFT:` AND NOT A FINDING, DECIDED MECHANICALLY. `codeshape.py 0d09703b levain/firing/openhands/tools.py` exits 0 — the AST is unchanged in the window, the file's only edits are comment lines — and the evidence is comment text. Both halves hold, so it does not enter the count.

⚡ AND IT IS THE THIRD GENERATION OF ONE DEFECT. `:1694` rotted; the comment retiring it published `:1703` and `:1717`; this commit deleted both coordinates and stated the distance in words — the right move, explicitly argued at :232-234 — and then attached a self-verifying command whose stated OUTPUT rots the same way a coordinate does. A derivation command is durable; a claim about what it returns is a figure.

▶ Drop the four words. `git log -S"1703" -- <this file>` is the durable half and it still finds `fd7c4d4`; "returns that commit alone" is the perishable half and carries nothing the sentence needs. Same subtraction the paragraph already applied to the line numbers, one clause short.

[prescription: reasoned]
  Tags: code_review, levain, semantic
```
