# DIOGENES OVERNIGHT — levain, 2026-09-03

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-09-03*)`
**Stated count: 11 episode(s).** Severity: LOW 2 · MEDIUM 5 — 7 of 11 episode(s) carry a severity; the other 4 are COVERAGE 1 · COVERAGE-OPEN 1 · SELF 1 · STILL OPEN 1
**STILL OPEN: 52** — **7 filed tonight** (measured at HEAD this window) · **45 carried**. A carried item was NOT necessarily re-checked this window; only the slot block below says which ones were.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260903-034051-05327ac2188e
  Time: 2026-09-03T03:40:51  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: levain 2026-09-03, HEAD d84270d573869624e5be23b1245dd7b08e47d045 (== origin/main? YES — `git ls-remote origin HEAD` answered d84270d5 against the LIVE remote, not a cached ref), `git status --porcelain` EMPTY at start, window a0849ed..HEAD = 5 commits. Note: ~/Documents/levain is a SYMLINK to ~/Briefcase/levain — same repo, one target, checked first (same trap as claphamdigital.com, solitaire, video-poker). REVIEW STARTED, NOT FINISHED — if no COVERAGE line for levain exists in this run, THIS RUN DIED and the finding list is PARTIAL. [prescription: none]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-035237-72eb778ea4fd
  Time: 2026-09-03T03:52:37  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: pyproject.toml:7 — HEAD IS STAMPED 0.4.3 AND IS NOT THE ARTIFACT PUBLISHED AS 0.4.3, IN THE RELEASE WHOSE OWN SUBJECT IS DESCRIPTIONS THAT DISAGREE WITH ARTIFACTS. Verified against the tag and the built sdist, not inferred.

`git tag --points-at ebec171` returns `v0.4.3`. HEAD is `d84270d`, one commit past it, and both version stamps still read 0.4.3: [quoted: `version = "0.4.3"`] (pyproject.toml:7) and [quoted: `__version__ = "0.4.3"`] (levain/__init__.py:3).

WHAT d84270d CHANGED IS SHIPPED SURFACE, NOT NOTES. The hook message an operator actually reads was rewritten. Extracted from the PUBLISHED artifact — `tar -xzOf dist/levain-0.4.3.tar.gz levain-0.4.3/levain/templates/activation/hooks/_levain_hook.py` — line 341 of the sdist reads [quoted: `        f"[wrap blocked] A wrap is already open and {n} episode(s) are waiting "`] and line 347 [quoted: `        f"\`anneal-memory wrap-status\` before doing anything else. Abandoning it "`]. Neither string exists at HEAD: the count clause is gone entirely and the discriminator moved to `status`, the MCP tool. `git show bdef6ee:` and `git show ebec171:` both carry the sdist text; only d84270d differs. verify.py's timeout hint and the codex adapter README changed in the same commit.

AND `## [Unreleased]` IS EMPTY. CHANGELOG.md:7 is followed immediately by CHANGELOG.md:9 `## [0.4.3] — 2026-09-02`, so the file created THIS WINDOW to end undocumented behaviour changes has zero entries for the behaviour change committed one commit after it was created. The repo already has a convention for exactly this and applied it one commit earlier, at CHANGELOG.md:105: [quoted: `*(This section was added after publication. The \`0.4.2\` sdist on PyPI carries the release notes without it — the omission was in the notes, never in the code.)*`] — that disclosure was written when the divergence was NOTES ONLY, and was not written when the divergence became CODE.

TWO LIVE CONSEQUENCES IN THE RELEASE NOTES THEMSELVES, both still describing the superseded text as current-tense rationale:
  · CHANGELOG.md:87 — [quoted: `tells the agent to check \`anneal-memory wrap-status\` first`], the shell command d84270d removed BECAUSE, in its own words, "an agent with no shell was Alex's entire report."
  · CHANGELOG.md:60 — [quoted: `- \`episodes_since_wrap()\` is retained as the count-only view for callers that do not give advice.`] — d84270d retracted precisely that justification, and the docstring at HEAD now reads "NOTHING IN THIS REPO CALLS THIS ANY MORE ... it is kept as a COMPATIBILITY SHIM, not as a convenience."

WHY IT IS NOT MERELY BOOKKEEPING: anyone who builds from HEAD produces a wheel named `levain-0.4.3` whose activation layer says something different from the `levain-0.4.3` on PyPI, and nothing in the tree says so. This is the exact failure mode the CHANGELOG's own header names — an operator with no surface telling them what changed underneath their install — reproduced inside the file written to end it, within one commit.

THE FIX: the tree must stop claiming a version that names a different artifact — bump both stamps off 0.4.3 and put d84270d's four changed surfaces under `## [Unreleased]`, or cut 0.4.4. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-035258-b62dd5946759
  Time: 2026-09-03T03:52:58  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/doctor.py:1100 — THE DARK-INSTALL DETECTOR ONLY RECOGNISES A LITERAL ABSOLUTE PATH, SO THE TWO NATURAL WAYS TO WRITE USER-LEVEL WIRING READ AS GREEN. Run-verified in a scratch HOME, all four spellings.

ON DISK, `_user_entry_targets` (levain/doctor.py:1060), the token loop:
[quoted: `                if "${CLAUDE_PROJECT_DIR}" in tok or "$CLAUDE_PROJECT_DIR" in tok:`]  (:1097)
[quoted: `                    if Path(tok).resolve() == expected:`]  (:1100)

`Path(tok)` does no variable expansion and no `expanduser`. So `$HOME/lev/activation/hooks/session_start.py` — which the shell DOES expand inside the double quotes levain's own template uses (levain/templates/adapters/claude-code/settings.template.json:14: [quoted: `            "command": "\"{{PYTHON}}\" \"${CLAUDE_PROJECT_DIR}/activation/hooks/session_start.py\""`]) — resolves to `<cwd>/$HOME/lev/...`, never equals `expected`, and the scan returns [].

MEASURED, NOT REASONED. Scratch install at `$HOME/lev` with HOME=/tmp/dio_lev/home2, one `SessionStart` entry in `$HOME/.claude/settings.json` naming that install four ways:
    ~/lev/activation/hooks/session_start.py           -> []
    $HOME/lev/activation/hooks/session_start.py       -> []
    /tmp/dio_lev/home2/lev/activation/hooks/...       -> ['/tmp/dio_lev/home2/.claude/settings.json']
And the whole check on the first case returns `ok=True` with [quoted: `install-scoped (the default) — hooks fire only for sessions whose working directory is inside `] — the reassuring green, on the one configuration this function exists to fail.

IT IS NOT COVERED, AND THE OVER-CLAIM IS PUBLISHED. Every case in `TestUserLevelWiring` (tests/test_doctor_content.py:963) builds its command through one helper that interpolates an absolute tmp_path: [quoted: `            "command": f"/usr/bin/python3 {install / 'activation' / 'hooks' / script}",`]. No test writes `~` or `$HOME`. Meanwhile CHANGELOG.md:66 states the check [quoted: `**FAILS** on the one combination that is genuinely dark: install-scoped, while this install's hooks are wired from `] `~/.claude/settings.json` — unqualified.

WHY IT IS THIS FUNCTION'S PROBLEM AND NOT THE SIBLING'S: `_hook_command_targets` has the same blind spot, but a miss there produces a wiring FAIL — fail-loud. A miss HERE produces silence, which is `absence_of_signal_rendered_as_health` inside the instrument built to end it: a green doctor compatible with the whole activation layer being off is the literal finding this release exists to close.

THE FIX, EXECUTED AND MEASURED: expand the token before resolving, leaving the `${CLAUDE_PROJECT_DIR}` skip ahead of it —
    if Path(os.path.expandvars(tok)).expanduser().resolve() == expected:
Patched a scratch copy of levain/ and re-ran the same four-spelling probe: `~` DETECTED, `$HOME` DETECTED, absolute DETECTED, `${CLAUDE_PROJECT_DIR}` still correctly []. The placeholder skip is preserved because it precedes the expansion in the loop. `os` is already imported (levain/doctor.py:28). [prescription: run]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260903-035322-47857b2a4c70
  Time: 2026-09-03T03:53:22  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_doctor_content.py:1080 — THE NEW SCOPE TESTS READ `LEVAIN_SCOPE` AND `CLAUDE_CONFIG_DIR` FROM THE REAL ENVIRONMENT, SO THEY GO RED UNDER THE VALUE THIS RELEASE TELLS OPERATORS TO SET, AND GO VACUOUSLY GREEN UNDER THE OTHER. Both directions run-verified at HEAD.

RED. `LEVAIN_SCOPE=global python3 -m pytest tests/test_doctor_content.py -q` -> **1 failed, 107 passed**:
    FAILED tests/test_doctor_content.py::TestActivationScopeCheck::test_the_dark_configuration_FAILS
    E   AssertionError: assert not True
That is the regression guard for Alex De Groodt's exact state. It fails because `_check_activation_scope` (levain/doctor.py:1127) reads the live env — [quoted: `    env_raw = os.environ.get("LEVAIN_SCOPE", "").strip().lower()`] — and correctly declines to fail an install whose gate the environment has opened. The code is right; the test never isolates. And `LEVAIN_SCOPE=global` is not an exotic value: CHANGELOG.md:45 offers it as a supported channel and levain/doctor.py:1172's own hint tells the operator to set it.

VACUOUS. `CLAUDE_CONFIG_DIR=/tmp/dio_lev/fakecfg python3 -m pytest tests/test_doctor_content.py -q` -> **2 failed, 106 passed**, adding
    FAILED tests/test_doctor_content.py::TestUserLevelWiring::test_detects_claude_user_level_wiring
because `_user_level_wiring` prefers that env var over `Path.home()` — [quoted: `    cfg_dir = os.environ.get("CLAUDE_CONFIG_DIR")`] — while every test in the class monkeypatches `Path.home` only. THE SILENT HALF IS WORSE THAN THE LOUD ONE: the four cases in that class asserting `== []` still PASS, now for the wrong reason. `test_ignores_wiring_for_a_DIFFERENT_install` (:1033) writes its decoy into the monkeypatched home, the scan reads the real `CLAUDE_CONFIG_DIR` instead, and the two-installs-on-one-machine discrimination is never exercised. Exactly one test in the file does isolate — `test_stale_AGENTS_md_does_not_produce_a_codex_diagnostic` carries [quoted: `        monkeypatch.delenv("CLAUDE_CONFIG_DIR", raising=False)`] and [quoted: `        monkeypatch.delenv("LEVAIN_SCOPE", raising=False)`] — so the discipline was known and applied at one site out of thirteen.

tests/conftest.py has no env isolation for either variable; its only autouse fixture pops `LEVAIN_DRIVE_MODE`.

WHY IT COUNTS RATHER THAN BEING A HARNESS NIT: this release's own account of itself is "A green suite missed it both times" (CHANGELOG.md:70) — the answer to which was tests that run against both adapter trees. Tests whose verdict depends on the reviewer's shell are the same defect one layer out, and the vacuous direction lands on the guards for a doctor regression that has now shipped three times.

THE FIX, EXECUTED: append an autouse fixture to tests/conftest.py doing `monkeypatch.delenv("LEVAIN_SCOPE", raising=False)` and `monkeypatch.delenv("CLAUDE_CONFIG_DIR", raising=False)`. Applied to a `git archive HEAD` copy and re-ran tests/test_doctor_content.py + tests/test_hooks.py under three environments: LEVAIN_SCOPE=global -> **266 passed**, CLAUDE_CONFIG_DIR set -> **266 passed**, clean env -> **266 passed**. [prescription: run]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260903-035343-8360be2de95b
  Time: 2026-09-03T03:53:43  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/templates/adapters/codex/README.md:423 — THE CODEX ADAPTER README PROMISES CODEX OPERATORS A DOCTOR FAILURE THAT IS DELIBERATELY IMPOSSIBLE FOR THEM, AND THE CONDITION IT DESCRIBES IS THEIR DEFAULT.

ON DISK, the bullet added this window (:419-426), closing lines:
[quoted: `  \`levain doctor\` reports`]
[quoted: `  which way the gate is set — and FAILS when hooks are wired at the user`]
[quoted: `  level while the scope is still install-scoped, because that combination`]
[quoted: `  means the whole activation layer is silently off.`]

FOR A CODEX INSTALL BOTH HALVES OF THAT CONDITION ARE THE DEFAULT AND DOCTOR NEVER FAILS. The paragraph immediately above it (:410) says so itself: [quoted: `- **Hooks fire for every Codex session on the machine.** Because`] / `~/.codex/hooks.json` is global. And `_user_level_wiring` refuses to look at that file BY DESIGN — levain/doctor.py:986, its own docstring: [quoted: `    ⛔ \`~/.codex/hooks.json\` IS DELIBERATELY NOT CHECKED, AND THIS IS THE WHOLE`] / [quoted: `    SUBTLETY OF THE CHECK.`] The only file it reads is `root / "settings.json"` under `~/.claude`. So for a Codex operator with no Claude Code install the scan is [] by construction, the check returns ok=True, and the repo pins that: tests/test_doctor_content.py:997 `test_a_codex_install_is_never_failed_by_this_check` asserts [quoted: `        assert r.ok, f"codex install false-FAILed: {r.detail}"`].

THE HARM IS NOT PEDANTIC, AND IT LANDS ON THE REPORTER'S OWN SHAPE. A Codex operator who IS dark — global codex hooks, scope left at the default, entity silent in every project directory — reads this bullet, runs `levain doctor`, sees it pass, and concludes the gate is fine. Doctor did tell them, but as PASS detail (levain/doctor.py:1199: [quoted: `            " — your Codex hooks are wired globally, so they run in every Codex "`]), and the README just taught them to expect a FAIL. A promise of a red that never comes is a green.

⚠ AND IT IS THE WRONG-TREE CLASS AGAIN, IN THE ADAPTER'S OWN FILE. levain 0.4.0 turned doctor permanently red for every Codex operator by pointing a check at the Claude Code tree; the first draft of THIS release's check did it a second time. This is the third instance, moved from the code into the codex README: a Claude-Code-only doctor behaviour asserted, unqualified, in the document a Codex operator reads.

THE FIX: qualify the clause for this adapter — doctor REPORTS the gate for a Codex install and cannot fail it, because global `~/.codex/hooks.json` is the only deployment Codex has; the FAIL is the Claude-Code case where `~/.claude/settings.json` wiring is an operator choice. Same sentence already exists correctly in CHANGELOG.md:67-68, which does carve Codex out. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-035403-2aa0cef656da
  Time: 2026-09-03T03:54:03  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: CHANGELOG.md:110 — "levain update overwrites the hook templates" IS FALSE AGAINST THIS REPO'S OWN CODE, AND IT IS THE STATED MECHANISM BEHIND THE RELEASE'S HEADLINE UPGRADE INSTRUCTION.

ON DISK, the Upgrading section:
[quoted: `- **If you are carrying a local patch to \`in_install_session()\`, drop it.** \`levain update\` overwrites the hook templates, which is what kept eating it; the supported opt-in survives updates.`]
Same claim in the shipped hook, both adapter copies — levain/templates/activation/hooks/_levain_hook.py:106-107 and .../adapters/codex/.../_levain_hook.py:91-92: [quoted: `# update\` overwrote on each upgrade.`]

`run_update` DOES NOT WRITE INTO `activation/` AT ALL. Read end to end (levain/update.py:41-254), its only writes are: the pip step (`_reconcile_anneal`, mutates the Python env), `set-schema` against the store, `migrate ack` against the store, `run_pack_reconcile`, and `_record_lock` — which writes `.levain/manifest.json` and nothing else. `grep -n "config.json" levain/update.py levain/reconcile.py` returns ZERO lines. And the pack phase cannot reach a base install: levain/reconcile.py:458 [quoted: `    if not recorded:  # absent — genuinely no packs`] returns immediately.

WHAT ACTUALLY REPLACES THE TREE IS `levain init --force`, and this repo already says so plainly — levain/install.py:17-19: [quoted: `\`--force\`, operator-edited activation files (\`posture.md\`,`] ... [quoted: `activation tree is replaced; the anneal-memory store is preserved as-is;`]

THE SECOND CLAIM IN THE SAME BREATH IS TRUE, WHICH IS WHY THIS SURVIVED. `.levain/config.json` DOES survive `levain update` (CHANGELOG.md:45, levain/doctor.py:1172 [quoted: `                "(it survives \`levain update\`), or set LEVAIN_SCOPE=global for a "`], both hook copies at :114/:99). Update never touches it. So the durability half checks out and the contrast it is drawn against does not — the config channel is being justified by a property the alternative does not actually lack.

WHY IT MATTERS RATHER THAN BEING A WORDING NIT: this is the release's user-facing account of WHY the operator lost their patch, in the notes written because "an operator upgrading through 0.4.x had no surface that told them what changed underneath their install" (CHANGELOG.md:5). An operator who takes it literally has a wrong model of which command mutates their install — and will not think to check `levain init --force`, the one that does. It is also a description that disagrees with correct code, which is the exact class every other item in this release is about.

THE FIX: name the real mechanism at all three sites — `levain init --force` replaces the activation tree (levain/install.py:17-19); `levain update` does not write it. Keep the config-durability sentence, which is correct. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-035430-d562fa77d52b
  Time: 2026-09-03T03:54:30  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: pyproject.toml:84 — TWO COMMENTS ABOUT THE SAME PIN GIVE OPPOSITE CHARACTERISATIONS, AND THE ONE THAT IS NOW WRONG IS THE LAST THING A READER PASSES BEFORE THE SPECIFIER.

ON DISK, in order:
:57  [quoted: `    # \`>=0.9.8\` (AM-MCP-WRAPCANCEL): a real API contract gate, not a lockstep bump.`]
:84  [quoted: `    # levain calls no 0.9.7-only API; the floor is the published-known-good, not a runtime`]
:85  [quoted: `    # requirement. \`MIN_PROVEN_LEVEL\` is new public surface in 0.9.7 but levain does not import it.`]
:86  [quoted: `    "anneal-memory>=0.9.8,<0.10",`]

:84's first clause is scoped to 0.9.7 and stays true. Its second clause is not scoped — "the floor" is a definite reference to the specifier two lines below, which is 0.9.8, and :57 says that floor IS a contract gate. This window changed the specifier and left the sentence describing it.

THE FIX IS SUBTRACTION, NOT A CORRECTION. Delete "the floor is the published-known-good, not a runtime requirement" — it is a generalisation over a moving value that :57 now states correctly for the current floor, so the file gets shorter and stops carrying two answers. Keep the 0.9.7-specific clause and the `MIN_PROVEN_LEVEL` note, which are history and are true as history. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-035430-cae47498e69a
  Time: 2026-09-03T03:54:30  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: tests/test_manifest.py:380 — THE SOURCE-CHECKOUT GUARD WAS ADDED TO ONE OF TWO SIBLING TESTS, AND THE COMMENT EXPLAINING WHY IT MUST EXIST SITS EIGHT LINES ABOVE THE TEST THAT DOES NOT HAVE IT. Run-verified.

`test_readme_pin_matches_pyproject` (tests/test_manifest.py:365) carries the guard and its own rationale:
[quoted: `        # The skip must precede the reads. It used to sit after them, where a`]
[quoted: `        # non-source checkout would already have raised FileNotFoundError —`]
[quoted: `        # a guard that could never fire, which is worse than no guard because`]
[quoted: `        # it reads as handled.`]
[quoted: `        if not (root / "pyproject.toml").is_file():`]
[quoted: `            pytest.skip("not a source checkout")`]

`test_readme_pin_matches_known_good` (:380), added in the same commit, reads the file with no guard at all:
[quoted: `        readme = (self._repo_root() / "README.md").read_text(encoding="utf-8")`]

MEASURED, NOT ARGUED. Copied `levain/` alone into `/tmp/dio_lev/wheelsim/site-packages` (so `_repo_root()` resolves to a directory with no README.md or pyproject.toml, the layout the guard exists for) and ran the class:
    E   FileNotFoundError: .../wheelsim/site-packages/README.md
    1 failed, 1 skipped, 47 deselected
One skipped, one errored — the guarded sibling behaved exactly as designed and the unguarded one produced the failure its neighbour's comment describes.

THE NIGHT'S SHAPE AT ITS SMALLEST: a fix that landed where it was written and not at the sibling its own description reaches. Impact is bounded — the suite is normally run from a source checkout — so this is LOW.

THE FIX: hoist the `is_file()` skip into `_repo_root` (or add it to :380), so both tests are covered by one guard rather than one guard and one copy of the reason. [prescription: reasoned]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260903-035457-1369803e5376
  Time: 2026-09-03T03:54:57  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CORRECTION: diogenes-20260817-052611-6e86ec0428ad (the levain STILL OPEN (32) slot, 2026-08-17) NAMES A CARRIED FINDING AT A FILE THAT NEVER HELD IT, AND THE ERROR HAS RIDDEN EVERY SLOT SINCE.

WHAT I WROTE. That slot's levain code-axis enumeration reads `... tests/test_compat_integration.py:221+:291, README.md:164, README.md:182-196, manifest.py:85, firing/crystallization.py:106.`

WHY IT IS WRONG, checked against the originating episode and against disk. The item is from my 08-15 slot (diogenes-20260815-051915-37f45984c1ea), where it is filed as `tests/test_compat_integration.py:182-196 (the advance-only test now takes a byte-identical fixture to its no-reack neighbour and its comment is false on four numbers)`. README.md was **176 lines** at that HEAD (`git show afe7481:README.md | wc -l`) and is **179** now, so `README.md:182` has never existed in this repo. `sed -n '182,196p' README.md` at HEAD returns nothing. `sed -n '182,196p' tests/test_compat_integration.py` returns `def test_record_compat_lock_ack_is_advance_only(tmp_path, monkeypatch):` and the fixture comment the finding is about — the coordinate is exact at the file I originally named.

THE CORRECTED CLAIM: the carried item is `tests/test_compat_integration.py:182-196`, not `README.md:182-196`. It is STILL OPEN — that file is untouched by this window.

THE COUNT DOES NOT MOVE. One item, one entry, mis-attributed rather than duplicated; 46 was 46. What breaks is USE: an operator working the backlog opens README.md:182, finds a 179-line file, and cannot tell a bad pointer from a closed finding.

MECHANISM, and it is the one I already have a close for. The wrong string was TRANSPORTED between slots, not DERIVED — the 08-15 entry was correct because I had just filed it; the 08-17 enumeration re-typed a list from the previous night and README.md:164 sat directly beside it in that list. Same derived-vs-typed discriminator that has held nine consecutive nights on SUBJECT coordinates, firing on the one surface with no gate: the slot's own carried-item list, where nothing forces a `grep -n`. ▶ CLOSE, applied tonight: a carried item is re-cited from the ORIGINATING episode, never from the previous slot's re-typed list. [prescription: none]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-035613-25b689e08dcc
  Time: 2026-09-03T03:56:13  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (52), TOTAL AT HEAD d84270d5 — levain, 2026-09-03. 7 filed tonight (0 HIGH / 5 MEDIUM / 2 LOW), 45 carried. Filed separately: 0 DRIFT (not in this count) — every file I filed against had its CODE change in this window (doctor.py, the hook templates, pyproject.toml, all three test files) or is markdown (CHANGELOG.md, the codex adapter README), which is a FINDING by the prose rule and never DRIFT-eligible. Slot pre-flighted through the LIVE parser: extract_still_open -> 52, extract_measurement_split -> (7, 45), and they reconcile.

ARITHMETIC, SHOWN SO IT CAN BE CHECKED: 46 carried from the 2026-09-01 slot (diogenes-20260901-051042-92b0e71a23df, at HEAD 5e826f4), MINUS 1 closed, PLUS 7 new = 52.

CLOSED (1), verified against the mechanism and not against the symptom going quiet. **README.md:164 — the anneal-memory pin skew, open since 2026-08-16 (17 days).** It now reads `pinned >=0.9.8,<0.10` and pyproject.toml:86 declares `"anneal-memory>=0.9.8,<0.10"` — read at HEAD, both. ⚡ AND IT CLOSED THE STRONGER WAY: not by correcting the number but by asserting it. tests/test_manifest.py:365 now compares the README prose against the pyproject specifier and :380 against `manifest.KNOWN_GOOD_ANNEAL`. That is the right shape for a defect class whose whole content is prose disagreeing with code.

NEW TONIGHT (7):
  - MEDIUM diogenes-20260903-035237-72eb778ea4fd — HEAD is stamped 0.4.3 and is not the artifact published as 0.4.3; `[Unreleased]` is empty one commit after the CHANGELOG was created to end exactly this.
  - MEDIUM diogenes-20260903-035258-b62dd5946759 — the dark-install detector only recognises a literal absolute path; `~` and `$HOME` wiring reads green. [run]
  - MEDIUM diogenes-20260903-035322-47857b2a4c70 — the new scope tests read LEVAIN_SCOPE / CLAUDE_CONFIG_DIR from the real environment: red under the value the release recommends, vacuous under the other. [run]
  - MEDIUM diogenes-20260903-035343-8360be2de95b — the codex adapter README promises codex operators a doctor FAIL that is deliberately impossible for them.
  - MEDIUM diogenes-20260903-035403-2aa0cef656da — "`levain update` overwrites the hook templates" is false against update.py; `levain init --force` is the command that does it.
  - LOW diogenes-20260903-035430-d562fa77d52b — pyproject.toml:84 contradicts :57 about the pin two lines below it.
  - LOW diogenes-20260903-035430-cae47498e69a — the source-checkout guard landed on one of two sibling tests, eight lines from the comment explaining why it must exist. [diagnosis run-verified]

ONE SELF-CORRECTION, diogenes-20260903-035457-1369803e5376: my 08-17 slot named a carried item at `README.md:182-196`; the file has never been that long, and the item is `tests/test_compat_integration.py:182-196`, exact at HEAD. Mis-attributed, not duplicated — the count does not move.

RE-DERIVED RATHER THAN CARRIED (5 of the 45):
  - **README.md:164 → closed** (above).
  - **manifest.py:85 — STILL OPEN, 19 days.** `sed -n '85p'` reads "Reconciled through 0.9.6" while :113 reads `TEMPLATES_RECONCILED_ANNEAL = "0.9.7"`. This window bumped KNOWN_GOOD_ANNEAL 0.9.7→0.9.8 in the same file and left the sentence.
  - **tests/test_firing_confinement.py — the openhands import gate, open since 2026-08-07, TWENTY-SEVEN DAYS.** RE-EXECUTED, not inferred: full suite here, **8 failed / 2221 passed / 18 skipped in 103.84s**, and `test_live_plain_config_cannot_be_unlinked_or_relocated` (:1261, under `@live`) FAILED rather than skipped again. The other 7 are all tests/test_app_server.py (mcp extra absent). Denominator checked: 2221 against 2085 on 09-01 — the window added 136 passing tests.
  - **The two seed findings from 09-01 — both STILL OPEN, re-read at their coordinates.** continuity.md:19 still ships [quoted: `graduates to 2x, then 3x`] against memory.md:102's [quoted: `2x, 3x, 4x, onward, **no ceiling**`]; memory.md:103 still ends with the co-cite clause grafted onto "not a target". No seed file is in `git diff --name-only 5e826f4..HEAD`.
  - **tests/test_compat_integration.py:353 — STILL OPEN**, file untouched; `sed -n '353p'` is still the lone positive-presence assertion.

⚙ THE SIX CARRIED doctor.py FINDINGS ARE STILL OPEN BY BYTE-IDENTITY, AND THEIR COORDINATES ALL MOVED. `git diff 5e826f4..HEAD -- levain/doctor.py` is **287 insertions, 0 deletions**, in two hunks (`@@ -79,6 +79,10 @@` and `@@ -912,6 +916,289 @@`). Every pre-existing line is byte-identical, so nothing could have closed — but every carried coordinate at or past the old :912 is now **+293**: :981-986 → :1274-1279, :989 → :1282, :1085 → :1378, :1098 → :1391, :1137/:1140 → :1430/:1433, :1166/:1169 → :1459/:1462. The routed reports in project_memory/ still cite the old numbers. Not filed as a finding — that is my own prose, and republishing it as a finding is the loop the DRIFT label exists to break — but an operator working the backlog needs the offset.

⚠ THE INHERITED CAVEAT IS UNCHANGED AND I AM NOT LAUNDERING IT. The 46 was arithmetic down a chain from the 08-22 slot, never a fresh census. Five of the 45 are re-derived above and six more rest on git byte-identity; **the remaining ~34 are unverified at this HEAD**, and by my own 09-02 close the file-unchanged heuristic may carry a finding once and never twice — several of these are past that. **52 is sound as a ledger and not as a census, and levain now needs a triage pass more than another review night.**
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-035655-377e61a4b1c7
  Time: 2026-09-03T03:56:55  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: levain 2026-09-03, HEAD d84270d5 **== origin/main against the LIVE remote** (`git ls-remote origin HEAD` answered d84270d5; not a cached ref). `git status --porcelain` EMPTY at start and EMPTY at finish — the repo was not touched: every probe ran read-only or in /tmp/dio_lev against `git archive HEAD` copies. ⚠ `~/Documents/levain` is a SYMLINK to `~/Briefcase/levain` — checked first, same trap as claphamdigital.com, solitaire and video-poker. Window `5e826f4..HEAD` = **6 commits** (dispatch said 5, the 24h slice; I took the wider one so it closes exactly on my 09-01 HEAD — no gap), 2,031 insertions / 59 deletions across 18 files.

RE-DERIVED BY RUNNING, NOT CARRIED FORWARD:
· **FULL SUITE at HEAD** — `python3 -m pytest -q`: **8 failed / 2221 passed / 18 skipped in 103.84s**, all 8 enumerated (7 × tests/test_app_server.py from the absent mcp extra, plus test_live_plain_config_cannot_be_unlinked_or_relocated). Denominator checked, not reused: 2221 against 2085 on 09-01.
· **THE COMMIT'S CENTRAL CLAIM, EXECUTED.** 12df6b3 asserts the reviewer's mutant "now dies at all four sites". I ran it: replaced all four `_wrap_state_compat` call sites with `state = (hook.episodes_since_wrap(), False)` in a scratch `git archive` tree and ran tests/test_hooks.py → **4 failed, 154 passed**, one failure per adapter × script, each naming the adapter and script. The claim is true and the test is real: `_run` stubs `_anneal_json` (the TRANSPORT) and drives the real `main()`, so the routing under test is not itself mocked.
· **THE DARK-INSTALL DETECTOR, PROBED IN A SCRATCH $HOME** — a real install at `$HOME/lev` with HOME=/tmp/dio_lev/home2 and one SessionStart entry in `$HOME/.claude/settings.json`, four command spellings: `~` → [], `$HOME` → [], absolute → detected, `${CLAUDE_PROJECT_DIR}` → [] (correctly). Then I EXECUTED the prescribed fix in a patched copy and re-ran all four: `~` detected, `$HOME` detected, absolute detected, placeholder still [].
· **THE ENVIRONMENT-DEPENDENCE OF THE NEW TESTS, MEASURED IN BOTH DIRECTIONS** — `LEVAIN_SCOPE=global pytest tests/test_doctor_content.py` → 1 failed / 107 passed; `CLAUDE_CONFIG_DIR=<dir> ...` → 2 failed / 106 passed. Then the conftest fix EXECUTED and re-verified across three environments → 266 passed each time.
· **THE PUBLISHED ARTIFACT OPENED, not inferred from the tag** — `tar -xzOf dist/levain-0.4.3.tar.gz .../\_levain_hook.py` and `git show` at bdef6ee / ebec171 / d84270d, plus `git tag --points-at`. That is what established repo-≠-sdist under one version string.
· **A NON-SOURCE CHECKOUT SIMULATED** — `levain/` alone on PYTHONPATH so `_repo_root()` has no README.md: TestReadmePinClaim → 1 skipped, 1 failed.
· **BOTH ADAPTER HOOK TREES DIFFED** — `_levain_hook.py` / `session_start.py` / `user_prompt_submit.py`, claude vs codex: divergence is docstrings only, as the parity test claims.
· **THE `levain update` CALL GRAPH READ END TO END** (update.py:41-254 + reconcile.run_pack_reconcile) to settle whether it writes the activation tree. It does not; `grep -n "config.json" levain/update.py levain/reconcile.py` returns zero lines.

SOURCE OPENED IN FULL OR IN THE RELEVANT SPAN: the entire diff; levain/doctor.py :1-110, :856-1210; all six hook template files; levain/update.py :1-300; levain/reconcile.py :433-500; levain/install.py :91-111; levain/manifest.py :60-115; levain/verify.py :240-255; pyproject.toml; README.md :95-170; CHANGELOG.md in full (120 lines); levain/templates/adapters/codex/README.md :380-430; tests/test_hooks.py :1343-1470 + the class/def index; tests/test_doctor_content.py :963-1105; tests/test_manifest.py :343-386; tests/conftest.py in full; levain/templates/seed/continuity.md:19 and memory.md:102-103.

⛔ **NOT CHECKED, AND SOME OF IT MATTERS.**
· **THE CODE HALF OF THIS RELEASE CLOSED EMPTY AND I WENT LOOKING.** `wrap_state`, `configured_scope`, the compat shim, the `$HOME`-identity guard, the fail-closed scope rule and the four routing sites all survived reading and probing. Six of my seven findings are in the RECORD surfaces the release produced — the CHANGELOG, the adapter README, the version stamp, the test environment — and the seventh is a coverage gap in a new detector. **That is not evidence the code is clean; it is evidence that the code got a same-day L3 mesh and the record did not.** Same shape as video-poker 24 hours ago.
· **I DID NOT RUN `levain doctor` END TO END, `levain init`, or `levain update` AGAINST A REAL INSTALL.** Everything about doctor tonight is function-level or scratch-fixture. The 0.4.2/0.4.3 story is explicitly that "a green suite missed it both times" and what caught it was running doctor against a real install of each adapter — **I did not do the thing that found the last two defects of this class.**
· **NO NETWORK ORACLE.** PyPI was not queried, so "0.4.2 and 0.4.3 are published" is taken from `dist/` and the tags, not from the index. anneal-memory 0.9.8's `wrap_cancel` MCP tool — the entire justification for the `>=0.9.8` floor — was NOT verified to exist; I checked only that levain names it.
· **~34 OF THE 45 CARRIED ITEMS ARE UNVERIFIED AT THIS HEAD.** Five re-derived, six argued by byte-identity. I did not re-census the ledger, and by my own 09-02 close several of these are past their one permitted un-re-derived carry.
· UNOPENED ENTIRELY: writes.py, dashboard.py, web_server.py, session.py, interview.py, tui.py, run.py, jobs.py, packs.py, firing/ — the same list my 08-18 coverage line named as never having been touched by a finding.

**52 is a count about RECORDS and one detector gap. It says nothing about whether a real install of either adapter comes up green.** [prescription: none]
  Tags: code_review, levain, semantic
```
