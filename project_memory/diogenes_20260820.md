# DIOGENES OVERNIGHT — levain, 2026-08-20

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-08-20*)`
**Stated count: 3 episode(s).** Severity: COVERAGE 1 · MEDIUM 1 · STILL OPEN 1
**STILL OPEN: 37**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260820-050931-fc0b4b907bbf
  Time: 2026-08-20T05:09:31  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/jobs.py:308 (+ the four sites that document the opposite) — THE LEASE IS DOCUMENTED AT FOUR PLACES AS MAKING A DEAD JOB READ `failed` AT POLL, AND THE PRUNE ON THE VERY NEXT WRITE TURNS IT INTO `unknown` — WHICH IS THE ONE STATUS THIS MODULE'S OWN READ-POLARITY DOCTRINE SAYS MUST NEVER BE FALSE, BECAUSE IT SENDS THE OPERATOR TO RE-PROPOSE AN EXPENSIVE JOB. _prune DROPS a lease-expired non-terminal record: jobs.py:308 is [quoted: `                continue  # expired (or un-ageable) → drop`]. Four separate places state the operator-visible outcome as the opposite. jobs.py:41 [quoted: `LEASE: a record older than :data:`JOB_LEASE_S` from ``created_at`` reads as ``failed`` at poll and`] — the next line adds 'is pruned on the next write', so the sentence asserts BOTH halves as if they compose. jobs.py:438, the JobRuntime CONCURRENCY-SLOT CONTRACT, [quoted: `    job READ as failed at poll, but it does NOT reclaim the live slot — the handler's own timeout`]. jobs.py:519, the worker's finish-failure path, [quoted: `                _log.error(\"job %s: finish write failed (%s); poll will lease-expire it\",`]. And the OPERATOR-FACING route docstring, web_server.py:557 [quoted: `    job) nor a false ``done``. The lease backstop (a non-terminal record past its window reads as`] — sitting in the same paragraph that reserves `unknown` for 'a missing / pruned id; the operator re-proposes'. RUN-VERIFIED, NOT REASONED, with the real classes at their real defaults in /tmp/levain_dio_20260820. A wedged job created + claimed running, polled at +700s: `{'job_id': 'wedgedjob', 'status': 'failed', 'error': 'interrupted'}`. Then ONE subsequent propose (any `create` — that is where _prune runs), same poll instant: `{'job_id': 'wedgedjob', 'status': 'unknown'}`. The claim is true for exactly as long as nobody else proposes a job. THE COST IS AT THE COMPOSITION, AND I MEASURED THAT TOO. JOB_LEASE_S=600 but RESULT_TTL_S=86400 and the idempotency window is 86400 — matched DELIBERATELY (jobs.py:77-84) so a replayed propose can still poll its handle. Non-terminal records get the SHORT lease, so: claim KEY-1 -> create+claim a job -> finalize the handle -> 20 min later another propose prunes the wedged record -> same-key retry at +2h returns `replay` with the ORIGINAL handle `{'ok': True, 'job_id': 'JOBWEDGED', 'status': 'pending', 'verb': 'consult'}` -> polling that job_id returns `{'job_id': 'JOBWEDGED', 'status': 'unknown'}`. The cached handle says PENDING and the store says it never existed. That is precisely the 'dead handle the operator can't re-run with the same key' the TTL-matching comment says the matching exists to prevent; the parenthetical at :83 waves at the asymmetry ('A NON-terminal orphan still reaps at the short lease') without following it to the handle. Reachable whenever a job never reaches terminal in a LIVE process — the wedged-handler case jobs.py:432-443 names as a real constraint, and the finish-write-failure case at :518. A process RESTART is not affected: sweep() marks non-terminal -> failed with finished_at=now, after which the record ages under result_ttl_s. THE CODE AND THE TEST AGREE WITH EACH OTHER AND DISAGREE WITH THE PROSE, which is why fourteen days of review never saw it: tests/test_jobs.py:215 PINS the drop, [quoted: `        assert s.read_status(\"old_pending\", _later(7200))[\"status\"] == \"unknown\"  # pruned`], inside a test named test_prune_drops_expired_terminal_and_orphans. Both authors saw a true observation; the divergence is ORDER-DEPENDENT, so neither could see the other's. -- FIX: I EXECUTED a candidate and measured its blast radius rather than proposing it blind. In a scratch copy, make _prune TRANSITION a lease-expired non-terminal record to failed/'interrupted' with finished_at=now (the exact shape sweep() already writes) instead of dropping it, so it then ages out under result_ttl_s like any other terminal record. Measured under the patch: poll reads failed at +20m and at +2h, and unknown at +30h once the result TTL expires — the documented behaviour, end to end. Blast radius: EXACTLY ONE test goes red, tests/test_jobs.py::TestLeaseAndRecovery::test_prune_drops_expired_terminal_and_orphans (1 failed, 211 passed across test_jobs.py + test_writes.py). ⚠ WHICH SIDE TO CORRECT IS NOT MINE TO CALL: the patch is a BEHAVIOUR change against a deliberately pinned test, and the conservative repair is instead to correct the four prose sites to say 'reads as failed at poll UNTIL the next write prunes it, then unknown'. Whether a lease-expired orphan is a record the operator should still see, or garbage, is intent I cannot read off disk. [prescription: run]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260820-051007-fb0d73de5661
  Time: 2026-08-20T05:10:07  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: STILL OPEN (37), TOTAL AT HEAD 55087f4 — levain, 2026-08-20. 36 carried, 1 new, ZERO CLOSED. The window is once again a single doc-only commit adding project_memory/diogenes_20260819.md — the FOURTEENTH consecutive levain window whose entire content is a routed Diogenes report. THE ARTIFACT VERIFIED AGAINST THE STORE, NOT THE DIFF: a SQL join on episode_tags (tag=levain, agent=diogenes, timestamp LIKE '2026-08-19%') returns FIVE rows; the committed file holds FIVE blocks; all five ids present and all five contents verbatim by substring match against the store; zero foreign passengers. Header 'Stated count: 5 episode(s)' and 'STILL OPEN: 36' both parsed. THE COMMIT BODY MAKES A CLAIM ITS OWN DIFF CANNOT SHOW, AND I CHECKED IT RATHER THAN ASSUMING EITHER WAY: it says 'The pickup file's pointer is updated to name this report', while --stat shows ONE file changed, all in project_memory/. The pointer is not in this repo — route_diogenes.py maps levain to flow's projects/levain/next.md — and it is CORRECT there: :19 reads 'DIOGENES — NEWEST: `diogenes_20260819.md` · **STILL OPEN: 36**' with ':20' giving '5 episode(s) — 1 LOW / 2 MEDIUM'. The claim is true; it is just unverifiable from the commit that makes it. NEW TONIGHT (1). levain/jobs.py:308 + jobs.py:41/:438/:519 + web_server.py:557, MEDIUM — four sites document a lease-expired job as reading `failed` at poll; _prune drops the record on the next write and it reads `unknown`, the status this module's own read-polarity doctrine reserves for 'the operator re-proposes'. Run-verified with the real classes at real defaults, including the composition with the 24h idempotency replay (a cached handle saying 'pending' over a record the store says never existed), and the candidate fix EXECUTED with its blast radius measured at exactly one pinned test. CARRIED LEVAIN TREE (25) — verified by GIT BYTE-IDENTITY over the widened pathspec: `git diff --numstat 6281cf6 HEAD -- . ':(exclude)project_memory'` is EMPTY, five consecutive routing-only commits, so nothing outside project_memory/ has moved since the 08-14 anneal-floor bump. That bounds all 25 without my having to enumerate a pathspec I could get wrong. CARRIED FLOW TREE (11) — RE-MEASURED, NOT CARRIED, because flow took 21 commits yesterday and byte-identity there is NOT available to me as an argument. `git log --oneline -3 -- scripts/prescription.py` shows HEAD for that file is still 35f7cbcb, the commit I filed against, so both flow-side items stand: the sweep table at :129-136 (16/4/1) still does not reproduce from the shipped classifier, and `_RETRACTION` at :125 is byte-unchanged [quoted: `_RETRACTION = re.compile(rf\"\\A\\s*SELF[- ]CORRECTION\\s+to\\s+({_ID})\", re.I)`]. Re-measured the reach TONIGHT rather than repeating last night's number: 43 diogenes episodes now OPEN with SELF-CORRECTION, the anchor matches 26, 17 invisible — the gap is unchanged at 17 while both ends grew. THREE CARRIED ITEMS RE-EXECUTED HERE. (1) FULL SUITE RUN: 8 failed / 2081 passed / 18 skipped in 102.67s — identical counts to 08-15 through 08-19, and test_live_plain_config_cannot_be_unlinked_or_relocated FAILED rather than skipped for the FOURTEENTH night, so tests/test_firing_confinement.py:1113's [quoted: `    from levain.firing.openhands.tools import SandboxedBashExecutor  # openhands leaf, gated by @live`] is still FALSE. The other 7 are all test_app_server.py (mcp extra absent). (2) README.md:164 re-read at HEAD, still `(pinned \`>=0.9.6,<0.10\`)` against pyproject.toml:74's [quoted: `    \"anneal-memory>=0.9.7,<0.10\",`]. I grepped on the VERSION not the package name, per my own 08-19 note that the package-name grep returns nothing here and would read as a false close. (3) scripts/check_seed_budget.py EXECUTED: clean, 38,759 B eager / 55% mechanism against limits of 40,000 B / 62% — same as last night. HYPOTHESES OPENED AND KILLED AGAINST DISK, stated because a dropped finding is evidence about the instrument. (a) I suspected `levain verify-hooks` under-quantifies: verify.py:147 and :171 enumerate exactly two literal hook names while its module docstring says it 'Invokes each hook script', and doctor.py:1129-1172 goes to real trouble over PACK-shipped and NESTED hooks that verify.py cannot see. KILLED: the harness config is a fixed template naming exactly those two paths (adapters/claude-code/settings.template.json), and packs layer only seed/ and activation/ (packs.py:340 order_activation_roots), never the adapter settings — so no pack can add a harness-fired hook and the enumeration is honest. A nested pack hook is installed and placeholder-substituted but never fires; that is install.py's business, not verify.py's. (b) I suspected the jobs/idempotency 'deliberately duplicated' pair had drifted the way web_server/init_server did on 08-19 — the exact propagation class. KILLED by grep: both carry the sidecar-flock, the short-write memoryview loop, the fsync, the atomic replace and the dir-fsync. Parity holds. (c) I checked whether _apply_undo can be walked outside the Class-A set by a forged audit record naming a Class-C seed file: it can, since undo does not re-assert _assert_class_a_target — but the audit log is a local file under .levain/, so anyone who can forge it can write the target directly. NO privilege gain, NOT FILED. NOT COUNTED AS OPEN, unchanged for the fourteenth night: the environment-dependent severity argument in the 08-04 report, and the pack-hook DELETION case I have declined on 08-06 through 08-19 and decline again. ARITHMETIC STATED SO THE TREND SURVIVES: 36 at 55087f4 = levain 25 + flow 11; tonight levain 25 + 1 = 26, flow 11 + 0 = 11; 26 + 11 = 37. REPO UNTOUCHED: `git status --porcelain` empty at 55087f4 before and after the suite run, HEAD == origin/main; flow's tree read only; the scratch copy, the candidate patch and every probe ran in /tmp/levain_dio_20260820. ALL QUOTED STRINGS PRE-FLIGHTED WITH `grep -cF` AGAINST THE FILE ON DISK BEFORE FILING — and the pre-flight EARNED ITS KEEP tonight: two of my six quotes returned 0 because I had reconstructed them across a line break, and I re-derived both from `sed -n 'Np'` rather than filing the paraphrase.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260820-051040-9317c6ae5acd
  Time: 2026-08-20T05:10:40  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: levain 2026-08-20, HEAD 55087f4 == origin/main, porcelain empty, window HEAD~1..HEAD — one doc-only commit adding project_memory/diogenes_20260819.md (57 lines, 5 blocks), the routed report of my own 08-19 findings. Fourteenth consecutive window of my own prose. RE-DERIVED BY RUNNING, NOT CARRIED FORWARD. (1) THE ARTIFACT CHECKED AGAINST THE STORE by SQL join on episode_tags: 5 rows, 5 blocks, all 5 ids present, all 5 contents verbatim by substring, zero foreign passengers; header 'Stated count: 5' and 'STILL OPEN: 36' both correct. (2) THE COMMIT BODY'S POINTER CLAIM CHECKED IN THE OTHER REPO, since the diff cannot show it: flow's projects/levain/next.md:19-20 names diogenes_20260819.md with STILL OPEN 36 and '5 episode(s) — 1 LOW / 2 MEDIUM'. Correct. (3) TREE BYTE-IDENTITY on the wide pathspec: `git diff --numstat 6281cf6 HEAD -- . ':(exclude)project_memory'` EMPTY. (4) FULL SUITE RUN HERE: 8 failed / 2081 passed / 18 skipped in 102.67s, all 8 enumerated — 7 mcp-absent in test_app_server.py plus test_live_plain_config_cannot_be_unlinked_or_relocated, which FAILED rather than skipped, re-executing the :1113 finding for the fourteenth night. (5) scripts/check_seed_budget.py EXECUTED: clean, 38,759 B / 55%. (6) THE JOBS FINDING RUN-VERIFIED AT THREE DEPTHS, not reasoned: the store-layer poll before and after a prune; the full composition against a real IdempotencyStore at real defaults (a replayed handle saying 'pending' over a pruned record); and THE CANDIDATE FIX ITSELF EXECUTED in a scratch copy with its blast radius measured (poll reads failed at +20m/+2h and unknown at +30h; exactly one pinned test goes red, named). (7) THE FLOW-SIDE CARRIES RE-MEASURED rather than assumed, because flow took 21 commits yesterday: prescription.py confirmed still at 35f7cbcb, and the _RETRACTION reach re-run tonight at 43 open / 26 matched / 17 invisible. (8) THREE HYPOTHESES KILLED AGAINST DISK and named in the slot — the verify-hooks enumeration (killed by the fixed settings template + packs.py's layer set), the jobs/idempotency duplication drift (killed by grep: parity holds), and an undo-path Class-A bypass (real but no privilege gain over direct file write). SOURCE OPENED IN FULL OR IN THE RELEVANT SPAN: levain/jobs.py IN FULL (527 lines, first time any report has opened it); levain/verify.py IN FULL (478, first time); levain/writes.py :100-250, :307-400, :508-740, :1006-1115, :1922-2129; levain/dashboard.py :1-135, :476-670, :980-1240, :1239-1582; levain/doctor.py :1100-1200; levain/install.py :1650-1710; levain/packs.py + manifest.py by grep; levain/web_server.py :545-563; tests/test_jobs.py by grep + :205-215; README.md:164; pyproject.toml:74; tests/test_firing_confinement.py:1113; the claude-code settings + mcp templates in full. Out of repo: flow's route_diogenes.py by grep, projects/levain/next.md, scripts/prescription.py :125/:129-137, the flow episodic store by SQL. NOT CHECKED, AND THE LIST IS STILL LONGER THAN THE CHECKED ONE. levain/session.py (1,052), levain/interview.py (914), levain/tui.py (821), levain/_tui_curses.py (834), levain/run.py (835), levain/reconcile.py (553), levain/idempotency.py (529 — I compared its IO discipline to jobs.py BY GREP and did not read it), levain/update.py, levain/turn_input.py, levain/answers.py, levain/wrap.py (910) and levain/cli.py (1,638) were NOT OPENED tonight. levain/install.py (2,169) got 60 lines. levain/dashboard.py is now PARTLY read for the first time in fifteen days — roughly 700 of 1,861 lines — and I did NOT read its layout() manifest, render_text or render_summary at all. I did NOT open levain/firing/ tonight at all, so the SBPL emitter and the backslash-escaping suspicion I dropped on 08-19 are STILL undropped-and-unexamined, a second night. I did NOT re-execute the two heavy 08-14 doctor.py repros (:1169, :1140) — they rest on git byte-identity plus an unchanged suite, which is a structural argument, not a re-run. I did NOT run `levain doctor`, `levain init`, `levain serve` or `levain verify-hooks` end to end as an operator would; every probe tonight was library-level. I did NOT re-run last night's two web_server socket probes. I did NOT check the public remote. THE THING WORTH SAYING PLAINLY. Tonight's finding is the first one I have filed here where THE CODE AND ITS TESTS AGREE WITH EACH OTHER AND BOTH DISAGREE WITH THE PROSE AT FOUR SITES — including the operator-facing route docstring. Every previous instance of this class had a test that was absent, or wrong, or stipulated its own answer; this one has a test that is correct, deliberate, and named for the behaviour it pins. That means the usual instrument — run the suite, believe the green — is not merely blind here, it actively CERTIFIES the half that contradicts the documentation. The only thing that found it was reading a 527-line module end to end with no diff to anchor on, which is exactly the work fourteen nights of routed-report windows have been crowding out. And I should be plain about the cost of that: jobs.py has existed for the whole time I have been reviewing this repo and no report had ever opened it. The full constellation drift sweep, the cognitive half of my dual lens, has now not run since 2026-05-24 — FOURTEEN WEEKS. [prescription: none]
  Tags: code_review, levain, semantic
```
