# DIOGENES OVERNIGHT — levain, 2026-09-16

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-09-16*)`
**Stated count: 6 episode(s).** Severity: LOW 1 · MEDIUM 2 — 3 of 6 episode(s) carry a severity; the other 3 are COVERAGE-OPEN 1 · SELF 1 · STILL OPEN 1
**STILL OPEN: 4** — **3 filed tonight** (measured at HEAD `58bb5ed`) · **1 carried**. A carried item was NOT necessarily re-checked this window; only the slot block below says which ones were.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260916-030307-d7cf988d2840
  Time: 2026-09-16T03:03:07  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: levain 2026-09-16, HEAD 58bb5ed7e356c3fbdd916d6751fb7853a9ab29a1 (== origin/main? YES — `git ls-remote origin HEAD` answered 58bb5ed7e356c3fbdd916d6751fb7853a9ab29a1 at start, live remote not a cached tracking ref), `git status --porcelain` DIRTY BY UNTRACKED NOISE ONLY: `?? .DS_Store`, `?? levain/.DS_Store`, `?? tests/.DS_Store` — nothing staged, nothing modified, and they were present before I started (they appear in the session's own opening git status snapshot). Window `33be8b6..HEAD` = 5 commits; dispatch said 4 and the extra is `daa1e99`, MY OWN 09-15 routing commit, so the 24h slice and the review window are the identical 5. Baseline from the store: `STILL OPEN (3)` at HEAD `33be8b6` (2 filed 09-15 — `levain/doctor.py:1662` MEDIUM and `levain/install.py:1758` LOW — plus 1 carried), and that pool is COMPLETE (it has both a slot and a COVERAGE line, no dead run behind it). ⚠ `~/Documents/levain` IS a symlink to `~/Briefcase/levain` — `pwd -P` run FIRST, and `os.path.realpath` confirms both resolve to /Users/phillipclapham/Briefcase/levain. REVIEW STARTED, NOT FINISHED — if no COVERAGE line for levain exists in this run, THIS RUN DIED and the finding list is PARTIAL. [prescription: none]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260916-031703-ebcc81112407
  Time: 2026-09-16T03:17:03  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/install.py:2289 — THE C6 AMENDMENT RULED THAT A DAMAGED RECEIPT IS NOT A PRE-RECEIPT INSTALL, AND THE REMEDY THAT C6'S OWN MESSAGE SENDS THE OPERATOR TO STILL TELLS THEM "NO INSTALL RECEIPT COVERS IT". `_report_and_rotate` receives `prior_status` and only ever compares it to `"ok"`, so `absent`, `corrupt` and `empty` all print the one absent-flavoured sentence.

[quoted, levain/install.py:2279] `        if prior_status == "ok" and prior_installed is not None:`
[quoted, levain/install.py:2287] `        else:`
[quoted, levain/install.py:2289] `                f"  Previous activation/ kept whole at {moved_to} (no install receipt "`
[quoted, levain/install.py:2290] `                f"covers it, so levain cannot tell whether you edited anything in it)"`

WHY IT IS NOT COSMETIC. This window's whole point (`54c017a`, doctor.py:1671) is that a corrupt or empty receipt is DAMAGE, not a pre-receipt install — the new detail text says so in those words. Doctor now says `is unreadable (corrupt) ... a damaged receipt is not a pre-receipt install` and exits 1, and the hint it prints is `levain init --force`. Run that hint and levain answers with the sentence doctor just spent an exit code refusing. Two surfaces of the same tool describe one state in mutually exclusive terms, inside one operator session, and the wrong one is the second — the one that decides whether the operator believes a receipt existed at all when they go looking for what levain could not compare.

BEHAVIOURAL, FALSIFIABLE SIGNATURE, observed 2026-09-16 against HEAD `58bb5ed` through the REAL CLI on a REAL `levain init --adapter claude-code` install (not a unit call). Damage `.levain/activation-manifest.json` three ways — delete it (`absent`), write `{` (`corrupt`), write `{"schema": 1, "files": {}}` (`empty`) — then run `levain init --force` on each. All three emit the BYTE-IDENTICAL line, differing only in the tree stamp:
  `  Previous activation/ kept whole at <install>/.levain/backups/activation/tree-<digits> (no install receipt covers it, so levain cannot tell whether you edited anything in it)`
The discriminator that proves the states really are distinct is `doctor` on the same three installs, same HEAD: corrupt -> exit 1 + `is unreadable (corrupt)`, empty -> exit 1 + `is unreadable (empty)`, absent -> exit 6 + `no install receipt covers them`. Doctor resolves three states into two; `init --force` resolves the same three into one, and picks the wrong one.

⛔ AND THE SUITE PINS THE CONFLATION, WHICH IS WHY IT SURVIVED THE ROUND. Two tests assert the SAME substring pair for OPPOSITE receipt states and both are green:
[quoted, tests/test_receipt_and_upgrade_exit.py:115] `    assert [m for m in said if "Previous activation/ kept whole" in m and "cannot tell" in m], said` — inside `test_a_damaged_or_empty_receipt_is_UNKNOWN_never_pristine`, parametrized over `"not json at all"` and `'{"schema": 1, "files": {}}'`.
[quoted, tests/test_install.py:2232] `    assert [m for m in said if "Previous activation/ kept whole" in m and "cannot tell" in m], said` — inside `test_a_receiptless_tree_is_preserved_WITHOUT_a_claim_either_way`, i.e. genuinely ABSENT.
An assertion that cannot tell corrupt from absent cannot go red when the code stops telling them apart either.

⚡ WHERE THE SWEEP MISSED IT, and this is the part worth carrying: `project_memory/TRIAGE_2026-09-15.md` has a section titled `## SIBLING GREP (DONE commit)` whose grep at :12 is over `levain/*.py` and therefore DID list `install.py:2092` (`prior_installed, prior_status = read_activation_receipt(dst.parent)`), but whose conclusion at :43-47 discusses only `doctor.py` and closes with "out of scope for this amendment". The command was right; the reading of its output was scoped to the file the finding named. `guard_scoped_by_symptom_misses_the_class`, and the grep that would have caught it was already in the record.

FIX: `_report_and_rotate` already has `prior_status` in its signature — branch it three ways instead of two. `absent` keeps today's sentence; `corrupt`/`empty` get one naming the damaged receipt at `_tree_receipt_path`/`activation_receipt_path` and saying the tree is kept BECAUSE the record is unreadable, matching doctor's wording. Then split the two assertions above so each pins its own state. [prescription: reasoned]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260916-031729-b13d39e5d60e
  Time: 2026-09-16T03:17:29  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: CHANGELOG.md:7 — THIS WINDOW CHANGED A `levain doctor` EXIT CODE FROM 6 TO 1 FOR A REAL INSTALL STATE, AND `## [Unreleased]` RECORDS NOTHING. The same file's 0.4.7 section is the one that told operators doctor's exit codes are a scripting contract and warned them in bold that `6` no longer matches a `1` branch.

[quoted, CHANGELOG.md:7] `## [Unreleased]`
[quoted, CHANGELOG.md:9] `Stamped `0.4.8.dev0`. **The tree past a release tag no longer claims the released version** — see *Versioning* at the foot of this file.`
That stamp note is the ENTIRE contents of `[Unreleased]` at HEAD `58bb5ed`; the next line is `## [0.4.7] — 2026-09-14`.

THE CONTRACT THE SAME FILE PUBLISHES, quoted from the 0.4.7 section:
[quoted, CHANGELOG.md:44] `⚠ **If a script branches on specific codes** (`case $? in 1) ...`), exit 6 no longer matches the `1` branch. Scripts that only test for nonzero (`levain doctor || alert`) behave as before.`
So this project has already established that (a) doctor's codes are consumed by scripts and (b) a change in which code a state yields is changelog-worthy at the ⚠ level.

MEASURED, NOT REASONED — both sides, same install, same damage, same command. A real `levain init --adapter claude-code` install; `os.system("id")` appended to `activation/hooks/_levain_hook.py`; `.levain/activation-manifest.json` overwritten with `{`. Then `levain doctor --path <install>` run twice, once with `sys.path` pointing at a `git archive 33be8b6` tree and once at a `git archive 58bb5ed` tree, each printing its own `levain.__file__` to prove which code ran:
  33be8b6 -> EXIT 6, `hook freshness: ... (no install receipt covers them, so doctor cannot tell an outdated hook from an edited one)`
  58bb5ed -> EXIT 1, `hook freshness: ... the install receipt at <path> is unreadable (corrupt), so doctor cannot tell an outdated hook from an edited one; a damaged receipt is not a pre-receipt install`
An operator whose CI does `levain doctor; case $? in 6) apply_upgrade;; 1) page;; esac` goes from a silent upgrade path to a page, on the same install, with nothing in the changelog to point at.

⛔ IT IS NOT "THE CHANGELOG IS WRITTEN AT RELEASE TIME" — THIS REPO'S OWN CONVENTION IS THE OPPOSITE, and I checked rather than assuming. `849c259` ("spore-861 + spore-900: fixes from item 3's L3 ...") is a mid-development commit on a seat branch, and `git show 849c259:CHANGELOG.md` has `## [Unreleased]` stamped `0.4.7.dev0` carrying full `### Changed —` sections written as the work landed. `git log --oneline -3 -- CHANGELOG.md` = `6629a0a` (version bump), `ec82c4e` (Release 0.4.7), `849c259` (mid-dev content). So entries accumulate under `[Unreleased]` during development here, and this window is the first since the 0.4.8 cycle opened to ship an operator-visible behaviour change without one.

⚠ SCOPE, STATED HONESTLY: of the window's two code commits only the C6 amendment (`54c017a`) is operator-visible. `aafce33` is an internal TOCTOU hardening of the receipt read with no behaviour an operator can observe, and I am NOT claiming it needs an entry.

FIX: one `### Changed` section under `[Unreleased]` — a stale hook plus a corrupt or empty `.levain/activation-manifest.json` is now exit 1, not 6, because a damaged receipt is damage rather than a pre-receipt install; absent is unchanged at 6. It is the same shape as the 0.4.7 exit-code section already in the file. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260916-031754-95ec2156063f
  Time: 2026-09-16T03:17:54  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CORRECTION — diogenes-20260916-031729-b13d39e5d60e (tonight's CHANGELOG MEDIUM). ONE WRONG COORDINATE IN A SUPPORTING CLAUSE. I wrote `[quoted, CHANGELOG.md:44]` for the ⚠ script-compatibility warning. IT IS AT `CHANGELOG.md:50`. `:44` is `- `0` — every check passed;`, six lines above it, inside the same exit-code list.

⛔ THE QUOTED TEXT IS EXACT AND THE DIAGNOSIS IS UNCHANGED. `awk NR==50 CHANGELOG.md` returns, byte for byte, `⚠ **If a script branches on specific codes** (`case $? in 1) ...`), exit 6 no longer matches the `1` branch. Scripts that only test for nonzero (`levain doctor || alert`) behave as before.` Every other coordinate in that finding — `CHANGELOG.md:7`, `CHANGELOG.md:9`, and the two measured exit codes — round-tripped before filing and stands. Read the warning at `:50`.

▶ CAUSE, AND IT IS THE ONE MECHANISM MY CLOSES ALREADY NAME. I ran a round-trip batch over fourteen coordinates before composing anything and all fourteen were exact. `CHANGELOG.md:44` was not in that batch: I reached for the warning WHILE WRITING THE PROSE, after the batch had run, and typed a number from memory of the `sed -n '1,60p'` dump I had read earlier. That is verbatim my 2026-09-09 anneal-memory close — "the batch validates the coordinates you DERIVED and is structurally blind to the ones you INVENT while writing the prose, which is the position of maximum confidence and zero measurement" — and the close I wrote for it was to take the batch's input FROM THE FILED TEXT. I have `coordcheck.py` at ~/consultation/diogenes/ which does exactly that, on stdin, and I did not run it on the composed body. The tool existed, was correct, was built for this, and the finding went out without it. `knowing_a_class_does_not_immunise_you_against_producing_it`, with the instrument sitting on disk.

▶ CLOSE, and it is not a new rule, it is the enforcement of an old one: `coordcheck.py` runs on the COMPOSED HEREDOC, every finding, before the write — not a pre-derivation batch that a later sentence can escape. I ran it against the remaining two findings of this run after this correction.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260916-031844-b4775bb21c55
  Time: 2026-09-16T03:18:44  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: project_memory/TRIAGE_2026-09-14.md:46 — THE ⚖ SECTION THAT IS THE ONLY DEFINITION OF C6 IN THIS REPO STATES THE PRE-AMENDMENT RULE WITH NO POINTER TO THE AMENDMENT PHILL MADE THE NEXT MORNING, AND IT IS THE RECORD A SEAT LANDS ON WHEN IT GREPS `C6`.

[quoted, project_memory/TRIAGE_2026-09-14.md:44] `## ⚖ C6 — Phill, 2026-09-14 (`spore-1027`)`
[quoted, project_memory/TRIAGE_2026-09-14.md:46] `A stale hook with no receipt entry is PENDING, doctor exit 6, as built in `849c259`. This refutes codex L3`

MEASURED, NOT ASSUMED. `grep -rn "C6" project_memory/*.md` at HEAD `58bb5ed` returns exactly two files: `TRIAGE_2026-09-14.md` (the section above) and `TRIAGE_2026-09-15.md` (twelve hits, the amendment). There is no index, no `next_steps.md`, and no `CLAUDE.md`/`AGENTS.md` at the repo root — `ls *.md` returns `CHANGELOG.md` and `README.md` only — so a grep IS the lookup path here, and it returns a dated ⚖ ruling section and a dated triage file with nothing connecting them. `git diff --stat 33be8b6..HEAD` confirms `TRIAGE_2026-09-14.md` was not touched this window.

WHY THIS ONE MATTERS MORE THAN A STALE FACT WOULD. A ruling functions as a standing refusal. The narrow half of C6 is precisely what got routed back to Phill as a premise question on 2026-09-15 rather than decided by a seat — so the next seat that reaches this section is, by construction, one asking whether doctor should refuse something. It will find "PENDING, doctor exit 6", a ⚖ glyph, Phill's name, a date, a spore id and a confirming transcript, and every one of those markers argues AGAINST re-checking. `a_stale_record_is_as_dangerous_as_it_is_trusted`, and the danger is proportional to how well-cited the stale record is.

⚠ THE COUNTER-ARGUMENT, STATED RATHER THAN BURIED, WHICH IS WHY THIS IS LOW AND NOT MEDIUM: `TRIAGE_2026-09-14.md` is a DATED file and a reader may reasonably treat it as history. The reason it still counts is that the section is not a triage entry — it is the ruling's only home, written as a standing rule in the present tense, and the newer file never names it as amended in a way the older file's reader can see.

FIX, one line and no re-litigation: append an amendment pointer to that section — "⚖ AMENDED 2026-09-15: a CORRUPT or EMPTY receipt is not a pre-receipt install and exits 1; `absent` is unchanged. See TRIAGE_2026-09-15.md." The conclusion for the world C6 described stands; only its boundary moved. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260916-032133-cf02e8c0c4e1
  Time: 2026-09-16T03:21:33  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (4), total live defect count for levain at HEAD 58bb5ed == origin/main (live `git ls-remote`, start AND finish). 3 filed tonight (0 HIGH / 2 MEDIUM / 1 LOW) + 1 carried. Filed separately: 0 DRIFT (not in this count). ONE SELF-CORRECTION.

· FILED: MEDIUM `install.py:2289` (diogenes-20260916-031703-ebcc81112407): `_report_and_rotate` collapses `absent`, `corrupt` and `empty` into one "no install receipt covers it" sentence, so `init --force` — the remedy C6's own new message prints — contradicts the exit code doctor just spent on it. Reproduced through the REAL CLI on a REAL install, three states, one byte-identical line. · MEDIUM `CHANGELOG.md:7` (diogenes-20260916-031729-b13d39e5d60e): `[Unreleased]` records nothing for a doctor exit-code change measured 6 -> 1 across this window on the same install; the same file's 0.4.7 section is the one that told operators those codes are a scripting contract. · LOW `project_memory/TRIAGE_2026-09-14.md:46` (diogenes-20260916-031844-b4775bb21c55): the ⚖ section that is C6's only definition still states the pre-amendment rule with no pointer to the 09-15 amendment, and a grep for `C6` is the lookup path here.

· CARRIED (1), RE-DERIVED BY DISCRIMINATOR AGAINST THE REPO, NOT BY RE-GREPPING A COORDINATE: MEDIUM — `web_server.py` `_Handler` / `init_server.py` `_InitHandler` duplicated request preamble. `git log 33be8b6..HEAD -- levain/web_server.py levain/init_server.py` is EMPTY (neither file touched this window). Both classes define their own `_host_ok` (web_server.py:664, init_server.py:190), `_reject` (:919, :261) and `_drain` (:908, :267); I compared the bodies line by line and the CODE is identical in both pairs, differing only in docstring wrapping. The repo's own record agrees and is the oracle: `project_memory/TRIAGE_2026-09-13.md:92` says **"STILL OPEN — ROUTED, not fixed"** with a dated rationale (an architectural extraction, out of `spore-929`'s budget), and no later TRIAGE closes it. ⚠ ONE REFINEMENT I OWE THE COUNT RATHER THAN CARRYING THE OLD WORDING: the security-critical half is NO LONGER duplicated — both `_host_ok` bodies delegate to the shared `host_header_allowed`, so the DNS-rebinding parse cannot diverge. What remains duplicated is the preamble around it. The finding stands; its blast radius is smaller than TRIAGE_2026-09-13's "duplicated verbatim" phrasing implies.

· CLOSED, EACH RE-DERIVED BY RUNNING RATHER THAN BY READING THE FIX — both of my 09-15 findings. (1) MEDIUM `doctor.py:1662` (C6): driven END TO END through the REAL `levain` CLI on a REAL `init --adapter claude-code` install with `os.system("id")` appended to `_levain_hook.py` — corrupt -> EXIT 1 `is unreadable (corrupt)`, empty -> EXIT 1 `is unreadable (empty)`, deleted -> EXIT 6 `no install receipt covers them`, intact -> EXIT 1 `changed since \`levain init\` wrote them`. Exactly the 4-case signature I filed, now observed at the process exit code rather than the function return. The mutant that deletes the new branch takes `tests/test_hook_freshness_receipt_status.py` to 4 failed / 3 passed. (2) LOW `install.py:1758`: `read_activation_receipt_file` now calls `_read_receipt_text`; reverting it to `stat()`-then-`read_text()` (anchor asserted == 1) fails the window's new TOCTOU test alone, 1 failed / 50 passed.

· NOT RE-FILED, known and dated by the repo itself: `TRIAGE_2026-09-14.md`'s five 0.5 candidates (rename 2/3 not reconciled from disk, notice precision, identical re-run still writing, whitespace-only change, an operator file under importlib's exact cache name), its two KNOWN RESIDUALs, and the tray's `spore-1005` / `spore-754` / `spore-1023`. ⚠ AND THE COUNT'S SCOPE, STATED SO IT DOES NOT READ AS TOTAL: 4 is MY pool. `TRIAGE_2026-09-15.md:49-59` declares its own live gap — glm was cut off mid-read TWICE on the C6 diff, so that diff carries 2 of 3 lineages. I confirmed that independently in `~/Briefcase/flow/state/verdicts.jsonl`: `input_id 497d8fa4135ab40c` has `complete: true` for complement and codex and `complete: false` for glm on BOTH rows. That is a coverage hole the repo named honestly and it is not a defect, so it is not in the 4.

· SLOT PRE-FLIGHT, through the router's OWN functions and not a reimplementation: `fetch_blocks("levain","24h","2026-09-16")` -> 5 blocks; `severity_line` -> `LOW 1 · MEDIUM 2 — 3 of 5 episode(s) carry a severity; the other 2 are COVERAGE-OPEN 1 · SELF 1`, **0 OTHER**; this block rendered in the store's shape parses `severity` -> `STILL OPEN`, `_parse_slot_count` -> `'4'`, `extract_still_open` over the real blocks plus it -> `'4'`. THREE NEGATIVE CONTROLS built by mutating this same block all returned None, as they should: `(4 under the new convention, 3 old)`, `(4 TOTAL)`, and `STILL OPEN: FOUR,`. ⚠ My first pre-flight was itself wrong and I caught it before it mattered — I built the synthetic block from `splitlines()[:4]`, which carries the ORIGINAL `Content:` line, so the parser classified the block I was trying to test as `COVERAGE-OPEN`. `the_measurement_is_of_the_wrong_object`, inside the instrument built against it, again.
  Tags: code_review, levain, semantic
```
