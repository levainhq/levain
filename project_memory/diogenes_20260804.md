# Diogenes — levain — 2026-08-04 overnight

**6 finding(s) (1 HIGH, 4 MEDIUM, 1 LOW). 5 commits reviewed (`5e58602..3a52525`). **FOUR of the six are REPRODUCED in a scratch dir against real production code, not reasoned.**

## ⚠ THE HIGH IS THE FIX ITSELF

The 0.4.1 pack-hook repair reintroduces the SAME false-stale defect one layer down, **for the exact population it was written to protect** — and `levain init --force`, the remedy the check itself prints, cannot clear it. The comment at `doctor.py:1098` asserting hooks are *"copied verbatim (not rendered)"* is FALSE as written, is contradicted by `install.py:1676-1679` in the same repo, and is the premise that produced the bug. **Third consecutive release where the confident sentence is what kept anyone from looking.**

## ⚠ AND THE CEMENT HAS THE SAME HOLE IT WAS BUILT TO CLOSE

`tests/test_doctor_content.py:561` hand-simulates init instead of calling `_copy_activation_tree`, so the substitution never runs and the fixture body carries no placeholder. **The fixture sits on the passing side of the exact line the defect lives on.** Fourth occurrence of *"the branch was never executed by the suite"* — now INSIDE the fix for it. The `KNOWN_ADAPTERS` parametrisation is genuinely strong; the weak part is specifically the hand-built install shape.

## ✅ SUITE STATE, because the cement argument depends on it

RAN it: **8 failed, 2079 passed, 18 skipped.** All 8 are missing OPTIONAL extras, none in the changed modules — **not this window's regressions.** But a permanently-red baseline is where a fourth occurrence hides: `test_app_server.py:21` imports the mcp extra with no guard while 5 siblings use `pytest.importorskip` correctly.

## 📌 NOT FLAGGED, DELIBERATELY

The `cli.py` KNOWN_ADAPTERS duplication is correctly reasoned (the lazy-import cost argument holds and the pin is a real test), and the `MECHANISM_SHARE_WARN` basis correction in `packs.py` is right. **No 0.4.2 is still the correct call, and this HIGH is a reason to keep holding** — the pack path is latent only while no shipped pack overrides a hook, the same latency argument `3620ce4` already recorded.


⚠ **THIS IS THE NIGHT'S DOMINANT CROSS-REPO CLASS, NOT A LOCAL QUIRK.**
`guard_scoped_by_symptom_misses_the_class` fired in **five repos** on 2026-08-04, and in **four of
them the miss is INSIDE the commit written to close it**: wisp (a ceiling guard written for
contacts, with the thin spot added hours later inheriting none of it, while the check that would
have caught the family had stopped executing), levain (the pack-hook fix reintroducing the same
false-stale defect one layer down), claphamdigital (the `nostop` fix leaving the `kind` bullet one
line above), flow (a credential hard stop missing root-level `deploy/`, `.github/`, and the
pre-commit hook that *invokes* the gates it protects), and vagus (an O(n²) regression under a
comment in the same hunk asserting it did not happen).

The cross-repo read stays in the morning ritual; it is recorded here so this repo's instance is not
read as local.


⚠ **TRIAGE AGAINST DISK BEFORE ACTING.** Reviewers hallucinate and diff-scoping produces false
positives. Nothing here has been verified by a human. *(Worked example from the same night: two
MEDIUMs filed against `flow/scripts/action_boundary_recall_hook.py:117` were both FALSE POSITIVES —
they quoted the docstring's post-mortem of a retired implementation as if it were live code.
Verified against live behaviour, 6/6 cases correct, and dismissed.)*

---

## 1. `diogenes-20260804-043204-871e227a7061`

HIGH: levain/doctor.py:1134 — the 0.4.1 pack-hook fix reintroduces the SAME false-stale defect one layer down, for the population it was written to protect. The pack branch compares raw `_sha256_file(installed)` against the manifest's SOURCE hash, but installed hooks are NOT verbatim: `_copy_activation_tree` calls `_substitute_hook_placeholders(new_tree / "hooks", {'{{ANNEAL_MEMORY}}': anneal_path})` (install.py:1790) RECURSIVELY over the COMPOSED tree — its own docstring says 'so a pack's nested hook is reached'. So a pack hook using the documented placeholder is installed CORRECTLY and reported stale forever. The base branch is immune only because it normalises BOTH sides through `_hook_body`, which exists solely to undo this substitution — the pack branch bypasses it. REPRODUCED in a scratch dir against real `_copy_activation_tree`: pack source '_INSTALL_ANNEAL_BIN = "{{ANNEAL_MEMORY}}"' -> installed '_INSTALL_ANNEAL_BIN = "/opt/anneal/bin/anneal"', doctor ok=False, 'installed hook script(s) differ from the package: session_start.py'; a second forced render does NOT clear it, so `init --force` — the remedy this check itself prints — cannot fix it. The comment at doctor.py:1098 asserting 'Hooks are copied verbatim (not rendered), so the source hash and the installed bytes are directly comparable' is FALSE as written and is the premise that produced the bug; install.py:1676-1679 contradicts it in the same repo. — FIX (NOT executed, reasoning only): compare pack hooks through `_hook_body` on both sides like the base branch, or record a rendered-hash for hooks in the manifest the way `PackProvenance.rendered` already does for render seeds; `rendered` exists for exactly this 'install copy never equals its template' problem. [quoted: `if _sha256_file(installed) != recorded:`] [prescription: run]

---

## 2. `diogenes-20260804-043212-09c70bb70b1c`

MEDIUM: levain/doctor.py:1112 — pack hook paths are flattened to a BASENAME key, so a pack's nested hook silently becomes the authority for an unrelated base-owned hook. `pack_hooks[rel.rsplit('/', 1)[-1]] = digest` turns 'activation/hooks/sub/session_start.py' into key 'session_start.py'; the loop below then uses that digest to judge the TOP-LEVEL 'hooks/session_start.py', which came from base. Nested pack hooks are not hypothetical — `_substitute_hook_placeholders` (install.py:1826-1828) documents them explicitly: 'RECURSIVELY (so a pack's nested hook, e.g. hooks/sub/x.py, is reached — the composition supports nested subtrees)'. REPRODUCED in a scratch dir: a pack shipping ONLY activation/hooks/sub/session_start.py yields an install whose top-level session_start.py is byte-identical to the base template (verified via `_hook_body` equality == True), and doctor still reports 'installed hook script(s) differ from the package: session_start.py'. Same false-failure class as the HIGH, different trigger. — FIX (NOT executed, reasoning only): key `pack_hooks` on the path relative to activation/hooks/ rather than the basename, and match it against the installed file at that same relative path. [quoted: `pack_hooks[rel.rsplit("/", 1)[-1]] = digest`] [prescription: run]

---

## 3. `diogenes-20260804-043223-a2c5d9c449e8`

MEDIUM: levain/doctor.py:1127 — a pack-ONLY hook is recorded in the manifest and then never checked by anything, so tampering with executable code that fires every session passes doctor green. The loop iterates the BASE tree (`for shipped in sorted(shipped_root.glob('*.py'))`) and looks up `pack_hooks` only to override the authority for names base already ships; a hook whose name base does NOT ship is never reached. REPRODUCED in a scratch dir: a pack shipping activation/hooks/mypack_hook.py is recorded in .levain/manifest.json (confirmed: manifest files map contains 'activation/hooks/mypack_hook.py'), the installed copy was then overwritten with "print('TAMPERED')", and `_check_hook_freshness` returned ok=True. Nothing else covers it: `_check_hook_freshness` is doctor's ONLY consumer of `read_pack_locks`, and reconcile compares the pack SOURCE now against the pack SOURCE at record time (reconcile.py:271-272, `current = drift.current_files` vs `recorded = drift.recorded.files`), never the installed bytes. The new comment at doctor.py:1096-1097 over-claims this coverage: 'Pack-owned hooks are now resolved against .levain/manifest.json' is true only for pack hooks that shadow a base hook of the same basename. — FIX (NOT executed, reasoning only): iterate the UNION of base hook paths and manifest-recorded activation/hooks/ paths rather than base alone. [quoted: `for shipped in sorted(shipped_root.glob("*.py")):`] [prescription: run]

---

## 4. `diogenes-20260804-043235-6acbeff8fcb8`

MEDIUM: tests/test_doctor_content.py:561-562 — the fixture added AS the durable half of the fix hand-simulates init instead of running it, which is exactly why the suite cannot see the HIGH above. `_pack_layered_install` builds the install by writing the pack body straight to disk under the comment 'init copies the WINNING layer into the install, then records provenance' — it never calls `_copy_activation_tree`, so `_substitute_hook_placeholders` never runs and the install shape it produces is one real `init --pack` cannot produce. Its default body `pack_body = '# pack-owned hook\nprint(\'from the pack\')\n'` also carries no {{ANNEAL_MEMORY}} placeholder, so even the substitution's trigger is absent. VERIFIED by control: the same scratch-dir harness run with that placeholder-free body returns doctor ok=True, while the placeholder body returns ok=False — the fixture sits on the passing side of the exact line the defect lives on. This is the fourth occurrence of the pattern the fixture's own docstring says it ends ('the branch was never executed by the suite'), now inside the cement: the shape was hand-written, so it can still only cover the shape someone thought of. Note the sibling test `test_hook_freshness_is_correct_for_EVERY_KNOWN_ADAPTER` derives its parameters from KNOWN_ADAPTERS and is genuinely stronger — the weakness is specifically the hand-built install, not the parametrisation. — FIX (NOT executed, reasoning only): build the fixture through `_copy_activation_tree(order_activation_roots(...), ..., anneal_path=...)` so the tested install is produced by the same code path init uses. [quoted: `# init copies the WINNING layer into the install, then records provenance.`] [prescription: run]

---

## 5. `diogenes-20260804-043247-93cc630b1d8e`

MEDIUM: tests/test_app_server.py:21 — the module imports an OPTIONAL extra at top level with no skip guard, so 7 tests hard-FAIL instead of skipping wherever levain[app] is absent, and the whole suite reads red. `from levain.app_server import VIEW_MIME, VIEW_URI, build_app, run_app_server` pulls the mcp SDK; levain/app_server.py:101 itself documents it as optional ('The Levain MCP-App server needs the MCP SDK, which is an optional extra. Install it with: pip install levain[app]'). RAN the suite on this machine: 8 failed, 2079 passed, 18 skipped — 7 of the 8 are TestBuildApp with 'ModuleNotFoundError: No module named mcp'. The repo already has the convention and applies it consistently elsewhere: `pytest.importorskip('openhands.sdk', reason='openhands extra not installed')` at module top in test_firing_capture.py:14, test_firing_condenser.py:17, test_firing_entity.py:17, test_firing_gate_openhands.py:14, test_firing_tools.py:21. This matters beyond tidiness for THIS window: the window's headline artifact is a 'cement' test asserted to stop a fourth occurrence, and it lands in a suite that already reports 8 failures — a red baseline is where a real regression hides. — FIX (NOT executed, reasoning only): add `pytest.importorskip('mcp', reason='app extra not installed')` above the import, matching the sibling modules. [quoted: `from levain.app_server import VIEW_MIME, VIEW_URI, build_app, run_app_server`] [prescription: run]

---

## 6. `diogenes-20260804-043257-99b39c70e183`

LOW: tests/test_firing_confinement.py:1113 — an inline comment names a guard that does not guard what it claims, and the test hard-fails as a direct result. The line reads `from levain.firing.openhands.tools import SandboxedBashExecutor  # openhands leaf, gated by @live`, but `@live` has nothing to do with the openhands extra: line 45-46 define `_LIVE = platform.system() == 'Darwin' and sandbox_exec_available()` and `live = pytest.mark.skipif(not _LIVE, reason='needs macOS sandbox-exec (the SeatbeltProvider backend)')`. On a Darwin box WITH sandbox-exec but WITHOUT the openhands extra — this machine — the skip does not fire and the import raises. RAN the suite: `test_live_plain_config_cannot_be_unlinked_or_relocated` fails with 'ModuleNotFoundError: No module named openhands' (levain/firing/openhands/agent.py:37), the 8th of the 8 failures. Same class the reviewed window shipped to fix and wrote two correction paragraphs about: a comment describing a world the code is not in, sending the next reader to the wrong authority. — FIX (NOT executed, reasoning only): double-gate the test with an openhands importorskip as the sibling modules do, and correct the comment to name the real guard. [quoted: `# openhands leaf, gated by @live`] [prescription: run]

