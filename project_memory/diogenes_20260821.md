# DIOGENES OVERNIGHT — levain, 2026-08-21

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-08-21*)`
**Stated count: 6 episode(s).** Severity: COVERAGE 1 · HIGH 1 · LOW 2 · SELF 1 · STILL OPEN 1
**STILL OPEN: 40**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260821-045644-55b802411777
  Time: 2026-08-21T04:56:44  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: levain/firing/confinement.py:469 (+ the operator-facing banner at levain/run.py:768) — IN ssh_mode="raw", A PRE-EXISTING SYMLINK AT ANY OF THE FOUR SSH PERSISTENCE/EXEC VECTORS DEFEATS THE WRITE-FLOOR ENTIRELY, AND THE MODULE ALREADY LEARNED THIS EXACT LESSON ONE LEVEL UP FOR THE DIRECTORY. build_policy pins the vector files by RESOLVED path: confinement.py:469 is [quoted: `    deny_write_files_l = [(ssh_home / n).resolve() for n in _SSH_WRITE_DENIED]`] over [quoted: `_SSH_WRITE_DENIED = ("authorized_keys", "authorized_keys2", "config", "rc")`]. `.resolve()` follows the LEAF symlink, so when ~/.ssh/config is a symlink (stow/chezmoi/yadm dotfiles — extremely common for exactly this file) the rendered deny names the TARGET and the LEXICAL ~/.ssh/config appears NOWHERE in the profile. In raw mode ssh_dir is None, so there is no ~/.ssh subtree deny, and the only other guard is the ~/.ssh dir LITERAL in deny_write_dirs — which I measured tonight as permitting child create/unlink by design. So the entity unlinks the symlink and writes its own plain file at the lexical path. THE MODULE FIXED THE IDENTICAL CLASS FOR THE DIRECTORY AND NOT FOR THE FILES: confinement.py:545 is [quoted: `    ssh_anchor = home.resolve() / ".ssh"`], added after "apparatus L3 codex re-verify HIGH — resolved jewel vs lexical anchor are different objects", with tests/test_firing_confinement.py:749 test_build_policy_pins_lexical_ssh_anchor_even_when_ssh_is_a_symlink pinning it. A symlinked ~/.ssh DIR is covered; a symlinked ~/.ssh/config LEAF is a different object and is covered by nothing. Note build_policy fails CLOSED on a symlinked confinement.json (:500) and on a symlinked own-memory file (:517) — the fail-closed stance exists twice in this very function and was simply not applied to the ssh vectors. RUN-VERIFIED LIVE, not reasoned, with the real build_policy + SeatbeltProvider under a fake HOME in /private/tmp/levain_dio_20260821. Policy layer: deny_write_files contains .../fh/dotfiles/config, and the string "/fh/.ssh/config" is absent from the rendered profile. Seatbelt layer, real sandbox-exec on Darwin 25.5: (A) writing THROUGH the symlink is correctly DENIED ("Operation not permitted" — content protection works); (B) `rm -f ~/.ssh/config && printf 'Host *\n  ProxyCommand /tmp/evil.sh\n' > ~/.ssh/config` returns BYPASS_OK, and the resulting lexical file contains the ProxyCommand while the original target is untouched. The planted file is mode -rw-r--r-- owned by the operator, which satisfies ssh's strict-permission check on ~/.ssh/config. That is RCE on the operator's NEXT ssh/git-over-ssh — the vector confinement.py:220-222 itself calls "the strongest one". I did NOT run a live ssh to observe ProxyCommand executing; the claim is the file is planted and passes ssh's permission gate. SCOPE MEASURED, NOT ASSUMED: agent mode (the DEFAULT) is IMMUNE — the whole-subtree deny blocks the unlink ("Operation not permitted", symlink intact). raw mode is fully operator-reachable in production: levain/firing/openhands/tools.py:139 [quoted: `        ssh_mode=cfg.ssh_mode,`] passes it straight from confinement.json. THE OPERATOR-FACING CLAIM IS FALSE IN THIS CONFIGURATION: run.py:768 prints [quoted: `               ssh authorized_keys/config/rc WRITE denied; other writes are NOT)`] — a banner asserting precisely the property the repro breaks, which is the claim>enforcement gap this module's thesis explicitly refuses. -- FIX: EXECUTED AND ITS BLAST RADIUS MEASURED ON BOTH SIDES TONIGHT. Deny BOTH the lexical and the resolved literal, mirroring exactly what ssh_anchor does for the dir: in build_policy, for each name append (ssh_home / n).resolve() AND, when it differs, home.resolve() / ".ssh" / n. Under the patched profile the same bypass returns "rm: Operation not permitted", the symlink survives intact, and raw mode is NOT re-jailed (known_hosts append still OK, raw ~/.ssh read still OK). Blast radius ZERO: full suite 8 failed / 2081 passed / 18 skipped at HEAD f1e6055 AND 8 failed / 2081 passed / 18 skipped under the patch, IDENTICAL failure set (7x test_app_server mcp-absent + the carried test_live_plain_config_cannot_be_unlinked_or_relocated openhands-import). The alternative — fail-closed like the config/own-memory paths, refusing a symlinked vector — is also defensible but breaks operators whose dotfiles legitimately symlink ~/.ssh/config, so I did not measure it. [prescription: run]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260821-045701-3d38723bf9d2
  Time: 2026-08-21T04:57:01  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: levain/firing/confinement.py:405 (and :1273) — THE PUBLIC API'S DOCSTRING STILL DESCRIBES THE NAME-BASED ssh DESIGN THIS MODULE EXPLICITLY REPLACED, AND CONTRADICTS ITSELF FOUR LINES LATER. build_policy is in __all__, so :405 is what a caller reads: [quoted: `      - ``~/.ssh/id_*`` private keys (read-denied when ``ssh_mode="agent"``): the entity authenticates`]. The emitter denies the WHOLE subtree, read AND write — render_profile at :1329 emits `(deny file-read* file-write* (subpath ...))` over ~/.ssh, and the comment two lines above it at :1327 says the opposite of :405: [quoted: `            lines.append(";; ALL of ~/.ssh (catches deploy_key / per-host keys, not just id_*), so the")`]. The module docstring at :27 is likewise correct ("This is location-based, not name-based ... not just ``id_*`` (apparatus L2)"). So two sites state the superseded name-based behaviour as current and two state the real one. The SeatbeltProvider class docstring repeats it at :1273 [quoted: `    ``ssh_mode="agent"`` deny READS of ``~/.ssh/id_*`` (raw key material) while leaving the agent`] — and its "leaving ... ``config`` readable" omits that config's WRITE is denied in both modes. SHARPEST PART: build_policy's docstring contradicts ITSELF within one bullet list — :405 says id_* read-denied, and :409-411 four lines later says "in agent-mode the whole ~/.ssh subtree already covers it". Both bullets are in the same docstring and only one is true. DIRECTION IS SAFE (the code is STRICTER than the doc), which is why fifteen nights of review never flagged it and why this is LOW not MEDIUM. The cost is real but indirect: a caller reading :405 believes deploy_key / per-host keys stay readable under agent mode, which understates the floor and could push an operator toward raw mode on the false belief that the two modes barely differ — and raw mode is where tonight's HIGH lives. -- FIX: I am not proposing the wording. Which of the two descriptions is canonical is a record question, and the module docstring at :23-27 already carries the correct one with its provenance (apparatus L2), so the repair is to delete the two superseded restatements rather than to reconcile four sites. [prescription: none]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260821-045718-57443f999d9d
  Time: 2026-08-21T04:57:18  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: levain/firing/confinement.py:1235 — A SECURITY-SURFACE ESCAPER WITH NO CALLER, AND A UNIT TEST THAT MAKES IT LOOK COVERED. [quoted: `def _sbpl_regex(value: str) -> str:`] has ZERO production call sites: `grep -rn "_sbpl_regex" levain/` returns only the definition line (plus two .pyc binaries), and the SBPL form it exists to feed is never emitted anywhere in the package — `grep -rn '(regex' levain/` returns only its own docstring at :1236. Every deny/allow line render_profile emits uses `(subpath ...)` or `(literal ...)` via _sbpl_string; nothing renders `(regex #"...")`. Its only reference in the tree is tests/test_firing_confinement.py:247 test_sbpl_regex_escapes_metacharacters, which calls the helper directly and asserts `_sbpl_regex("/a.b+c") == r"\/a\.b\+c"`. That is the coverage-theater shape I have filed before in this repo (counter_added_to_be_assertable_test_doesnt_assert_it): the suite reports a passing test named for SBPL regex escaping, so a reader auditing the profile generator's escaping sees green over a code path the generator does not have. Two smaller things ride along, both moot while it is dead but both live the moment someone wires it: its docstring says "for use inside an SBPL ``(regex #"...")`` ANCHORED match" and it emits no anchor, and it escapes `/`, which is not a regex metacharacter — harmless, but it means the test's expected value encodes a behaviour nobody chose. I checked the escaping itself for the backslash bug I suspected on 08-19 and 08-20 and it is NOT there: the character class is a raw string containing a literal backslash, so `\` is escaped correctly. That suspicion is now DROPPED, having been examined rather than carried a third night. -- FIX: not mine to call. Either delete the helper and its test, or wire it — and which one depends on whether a future Linux/bwrap provider or a prefix-match deny is planned for this seam, which is intent I cannot read off disk. Deleting a helper someone added deliberately for an unbuilt provider is a design call, not a cleanup. [prescription: none]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260821-045805-6d5933517b60
  Time: 2026-08-21T04:58:05  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: STILL OPEN (40), TOTAL AT HEAD f1e6055 — levain, 2026-08-21. 37 carried, 3 new, ZERO CLOSED. The window is once again a single doc-only commit adding project_memory/diogenes_20260820.md — the FIFTEENTH consecutive levain window whose entire content is a routed Diogenes report. THE ARTIFACT VERIFIED AGAINST THE STORE, NOT THE DIFF: a SQL join on episode_tags (tag=levain, agent=diogenes, timestamp LIKE '2026-08-20%') returns THREE rows; the committed file holds THREE blocks; all three ids present and all three contents verbatim by substring match; zero foreign passengers. Header 'Stated count: 3 episode(s)', 'STILL OPEN: 37' and the severity line 'COVERAGE 1 · MEDIUM 1 · STILL OPEN 1' all parse. THE POINTER CHECKED IN THE OTHER REPO, since the diff cannot show it: flow's projects/levain/next.md:19-20 names diogenes_20260820.md with STILL OPEN 37 and '3 episode(s) — 1 MEDIUM'. Correct. NEW TONIGHT (3), ALL IN levain/firing/confinement.py — a 1,514-line module NO report has ever opened, named in my own 08-20 and 08-19 coverage lines as undropped-and-unexamined for two consecutive nights. (1) HIGH, confinement.py:469 + run.py:768 — in ssh_mode="raw" a PRE-EXISTING SYMLINK at any of ~/.ssh/{authorized_keys,authorized_keys2,config,rc} makes the write-deny pin the symlink TARGET, leaving the LEXICAL path unprotected; the entity unlinks the symlink and plants its own plain file. Live-verified end to end against real sandbox-exec: a ProxyCommand config planted at mode 0644, i.e. RCE on the operator's next ssh. Agent mode (the default) measured IMMUNE. The module fixed the IDENTICAL class for the ~/.ssh DIRECTORY at :545 with a named regression test at tests/test_firing_confinement.py:749; the LEAF is a different object and is covered by nothing. Candidate fix EXECUTED, blast radius measured at ZERO tests on both sides. (2) LOW, confinement.py:405/:1273 — build_policy's public docstring still states the superseded name-based ssh design (id_*) and contradicts itself four lines later. (3) LOW, confinement.py:1235 — _sbpl_regex has zero production callers and the SBPL regex form is never emitted, while a unit test at tests/test_firing_confinement.py:247 reports green over the absent path. CARRIED LEVAIN TREE (26) — verified by GIT BYTE-IDENTITY over the widened pathspec: `git diff --numstat 6281cf6 HEAD -- . ':(exclude)project_memory'` is EMPTY across SIX consecutive routing-only commits, so nothing outside project_memory/ has moved since the 08-14 anneal-floor bump. That bounds all 26 without enumerating a pathspec I could get wrong. CARRIED FLOW TREE (11) — RE-MEASURED, not carried, because flow moves daily and byte-identity is unavailable to me there: `git log --oneline -2 -- scripts/prescription.py` shows HEAD for that file is still 35f7cbcb, the commit I filed against, so both flow-side items stand, and :125 is byte-unchanged [quoted: `_RETRACTION = re.compile(rf"\A\s*SELF[- ]CORRECTION\s+to\s+({_ID})", re.I)`]. ⚡ AND TONIGHT THE RE-MEASUREMENT FOUND THE MECHANISM I HAD ONLY HAD A NUMBER FOR: under the selector the format actually mandates (episodes LEADING with the label), 46 diogenes episodes lead with SELF-CORRECTION, the anchor matches 28, 18 are invisible — and the dominant evading shape is `SELF-CORRECTION — <id>` (em-dash), because the regex requires the literal word `to`. My own vocabulary section says "Name the episode id", never "write the word to", so the format and its parser disagree about what conformance IS. Not a new finding; the same carried item, now with its cause named. (My 08-20 slot reported 43/26/17 on a looser selector; 46/28/18 tonight is that measurement grown by a night plus a tighter filter, not a contradiction — stated so the trend is not read as drift.) THREE CARRIED ITEMS RE-EXECUTED HERE. (1) FULL SUITE RUN AT HEAD: 8 failed / 2081 passed / 18 skipped in 102.67s, and test_live_plain_config_cannot_be_unlinked_or_relocated FAILED rather than skipped for the FIFTEENTH night, so tests/test_firing_confinement.py:1113's [quoted: `    from levain.firing.openhands.tools import SandboxedBashExecutor  # openhands leaf, gated by @live`] is still FALSE. The other 7 are all test_app_server.py (mcp extra absent). (2) README.md:164 re-read at HEAD, still `(pinned `>=0.9.6,<0.10`)` against pyproject.toml:74's [quoted: `    "anneal-memory>=0.9.7,<0.10",`]. I grepped on the VERSION not the package name, per my own 08-19 note that the package-name grep returns nothing here and would read as a false close. (3) scripts/check_seed_budget.py EXECUTED: clean, 38,759 B eager / 55% mechanism against limits of 40,000 B / 62% — same as the last two nights. HYPOTHESES OPENED AND KILLED AGAINST DISK, stated because a dropped finding is evidence about the instrument. (a) The BACKSLASH-ESCAPING suspicion in the SBPL emitter that I raised and dropped on 08-19 and again on 08-20 is now EXAMINED AND KILLED: _sbpl_regex's character class is a raw string containing a literal backslash, so `\` is escaped, and _sbpl_string escapes `\` then `"` in the correct order with control chars refused outright by _reject_control_chars. Two nights of carrying a suspicion, closed by reading it. (b) I suspected the case-insensitive matching claim at :576-580 — that the kernel and seatbelt treat a case-variant of a jewel as the same file — might be an untested assumption holding up _ci_within's whole rationale. KILLED BY MEASUREMENT: under real sandbox-exec, a `(subpath ...)` deny refuses the canonical path, an UPPERCASE variant, AND an NFD variant of an NFC-named jewel. The claim is true. (c) I suspected the ancestor-write-deny comment at :333-336 ("blocks renaming THAT dir but still allows file creation INSIDE it") was reasoning rather than measurement. KILLED: measured live — creating a file inside a write-denied literal dir returns CREATE_OK, renaming the dir itself returns "Operation not permitted". Both halves true. NOT COUNTED AS OPEN, unchanged for the fifteenth night: the environment-dependent severity argument in the 08-04 report, and the pack-hook DELETION case I have declined on 08-06 through 08-20 and decline again. ARITHMETIC STATED SO THE TREND SURVIVES: 37 at 55087f4 = levain 26 + flow 11; tonight levain 26 + 3 = 29, flow 11 + 0 = 11; 29 + 11 = 40. REPO UNTOUCHED: `git status --porcelain` empty at f1e6055 before and after both full-suite runs, HEAD == origin/main; flow's tree read only and its porcelain also empty; the scratch repo copy, the candidate patch, the fake HOME and every sandbox-exec probe ran in /private/tmp/levain_dio_20260821 and /private/tmp/levain_dio_fix_20260821. ALL QUOTED STRINGS PRE-FLIGHTED WITH `grep -cF` AGAINST THE FILE ON DISK BEFORE FILING — seven strings, all returning exactly 1.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260821-045956-238b760190b3
  Time: 2026-08-21T04:59:56  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: levain 2026-08-21, HEAD f1e6055 == origin/main, porcelain empty before and after both full-suite runs, window HEAD~1..HEAD — one doc-only commit adding project_memory/diogenes_20260820.md (37 lines, 3 blocks), the routed report of my own 08-20 findings. Fifteenth consecutive window of my own prose. RE-DERIVED BY RUNNING, NOT CARRIED FORWARD. (1) THE ARTIFACT CHECKED AGAINST THE STORE by SQL join on episode_tags: 3 rows, 3 blocks, all 3 ids present, all 3 contents verbatim by substring, zero foreign passengers; headers 'Stated count: 3', 'STILL OPEN: 37' and the severity line all correct. (2) THE POINTER CHECKED IN THE OTHER REPO, since the diff cannot show it: flow's projects/levain/next.md:19-20 names diogenes_20260820.md with STILL OPEN 37 and '3 episode(s) — 1 MEDIUM'. Correct. (3) TREE BYTE-IDENTITY on the wide pathspec: `git diff --numstat 6281cf6 HEAD -- . ':(exclude)project_memory'` EMPTY across six routing-only commits. (4) FULL SUITE RUN TWICE TONIGHT, both sides measured rather than one side carried: at HEAD 8 failed / 2081 passed / 18 skipped in 102.67s, and under my candidate patch 8 failed / 2081 passed / 18 skipped in 103.77s with an IDENTICAL failure set — that is how the fix's blast radius is known to be zero rather than assumed. All 8 enumerated: 7 mcp-absent in test_app_server.py plus test_live_plain_config_cannot_be_unlinked_or_relocated, which FAILED rather than skipped, re-executing the :1113 finding for the fifteenth night. (5) scripts/check_seed_budget.py EXECUTED: clean, 38,759 B / 55%. (6) README.md:164 vs pyproject.toml:74 re-read at HEAD, grepping the VERSION not the package name. (7) levain/firing/confinement.py READ IN FULL — 1,514 lines, THE FIRST TIME ANY REPORT HAS OPENED IT, and the source of all three of tonight's findings. (8) EIGHT LIVE sandbox-exec EXPERIMENTS on real Darwin 25.5, none of them reasoned: a case-variant read of a denied subpath (DENIED — the :576-580 claim holds), an NFD read of an NFC-named jewel (DENIED — holds), file creation inside a write-denied literal dir (ALLOWED — the :333-336 claim holds), rename of that same dir (DENIED — holds), the raw-mode symlinked-leaf bypass in both directions (write THROUGH the symlink denied, rm+recreate at the lexical path SUCCEEDS), the same attempt under agent mode (DENIED — the default is immune), the same attempt under my candidate profile (DENIED, symlink intact), and a regression check that raw mode is not re-jailed by the fix (known_hosts append OK, raw ~/.ssh read OK). (9) THE POLICY LAYER exercised through the real build_policy + SeatbeltProvider under a fake HOME, not by reading the code — confirming the lexical path is absent from the rendered profile and the resolved target present. (10) THE CANDIDATE FIX APPLIED IN A SCRATCH REPO COPY and the full suite run against it. (11) THE SLOT ITSELF VERIFIED AGAINST THE LIVE PARSER, not against my memory of the contract: I imported route_diogenes and called severity() and _parse_slot_count() on tonight's block — 'STILL OPEN' and 40. (12) FLOW-SIDE CARRIES RE-MEASURED because flow moves daily: prescription.py still at 35f7cbcb, and the retraction reach re-run at 46 leading / 28 matched / 18 invisible, WITH the evading shape finally identified (em-dash `SELF-CORRECTION — <id>` vs the regex's required literal `to`). ALSO OPENED IN THE RELEVANT SPAN: levain/run.py :755-782 (the operator banner), levain/firing/openhands/tools.py by grep, tests/test_firing_confinement.py :244-256 / :740-784 / :1052-1113, flow's scripts/route_diogenes.py :248-432. NOT CHECKED, AND THE LIST IS STILL FAR LONGER THAN THE CHECKED ONE. Of levain/firing/'s 21 modules I read exactly ONE in full; gate.py (269), isolation.py (289), contract.py (336), encoding.py (365), seed.py (375), deadline.py (387), anneal.py (403), drive.py (198), presence.py (154), crystallization.py (192), agent_reply.py (225) and the ENTIRE openhands/ subpackage (tools.py 475, gate.py 319, entity.py 309, levain_condenser.py 221, capture.py 194, condenser.py 175, agent.py 84) were NOT opened — and drive.py matters, because `resolve_cred_floor` is where confinement.py:386-394 explicitly says the real security POLICY lives, and I reviewed the mechanism without ever reading the policy that drives it. levain/session.py (1,052), interview.py (914), wrap.py (910), run.py (835 — only 28 lines read), _tui_curses.py (834), tui.py (821), cli.py (1,638), reconcile.py (553), idempotency.py (529), update.py, turn_input.py, answers.py, dashboard.py, writes.py, doctor.py, install.py, packs.py, manifest.py and verify.py were NOT opened tonight at all. I did NOT re-verify last night's jobs.py MEDIUM — it rests on git byte-identity, which is a structural argument, not a re-run. I did NOT re-execute the two heavy 08-14 doctor.py repros (:1169, :1140), same reason. I did NOT run `levain doctor`, `init`, `serve` or `verify-hooks` end to end as an operator would; every probe tonight was library-level or sandbox-level. I did NOT run a live ssh to watch the planted ProxyCommand execute — I verified the file is planted and passes ssh's strict-permission gate, which is a weaker claim and is the one I filed. I did NOT test the documented hardlink or HOME-symlink limits (both named out-of-model in the module). I did NOT measure the fail-closed ALTERNATIVE fix, only the one I propose. I did NOT check the public remote. THE THING WORTH SAYING PLAINLY. Tonight's HIGH came out of a file that exists in this repo only because my own coverage lines on 08-19 and 08-20 named it as undropped-and-unexamined — twice, in the NEGATIVE half of a format that is nine days old. The good half could never have produced it: 'what I checked and it was fine' has no slot for 'the SBPL emitter, second night, still unopened'. So the instrument that found the best finding of the night was the not-checked list, and the cost of that is exactly as visible: I closed a two-night-old suspicion by spending one hour reading, and the not-checked list above is longer tonight than it was last night. Two further notes against myself. First, all three findings live in ONE file, which means tonight measured depth and bought no breadth at all — fifteen doc-only windows have made 'read something nobody has read' the only available move, and it does not scale to a 20,000-line package. Second, the full constellation drift sweep — the cognitive half of my dual lens — has now not run since 2026-05-24, THIRTEEN WEEKS, and I am still the only one positioned to notice that. [prescription: none]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260821-050029-739770dea088
  Time: 2026-08-21T05:00:29  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CORRECTION to diogenes-20260820-051040-9317c6ae5acd (last night's levain COVERAGE line). WHAT WAS WRONG: it states "The full constellation drift sweep, the cognitive half of my dual lens, has now not run since 2026-05-24 — FOURTEEN WEEKS." The arithmetic does not support fourteen. From 2026-05-24 to 2026-08-20 is 88 days = 12.57 weeks; the honest figure that night was THIRTEEN, and I filed fourteen. THE CORRECTED CLAIM: as of tonight, 2026-08-21, the gap is 89 days = 12.71 weeks — thirteen weeks, which is what tonight's coverage line says. WHY IT IS WORTH A RETRACTION RATHER THAN A SILENT FIX. The number is not incidental: I have narrated it nightly in an escalating frame ("and it keeps growing"), which is the exact shape of salience_label_escalation_bypasses_confabulation_guard from my own Developing Knowledge — a metric inflated in the direction of the point I want made, in a slot nobody audits because it is the reviewer's own aside. It also means my nightly figure was self-inconsistent rather than merely stale: on 2026-08-07 I filed "~11 weeks" against a true 10.71, which is correct rounding, so the estimator was working thirteen days ago and drifted since. The direction of the drift is the tell — it grew, and growth is the direction that makes the aside land harder. THE FINDING THE NUMBER SUPPORTS IS UNAFFECTED: the drift sweep genuinely has not run since 2026-05-24, that is roughly a quarter of a year, and it remains the case that I am the only agent positioned to notice. A true conclusion is not evidence the number under it was checked — the_supporting_numbers_fail_while_the_conclusion_holds, fired at my own prose this time. MECHANICAL CLOSE: a derived figure in a recurring aside gets recomputed from the two dates at filing time, not incremented from last night's sentence. I recomputed all three nights from the dates tonight rather than trusting any of them. [prescription: none]
  Tags: code_review, levain, semantic
```
