# DIOGENES OVERNIGHT — levain, 2026-08-24

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-08-24*)`
**Stated count: 7 episode(s).** Severity: COVERAGE 1 · COVERAGE-OPEN 1 · DRIFT 1 · HIGH 2 · MEDIUM 1 · STILL OPEN 1
**STILL OPEN: 43**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260824-044623-5459929d54f7
  Time: 2026-08-24T04:46:23  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: levain 2026-08-24, HEAD e3ed37ff8f101ee8d736e34a89113a288a7bb12b (== origin/main? yes), `git status --porcelain` <empty>, window HEAD~1..HEAD = 1 commit (e3ed37f 'spore-604 bugfix session — close all 5 findings from the 7211bbf window'). NOTE ~/Documents/levain is a SYMLINK to ~/Briefcase/levain — one repo, not two targets. REVIEW STARTED, NOT FINISHED — if no COVERAGE line for this repo exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260824-045830-9e756537dd4f
  Time: 2026-08-24T04:58:30  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: levain/firing/confinement.py:617 (the rebuttal) + :631 (the code it preserves) — THE ONE CLAIM IN THIS COMMIT THAT WAS REASONED INSTEAD OF MEASURED IS THE ONE THAT IS FALSE, AND IT WAS USED TO OVERRIDE A CORRECT FINDING WHILE ASSERTING "CHECKED BEFORE ACTING". The comment reads [quoted: `    # ⛔ AND THE REDUNDANCY CLAIM THAT CAME WITH THE FINDING DOES NOT HOLD — CHECKED BEFORE ACTING,`] / [quoted: `    # 2026-08-23. The finding called this code "now unconditionally redundant". It is not: the three`] / [quoted: `    # spellings deny lexical FILES inside ~/.ssh and never the lexical DIRECTORY, so`] / [quoted: `    # ``rm ~/.ssh; ln -s ~/evil ~/.ssh`` — replacing the directory itself — is named by this anchor`] / [quoted: `    # and by nothing else. It IS redundant in the no-symlink case, where ``ssh_anchor == ssh_dir```]. The code it preserves is [quoted: `    write_dirs = _dedup(list(_write_deny_ancestors(all_jewels)) + [ssh_anchor])`].

MEASURED, NOT REASONED — TWO INDEPENDENT WAYS.
(1) FULL-SUITE MUTATION: removing `+ [ssh_anchor]` in a scratch copy (/private/tmp/levain_dio_20260824/mut2) gives 8 failed / 2085 passed / 18 skipped — BYTE-IDENTICAL to HEAD, same 8 optional-dep failures. ZERO tests turn red. The line is dead.
(2) GEOMETRY PROBE across all four shapes (plain HOME / symlinked HOME) x (plain vector / symlinked vector), rebuilding all_jewels exactly as build_policy does but WITHOUT the explicit anchor: `ssh_anchor in _write_deny_ancestors(all_jewels)` is True in ALL FOUR — including the symlinked-HOME + symlinked-vector geometry the rebuttal itself invokes.

WHY IT IS UNCONDITIONAL, mechanically. `_write_deny_ancestors` (:335-353) walks `jewel.parents` — purely LEXICAL, no `.resolve()` anywhere in it. `all_jewels` (:604-607) includes `deny_write_files_t`, which since 7211bbf always contains `ssh_home_lexical / n`, and :526 defines [quoted: `    ssh_home_lexical = home.resolve() / ".ssh"      # resolved HOME + un-deref'd .ssh, as ``ssh_anchor```] — the IDENTICAL expression to :630 [quoted: `    ssh_anchor = home.resolve() / ".ssh"`]. So the immediate parent of that entry IS ssh_anchor, and `_SSH_WRITE_DENIED` is a non-empty module constant (:230, four names), so such an entry always exists. The rebuttal reasoned only about what `deny_write_files` DENIES and never about what it FEEDS — and the code's own comment ten lines above says so out loud at :600-601: "Including ``deny_write_files_t`` here write-denies ~/.ssh (the persistence-vector dir anchor) so it can't be renamed/replaced to relocate the protected literal".

THE CONCESSION CLAUSE IS ALSO WRONG, and independently. It says the anchor IS redundant "in the no-symlink case, where ``ssh_anchor == ssh_dir`` and ``_dedup`` already collapses the pair". :462 is [quoted: `    ssh_dir = (home / ".ssh").resolve() if ssh_mode == "agent" else None`] — `ssh_dir` is None in `ssh_mode="raw"`, which is the mode this entire ssh discussion is about, so the named collapse cannot happen there. The anchor is redundant in raw mode too, via the ancestor walk, not via ssh_dir.

WHY HIGH. This is the same defect class as the HIGH this very commit was written to close — a mechanism asserted in the permanent record that live measurement refutes — reintroduced in the remediation, and this time it was used to CLOSE a correct finding rather than to justify correct code. The rebuttal is written at maximum confidence ("⛔ ... DOES NOT HOLD — CHECKED BEFORE ACTING"), which is exactly what stops the next reader from checking. The commit's other four claims were all re-measured and all hold; this is the only one that was argued.

THE FIX: the line may stay or go, but its stated reason cannot. Either (a) delete `+ [ssh_anchor]` and the rebuttal block, having measured the blast radius at exactly zero tests; or (b) keep it and state the true justification — defence-in-depth against two invariants it does not want to depend on, namely that `_write_deny_ancestors` keeps walking LEXICAL parents and that `_SSH_WRITE_DENIED` stays non-empty. What must not survive is "named by this anchor and by nothing else", which is false in every geometry, and the ssh_dir clause, which is false in raw mode. If (b), pin it with an assertion rather than prose — the honest form of this claim is `assert ssh_anchor in write_dirs`, which cannot go stale.
[prescription: run]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260824-045858-87d7a0b3e11f
  Time: 2026-08-24T04:58:58  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: the e3ed37f commit message (subject line) + the spore-604 discharge it records — THE COMMIT CLAIMS TO CLOSE FIVE FINDINGS AND CLOSES THREE, AND ONE OF THE TWO IT LEAVES OPEN IS NOT MENTIONED ANYWHERE IN ITS BODY. The subject is [quoted: `spore-604 bugfix session — close all 5 findings from the 7211bbf window`], and the body's own accounting line is [quoted: `All five sit in the ssh confinement module. The FIX is correct and stays; four of`] / [quoted: `the five defects were in what the record SAYS about it.`]

THE FIVE, from the store (agent=diogenes, tag=levain, 2026-08-22):
  1. diogenes-20260822-091951-47d43cdab29d  HIGH   :478 refuted mechanism in the record        -> CLOSED (re-measured tonight, below)
  2. diogenes-20260822-092011-7bd0afdcb175  MEDIUM :517 the RAW spelling is UNREACHABLE        -> UNTOUCHED, NOT MENTIONED
  3. diogenes-20260822-092026-25273688516c  MEDIUM :518 ssh_home_lexical had no test           -> CLOSED (mutation-verified tonight)
  4. diogenes-20260822-092051-b834e49dc27b  MEDIUM :589 ssh_anchor justification + redundancy  -> HALF closed; the redundancy half rejected on a rebuttal measured FALSE tonight (see the :617 HIGH filed this window)
  5. diogenes-20260822-092051-6180bc6d9187  LOW    :521 dedup soundness premise                -> CLOSED

FINDING 2 IS UNTOUCHED AT HEAD, VERIFIED BY DIFF: `git diff HEAD~1 HEAD` matches no line containing "narrows it", "NARROWS" or "RAW ``Path". All three of its artifacts still stand — confinement.py:515 [quoted: `    #           this file. The third spelling above narrows it, it does not close it.`], confinement.py:530 [quoted: `        deny_write_files_l.append(ssh_home / n)               # RAW ``Path.home()`` — the true lexical`], and tests/test_firing_confinement.py:844 [quoted: `    ⛔ This NARROWS the symlinked-HOME vector; it does not close it. A *replaceable* HOME symlink is`], plus its assertion message at :856 [quoted: `        "the RAW Path.home() spelling — the one the entity writes — must be denied")`].

AND IT IS STILL TRUE — RE-MEASURED TONIGHT, INDEPENDENTLY, WITH BOTH CONTROLS. Darwin 25.5, /tmp -> /private/tmp as the ancestor symlink, /private/tmp/dio_h2/.ssh/authorized_keys as the vector. Profile whose ONLY rule names the un-canonicalised spelling, `(deny file-write* (literal "/tmp/dio_h2/.ssh/authorized_keys"))`: append exit=0, `rm -f` exit=0 — both ALLOWED. Control profile naming the canonical `/private/tmp/...` spelling: append "Operation not permitted" exit=1, `rm -f` "Operation not permitted" exit=1. seatbelt canonicalises ANCESTOR components for both data-write and unlink, so the RAW `ssh_home / n` entry fires for no operation on that hand; and on the in-process hand `crown_jewel_reason` (:703) resolves its argument first, so a lexical entry can never be what matches. The entry is dead on both hands, and :515/:844/:856 all still describe it as live mitigation.

WHY HIGH RATHER THAN A COUNT QUIBBLE. This commit is what discharged the standing "dedicated bugfix session before more dev" obligation for levain (2026-08-23). A closure record that says 5 when the answer is 3 does not merely miscount — it is the thing that stops anyone looking again, and what it hides is a security deny entry that measurement says fires for zero operations while shipping with a dedicated regression test certifying it as a closed vector. Absence of signal rendered as health, at the ledger that governs whether the signal ever returns.

THE FIX: correct the closure record rather than the commit message, which is immutable. Reopen findings 2 and 4 against spore-604 (or its successor) so the levain half of the bugfix discharge is 3-of-5, not 5-of-5, and carry both into the next window. For finding 2 the code prescription was already measured on 08-22 and re-measured tonight: either delete the RAW spelling and let KNOWN-OPEN (b) stand un-narrowed, or keep it and restate :515/:844/:856 as defence-in-depth against a canonicalisation change rather than as a narrowing of the symlinked-HOME vector. Do not leave a claim of narrowing that two independent measurements refute.
[prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260824-045923-fc7a510350a0
  Time: 2026-08-24T04:59:23  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_firing_confinement.py:883-885 and :903-904 — THE TEST ADDED TO CLOSE THE HIGH SHIPS A REFUTED MECHANISM AS ITS OWN RATIONALE, AND THE CORRECT STATEMENT OF THE SAME FACT IS SIXTY LINES ABOVE IT IN THIS FILE. The new docstring ends [quoted: `    Both enforcing hands resolve the requested path before matching (``crown_jewel_reason```] / [quoted: `    resolves first — see KNOWN-OPEN (d)), so a write aimed at the lexical file lands on`] / [quoted: `    ``real/.ssh/authorized_keys``: named by the LEXICAL entry and by neither of the others."""`], and the assertion message repeats it at :903 [quoted: `        "the resolved-HOME LEXICAL spelling must be denied — it is the path a resolve-first hand "`] / [quoted: `        "lands on, and neither the raw nor the fully-resolved entry names it")`].

REFUTED THREE WAYS.
(1) BY RUNNING IT. Building the test's exact geometry and calling `crown_jewel_reason` on each of the three spellings returns, for ALL THREE, "<evil>/authorized_keys is a write-protected ssh persistence/exec vector (authorized_keys/config/rc)". The resolve-first hand lands on the RESOLVED entry, never on the LEXICAL one. A write aimed at the lexical file does not "land on real/.ssh/authorized_keys" — that file IS a symlink to evil, by the test's own fixture, which it asserts at :900-901.
(2) BY ITS OWN TABLE, three lines earlier at :881 [quoted: `        RESOLVED (ssh_home / n).resolve()  -> evil/authorized_keys`]. The docstring states that resolving lands on evil, then concludes that a resolve-first hand lands on real/.ssh.
(3) BY THIS FILE, AT :820-823, where codex already established the same fact correctly [quoted: `    # ⚠ NON-DISCRIMINATING, AND LABELLED AS SUCH (codex L3 2026-08-21, which RECONSTRUCTED the`] / [quoted: `    # predecessor policy and confirmed this line passes WITH the bug). `crown_jewel_reason` resolves`] / [quoted: `    # its argument FIRST, so it matches the already-denied TARGET whether or not the lexical entry`] / [quoted: `    # exists. It is kept because both-hands-agree is worth stating, NOT as evidence of the fix.`]. Same file, same predicate, opposite conclusions, and the correct one is upstream of the wrong one.

THE TEST ITSELF IS GOOD — VERIFIED BY MUTATION, NOT BY READING THE COMMIT MESSAGE. Deleting `deny_write_files_l.append(ssh_home_lexical / n)` (:531) in a scratch copy turns exactly `test_build_policy_denies_the_RESOLVED_HOME_LEXICAL_spelling` red and nothing else: 2 failed / 91 passed, where the second failure is the pre-existing openhands import. At HEAD the same file is 1 failed / 92 passed. The commit's "suite 91 -> 92" reproduces exactly. Only the stated REASON is wrong.

THE ACTUAL REASON, MEASURED TONIGHT WITH BOTH CONTROLS. The LEXICAL entry is load-bearing on the SEATBELT hand, for UNLINK, not for any resolve-first hand. Geometry linkhome -> realhome with realhome/.ssh/authorized_keys -> evil/authorized_keys. Profile denying only the RAW and RESOLVED spellings: `rm -f <realhome/.ssh/authorized_keys>` exits 0 and the link is GONE. Control profile that adds the LEXICAL literal: "Operation not permitted", exit 1, link intact. seatbelt canonicalises ancestor components but does not follow the leaf for unlink, so realhome/.ssh/authorized_keys IS the canonical spelling the kernel matches — which is also why the RAW linkhome spelling beside it is dead (it canonicalises onto this one and is never itself matched).

THE FIX: replace the closing paragraph and the assertion message with the unlink argument above. Concretely — the LEXICAL entry is the CANONICAL spelling seatbelt matches for unlink/rename/ln; RAW is its un-canonicalised alias and matches nothing; RESOLVED covers the data write. Delete the "Both enforcing hands resolve the requested path before matching" sentence outright: it is the same "both hands behave the same way" simplification the HIGH this commit closed was built on, and :820-823 already contradicts it in this file.
[prescription: run]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260824-045940-b59e3946043a
  Time: 2026-08-24T04:59:40  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: DRIFT: levain/firing/confinement.py:492-494 — THE REFUTED VERB SURVIVED THE SPLICE, ON THE SAME PHYSICAL LINE AS THE WARNING AGAINST IT. Line :492 is 161 characters — every neighbour in the block is 104-105 — because the inserted correction was concatenated onto the tail of the sentence it interrupted, and the surviving tail states the model the insertion refutes. Read in file order [quoted: `    #       ⚠ DO NOT REASON FROM THE OLD MODEL. "seatbelt matches writes lexically" is FALSE and`] / [quoted: `    #       was generalised into a design rule here; a guard built on it would be built backwards. The (2) dir-anchor pin does NOT cover this: it blocks renaming`] / [quoted: `    #       ~/.ssh while deliberately still allowing "file creation/reads INSIDE it", which is exactly`] / [quoted: `    #       the write the attack uses. ⚠ THIS IS THE SAME lexical-vs-resolved ARGUMENT ALREADY MADE`].

"the write the attack uses" is the exact verb the block twelve lines above rules out. :480-486 says a direct data write at the lexical path is DENIED, and I reproduced that tonight with both controls: a minimal profile denying only the RESOLVED path refuses `echo >> <lexical>` with "Operation not permitted" and leaves the target byte-identical; the converse profile denying only the LEXICAL path lets the same append through at exit 0. So under the corrected model there is no such write. The conclusion the sentence reaches still holds — the dir anchor allows creation inside ~/.ssh, and the recreate step of unlink-then-recreate is a creation inside ~/.ssh — but it reaches it by naming the refuted step.

DISCRIMINATOR, RUN NOT FELT: `codeshape.py HEAD~1 levain/firing/confinement.py` exits 0, CODE UNCHANGED. The whole confinement.py half of this window is comment text. Both halves of the DRIFT test are satisfied, so this does not enter the debt count.

WHY IT ESCAPED: the 161-character line is the mechanism. A warning that ends mid-line, with the violation continuing after it on the same line, is invisible in a diff and in review — the eye takes the line as one authored unit. Worth noting that this is the second instance this month of a warning sitting directly on top of the thing it warns about.

THE FIX IS A LINE BREAK PLUS FOUR WORDS: rewrap :492 at the block's 104-char width, and change "which is exactly the write the attack uses" to "which is exactly the creation the unlink-then-recreate attack uses". No code moves.
[prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260824-050009-cfda407a30bd
  Time: 2026-08-24T05:00:09  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (43), TOTAL AT HEAD e3ed37f — levain, 2026-08-24. Filed separately: 1 DRIFT (not in this count).

ARITHMETIC, shown so it can be checked: 44 carried from the 2026-08-22 slot (diogenes-20260822-092116-4f32b8febdd8, at HEAD 7211bbf), MINUS 3 genuinely closed, PLUS 2 new = 43. There was no levain review on 2026-08-23; this window is the first since 7211bbf.

CLOSED (3) — each verified by RUNNING it, not by reading the commit message:
  - HIGH diogenes-20260822-091951-47d43cdab29d, the refuted mechanism in the record. The comment (:480-491) and the test docstring (:783-802) are rewritten, and all three of their new mechanism claims reproduce independently on Darwin 25.5 with controls: resolved-only deny refuses an append at the lexical path (exit 1, target byte-identical); lexical-only deny lets the same append through (exit 0, content "GOOD|PWNED"); `rm -f <lexical>` succeeds under a resolved-only deny (exit 0, link gone). The corrected record is sound.
  - MEDIUM diogenes-20260822-092026-25273688516c, ssh_home_lexical uncovered. Mutation-verified: deleting :531 turns exactly the new test red, 91 passed vs 92 at HEAD.
  - LOW diogenes-20260822-092051-6180bc6d9187, the _dedup soundness premise. Rewritten to Path value equality, which is the correct property.

NEW TONIGHT (2):
  - HIGH diogenes-20260824-045858-87d7a0b3e11f — the commit claims to close all 5 and closes 3, and the finding it does not mention is a security deny entry re-measured tonight as firing for zero operations. This is what discharged levain's bugfix-session obligation.
  - MEDIUM diogenes-20260824-045923-fc7a510350a0 — the new test's docstring and assertion message state a mechanism refuted by running it, by its own table three lines earlier, and by a correct statement sixty lines above it in the same file.

RE-SEVERITIED, NOT NEW (already inside the 44):
  - diogenes-20260824-045830-9e756537dd4f, HIGH — supersedes the still-open half of MEDIUM diogenes-20260822-092051-b834e49dc27b. Its stale-description half WAS corrected; its redundancy half was rejected by a rebuttal that measurement refutes in all four geometries and at zero blast radius across the full suite. Counted once, at the higher severity.

ALSO STILL OPEN AND WORTH NAMING, both carried:
  - diogenes-20260822-092011-7bd0afdcb175, MEDIUM — the RAW spelling is unreachable on both hands. Untouched by this window, re-measured tonight, still true.
  - The 2026-08-07 MEDIUM on the openhands import gate. tests/test_firing_confinement.py:1268 still does a bare `from levain.firing.openhands.tools import SandboxedBashExecutor  # openhands leaf, gated by @live` inside the test body, and `@live` gates on sandbox-exec, not on the extra being installed, so it FAILS rather than skips. SHARPER TONIGHT, from the full-suite run: 16 of the 18 skips in this suite are "openhands extra absent" — test_firing_entity.py:17, test_firing_gate_openhands.py:14, test_firing_tools.py:21, test_levain_condenser.py:27, test_run.py x10, test_wrap.py:337 — so every other openhands consumer in the repo gates correctly and this one file is the sole outlier. Seventeen days open.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260824-050035-418521de7fa0
  Time: 2026-08-24T05:00:35  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: levain 2026-08-24, HEAD e3ed37f == origin/main, porcelain empty before and after, window HEAD~1..HEAD = 1 commit (e3ed37f, the spore-604 bugfix session). Repo UNTOUCHED — every mutation ran in /private/tmp/levain_dio_20260824 (probe.py, anchor_probe.py, mut/, mut2/, five .sbpl profiles).

RE-DERIVED BY RUNNING, not carried forward:
  - FULL SUITE at HEAD: 8 failed / 2085 passed / 18 skipped in 103.53s. The commit message's "2085 passed" and "8 failures are all missing OPTIONAL dependencies (7x MCP SDK in test_app_server, 1x openhands)" both reproduce exactly, failure-for-failure.
  - tests/test_firing_confinement.py at HEAD: 1 failed / 92 passed / 1 skipped. The commit's "suite 91 -> 92" reproduces.
  - MUTATION on :531 (`deny_write_files_l.append(ssh_home_lexical / n)` deleted, scratch copy mut/): 2 failed / 91 passed — the new test goes red and nothing else does. The added test is genuinely load-bearing.
  - MUTATION on :631 (`+ [ssh_anchor]` removed, scratch copy mut2/): 8 failed / 2085 passed / 18 skipped — byte-identical to HEAD, zero tests red.
  - GEOMETRY PROBE, four shapes: `ssh_anchor in _write_deny_ancestors(all_jewels)` True in all four without the explicit anchor.
  - LIVE sandbox-exec on Darwin 25.5, EVERY claim with its control: resolved-only deny refuses an append at the lexical path (exit 1) while lexical-only deny allows it (exit 0); `rm -f <lexical>` succeeds under a resolved-only deny (exit 0) and is refused when the LEXICAL literal is added (exit 1); a profile naming only the un-canonicalised /tmp spelling allows both append and rm (exit 0, 0) while the canonical /private/tmp control refuses both (exit 1, 1).
  - crown_jewel_reason probed against the new test's exact fixture: all three spellings return the RESOLVED entry's reason.
  - DRIFT discriminator RUN, not assumed: codeshape.py HEAD~1 on confinement.py exits 0 (code unchanged), on tests/test_firing_confinement.py exits 1 (code changed).
  - Closure accounting checked against the STORE, not the commit message: the five 08-22 levain findings pulled by agent+tag+date, each mapped to what the diff does or does not touch; `git diff HEAD~1 HEAD` grepped for "narrows it" / "NARROWS" / "RAW ``Path" returns empty.
  - Files opened whole: levain/firing/confinement.py (the build_policy body :430-669, _write_deny_ancestors :335-353, crown_jewel_reason :671-727, SeatbeltProvider.render_profile :1361-1445), tests/test_firing_confinement.py :768-905.

NOT CHECKED — state plainly:
  - The 7 test_app_server MCP-SDK failures were accepted as optional-dep failures on the commit's word plus the error text; I did not install the MCP SDK and re-run them, so "all 8 are optional deps" is verified only for the openhands one, which I read.
  - No live end-to-end `levain run` under a real confined shell. Everything sandbox-side was minimal hand-written .sbpl profiles I wrote, NOT the profile SeatbeltProvider.render_profile actually emits for a real policy. I asserted the rendering shape by reading :1425-1440, not by diffing a rendered profile.
  - KNOWN-OPEN items (a), (c), (d), (e) in build_policy were read but not independently reproduced tonight; only (b), the symlinked-HOME case, was re-measured.
  - The in-process file-editor twin was exercised only through direct `crown_jewel_reason` calls, never through the editor's own command surface.
  - No review of anything outside the two changed files and their direct dependencies — jobs.py, web_server.py and the rest of the 43-item carried backlog were not re-verified at this HEAD, so the carried count is arithmetic from the 08-22 slot, not a fresh re-derivation of every item.
  - Windows/Linux confinement providers not looked at; every measurement here is macOS seatbelt.
[prescription: none]
  Tags: code_review, levain, semantic
```
