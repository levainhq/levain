# DIOGENES OVERNIGHT — levain, 2026-08-19

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-08-19*)`
**Stated count: 5 episode(s).** Severity: COVERAGE 1 · LOW 1 · MEDIUM 2 · STILL OPEN 1
**STILL OPEN: 36**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260819-045815-96fa44b83d94
  Time: 2026-08-19T04:58:15  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/web_server.py:973-975 (+ levain/init_server.py:303-305) — THE CONTENT-LENGTH GUARD USES A PREDICATE THAT ACCEPTS A VALUE ITS OWN CONSUMER REJECTS, SO A ONE-BYTE HEADER TURNS A DELIBERATE 411 INTO AN UNHANDLED EXCEPTION AND NO RESPONSE AT ALL — ON BOTH HTTP SURFACES. The guard is [quoted: `        if clen_raw is None or not clen_raw.isdigit():`] at web_server.py:973, and its only consumer is the very next line [quoted: `        clen = int(clen_raw)`] at :975. `str.isdigit()` is TRUE for the Unicode superscript digits and `int()` raises ValueError on all of them: measured, U+00B9 / U+00B2 / U+00B3 are each isdigit=True, isdecimal=False, int()->ValueError. All three live in latin-1 (bytes 0xB9/0xB2/0xB3), and http.client decodes request headers as iso-8859-1, so a single raw byte in the header reaches int() as a non-decimal digit. `.isdecimal()` is the predicate that matches int(); `.isdigit()` is a superset of it. RUN-VERIFIED THREE WAYS AGAINST A REAL BOUND SERVER, not reasoned — a temp anneal store, make_server(host=127.0.0.1, port=0), raw sockets. `Content-Length: 12` -> `HTTP/1.1 422 Unprocessable Content`. `Content-Length: abc` -> `HTTP/1.1 411 Length Required`, the deliberate refusal. `Content-Length: <0xB2>` -> NO RESPONSE AT ALL, connection closed, and a ValueError traceback on the operator's console: `File "/Users/phillipclapham/Briefcase/levain/levain/web_server.py", line 975, in do_POST / clen = int(clen_raw) / ValueError: invalid literal for int() with base 10: '2'`. THE SIBLING SURFACE HAS THE IDENTICAL LINE AND THE IDENTICAL BEHAVIOUR, and I made it emit rather than inferring it: init_server.py:303 is byte-identical to web_server.py:973, and the same three probes against a real make_init_server give 400 / 411 / NO RESPONSE. So this is one predicate defect at two of the project's two HTTP surfaces, not a one-off. WHAT IT COSTS, AT ITS REAL SIZE. The exception fires at :975, BEFORE the request-gate is acquired at :979, so no gate slot leaks; the thread is per-connection and dies. There is no auth bypass — on an off-box writable bind the token check at :954 precedes this line, so an unauthenticated tailnet node is 403'd first. The reachable populations are a same-machine process (which can already edit the files) and an unauthenticated node against a READ-ONLY off-box bind, where _write_token_required() is False by design. So: a crash and a dead connection where the code intends a clean 411, not a governance hole. THE DOCSTRING NAMES THIS CHECK IN THE LIST IT IS NOT HONOURING. do_POST:930-931 reads [quoted: `        The cheap fail-closed checks (Host → CSRF → Content-Type → route →`] / [quoted: `        Content-Length) run BEFORE any body read and each closes the connection on`] — Content-Length is enumerated as one of the checks that 'closes the connection on rejection', and for this input it does not reject, it raises. AND NOTHING IN THE SUITE CAN SEE IT: `grep -rn '411|length_required' tests/` over the WHOLE test tree returns ZERO hits. Neither server's length_required path has a single test, so the one guard with a predicate bug is the one with no coverage. -- FIX (worked out, NOT executed): replace `.isdigit()` with an ASCII-decimal test at both sites — `clen_raw.isascii() and clen_raw.isdigit()`, or a plain `re.fullmatch(r'[0-9]+', clen_raw)`. Prefer the ASCII form over bare `.isdecimal()`: isdecimal is true for Arabic-Indic digits, which int() would silently ACCEPT (int('٥') == 5) — not reachable through latin-1 header decoding today, but RFC 7230 wants ASCII DIGIT and the strict form costs nothing. The acceptance test is the one the class is missing: assert a 411 for a non-ASCII-digit Content-Length on BOTH servers. NOT EXECUTED because the same predicate appears a third time at levain/firing/anneal.py:98 (`env.isdigit() and int(env) >= 1`, same shape, env-var reachable) and the durable fix is one shared helper, which is a design call I should not make from a review. (levain/daemon.py:602's `pid.isdigit()` is NOT an instance — the value is never converted.) [prescription: run]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260819-045841-c9b4b2186250
  Time: 2026-08-19T04:58:41  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/web_server.py:987 — A DRAIN-HARDENING FIX LANDED IN ONE OF THE TWO HTTP SURFACES AND NEVER REACHED THE OTHER, SO THE OPERATOR-FACING DASHBOARD SILENTLY EATS THE 413 ITS SIBLING SENDS. Both servers refuse an oversize POST by draining a bounded body so the client can read the 413 on a kept-alive socket. init_server.py GUARDS that drain: :313-316 read [quoted: `            if clen <= _DRAIN_CAP:`] / [quoted: `                try:`] / [quoted: `                    self._drain(clen)`] / [quoted: `                except OSError:`], under a comment at :311 that names the reason and its provenance: [quoted: `            # strand the thread past the socket timeout (codex LOW / nemotron).`]. web_server.py calls the SAME method BARE at :987 — [quoted: `                    self._drain(clen)`] — with no except of any kind between it and do_POST's only `finally` (the gate release). RUN-VERIFIED WITH ONE PROBE AGAINST BOTH REAL SERVERS, byte-identical request shape, only the port differs. Declare a drainable-oversize Content-Length and then send ten bytes and stall: init_server (CL=263144) answers `HTTP/1.1 413 Content Too Large` after 30.0s; web_server (CL=328680) answers NOTHING after 30.0s and closes. `self.rfile.read()` hits the 30s socket timeout, raises socket.timeout, and `socket.timeout is TimeoutError` is True on this interpreter. AND THE FAILURE IS SILENT, WHICH IS THE HALF THAT MAKES IT WORTH FILING. _LevainHTTPServer.handle_error at :618 reads [quoted: `        if isinstance(exc, (ConnectionError, TimeoutError)):`] and returns — a deliberate filter for benign browser keep-alive resets, correct for what it was written for. It also swallows THIS TimeoutError. So the operator sees no traceback, the client sees no status, and the two surfaces disagree about what an oversize request means with nothing in either log to say so. absence_of_signal_rendered_as_health, at the one place the code took the trouble to promise otherwise. THE DOCSTRING PROMISES THE RESPONSE THAT NEVER ARRIVES: :935 reads [quoted: `        read+drain+write phase is itself concurrency-bounded; a slowloris is capped by`] — the gate and the 30s timeout do cap it, and the 413 the same paragraph is describing is never sent. A SECOND DIVERGENCE IN THE SAME HUNK, stated because it is the same missing propagation: init_server sets [quoted: `            self.close_connection = True`] at :312 UNCONDITIONALLY on oversize, under a comment naming why ('never keep-alive after a rejected body — a truncated/lying body would otherwise desync the next request on the socket'); web_server sets it only in the ELSE branch at :989, so a drained oversize request keeps the connection alive with whatever the lying client left in the buffer. THE TEST OVER THIS CODE STRUCTURALLY CANNOT FAIL ON IT. tests/test_web_server.py:1191 test_oversize_413 builds an HONEST body — [quoted: `        big = \"z\" * (_MAX_POST_BYTES + 1024)`] at :1195 — and urllib sends every declared byte, so the drain always completes and the 413 always arrives. The defect only exists when Content-Length LIES, which no test in either suite constructs. Same shape as my 08-18 finding one file over: a passing assertion that excludes the variable the bug lives in. -- FIX (worked out, NOT executed): port init_server's guard verbatim — wrap :987 in `try: self._drain(clen) / except OSError: pass` and hoist `self.close_connection = True` out of the else so it fires on every oversize refusal. Acceptance test: the probe above, asserting a 413 (not a dead socket) for a Content-Length that overstates the body. I did NOT execute it because the durable fix is that these two handlers are ~90% duplicated envelope — Host, Sec-Fetch-Site, Content-Type, route, Content-Length, drain, 413 — maintained as two copies, and which of them should own the shared code is a design call the review does not get to make. [prescription: run]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260819-045906-7ee9a78126ad
  Time: 2026-08-19T04:59:06  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: ~/Briefcase/flow/scripts/prescription.py:129-136 — THE SWEEP TABLE THAT JUSTIFIES THE NEW CLASSIFIER IS NOT REPRODUCIBLE FROM THE CLASSIFIER, AND THE BUCKET IT OMITS IS ONE THE SAME COMMENT DOCUMENTS TWENTY LINES LOWER. 35f7cbcb ('prescription: classify a self-correction by what it does, instead of voiding all of them') added a measured justification block. :129 reads [quoted: `# the module treated every one of them as a withdrawal. Swept: **21 anchored self-corrections`], and the table enumerates three kinds summing to exactly that population — :132 [quoted: `#   16  AMEND      the finding stands; a coordinate or a [quoted:] string is wrong.`], then 4 RETAG at :134 and 1 VOID at :136. MEASURED AGAINST THE SHIPPED CODE OVER THE SAME POPULATION, NOT ARGUED. Importing the module and running `corrections()` over every diogenes episode with timestamp < 2026-08-19 (the cutoff that makes the denominator match): `_RETRACTION` matches 21 — the stated number reproduces EXACTLY, so the denominator is right — but the classification comes back amend 12 / retag 4 / void 1 / UNCLASSIFIED 3, over 20 distinct targets (one episode is corrected twice and last-writer-wins collapses it, which the docstring documents). The table's largest bucket is off by four and its fourth bucket is absent. THE THREE UNCLASSIFIED ONES ALL MEAN AMEND, which is why this is a record defect and not a behaviour defect: diogenes-20260812-042838-aba6a6e30e83 ('THAT IS WRONG — there are TWO'), diogenes-20260813-042553-3693f318a36d and diogenes-20260814-050623-d711a5725b60 ('I ATTRIBUTED THE AMENDMENT TO THE WRONG EPISODE AND THE WRONG TIME') each correct a fact and leave the finding standing, and none of them uses a phrasing `_STANDS` matches. So the human sweep's 16 is probably right about MEANING and the code recognises 12. COST MEASURED RATHER THAN ASSERTED, AND IT IS CURRENTLY ZERO. :154 states UNCLASSIFIED holds a `run` verdict down to `reasoned`. I looked up all three targets: every one already carries `[prescription: reasoned]`, so nothing was silently downgraded. The exposure is forward-looking — the next amend-shaped correction of a `run` finding gets demoted, which is the exact tax the commit was written to stop. WHY IT MATTERS AT ALL, GIVEN ZERO COST TODAY: this comment is the whole evidentiary record for a deliberately non-obvious branch, and it closes by instructing the next reader [quoted: `# cannot reach a seat. Do not \"simplify\" this to a two-way branch.`]. A reader who accepts that instruction and then re-derives the table to check it finds four of sixteen missing and a bucket that is not in the table at all. THE POPULATION LINE IS SCOPED HONESTLY and I checked before writing this — 'anchored' is the correct word, and my separate carried MEDIUM about the anchor's reach is the reason the word is load-bearing: tonight 40 diogenes episodes OPEN with SELF-CORRECTION and `_RETRACTION` matches 23 of them, so 17 are outside this table by construction and the comment does not claim otherwise. -- FIX: none proposed. The right correction depends on whether the 16 was a hand-classification of MEANING (then annotate the table with what the code actually emits) or a claimed code output (then `_STANDS` needs widening and that is a behaviour change with its own evidence burden). That is intent I cannot read off disk. [prescription: none]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260819-050004-9175f630b11d
  Time: 2026-08-19T05:00:04  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: STILL OPEN (36), TOTAL AT HEAD 344d684 — levain, 2026-08-19. 33 carried, 3 new, ZERO CLOSED. The window is again a single doc-only commit adding project_memory/diogenes_20260818.md — the THIRTEENTH consecutive levain window whose entire content is a routed Diogenes report, and the commit body says so itself. Last night's coverage line ended 'the shallow end of the room was clean — not that the room is', so tonight went straight past the router into source that eleven reports have never named. Both new levain findings are in files that had never appeared in a finding. THE ARTIFACT VERIFIED AGAINST THE STORE, NOT THE DIFF: a SQL join on episode_tags (tag=levain, agent=diogenes, timestamp LIKE '2026-08-18%') returns FOUR rows; the committed file holds FOUR blocks; all four ids present and all four contents verbatim by substring match against the store; zero foreign passengers. The header's 'Stated count: 4' and 'STILL OPEN: 33' both parsed correctly, so flow's --tags fix (d76d5123) is holding a second night and severity() emitted no phantom label — the instance stays closed while the class stays open. NEW TONIGHT (3). levain/web_server.py:973-975 + levain/init_server.py:303-305, MEDIUM — the Content-Length guard is `.isdigit()` and its only consumer is `int()`, which rejects the superscript digits isdigit accepts; all three are latin-1-reachable in an HTTP header, so one byte turns a deliberate 411 into an unhandled ValueError and NO response. Run-verified on both servers, three probes each. levain/web_server.py:987, MEDIUM — the oversize drain is unguarded where the sibling init_server.py:313-316 wraps the identical call in try/except OSError under a comment crediting the review that found it; one probe against both real servers gives 413 from init_server and NOTHING from web_server, with the TimeoutError swallowed by handle_error:618. ~/Briefcase/flow/scripts/prescription.py:129-136, LOW — the sweep table justifying 35f7cbcb (16 AMEND / 4 RETAG / 1 VOID = 21) does not reproduce from the shipped classifier over the same population (12/4/1 plus 3 UNCLASSIFIED over 20 distinct targets); measured cost today is zero, all three affected targets already carried `reasoned`. CARRIED FLOW-SIDE, RE-MEASURED RATHER THAN ASSUMED, AND THE FIX THAT TOUCHED THE FILE DID NOT CLOSE IT: 35f7cbcb rewrote prescription.py's semantics but left `_RETRACTION` byte-unchanged at :125 — [quoted: `_RETRACTION = re.compile(rf\"\\A\\s*SELF[- ]CORRECTION\\s+to\\s+({_ID})\", re.I)`] — so my 08-17 reach MEDIUM stands, and tonight's number is worse than the one I filed: 40 diogenes episodes now OPEN with SELF-CORRECTION and the anchor matches 23, leaving 17 invisible. I applied my own 08-07 rule (when the window's diff touches the function an open finding lives in, re-run the repro against both sides) and it is why this is filed as still-open-and-moved rather than closed. HONEST SUBTRACTION: none of the 17 invisible ones carries an explicit 'correct tag is [prescription: X]' or matches `_VOID`, so the dangerous silent-dispatch case the commit's own comment warns about is NOT live in them today. route_diogenes.py severity() re-read at :310-324 and byte-unchanged. LEVAIN TREE (25) — 23 carried, 2 new. The 23 are verified by GIT BYTE-IDENTITY over the widened pathspec: `git diff --stat 6281cf6 HEAD -- levain/ tests/ pyproject.toml README.md docs/ scripts/` is EMPTY, and `git diff --numstat 6281cf6 HEAD -- . ':(exclude)project_memory'` is EMPTY too — four consecutive routing-only commits, so nothing outside project_memory/ has moved since the anneal-floor bump. FLOW TREE (11) — 10 carried, 1 new. THREE CARRIED ITEMS RE-EXECUTED RATHER THAN CARRIED. (1) FULL SUITE RUN HERE: 8 failed / 2081 passed / 18 skipped in 103.22s — identical counts to my 08-15/16/17/18 runs, and test_live_plain_config_cannot_be_unlinked_or_relocated FAILED rather than skipped for the THIRTEENTH night, so tests/test_firing_confinement.py:1113's [quoted: `    from levain.firing.openhands.tools import SandboxedBashExecutor  # openhands leaf, gated by @live`] is still FALSE. The other 7 are all test_app_server.py (mcp extra absent). (2) README.md:164 re-read at HEAD and still reads `(pinned \`>=0.9.6,<0.10\`)` against pyproject.toml:74's [quoted: `    \"anneal-memory>=0.9.7,<0.10\",`]. ⚠ WORTH RECORDING: my first grep tonight (`grep -n 'anneal-memory>=' README.md`) returned NOTHING and would have read as a closure — the README writes the pin with the package name and the specifier in separate backticked spans, so the pattern that matches pyproject cannot match prose. I re-grepped on the version instead of the package. A negative grep nearly reported a false close. (3) scripts/check_seed_budget.py EXECUTED: clean, 38,759 B eager / 55% mechanism against limits of 40,000 B / 62%. HYPOTHESES OPENED AND KILLED AGAINST DISK, stated because a dropped finding is evidence about the instrument: (a) I suspected writes.py's _append_audit:577 claim 'Single-writer (the server serializes writes)' was false, since the web server's gate is a BoundedSemaphore of 8, not a mutex — FALSE, `_WRITE_LOCK` exists at writes.py:161 and apply_edit holds it at :652, so every audit append it reaches is serialized; I would have filed a fabricated MEDIUM off a correct reading of the wrong layer. (b) I suspected _sbpl_regex (confinement.py:1235) single-escapes a backslash where an SBPL regex-inside-a-string needs it doubled — could not ground it without executing sandbox-exec against a crown-jewel path containing a literal backslash, so DROPPED rather than filed as reasoning. (c) I checked whether _audit_verb's unlocked _append_audit at :1309 escapes the lock — it does not; all six callers are Class-B handlers dispatched under apply_edit's lock. NOT COUNTED AS OPEN, unchanged for the thirteenth night: the environment-dependent severity argument in the 08-04 report, and the pack-hook DELETION case I have declined on 08-06 through 08-18 and decline again. ARITHMETIC STATED SO THE TREND SURVIVES: 33 at 344d684 = levain 23 + flow 10; tonight levain 23 + 2 = 25, flow 10 + 1 = 11; 25 + 11 = 36. REPO UNTOUCHED: `git status --porcelain` empty at 344d684 before and after the suite run, HEAD == origin/main; flow's tree read only; every probe and server ran in /tmp/levain_dio_20260819 against temp stores. ALL QUOTED STRINGS PRE-FLIGHTED WITH `grep -cF` AGAINST THE FILE ON DISK BEFORE FILING, and every coordinate confirmed with `sed -n 'Np'` at HEAD.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260819-050035-4aa776e7f651
  Time: 2026-08-19T05:00:35  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: levain 2026-08-19, HEAD 344d684 == origin/main, porcelain empty, window HEAD~1..HEAD — one doc-only commit adding project_memory/diogenes_20260818.md (47 lines, 4 blocks), the routed report of my own 08-18 findings. Thirteenth consecutive window of my own prose. Last night I wrote that I had 'finally opened the door and the shallow end of the room was clean — not that the room is', so tonight the router got a bounded verification pass and everything else went into source. RE-DERIVED BY RUNNING, NOT CARRIED FORWARD. (1) THE ARTIFACT CHECKED AGAINST THE STORE by SQL join on episode_tags: 4 rows, 4 blocks, all 4 ids present, all 4 contents verbatim by substring, zero foreign passengers; header 'Stated count: 4' and 'STILL OPEN: 33' both correct. (2) TREE BYTE-IDENTITY MEASURED ON A WIDER PATHSPEC THAN THE FINDING NEEDED: `git diff --numstat 6281cf6 HEAD -- . ':(exclude)project_memory'` is EMPTY, which bounds all 23 carried levain findings without enumerating a pathspec I could get wrong. (3) FULL SUITE RUN HERE: 8 failed / 2081 passed / 18 skipped in 103.22s, all 8 enumerated — 7 mcp-absent in test_app_server.py plus test_live_plain_config_cannot_be_unlinked_or_relocated, which FAILED rather than skipped, re-executing the :1113 finding for the thirteenth night. (4) scripts/check_seed_budget.py EXECUTED: clean, 38,759 B / 55%. (5) BOTH NEW web_server FINDINGS RUN-VERIFIED AGAINST REAL BOUND SERVERS, not reasoned: three raw-socket Content-Length probes each against a live make_server AND a live make_init_server (12/abc/0xB2 -> 422/411/no-response and 400/411/no-response), and one lying-Content-Length drain probe against both (init_server 413 after 30.0s, web_server nothing after 30.0s). Temp anneal stores, ephemeral ports, torn down. (6) THE PRESCRIPTION CLASSIFIER RUN, not read: prescription.py imported and `corrections()` executed over all 1,075 diogenes episodes and over the <2026-08-19 cutoff, which is how the 21-anchored denominator was confirmed to reproduce exactly and the 16-vs-12 bucket gap was found; and how the carried reach finding was re-measured at 23 of 40. (7) THREE HYPOTHESES KILLED AGAINST DISK and named in the slot (the _WRITE_LOCK one would have been a fabricated MEDIUM). SOURCE OPENED IN FULL OR IN THE RELEVANT SPAN: levain/web_server.py :1-300, :560-820, :895-1039; levain/init_server.py :268-325, :615-660; levain/writes.py :123-170, :280-460, :508-632, :1265-1320, :2085-2129; levain/firing/confinement.py :1-130, :1226-1290; levain/firing/anneal.py :88-105; levain/daemon.py :596-610; tests/test_web_server.py :1-90, :1185-1205; README.md:164; pyproject.toml:74; tests/test_firing_confinement.py:1113. Out of repo: ~/Briefcase/flow/scripts/prescription.py in its entirety, route_diogenes.py :305-326, the flow episodic store by SQL, and my own 08-17 and 08-18 episodes read from the store rather than from the committed reports. NOT CHECKED, AND THE LIST IS STILL LONGER THAN THE CHECKED ONE. levain/dashboard.py (1,861 lines), levain/session.py (1,052), levain/interview.py (914), levain/tui.py (821), levain/_tui_curses.py (834), levain/run.py (835), levain/jobs.py, levain/verify.py, levain/reconcile.py and levain/install.py (2,169) were NOT OPENED AT ALL tonight — fourteen days in, install.py is named in 8 prior findings and dashboard.py in none. I read fragments of writes.py and 130 of confinement.py's 1,514 lines; I did NOT audit the SBPL profile emitter, did NOT run sandbox-exec, and DROPPED a live backslash-escaping suspicion in _sbpl_regex rather than file it unrun. I did NOT re-execute the two heavy 08-14 doctor.py repros (:1169 pack-hook false red, :1140 corrupt-pack-lock bypass) — they rest on git byte-identity plus an unchanged suite result, which is a structural argument, not a re-run. I did NOT run `levain doctor`, `levain init`, or `levain serve` end to end as an operator would; the two web findings are run-verified at the socket, not through the CLI that starts these servers. I did NOT patch either file, did NOT write the acceptance tests I prescribed, and did NOT run the suite under any change. I did NOT check the public remote tonight. THE THING WORTH SAYING PLAINLY, AND IT IS DIFFERENT FROM LAST NIGHT'S. Both new findings are in HTTP request-envelope handling, both are grounded to a running socket, and both are the SAME shape: a correction that exists in one place and not its sibling. The Content-Length predicate is wrong at BOTH surfaces; the drain guard is right at ONE, with the review that produced it named in the comment. That is the class flow's own memory calls its weakest — propagation, not detection — showing up in levain's code rather than in its records. It also means the two most security-shaped files I opened tonight yielded envelope defects, not policy defects, because I ran out of night before the policy layer. The full constellation drift sweep, the cognitive half of my dual lens, has now not run since 2026-05-24 — THIRTEEN WEEKS. [prescription: none]
  Tags: code_review, levain, semantic
```
