# TRIAGE — 2026-09-14 · `0914+9 levain-seat` · branch `seat/0914-l3triage`

Input: L3 `16dcb7eebc6d3775` (review of `db7384e..849c259`, seats complement / codex / glm) and
Diogenes `diogenes_20260914.md`. Every disposition below is against `849c259`.

**Re-derive, do not read:**
```
grep 16dcb7ee ~/Briefcase/flow/state/verdicts.jsonl            # the rows triaged here
git log --oneline 849c259..origin/seat/0914-l3triage            # what this triage committed
python -m pytest -q tests/test_l3_16dcb7ee_fixes.py             # one test per reproduced finding
```

## FIXED — each reproduced against `849c259` first, each with one test built from that reproduction

| Finding | What the reproduction showed | Fix |
|---|---|---|
| complement HIGH + glm MED ×2 (`_copy_activation_tree` rollback) | Three shapes, one defect. Each left the tree intact with **no live receipt**: backups dir refused plus sibling move failed, while the error said "Nothing was moved or deleted"; sibling fallback succeeded, then the swap failed; symlinked `activation/`, swap failed. | Restore the receipt from its text, read before the invalidation, whenever the original tree is back. |
| codex MED (rollback rename fails) | Reasoned, not run: the rollback's `os.replace` escaped and masked the original error. | `InitError` naming where the tree is. |
| codex HIGH (`doctor._check_mcp_command` realpath) | Two venvs' `bin/python` realpath-equal; venv B's `.pth` executed under `-P`. | Same path, or the same binary in this directory; the probe runs `sys.executable`. |
| codex HIGH (`_is_bytecode_residue`) | `hooks/__pycache__/h.notes.pyc` beside `h.py` returned True. | Only importlib's own cache name counts. |
| codex HIGH (argv `--version` ahead of `--db`) | Doctor returned OK. | Six-element exact argv. |
| codex HIGH (`anneal-memory-malware`) | `upgrade_pending=True` on a resolvable executable. | Exact basename plus exact `--db <store> serve`. |
| codex MED (receipt `stat` then read) | Reasoned, not run. | One bounded open (`_read_receipt_text`). |
| Diogenes MEDIUM (codex `content_changed`) | His signature exactly: 7 lines, the fourth ending "that customisation is gone", one `config.toml.bak`. | Compare without levain's `command`/`args`; a levain-only change prints what it now runs. |
| Diogenes LOW (bare `anneal-memory` advice) | Structural. | `manifest.anneal_invocation()` / `pip_invocation()` across doctor, install, manifest, wrap, update and both adapter READMEs. |

## NOT FIXED — and why

- **codex #3** (editor writing into a retained backup between the second proof and `rmtree`) and **codex #5**
  (concurrent force-inits leave tree B beside receipt A): the concurrency class of `spore-1023`, which now carries
  a note naming both by input_id. Not reproduced.
- **glm MED** (receipted file missing → proves pristine) and **codex MED** (operator-added empty directory):
  pruning such a tree loses no bytes. LOW, not fixed. `install` creates empty directories itself, so counting
  directories would make every tree unprovable.
- ⚠ **KNOWN RESIDUAL, carried on purpose:** both `_levain_hook.py` templates still offer
  `anneal-memory wrap-cancel` beside the MCP tool. Editing a hook template makes every existing install's hooks
  stale on upgrade.
- ⚠ **KNOWN RESIDUAL:** `pip install ... levain` advice strings (`update.py`, `docs.py`, `packs.py`,
  `init_server.py`, `docs_server.py`, `doctor.py`) are the same pip-vs-interpreter shape for the levain package
  itself. Not in Diogenes' finding and not changed here.
- ⚠ **KNOWN RESIDUAL:** a file an operator writes under importlib's exact cache name inside `__pycache__` is
  still skipped by the pristine proof. A filename cannot prove provenance.

## ⚖ C6 — Phill, 2026-09-14 (`spore-1027`)

A stale hook with no receipt entry is PENDING, doctor exit 6, as built in `849c259`. This refutes codex L3
`5726f23da7f826dc`'s HIGH by ruling. **Confirmed on a scratch HOME**, transcript
`RESIDUE_UPGRADE_0.4.6_TO_BRANCH_2026-09-14_single-anneal.txt` (`grep -n "exit=" <file>`).

## RESIDUE — the upgrade nobody had run

**RUN:** published levain 0.4.6 from PyPI → `init --adapter codex` → upgrade to this branch → `doctor` →
`init --force` → `doctor`, in a scratch HOME and CODEX_HOME. Two transcripts beside this file.

- Single-anneal PATH: C6 holds; the upgrade prints no lost-customisation alarm and writes no
  `config.toml` backup.
- ⚠ **OBSERVATION, NEEDS A RULING BEFORE ANYONE CHANGES IT:** with a second anneal earlier on PATH (the
  `spore-751` split), published 0.4.6 records **that** anneal's version in the lock. After the upgrade, doctor
  reads the venv's anneal as a DOWNGRADE, reports "an out-of-band upgrade", and exits 1, not 6
  (`…two-anneal-PATH.txt`). That is `849c259`'s "a downgrade is not pending" rule meeting a lock that 0.4.6
  wrote from the wrong anneal. Unchanged.
- **UNDISCHARGED:** no live Codex or Claude Code client was started against the new
  `<python> -P -m anneal_memory` registration.

## Suites at `e8b661e`'s tree — FLAKY, NOT GREEN

- `~/Briefcase/levain/.venv` (worktree import asserted): one failure, `Bind to port 64831 on 0.0.0.0 failed:
  Address already in use`. The run's output was filtered and did not keep the test id. All four port-binding
  files (`test_firing_confinement`, `test_init_server`, `test_docs_server`, `test_web_server`) pass when run
  alone. Recorded as a port-collision flake, **not** as a green suite.
- `~/Briefcase/flow/venv`: the two `test_manifest.py` pip_floor tests fail, which `next.md` records as failing
  from any worktree under that interpreter.
- ruff and mypy clean on the changed modules.

The split-PATH exit question above is written for Phill in flow's `projects/levain/next.md` ("PENDING PHILL —
THE SPLIT-PATH UPGRADE EXIT").

## Re-pass

Fix-diff re-pass dispatched through the fan-in: `deep_review.py --diff 849c259 --paths <the six code paths>
--seats complement,codex,glm`. Exited 1: **COVERAGE INCOMPLETE**, because glm was cut off part-way. Its
findings stand; the unread remainder is not coverage. complement and codex completed.

**Decision written BEFORE any further pass:** fix every HIGH that reproduces against the current tree, then run
ONE re-pass scoped to that fix diff (`--diff e8b661e`), and repeat only while a HIGH reproduces. glm's statement
that the `refused` test fails is refuted: that test passed at `e8b661e`.

### Round 2 — input_id `4cd2242f17466a28`

Every HIGH sits inside round 1's own fixes. Each was reproduced against `e8b661e`, and its test fails there.

| Finding | Reproduction | Fix |
|---|---|---|
| complement + glm HIGH (consensus) | A symlinked `activation/` whose own rename is refused keeps its link and loses its receipt: `moved_to` was assigned before the rename. | `moved_to` is assigned only after a successful rename; the receipt is restored only when the original is at `activation/`. |
| codex HIGH, a round-1 regression | A wrapper `command` plus `--trace` on the same store is replaced with no backup and no warning. | `_is_levain_launcher`: a levain-only change also requires the old launcher to be one levain wrote. |
| codex HIGH | `h.notes-1.pyc` counted as residue; the dotted `foo.bar.cpython-313.pyc` did not. | CPython cache tag or this interpreter's own; dotted stems allowed. |
| codex MED | Reasoned: the rollback dropped a good carried receipt when the in-memory read had failed. | The carried copy is preferred. |
| codex MED | By reading: advice quoted the interpreter but not the `--db` path. | `anneal_invocation(*args)` uses `shlex.join`. |

Next: one re-pass scoped to this diff (`--diff e8b661e`), requested through the fan-in.

### Round 3 — re-pass of round 2's diff (`--diff e8b661e`)

Exited 1 on COVERAGE INCOMPLETE again, because glm was cut off. Two HIGHs, both inside round 2's own fixes:
- **F1, all three seats:** `_is_levain_launcher`'s current-shape branch never checks `command`, so an operator
  wrapper with levain's exact args is overwritten silently. glm's `command == sys.executable` is rejected: it
  would restore Diogenes' false alarm on a re-init from another venv. Codex's minimum is taken: the command's
  basename must be a Python interpreter name.
- **F2, codex:** a SIGINT landing after `os.replace(dst, …)` succeeds but before `moved_to = …` reads as "never
  moved". Round 2's assign-after-success traded round 1's window for this one. Codex's reconcile-from-disk fix
  closes both.

**Decision written BEFORE fixing:** fix what reproduces; bundle Phill's Option-3 anneal-lock text into the same fix
diff; ONE re-pass scoped to that diff; repeat only while a HIGH reproduces.

**⚖ F1's shape, chosen after the fan-in's design note (seat ruling, `spore-865`):** the codex config is now
**backed up on ANY change to the block**, and the launcher heuristic chooses only the NOTICE: "keeps its store and
now starts …" or "customisation is gone". Three rounds each found a launcher the classifier misjudged: a wrapper,
then a wrapper keeping levain's args, and next would be a wrapper named `python3`, a pyenv shim or `uv run`. A
heuristic that gates a destructive action keeps producing data loss; one that picks a sentence produces only a
wrong sentence. Diogenes' MEDIUM objected to the false TEXT and allowed the backup, so the residue bar is now
"one backup written, benign line printed". The Python-name check stays, as the wording selector.

### Round 4 — input_id `d3cb07653d40ecdb` (`--diff 196a8ca`), ALL THREE SEATS COMPLETE

The first full-coverage pass. complement filed one HIGH; codex filed that same shape as MED #1, plus MED #2, MED
#3 and LOW #4; glm filed two LOWs and no HIGH.

**Decision written BEFORE reproducing:** the interrupt-after-final-swap HIGH (complement; codex MED #1) and codex
MED #2 (an interrupt after the rollback rename) are **covered if they fail closed**: both trees survive, only a
receipt is missing, and receipt-less trees are unproven, so they are kept and never pruned. That gets a recorded
reproduction, not a patch. If either reproduces actual loss, it is a HIGH: fix it, then run one more re-pass. codex
MED #3 (a comment-only change inside a block that parses identically, replaced without a backup) is fixed if it
reproduces, because "preserve always" has to be literally true. LOW #4 (`.devN` lock) and glm's LOWs are recorded,
not fixed.

**Reproduced, against `b2bb77e`'s code:**
- **Interrupt after the final swap: FAILS CLOSED, not a HIGH.** A mock that performs the real `os.replace(new_tree,
  dst)` and then raises `KeyboardInterrupt` leaves the NEW tree live and the OLD tree retained under
  `backups/activation/tree-<ns>`. Both receipts are gone. `_prune_activation_backups(keep=0)` then KEEPS that tree
  ("Kept, because they may hold edits"). Nothing is deleted; the cost is a tree that can never prove itself, so it
  stays on disk. codex MED #2 (interrupt after the rollback rename) has the same retention shape. Neither is patched;
  both are routed as MED. A structural fix would reconcile the second and third renames from disk the way round 3
  reconciled the first.
- **codex MED #3: REPRODUCED.** A parsed-identical block carrying `# corporate runbook` produced 0 backups, 0 notices,
  and the comment was gone. Fixed: the backup gate is now the two facts, the parsed block differs or its raw text
  differs, and the named cases choose only the notice. Per `0914+12 fanin`, a scoped re-pass is owed on this fix
  before merge, because the gate decides a destructive action and complement filed a HIGH this round.

**📅 0.5 CANDIDATES, dated 2026-09-14 — known, routed, NOT new if re-filed:**
- Rename 2 (`os.replace(new_tree, dst)`) is not reconciled from disk: an interrupt after it lands leaves the new tree
  live without a receipt and drops the old tree's carried receipt (complement HIGH, re-graded MED because it fails
  closed; codex MED #1, input_id `d3cb07653d40ecdb`).
- Rename 3 (the rollback's `os.replace(moved_to, dst)`) is not reconciled from disk either: an interrupt after it lands
  skips the receipt restore (codex MED #2, same input_id).
Both leave every tree on disk, so both are retention costs, not loss. The fix direction is round 3's candidate plus
disk-reconcile, applied to those two renames.
