# DIOGENES OVERNIGHT — levain, 2026-09-15

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-09-15*)`
**Stated count: 5 episode(s).** Severity: LOW 1 · MEDIUM 1 — 2 of 5 episode(s) carry a severity; the other 3 are COVERAGE 1 · COVERAGE-OPEN 1 · STILL OPEN 1
**STILL OPEN: 3** — **2 filed tonight** (measured at HEAD `33be8b6`) · **1 carried**. A carried item was NOT necessarily re-checked this window; only the slot block below says which ones were.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260915-020626-97d10fb902de
  Time: 2026-09-15T02:06:26  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: levain 2026-09-15, HEAD 33be8b6 (== origin/main? yes — `git ls-remote origin HEAD` answered 33be8b6fc2c8 live), `git status --porcelain` dirty: untracked .DS_Store x3 only (nothing staged/modified), window fd88986^..33be8b6 = 14 commits (prompt said 13; fd88986 at 06:52 09-14 sits on the 24h edge, included). REVIEW STARTED, NOT FINISHED — if no COVERAGE line for this repo exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260915-021123-1d69fd31cba5
  Time: 2026-09-15T02:11:23  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/doctor.py:1662 — `_check_hook_freshness` throws away the receipt STATUS, so a CORRUPT or EMPTY install receipt downgrades an EDITED hook from broken (exit 1) to "post-upgrade step pending" (exit 6), and the detail text asserts no receipt exists. [quoted: `        receipt_files, _status = read_activation_receipt(install)`] [quoted: `            entry = (receipt_files or {}).get(f"hooks/{rel}")`] [quoted: `"no install receipt covers them, so doctor cannot tell an outdated "`]
WHY IT MATTERS: the C6 ruling (Phill 2026-09-14) makes a RECEIPT-LESS stale hook pending, and the in-code justification names its premise: [quoted doctor.py: `# made before the receipt existed — the hook stays pending`]. That premise holds for ABSENT only. A corrupt or empty receipt cannot come from a pre-receipt install; install.py itself classes those as distinct states (`"ok"`, `"absent"`, `"corrupt"` or `"empty"`). The L2 HIGH this window fixed ("a hook with an appended `os.system(...)` exited 6") comes back whenever `.levain/activation-manifest.json` is half-written or truncated. That is the realistic path the pack-provenance branch of this same function already guards against with its own `pack_status == "corrupt"` refusal 90 lines up. I grepped levain/doctor.py: :1662 is doctor's ONLY receipt read, so no other check reports the damaged receipt.
BEHAVIOURAL, FALSIFIABLE SIGNATURE, observed 2026-09-15 at HEAD 33be8b6 under .venv (py3.12). Build a receipted hooked install, append a payload line to activation/hooks/_levain_hook.py, then damage the receipt four ways and call `_check_hook_freshness(install)[0]`. Expected `(ok, upgrade_pending)` pairs: intact → (False, False), detail starting "installed hook script(s) changed since `levain init` wrote them"; `{` → (False, True); `{"schema":1,"files":{}}` → (False, True); deleted → (False, True). The last three all end in "(no install receipt covers them, so doctor cannot tell an outdated hook from an edited one)". Only the deleted case is the ruled one.
FIX: branch on the status. `absent` keeps C6 (pending). `corrupt`/`empty` fail WITHOUT `upgrade_pending`, with a detail naming the unreadable receipt at `activation_receipt_path(install)` and the `init --force` remedy. ⚖ This is a PREMISE route, not a re-litigation: C6's conclusion stands for the world it described. Whether a damaged receipt belongs in that world is Phill's call, and no seat should decide it silently either way. [prescription: reasoned]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260915-021123-837af5a66f91
  Time: 2026-09-15T02:11:23  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: levain/install.py:1758-1762 — `read_activation_receipt_file` still does the stat-then-separate-read that codex's MED named, under a comment claiming it is bounded. The fix went into a sibling helper 30 lines up (`_read_receipt_text`), which only the rollback's in-memory copy uses. [quoted: `        # Bounded (codex MED): a damaged or hostile receipt of any size must read as corrupt,`] [quoted: `        if path.stat().st_size > _RECEIPT_MAX_BYTES:`] [quoted: `        raw = path.read_text(encoding="utf-8")`] vs [quoted install.py:1733-1734: `def _read_receipt_text(path: Path) -> str:` / `    """A receipt's text from ONE open, reading at most `_RECEIPT_MAX_BYTES` (codex MED: a`]. A file that grows between the `stat` and the `read_text` is read whole. That reader is the one used by doctor's hook freshness (doctor.py:1662), by `_tree_proves_pristine` (the gate before `rmtree` in rotation) and by `prior_installed` at install.py:2096. TRIAGE_2026-09-14.md records the codex MED as FIXED with "One bounded open (`_read_receipt_text`)", which is true of one of the two readers only. STRUCTURAL: nothing ran, and the race needs a writer under .levain/ mid-read. FIX: `raw = _read_receipt_text(path)` inside the existing try, adding `ValueError` to the `corrupt` except. Then the comment is true and one bounded-read implementation remains. [prescription: reasoned]
  Tags: code_quality, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260915-021204-8bb8f7aec660
  Time: 2026-09-15T02:12:04  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (3), total live defect count for levain at HEAD 33be8b6 == origin/main (live ls-remote). 2 filed tonight (0 HIGH / 1 MEDIUM / 1 LOW) + 1 carried. Filed separately: 0 DRIFT (not in this count). 0 SELF-CORRECTIONS.
· FILED: MEDIUM doctor.py:1662 (diogenes-20260915-021123-1d69fd31cba5): a corrupt or empty activation receipt reclassifies an EDITED hook as a pending upgrade (exit 6), and the text says no receipt exists. Reproduced, with a 4-case signature. C6's premise covers absent only, so it routes to Phill. · LOW install.py:1758-1762 (diogenes-20260915-021123-837af5a66f91): the receipt reader keeps the stat-then-read_text the codex MED named, under a "Bounded" comment; the bounded helper exists and that reader does not use it.
· CARRIED (1), resolved against the REPO: MEDIUM web_server.py `_Handler` / init_server.py `_InitHandler` duplicated request preamble. At HEAD `_host_ok` is at init_server.py:190 and web_server.py:664, `_reject` at :261 and :919, `_drain` at :267 and :908. Neither file was touched in this window (`git log fd88986^..HEAD` on both is empty).
· CLOSED, verified against the tree and not carried: both of my 09-14 findings. (1) The codex `content_changed` false alarm: `_without_levain_keys` + `_is_levain_launcher` are at install.py:2527-2540, and TRIAGE_2026-09-14 reproduced my exact 7-line signature before fixing. (2) Bare `anneal-memory` advice: now `_anneal_invocation`/`anneal_invocation` in doctor, install, manifest, wrap and update. A quoted re-grep of levain/*.py finds only two error DESCRIPTIONS (manifest.py:421, :432) and one docstring (install.py:2377), none of them advice. The hook templates' `wrap-cancel` string is TRIAGE's declared residual and is not re-filed.
· NOT RE-FILED, known and dated in TRIAGE_2026-09-14 as 0.5 candidates: rename 2/3 not reconciled from disk (an interrupt after the final swap drops the carried receipt; fails closed), notice precision, identical re-run still writing, whitespace-only change, and an operator file under importlib's exact cache name.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260915-021204-14528f5db446
  Time: 2026-09-15T02:12:04  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: levain 2026-09-15, HEAD 33be8b6 == origin/main (live `git ls-remote`). Porcelain was untracked .DS_Store x3 only; I modified nothing in the repo. Window fd88986^..33be8b6 = 14 commits, 24 files, +3777/-500. ~/Documents/levain is a symlink to ~/Briefcase/levain.
RAN: the full suite under levain's OWN .venv (py3.12), `pytest -q` → 2614 passed, 1 skipped (the LEVAIN_LIVE_SSH live test), 0 failed. It left pytest rm_rf warnings on an unreadable `.levain-activation-prev-*` temp dir from test_copy_activation_unreadabl*. That is harness litter, not filed. A scratch repro of `_check_hook_freshness` with an intact, corrupt, empty or absent receipt over an edited hook produced tonight's MEDIUM; the scratch dir was removed.
READ IN FULL, as diffs: install.py (receipt read/write, `_is_bytecode_residue` + `_BYTECODE_NAME`, `_edits_against_receipt`, `_tree_proves_pristine`, `_prune_activation_backups`, the whole `_copy_activation_tree` swap/rollback, `_report_and_rotate`, `_without_levain_keys`, `_is_levain_launcher`, the `_merge_codex_config` gate + notices), doctor.py (exit codes, `run_doctor` summary, `_check_compat_set` pending axes, `_check_hook_freshness` whole, `_check_mcp_command`), manifest.py (`anneal_invocation`, `pip_invocation`, the option-3 pre-751 lock text), update.py (confirmed `_record_lock` → `write_lock` stamps `installed.levain` with an anneal read from `resolve_anneal_bin()`, so the option-3 hint "either records the anneal levain's own interpreter imports" holds by reading), wrap.py, cli.py, the codex mcp template, TRIAGE_2026-09-14.md whole and diogenes_20260914.md. I read the swap's rollback paths by hand against TRIAGE's rounds 2-4 and found nothing beyond what TRIAGE already dates as 0.5.
NOT CHECKED: no real `levain init` / `doctor` / `update` end to end, and I did not re-run the five RESIDUE_UPGRADE_* transcripts; I did not open them. No Codex or Claude Code client launched against the `-P -m anneal_memory` registration (TRIAGE also marks this UNDISCHARGED). The three new/expanded test files (test_receipt_and_upgrade_exit.py 764 lines, test_l3_16dcb7ee_fixes.py 282, test_install.py +302/-?) were read only where I needed a fixture, and none were mutation-tested. CHANGELOG.md, README.md, pyproject.toml and the adapter READMEs were not read line by line, so no claim in the 0.4.7 CHANGELOG was checked against code. `_check_mcp_command`'s same-binary rule was not exercised against a pipx or pyenv-shim layout. The codex block regex `_CODEX_MCP_BLOCK_RE` (from 9450e75, pre-window) swallows comment lines that precede the NEXT table. With round 4's gate that now produces a backup plus a notice calling them comments "inside that block". I reasoned this from the regex and did not run it, so it was not filed. No mypy or ruff run. No concurrency test of rotation against a concurrent `init --force` (spore-1023's class). [prescription: none]
  Tags: code_review, levain, semantic
```
