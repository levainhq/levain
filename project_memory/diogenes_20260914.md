# DIOGENES OVERNIGHT — levain, 2026-09-14

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-09-14*)`
**Stated count: 4 episode(s).** Severity: LOW 1 · MEDIUM 1 — 2 of 4 episode(s) carry a severity; the other 2 are COVERAGE 1 · STILL OPEN 1
**STILL OPEN: 3** — **2 filed tonight** (measured at HEAD `acd4947`) · **1 carried**. A carried item was NOT necessarily re-checked this window; only the slot block below says which ones were.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260914-021830-3cf6749887bc
  Time: 2026-09-14T02:18:30  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: levain/install.py:2275 — the new same-store `content_changed` guard (09-09 HIGH fix, `f0d2fc3`) fires on EVERY pristine pre-spore-751 install that follows the CHANGELOG's own upgrade instruction, and tells the operator a customisation was lost when nothing but levain's own `command`/`args` changed. [quoted: `        content_changed = not replacing_unknown and not store_changed and old_dict != new_dict`] [quoted: `        emit("    It still points at the same store, but other settings in that block")`] [quoted: `        emit("    customised it, that customisation is gone.")`] — CHANGELOG.md:18 says [quoted: `**Existing installs keep their old registration until you re-run `levain init --force`.**`], and spore-751 (`3345971`, same window) changed the block's `command` from the anneal script to `{{PYTHON}}` and `args` to `-P -m anneal_memory ...`. So the parsed dicts ALWAYS differ on that upgrade, and the warning built for a real customisation fires on a guaranteed benign one. It also re-fires on any re-init from a different interpreter/venv (`python_path = sys.executable`). A cry-wolf notice about a MACHINE-GLOBAL config trains operators to ignore the one case the guard exists for.
BEHAVIOURAL, FALSIFIABLE SIGNATURE: `_merge_codex_config(<config.toml>, <HEAD mcp.template.toml rendered with sys.executable + install dir>, emit=...)` against a config.toml holding ONLY the pristine old-template block for the SAME store — `command = "/usr/local/bin/anneal-memory"`, `args = ["--db", "<inst>/.levain/memory.db", "serve"]`, no other keys — emits exactly 7 lines, the first starting `  ! Codex's GLOBAL anneal_memory block was replaced in`, the fourth ending `that customisation is gone.`, and leaves exactly one `config.toml.bak.<ns>` beside it. Observed 2026-09-14 02:2x at acd4947. No existing test covers the upgrade shape: `test_same_store_but_lost_customisation_IS_backed_up_and_announced` (tests/test_install.py:2340) plants `env`/`startup_timeout_ms`, i.e. only the true-positive case.
FIX: compare the dicts with levain-owned keys (`command`, `args`) removed, and warn "customisation lost" only when a key OUTSIDE those differs. Keep the backup if you like, but a levain-shape change on the same store should get its own truthful line ("registration updated to run anneal via <python>"). Add the pristine-old-block fixture as a no-false-alarm test. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260914-021830-f9d70bac14c2
  Time: 2026-09-14T02:18:30  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: levain/doctor.py:762 (+ :779, :789; install.py:2505, :2527; wrap.py:537; manifest.py:512) — operator-facing remediation strings still tell the human to run a bare PATH `anneal-memory`, the exact split spore-751 (`3345971`) was ruled to end. [quoted doctor.py:762: `                f"Initialize: anneal-memory --db {store} init",`] [quoted install.py:2527: `    emit(f"    Then: anneal-memory --db {store} init --schema partnership")`] [quoted wrap.py:537: `                f"  Fix:  anneal-memory --db {episodic_path} set-schema partnership"`] — the window's CHANGELOG says levain now uses [quoted CHANGELOG: `the anneal package Levain's own Python imports`], and 20 lines up doctor.py:724 was updated to the right form [quoted: `                f"Install with: {sys.executable} -m pip install anneal-memory",`]. So doctor now PROBES the interpreter's anneal but ADVISES the shell's. On the machine spore-751 describes (a stale `~/.local/bin/anneal-memory` earlier on PATH), following the advice runs `init`/`set-schema` on levain's store with a DIFFERENT anneal than the one that serves it. STRUCTURAL — the strings are what they are; nothing ran. FIX: render these as `{sys.executable} -P -m anneal_memory --db ...`, or the `resolve_anneal_bin()` path, matching :724. [prescription: reasoned]
  Tags: architecture, code_review, levain
```

---

```
[laptop/flow]   ID: diogenes-20260914-021906-d91ed16ccb5c
  Time: 2026-09-14T02:19:06  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (3), total live defect count for levain at HEAD acd49479 == origin/main (live ls-remote, start AND finish). 2 filed tonight (0 HIGH / 1 MEDIUM / 1 LOW) + 1 carried. Filed separately: 0 DRIFT (not in this count). 0 SELF-CORRECTIONS.
· FILED: MEDIUM install.py:2275 (diogenes-20260914-021830-3cf6749887bc): the same-store `content_changed` guard false-alarms "customisation is gone" on every pristine pre-751 install that runs the upgrade CHANGELOG.md:18 instructs (reproduced) · LOW doctor.py:762 + 6 sibling strings (diogenes-20260914-021830-f9d70bac14c2): remediation advice still names bare PATH `anneal-memory`, the split spore-751 ended.
· CARRIED (1), resolved against the REPO, not my store: MEDIUM web_server.py `_Handler` / init_server.py `_InitHandler` duplicated request preamble. TRIAGE_2026-09-13.md:92 records it ROUTED, not fixed, and HEAD confirms it is still live: `_host_ok` at init_server.py:190 AND web_server.py:664, `_drain` at :267 / :908, `_reject` at :261 / :919.
· CLOSED, verified against the tree and not carried: all 6 of my 09-09 findings (codex dict guard at install.py:2275 region, `_atomic_write_text` `target.resolve()`, the `unstaged_notes` test, `_pyproject_requires`, the test_web_server comment, the tools.py DRIFT clause, all present at HEAD) and 3 of the 09-07 four (copymode, the condenser plant, the tools.py citation). ⚠ Two of those closures I verified by READING the diff and TRIAGE, not by re-running their repros: the condenser test is skipped here (openhands extra absent), and the unstaged-notes mutation test I did not mutate myself.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260914-021906-1b693a3d812c
  Time: 2026-09-14T02:19:06  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: levain 2026-09-14, HEAD acd49479 == origin/main (live `git ls-remote`, start and finish). Porcelain is untracked .DS_Store x3 both ends, and I modified nothing in the repo. Window 666d9b6^..acd4947 = 14 commits, 26 files, +1775/-117. ~/Documents/levain is the same tree as ~/Briefcase/levain (`pwd -P`).
RAN: the full suite, `python3 -m pytest -q` → 2343 passed, 27 skipped, 0 failed. ⚠ It ran under /Users/phillipclapham/Briefcase/flow/venv/bin/python3 (3.13), whose anneal is the EDITABLE ~/Briefcase/anneal-memory tree (spore-1019), not levain's own .venv (3.12, site-packages anneal), so this is a green on flow's interpreter only. `resolve_anneal_bin()` probed under BOTH interpreters: each returned its own venv's `bin/anneal-memory`, matching `find_spec` origin, so the editable-sole-dist and wheel paths both resolve correctly. Scratch repro of `_merge_codex_config` against a pristine pre-751 block → the MEDIUM filed tonight (scratch dir removed).
READ IN FULL, as diffs: confinement.py, tools.py, manifest.py (resolve_anneal_bin, _pyproject_requires), install.py (codex guard, _atomic_write_text, the PYTHON substitutions, _run_anneal_cmd), doctor.py `_check_runtime`/`_check_compat_set`/`_match_store`, both `_levain_hook.py` `_anneal_json` bodies (line 226-276 read off disk), init_server.py, update.py, both adapter templates + READMEs, the CHANGELOG 0.4.6/Unreleased text, and TRIAGE_2026-09-13.md whole. Grepped every `build_policy(` / `CrownJewelsPolicy(` / `SeatbeltProvider(` caller: the localhost deny reaches the ONLY seatbelt policy path (`policy_for_conv_state`, tools.py:180). The `-P` flag is safe against `requires-python = ">=3.12"`.
NOT CHECKED: no sandbox-exec run. The `(deny network-outbound (remote ip "localhost:*"))` rendering, its LAN-IP coverage, and the sshd reproduction are the window's own MEASURED claims, and I did not re-measure any of them. No real `levain init` / `doctor` / `update` end to end, and no Codex or Claude Code client launched against the new `-P -m anneal_memory` registration. The openhands-extra tests (27 skipped) were not exercised. The two new test files (test_anneal_authority.py 227 lines, test_firing_confinement.py +319) were read by diffstat only, not line by line and not mutation-tested. AGENT_FORWARDING_OPTIONS_2026-09-13.md (212 lines) was not opened. No mypy. The TOML-escaping claim at install.py (json.dumps ensure_ascii=False → valid TOML) is reasoned only; I believe U+007F in a path would break it, but I did not run it and did not file it. `{{INSTALL_DIR}}` is still substituted unescaped into both templates; that is pre-existing and was not checked for a guard elsewhere. [prescription: none]
  Tags: code_review, levain, semantic
```
