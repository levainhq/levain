# DIOGENES OVERNIGHT — levain, 2026-09-17

**Source selector:** `episodic.py read --since 24h --tags levain  (filtered to Agent: diogenes at 2026-09-17*)`
**Stated count: 6 episode(s).** Severity: LOW 2 · MEDIUM 1 — 3 of 6 episode(s) carry a severity; the other 3 are COVERAGE 1 · COVERAGE-OPEN 1 · STILL OPEN 1
**STILL OPEN: 4** — **3 filed tonight** (measured at HEAD `839df1a`) · **1 carried**. A carried item was NOT necessarily re-checked this window; only the slot block below says which ones were.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260917-022858-2255034e095d
  Time: 2026-09-17T02:28:58  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: levain 2026-09-17, HEAD 839df1a6d197d36617b2f2a97348b50df433b61b (== origin/main? YES — after `git fetch`, origin/main resolves to the same sha, and `git ls-remote origin HEAD` was run before this write), `git status --porcelain` DIRTY BY UNTRACKED NOISE ONLY: `?? .DS_Store`, `?? levain/.DS_Store`, `?? tests/.DS_Store` (present in the session's opening snapshot, same three as 09-16). Window `58bb5ed..HEAD` = 5 commits (last night's review HEAD was 58bb5ed); dispatch said 8, and HEAD~8 would reach back into commits last night already reviewed, so the review window is the 5 unreviewed ones: eb34122 (my own routing), be16489 (code: _report_and_rotate), 76bb0a9 / 1651004 / 839df1a (README). Baseline: `STILL OPEN (4)` at HEAD 58bb5ed, a complete pool (slot + COVERAGE present). `~/Documents/levain` is a symlink to `~/Briefcase/levain`, resolved with `pwd -P`. REVIEW STARTED, NOT FINISHED — if no COVERAGE line for levain exists in this run, THIS RUN DIED and the finding list is PARTIAL. [prescription: none]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260917-023757-f52bd19a2d86
  Time: 2026-09-17T02:37:57  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: README.md:13 — THE THREE SCREENSHOTS THIS WINDOW ADDED USE REPO-RELATIVE PATHS, AND `README.md` IS THE PyPI LONG DESCRIPTION, SO THE NEXT RELEASE PUBLISHES A PROJECT PAGE WHOSE FIRST VISUAL — THE HERO DIRECTLY UNDER THE PITCH — IS A BROKEN IMAGE. The repo already decided relative links do not survive PyPI and fixed every other link for exactly that reason; the images went in beside those fixed links without it.

[quoted: `![The cockpit's Health and Cognition Trace panels — Hebbian links, graduation counts, episode counts, and a live per-wrap oscilloscope, all real numbers from the author's own long-running memory store.](docs/img/cockpit-health-trace.png)`]
README.md:183 [quoted: `![The Operate zone — Open Loops, Tray, and Keep panels, each editable from the browser.](docs/img/cockpit-operate.png)`]
README.md:187 [quoted: `![The State, Active Threads, Patterns, Decisions, and Context panels from the same demo install.](docs/img/cockpit-state.png)`]

WHY THIS IS A DEFECT AND NOT A GUESS ABOUT PyPI. (1) pyproject.toml:9 [quoted: `readme = "README.md"`] makes this file the long description PyPI renders. (2) The project's own record says relative links break there: commit `93e75fe` ("docs(readme): rewrite to current 0.3.5 reality") says in its body "In-repo links absolutized to GitHub URLs so they survive PyPI rendering". (3) At HEAD every OTHER in-repo link obeys that rule — README.md:55 [quoted: `(https://github.com/levainhq/levain/blob/main/examples/accrual/growth_timeline.md)`] and README.md:155 [quoted: `(https://github.com/levainhq/levain/blob/main/CHANGELOG.md)`]. The only three repo-relative targets in the file are the three `docs/img/*.png` images added in `1651004`. (4) MANIFEST.in does not include `docs/`, so the images are not in the sdist either — there is nothing on PyPI for a relative path to resolve to, whatever the renderer does.

This is STRUCTURAL: visible by reading, nothing to replicate. GitHub renders these correctly today, which is why a visual check on github.com passed; the failure is on the other surface this file publishes to, and it lands on the next `twine upload` with nothing in the pre-push gate that looks at it.

FIX: point the three image targets at absolute raw URLs, e.g. `https://raw.githubusercontent.com/levainhq/levain/main/docs/img/cockpit-operate.png` — that URL answered HTTP 200 to curl during this review on 2026-09-17, and `https://github.com/levainhq/levain` answered 200, so the repo is public and the raw path resolves. Pinning to a tag instead of `main` would stop a later image change from altering an already-published release's page; that is a taste call for the author. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260917-023818-e83097b31f14
  Time: 2026-09-17T02:38:18  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: README.md:151 — THE NEW SEAT SECTION SAYS THE SEAT CONSOLIDATES "ON THE SAME CADENCE" AND NAMES `--consolidate-every` AS THE KNOB FOR IT. THAT FLAG IS AN EPISODE COUNT, NOT A CADENCE, SO AN OPERATOR WHO TAKES THE SENTENCE AT ITS WORD AND PASSES A NUMBER OF SECONDS GETS A SEAT THAT EFFECTIVELY NEVER CONSOLIDATES.

[quoted: `And it consolidates its own memory on the same cadence by default (`--consolidate-every`)`]

WHAT THE CODE SAYS THE FLAG IS:
levain/cli.py:740 [quoted: `d_seat.add_argument("--consolidate-every", type=int, default=None, dest="consolidate_every",`] — its help (`levain daemon install-seat --help`, run during this review) reads "Episodes that must accumulate before the seat consolidates (default: the same threshold the human-facing wrap-nudge uses)."
levain/cli.py:1238 [quoted: `thr = f"every {every} episodes" if every is not None else "at the default episode threshold"`] — the seat's own install banner renders it in episodes.
The only range check on it among the `consolidate_every` references in cli.py is levain/cli.py:1395 [quoted: `if getattr(args, "consolidate_every", None) is not None and args.consolidate_every < 1:`], so `--consolidate-every 3600` passes that check and, by the help text, means "wait for 3,600 episodes".

The cadence in that section is `--interval` (seconds; the section's own example command at README.md:146 passes `--interval 3600`), which is exactly the reading the sentence invites: same cadence, next to a flag. STRUCTURAL, visible by reading; nothing to replicate. LOW because the default (no flag) does the right thing, and the harm needs an operator to act on the sentence.

FIX: say what it is — the seat checks after each scheduled turn and consolidates once enough new episodes have built up (`--consolidate-every N` episodes; default: the wrap-nudge threshold). Markdown, so FINDING by rule, not DRIFT. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260917-023901-b39b62dfd983
  Time: 2026-09-17T02:39:01  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: levain/cli.py:137 — `levain init --help` TELLS AN OPERATOR THAT A BLANK ANSWER IS REFUSED EVERYWHERE EXCEPT OPTIONAL SLOTS; THE VALIDATOR ACCEPTS BLANK ON EVERY SLOT BUT THE TWO NAMES. This window's README rebuild describes the real rule correctly, so the CLI help is now the one surface that disagrees.

[quoted: `"Every slot must be present; \"\" is allowed only for an optional "`]

WHAT THE VALIDATOR ACTUALLY DOES, per its own module docstring, levain/answers.py:32 [quoted: `an EMPTY value for an IDENTITY slot (``OPERATOR_NAME``, ``ENTITY_NAME``) is an`] and levain/answers.py:33 [quoted: `ERROR. Blank is allowed everywhere else — including other non-optional fields —`], with the reason given there: the terminal interview and web form both accept a blank, so the file surface must not be stricter. README.md:106 (changed this window) agrees with the code [quoted: `meaning "skip this," exactly as pressing enter does in the terminal — except for`].

MEASURED, NOT READ. At HEAD 839df1a I ran `levain init --answers-template` (17 slots), set `OPERATOR_NAME` and `ENTITY_NAME`, left the other 15 as `""` — including `LOCATION`, `COGNITION`, `INTERESTS` and `WORK`, which the template guide lists WITHOUT its `[may be ""]` marker — and ran `levain init --adapter claude-code --answers <file> --path <tmp>`. It installed and printed its "Open in Claude Code" next steps. Help text says that file should have been refused.

⚠ SAME DISAGREEMENT, SECOND SURFACE: the `--answers-template` field guide on stderr prints `[may be ""]` only for fields where `is_required` is false — levain/answers.py:301 [quoted: `req = "" if is_required(f) else "  [may be \"\"]"`] — so it also implies that `LOCATION` et al. cannot be blank. `is_required` never refuses anything at init: its only other caller, levain/doctor.py:442, uses it to build an [OK] line. I ran `levain doctor` on the same blank-field install and it printed `[OK] seed content (answers present): 17 recorded field(s); identity present; left blank: BOUNDARIES, COGNITION, COMMUNICATION, INTERESTS, JOB, LOCATION, ROLES, SUBSTRATE, WORK (a thinner seed, not a broken one)` — so doctor agrees with the validator, and only the help and the guide disagree.

STRUCTURAL; the fix is text. Help text is a runtime string, not a comment, so it is not DRIFT; cli.py was not touched this window and this is pre-existing, found while checking a README claim. LOW: the help is stricter than the code, so it fails safe — an operator fills fields they did not need to.

FIX: change the help to the validator's rule (every slot present; `""` allowed on every slot except `OPERATOR_NAME` and `ENTITY_NAME`), and either rename the guide's marker to mean "optional section/field" or drop it for non-identity slots. The alternative — make the validator strict — would reverse a documented decision in answers.py and is the author's call, not mine. [prescription: reasoned]
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260917-023927-9a7ed730fa46
  Time: 2026-09-17T02:39:27  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (4), total live defect count for levain at HEAD 839df1a == origin/main (checked with `git ls-remote origin HEAD` at start and at finish). 3 filed tonight (0 HIGH / 1 MEDIUM / 2 LOW) + 1 carried. Filed separately: 0 DRIFT (not in this count). 0 SELF-CORRECTION.

· FILED: MEDIUM `README.md:13` (diogenes-20260917-023757-f52bd19a2d86): the three new `docs/img/*.png` screenshots use repo-relative paths in the file that is the PyPI long description, after `93e75fe` absolutized every other in-repo link "so they survive PyPI rendering"; the next release publishes a broken hero image. · LOW `README.md:151` (diogenes-20260917-023818-e83097b31f14): "consolidates ... on the same cadence (`--consolidate-every`)" — that flag counts episodes, not time. · LOW `levain/cli.py:137` (diogenes-20260917-023901-b39b62dfd983): `init --answers` help says `""` is allowed only on optional slots; measured, the validator (and doctor) accept blank on every slot except the two names, as the README and answers.py say.

· CLOSED, all three from 09-16, each checked against the repo at HEAD: (1) MEDIUM `install.py:2289` — `be16489` branches `_report_and_rotate` three ways. I made a mutant in a scratch worktree (`elif prior_status == "absent":` -> `elif True:`) and ran `test_receipt_and_upgrade_exit.py` + `test_install.py`: 2 failed (the corrupt and empty cases of the new test), 184 passed. Revert kills it. (2) MEDIUM `CHANGELOG.md:7` — `[Unreleased]` now has a `### Changed` section for doctor's 6 -> 1 exit on a corrupt/empty receipt, plus the `init --force` notice. (3) LOW `TRIAGE_2026-09-14.md:46` — a dated "⚖ AMENDED, Phill, 2026-09-15" pointer now follows the C6 section.

· CARRIED (1): MEDIUM — `web_server.py` `_Handler` / `init_server.py` `_InitHandler` duplicated request preamble. `git log 58bb5ed..HEAD` on both files is empty; `_host_ok` / `_reject` / `_drain` still exist in both (web_server.py:664/919/908, init_server.py:190/261/267); no TRIAGE file after 09-13 closes it. Blast radius still the smaller one noted 09-16 (both `_host_ok` delegate to the shared host check); I did NOT re-diff the bodies tonight, only confirmed the files did not change.

· NOT RE-FILED, as on 09-16: the repo's own dated 0.5 candidates and KNOWN RESIDUALs in TRIAGE_2026-09-14, and the tray items spore-1005 / spore-754 / spore-1023. 4 is my pool, not the repo's total.
  Tags: code_review, levain, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260917-023938-fae85c02316c
  Time: 2026-09-17T02:39:38  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: levain 2026-09-17, HEAD 839df1a == origin/main, window 58bb5ed..HEAD = 5 commits, porcelain untracked .DS_Store noise only, repo left as found (the scratch worktree I added for the mutant was removed; `git worktree list` shows only main). RE-DERIVED BY RUNNING: full suite `pytest -q` = 2420 passed, 27 skipped (openhands extra absent), against `levain/__init__.py` in this tree (0.4.8.dev0). A mutant of `be16489`'s branch fails its new test in 2 cases. `--help` for all 13 top-level commands plus `daemon install-seat` / `would-install` / `restart`, checked against the README's command list, flags, defaults (serve 7420, glm-5.2:cloud, base-url localhost:11434, interval 3600) and exit codes (seat 4 on a halt, 5 on the wall-clock bound, doctor 1/6). A real `levain init --adapter claude-code --answers` run with 15 blank slots, then `levain doctor` on it. curl: raw.githubusercontent image path and the GitHub repo both 200. READ, NOT RUN: `be16489` diff in full; `read_activation_receipt_file`, `_write_activation_receipt`, `_copy_activation_tree`'s receipt/rotate path, `_report_and_rotate`; README.md whole (270 lines) and its window diff; the gate's `resolve_gate_mode` and the file-editor `view` afferent rule; install-seat's ungated warning in cli.py; `scripts/install_hooks.sh` + `scripts/hooks/pre-push` header; `examples/accrual/growth_timeline.md` sections and line counts against README:55; pyproject `readme`/`requires-python`/anneal pin; MANIFEST.in; all three screenshots viewed — the demo-install ones show only invented "Ridge" content, the hero shows counts only, no memory text.
NOT CHECKED: whether the hero screenshot's numbers really come from the author's store (not compared to `~/.anneal-memory`); whether PyPI renders relative images — reasoned from the repo's own `93e75fe` note and MANIFEST.in, not tested by building and rendering the long description; README:139's macOS-only sandboxed shell and non-macOS file-editor-only claim beyond one grep of confinement.py; README:151's "unattended consolidate is refused crystallize" beyond the `wrap --help` text; README:166's doctor check list; README:239's `levain.kernel` / `make_server(...)` claims (kernel.py not opened); README:257-259 roadmap statements (bwrap design, `/turn`) not checked against any design doc; `levain serve --write` / tui / serve-app not launched; the new test's other assertions not mutated beyond one mutant; the carried duplication's bodies not re-diffed. No L3 lineage run on the window. [prescription: none]
  Tags: code_review, levain, semantic
```
