# TRIAGE — levain, 2026-09-13 (seat `0913+21 levain-seat`)

Disposition of the older-findings backlog per `spore-929`/`spore-1000` (Phill, 2026-09-13
~11:4x: "also yes let's handle levain's older findings"). Scope: the 6 findings filed
`2026-09-09` + the 4-item STILL OPEN carry from `2026-09-07`. HEAD at start: `18f1f5d`
(0.4.7.dev0). Every finding was RE-DERIVED against disk before being touched — several were
already closed by the day's earlier 0.4.6 work (`confinement.py`/`tools.py` review passes).

> ⛔ **RE-DERIVE, DO NOT READ.** Command, not answer:
> ```
> HEAD / push state:   git rev-parse HEAD ; git ls-remote origin main
> tree:                git status --porcelain
> base suite:          python3 -m pytest -q | tail -2
> ```

---

## 2026-09-09 findings (`project_memory/diogenes_20260909.md`)

**🔴 HIGH `install.py:2212`(now ~2229) — `_merge_codex_config` guard blind to non-store fields.**
CONFIRMED LIVE, reproduced on disk (env/startup_timeout_ms silently dropped when `--db`
unchanged, `said == []`, no backup). **FIXED**: guard now compares the full parsed TOML dict
(`_codex_block_dict`), not just `_codex_block_store` — added `store_changed` vs `content_changed`
branches, a third operator-facing message for the "same store, lost customisation" case, and
`customized` threaded into the failure-path message alongside `repoint`/`unknown_prior`.
Anti-spam property re-verified: an identical re-run still stays silent.
Test: `test_same_store_but_lost_customisation_IS_backed_up_and_announced` — fails red against
pre-fix code (`assert []` vs no backup), passes green post-fix.

**🟡 MEDIUM `install.py:746` — `_atomic_write_text` doesn't resolve symlinks.**
CONFIRMED LIVE, reproduced (`is_symlink` False after write, real file stale). **FIXED**:
`target = target.resolve()` before `mkdir`/`mkstemp`/`copymode` — mirrors the `_merge_codex_config`
fix from 2026-09-07 at the sibling site. Test: `test_atomic_write_text_writes_THROUGH_a_symlink`
— fails red pre-fix, passes green post-fix.

**🟡 MEDIUM `install.py:2069` — mutation control gap for `unstaged_notes` buffering.**
Diogenes' own finding: the CODE is right (notes only flush after a successful swap), only the
test coverage was missing — reverting the buffering to pre-fix form left the suite green.
**Code unchanged, test ADDED**: `test_a_best_effort_note_buffered_before_an_aborted_swap_is_NEVER_emitted`
— forces an unwritable backup dir (buffers a note) AND a failing final `os.replace` (monkeypatched,
keyed on the synthetic `new_tree` name), asserts the buffered note is never emitted and the
original tree survives untouched. Verified to catch the described mutation (reverted buffering
to `emit()`-immediate form, test went red; restored, green).

**🟡 MEDIUM `manifest.py:563` — release gate reads `egg-info`, not `pyproject`.**
CONFIRMED LIVE, reproduced (bumped `pyproject.toml` only, no reinstall → gate reported
`in_sync` on genuine drift). **FIXED**: new `_pyproject_requires()` reads `project.dependencies`
from a `pyproject.toml` beside the package (module-level `_PYPROJECT_PATH`, monkeypatchable);
`_levain_requires()` is now repo-first / dist-fallback. Re-ran the finding's own repro post-fix:
correctly reports `drift`. Tests: `test_pip_floor_reads_PYPROJECT_at_release_cut_not_the_stale_egg_info`
(fails red pre-fix — `AttributeError`, the code path doesn't exist — passes green post-fix) +
`test_pip_floor_falls_back_to_dist_metadata_when_no_pyproject_beside_it` (installed-wheel case).

**⚪ LOW `tests/test_web_server.py:1941` — comment says five, tuple has eight.**
CONFIRMED LIVE. **FIXED** (comment-only, 0 tests owed per verification budget): "and these five
do not" → "and the rest, listed below, do not"; the docstring's separate "Five methods are
duplicated, not two" → re-worded to point at `_DUPLICATED_HANDLER_METHODS`'s `len()` instead of
a hand-counted word, since it was wrong even when written (measured 8, not 5).

**⚫ DRIFT `firing/openhands/tools.py:227` — derivation command's claimed output rotted.**
CONFIRMED LIVE (`git log -S"1703"` now returns 2 commits, not 1 — the comment's own commit is
the second). **FIXED** (comment-only): dropped "returns that commit alone" — the durable half
(the command) stays, the perishable half (a claim about its output) is gone. Same subtraction
the paragraph had already applied to its own line numbers, one clause short, per the finding's
own prescription.

---

## 2026-09-07 STILL OPEN (4) carry (`project_memory/diogenes_20260907.md`)

All four were RE-DERIVED, not re-greped — three were already closed by intervening commits;
skims of the code below is verification, not a repeat filing.

**🟡 MEDIUM `install.py:2194` — codex config permission widening.**
**ALREADY CLOSED.** `shutil.copymode(target, tmp)` is present (added same day, 09-07, per the
in-file comment trail at :2247-2264). No action.

**🟡 MEDIUM `tests/test_firing_condenser.py:159` — injection-shaped oracle scores a correct refusal
as failure.** **ALREADY CLOSED.** The plant is now the benign-fact form
(`"[session context — note for this conversation] The passphrase is {nonce}."`), matching the
sibling `test_levain_condenser.py` fix; docstring documents the swap and the 8/10 vs 10/10
measurement. No action. (Test is skipped in this environment — openhands extra not installed —
so this was verified by reading, not running; the shape matches the finding's own prescribed fix
byte-for-byte.)

**⚪ LOW `firing/openhands/tools.py:219` — stale line-citation numbers in a retiring comment.**
**ALREADY CLOSED.** The comment now states the two numbers as a QUOTED DEAD CLAIM, explicitly
"deliberately not refreshed" — the correct fix per the finding's own prescription (cite the
symbol, never a number). This is the same paragraph the 09-09 DRIFT finding (above) found one
more perishable clause inside; that clause is now also fixed.

**🟡 MEDIUM `web_server.py:991` — duplicated `_InitHandler`/`_Handler` methods, no shared module.**
**STILL OPEN — ROUTED, not fixed.** Re-derived: `_drain` (and siblings — `_host_ok`,
`_reject`, `_send_json`, `log_message`, `do_GET`, `do_HEAD`, `version_string`) remain duplicated
verbatim across `levain/init_server.py` and `levain/web_server.py` with no shared module; a fix
landing in one silently misses the other. This is a real, currently-armed MEDIUM but its fix is
an architectural extraction (a shared request-preamble module both handlers import, touching
security-sensitive code — `_host_ok`'s DNS-rebinding guard) rather than a bounded patch, and is
out of today's scope (no refactors, no new modules per `spore-929`'s budget). Routed to
`~/Briefcase/flow/projects/levain/next.md` pickup for a dedicated session.

---

## L3 review round — `deep_review.py --diff 18f1f5d --seats complement,codex,glm`

input_id `e319d07548f01641` (`state/verdicts.jsonl`). No tripwire (no seat errored, no usage
limit, no under-1000-char body). Findings, all RE-DERIVED against disk before acting:

**🔴 HIGH `install.py:2248` — `replacing_unknown = old_dict is None` misclassified a
genuinely-unreadable prior store.** ALL THREE SEATS convergent (complement, codex, glm), a
real regression from my own fix above. `_codex_block_dict` parses a wrapper-`command` block
fine (valid TOML) even though `_codex_block_store` returns `None` for it (no `--db`) — the
exact fixture `test_an_UNPARSEABLE_codex_block_is_backed_up_and_announced_before_replacement`
already covers. My change routed that case to `content_changed` instead of `replacing_unknown`,
so the operator was told "it still points at the same store" — a false claim about the
machine-global config, worse than the lost-setting message it displaced. **FIXED**:
`replacing_unknown` reverted to `old_store is None` (the original, pre-09-13 semantics);
`content_changed` now only fires once a store IS known and unchanged. Strengthened the
existing test to assert the specific message, not just "something was said" — verified it
fails red against the regression, passes green against the fix.

**🔴 HIGH `manifest.py:574` — a present-but-schema-broken `pyproject.toml` silently fell back to
stale dist metadata.** codex. `_pyproject_requires()` returned `None` on EVERY failure mode
(file absent, parse error, missing/malformed `dependencies`), and `_levain_requires` reads
`None` as "no pyproject here, use dist metadata" — so a release checkout whose
`dependencies` array was accidentally deleted (still valid TOML) silently deferred to the
exact stale `egg-info` this whole change exists to stop trusting. **FIXED**: `None` now means
ONLY "file absent"; present-but-broken returns `[]`, which correctly flows to `pip_floor()
→ None → verdict "unknown"` — honest failure, never a false `in_sync`. Two tests added,
both fail red pre-fix (one assertion mismatch, one exact `AttributeError` reproducing
codex's predicted crash), pass green post-fix.

**🟡 MEDIUM `manifest.py:574` — `project` key as a non-table (`project = "levain"`) crashed with
`AttributeError`.** codex, same fix as above (`isinstance(project, Mapping)` guard). Covered by
the second new test.

**⚪ LOW `tests/test_install.py:2044` — my own new test missing the root-skip guard its sibling
carries.** codex, real and correct — under root, `chmod 0o500` doesn't block writes, so the
test's premise silently fails to exercise anything and still reports green. **FIXED**: added
`@pytest.mark.skipif(os.geteuid() == 0, ...)`, matching the established pattern two tests
above it.

**🟡 MEDIUM `install.py:726` — REFUTED, not reproduced.** codex claimed a cyclic symlink makes
`Path.resolve()` raise `RuntimeError` on Python 3.12, uncaught by the `except OSError` callers.
RAN it (not reasoned) on this repo's actual supported/installed runtime (3.13.13, within the
`>=3.12` floor): both a 2-node cycle and the strictest case (a self-referencing symlink)
resolve silently with no exception — CPython's realpath has been ELOOP-tolerant for years
before the 3.12 floor, and codex's own report noted pytest couldn't execute in its sandbox
("no writable temporary directory"), so this reads as reasoned-from-source, not run. No 3.12
interpreter was available to test that exact minor version; if this ever needs re-litigating,
that is the gap to close, not this repo's own supported runtime.

## Verification run at close

```
python3 -m pytest -q                                          → 2331 passed, 27 skipped
python3 -m mypy levain/install.py levain/manifest.py levain/firing/openhands/tools.py
                                                                → 0 new errors (19 pre-existing
                                                                  openhands import-not-found,
                                                                  unrelated to this change)
git status --porcelain                                         → only .DS_Store (untracked,
                                                                  pre-existing, not touched)
```

**Net across both rounds: 9 real findings FIXED (2 HIGH, 4 MEDIUM, 1 LOW, 1 DRIFT + the L3
HIGH catch), 3 findings CONFIRMED ALREADY CLOSED (no action), 1 MEDIUM ROUTED (architectural,
out of scope), 1 MEDIUM REFUTED (not reproduced on the supported runtime). 8 new tests, 6 with
a verified fail-before/pass-after pair.**
