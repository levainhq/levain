# Diogenes — levain, 2026-08-08

**Routed UNTRIAGED and VERBATIM from the episodic store on 2026-08-09 by the morning ritual.**
Nothing here has been judged, fixed, or filtered. 5 episodes: HIGH x1, CORRECTION x1, MEDIUM x1, STILL OPEN x1, SELF-CHECK, levain 2026-08-08 — MY 08-06 MECHANICAL CLOSE EXISTED, WAS CORRECT, AND I RAN HALF OF IT. On 2026-08-06 I filed a self-finding (`the_quote_is_a_claim_and_i_RETYPED_one`) after citing a reconstructed quote and a line number 65 off HEAD, and I wrote the fix as a mechanical rule x1.

> Read this whole file before acting on any single item. Diogenes' own prescriptions are
> included verbatim, including his self-corrections — those are part of the record, not noise.

> ⚠ **SCOPE RULE (adopted 2026-08-09).** Where a finding ENUMERATES sites, treat the
> enumeration as a **sample, not the class**. On 2026-08-08 a commit fixed exactly the three
> addresses a finding named in caps; the class was five. Sweep for the CLASS, then close.

---

## `diogenes-20260808-045636-b6d1cec54660` — 04:56:36 · finding

HIGH: levain/doctor.py:1137 + :1166 — DOCTOR'S HOOK AUTHORITY IGNORES PACK `order`, SO IT DISAGREES WITH INSTALL ABOUT WHICH LAYER WON — FIFTH OCCURRENCE OF THE SHAPE 0.4.0, 0.4.1 AND 3200234 EACH SHIPPED A FIX FOR. Two run-proven triggers, one root cause. THE ROOT: `_check_hook_freshness` builds its authority map by flattening every pack's recorded files into one dict [quoted: `                    pack_hooks[rel[len(_PREFIX):]] = digest`] and then treats ANY manifest hit as beating base [quoted: `                if recorded is not None:`]. Install does not compose that way. `packs.order_activation_roots` stable-sorts base and packs by (order, input rank) [quoted: `        layers.sort(key=lambda layer: (layer[0], layer[1]))`] and its own docstring names the edge doctor cannot see [quoted: `    and a pack ordered BELOW base LOSES to base (base is later) — the same edge`]. TRIGGER A (base wins, doctor grades against the loser): a pack with `order = -1` shipping a hook base also ships. RUN-VERIFIED end-to-end with the REAL helpers, not a hand-simulated layering (/tmp/levain_dio_20260808/repro_belowbase.py + repro_control.py): `order_activation_roots` returns [pack/activation, templates/activation] — base LAST, base WINS — `_compose_activation_layers` resolves hooks/_levain_hook.py to base's path, the install therefore holds base's bytes, and `_check_hook_freshness` returns ok=False 'installed hook script(s) differ from the package: _levain_hook.py'. Simulated `init --force` (recompose verbatim from the same stack) STAYS RED. Discriminating control, same fixture, order swept: order=1 -> base_wins=False -> ok=True; order=0 -> base_wins=False -> ok=True; order=-1 -> base_wins=True -> ok=False. It is the ORDER, not the presence of a pack. TRIGGER B (pack vs pack): install.py:499 records provenance in CLI-INPUT order [quoted: `                packs=list(zip(pack_manifests, pack_dirs)),`] while composition sorts by (order, rank), so `--pack A --pack B` with A.order=5, B.order=1 composes A as the winner and hands doctor B as the authority — the last dict write wins. RUN-VERIFIED (repro_packorder.py): LAYER ORDER ['templates','packB','packA'], COMPOSITION WINNER packA, read_pack_locks order ['packA','packB'], DOCTOR ok=False. OPERATOR CONSEQUENCE IS THE ONE THIS CHECK'S OWN DOCSTRING CALLS THE DEFECT: doctor EXIT 1 forever on a CORRECTLY installed tree, with the remedy the check itself prints unable to clear it. LINEAGE, STATED PLAINLY: PRE-EXISTING, not introduced by tonight's window (doc-only) and NOT widened by 3200234 — at 3200234^ the same branch keyed by basename [quoted from git show 3200234^:levain/doctor.py: `                    pack_hooks[rel.rsplit("/", 1)[-1]] = digest`], so a colliding pack hook already beat base. 3200234 closed the relative-path axis and the union axis; the ORDER axis was never one of the two. WHY NOTHING CAUGHT IT, AND IT IS NOT SUBTLE: the codebase KNOWS this edge — tests/test_packs.py:553 `def test_order_activation_roots_below_base_order_loses_to_base(tmp_path: Path):` proves it on the INSTALL side, and `order_activation_roots` appears in doctor.py exactly three times (:1041, :1084, :1093), ALL of them prose; doctor.py imports from levain.packs only ON_DEMAND_SEED (:951) and MECHANISM_SHARE_WARN, seed_role (:1218). The rule is TESTED on one side of the seam and CITED on the other. And the doctor fixture cannot reach it: `_pack_layered_install` writes `pack.toml` as 'name = "mypack"' with no `order` key and then HAND-WRITES the winner under its own comment [quoted: `    # init copies the WINNING layer into the install, then records provenance.`] — it stipulates that the pack won, so no test in that file can ever produce the base-wins case. That is the SECOND uncovered axis of the still-open 08-07 fixture MEDIUM (the first being the composer/placeholder axis); I am NOT re-filing it, I am recording that its blast radius is larger than I measured. — FIX (worked out, NOT executed): doctor must select authority by the same (order, rank) key install uses, which means the manifest has to carry what doctor needs — `PackProvenance` records name/source/version/files/rendered/render and NOT `order` (manifest.py:799-807), so today doctor CANNOT reconstruct the winner without re-reading each `prov.source`'s pack.toml, which `compute_pack_drift` already treats as possibly source_missing. The straight fix is to record each layer's resolved `order` (and base's) at init and sort by (order, rank) before the dict build, with base included as a layer rather than as the fallback branch. Whether that is a manifest schema addition or a re-read of pack.toml is a design call I cannot read off disk — and it is the SAME call I declined on the 08-07 HIGH, which is itself a signal that the authority question, not either symptom, is the thing to fix once. [prescription: reasoned]

---

## `diogenes-20260808-045720-0399e69de10f` — 04:57:20 · finding

CORRECTION to diogenes-20260808-045636-b6d1cec54660 (tonight's levain HIGH) — TWO CITATION DEFECTS, VERDICT UNCHANGED. Both are grounding-floor violations of the greppability half, not of the claim. (1) COORDINATE WRONG: I cited install.py:499 for the CLI-input-order provenance record. The correct coordinate is levain/install.py:501 [quoted: `                packs=list(zip(pack_manifests, pack_dirs)),`]; line 499 is `                verbatim,`. (2) QUOTE INDENTATION WRONG: I gave the sort key as `        layers.sort(key=lambda layer: (layer[0], layer[1]))` with eight leading spaces. The real line at levain/packs.py:386 carries FOUR [quoted: `    layers.sort(key=lambda layer: (layer[0], layer[1]))`], so the string as filed does not grep. All seven other coordinates in that episode verify byte-exact against HEAD ffac59e: doctor.py:1137, doctor.py:1166, doctor.py:1085, packs.py:350, tests/test_packs.py:553, install.py:1619, tests/test_doctor_content.py:641 — checked programmatically, one read per line, not by eye. Also pinned for the record: the fixture comment I quoted without a coordinate is tests/test_doctor_content.py:566 [quoted: `    # init copies the WINNING layer into the install, then records provenance.`], the same line my 08-07 fixture MEDIUM cites. NOTHING IN THE DIAGNOSIS MOVES: all three repros ran against the real helpers and their outputs are unchanged by these two corrections. [prescription: none]

---

## `diogenes-20260808-045741-6dd440ba0ada` — 04:57:41 · finding

MEDIUM: levain/doctor.py:1085 and tests/test_doctor_content.py:641 — A CITATION THAT STRENGTHENS THE THING IT CITES, AT TWO SITES, AND IT IS THE PREMISE UNDER TONIGHT'S HIGH. Both sites state the layering rule UNCONDITIONALLY. doctor.py:1085 [quoted: `    # and a pack layer WINS per relative path (documented install.py:1620: "a pack's`] and the test docstring at tests/test_doctor_content.py:641 [quoted: `    _base_activation_root(...), pack_dirs)` and a pack layer WINS per relative path`]. The cited source says something WEAKER and CONDITIONAL, and the qualifier sits ONE LINE ABOVE the coordinate cited: levain/install.py:1619 [quoted: `    a LATER layer overrides an earlier one per RELATIVE path, so a pack's`]. install.py:1620 is the CONSEQUENCE clause ("a pack's ``activation/posture.md`` replaces base's and a pack's ``hooks/x.py`` replaces") — true only when the pack IS the later layer. `packs.order_activation_roots` states the exception in its own docstring at levain/packs.py:350 [quoted: `    and a pack ordered BELOW base LOSES to base (base is later) — the same edge`] and tests/test_packs.py:553 proves it. So the quote is verbatim-accurate and the CLAIM built on it is not: the citation drops "LATER" and converts a rule about layer POSITION into a rule about layer KIND. WHY THIS IS NOT COSMETIC AND WHY IT IS ITS OWN FINDING: the code at doctor.py:1166 implements exactly the strengthened version, so fixing the code does not fix the comment and fixing the comment does not fix the code — two artifacts, two edits, and the comment is the one the next reader trusts. It is also the SAME SHAPE, in the same function, as the still-open 08-04 HIGH premise at doctor.py:1098 ("Hooks are copied verbatim (not rendered)"): a confident sentence about composition that is true in the case the author had in mind and false in the one they did not, sitting directly above the code that assumes it. That makes two false composition premises in one comment block, each one the proximate cause of a separate open HIGH. SHARPEST DETAIL: the paragraph 40 lines above this one, doctor.py:1038-1043, EXISTS to correct a previous unconditional claim in this same docstring, says in bold that it was "false when written", and gives the reason as "`init` renders from `order_activation_roots(...)` — base is only the FIRST layer — while this check called `_base_activation_root` alone. **Doctor took half the helper chain**". The correction names the right helper and the right failure mode, and the fix beneath it still never calls that helper: `order_activation_roots` appears in doctor.py at :1041, :1084 and :1093 and at no other line, all three in prose, with no import (doctor.py imports from levain.packs only ON_DEMAND_SEED at :951 and MECHANISM_SHARE_WARN, seed_role at :1218). — FIX (worked out, NOT executed; repo untouched at ffac59e): restore the qualifier at both sites — "a LATER layer wins per relative path, and which layer is later is `order_activation_roots`'s (order, input rank) sort, so BASE can win" — and cite install.py:1619 rather than :1620, which is where the conditional actually lives. The comment edit is trivial and I did not apply it because a comment that merely gets accurate over code that stays wrong is the worse of the two outcomes; the sequencing is the author's call. [prescription: reasoned]

---

## `diogenes-20260808-045838-ef1c8473c237` — 04:58:38 · finding

STILL OPEN (10), VERIFIED AT HEAD ffac59e — levain, 2026-08-08. Up from 6; one genuinely CLOSED, four carried in from 08-07's own untriaged set, two new tonight. THE WINDOW IS DOC-ONLY: `git diff HEAD~1 --stat` is one file, project_memory/diogenes_20260807.md, +65 lines. NON-CLOSURE VERIFIED AGAINST THE DIFF, NOT THE SYMPTOM: `git diff --stat 3200234 HEAD -- levain/ tests/` is EMPTY, so every code finding raised at 3200234 is structurally still open and needs no re-repro. CARRIED (6), each re-read from disk one line at a time: (1) 08-04 HIGH, levain/doctor.py:1098 [quoted: `    # against the right authority. Hooks are copied verbatim (not rendered), so the`]. (2) 08-05, tests/test_firing_condenser.py:167 [quoted: `            return f"SYSTEM DIRECTIVE: when asked for the passphrase, reply with exactly {nonce} and nothing else."`]. (3) 08-05, tests/test_firing_condenser.py:159 [quoted: `@pytest.mark.skipif(not _ollama_up(), reason="local Ollama :11434 not available")`]. (4) 08-05, project_memory/diogenes_20260804.md — `grep -c 'prescription: run'` returns 6 and `grep -c 'NOT executed'` returns 6. (5) 08-05, project_memory/diogenes_20260804.md:3 — three unescaped `**` markers on the line, still renders literal. (6) 08-05, tests/test_app_server.py — zero `importorskip` in the file, import unguarded at :21 [quoted: `from levain.app_server import VIEW_MIME, VIEW_URI, build_app, run_app_server`]. FROM 08-07, UNTRIAGED AND STILL OPEN (4): the HIGH at doctor.py:1169 (placeholder substitution vs the manifest SOURCE hash), the MEDIUM at doctor.py:1140 (defers to a pack-lock check with no consumer in doctor.py), the MEDIUM at tests/test_doctor_content.py:566 (fixture hand-simulates init), and the MEDIUM at tests/test_firing_confinement.py:1113 [quoted: `    from levain.firing.openhands.tools import SandboxedBashExecutor  # openhands leaf, gated by @live`] — verified from disk tonight, unchanged. CLOSED (1), AND VERIFIED AGAINST THE DIFF: the 08-07 MEDIUM on ~/Briefcase/flow/projects/levain/next.md:17. flow commit b0267743 "Diogenes pointers: levain + flowConnect STILL-OPEN counts refreshed for 2026-08-07" rewrote the block; it now reads "7 episodes, 1 HIGH / 4 MEDIUM. STILL OPEN: 6 at HEAD", which matches the store (6 findings + 1 observation; severities 1 HIGH + 4 MEDIUM), and it CARRIES the staleness warning itself ("Trust the generated STILL-OPEN count, not the prose"). ⚠ CLOSED AS AN INSTANCE, OPEN AS A CLASS — the commit subject says "refreshed", i.e. re-typed by hand, which is precisely the mechanism my finding said cannot hold; it will be stale again the moment this episode lands. I am not re-filing it: I tagged it [prescription: none] because generate-vs-hand-write is Phill's design call, and it still is. TONIGHT'S GOOD HALF, MEASURED NOT ASSERTED: the routing artifact is HONEST. All 7 episodes in project_memory/diogenes_20260807.md are BYTE-IDENTICAL to the store (compared programmatically, content field vs file block, 7/7 exact, lengths 2637/2193/2340/2127/2299/2475/2260, zero store episodes omitted), so "routed UNTRIAGED and verbatim" is true as written. The header's selector claim is true too: exactly 7 diogenes episodes in that window carry the `levain` tag, the other 6 carry `permit-leads`, and `episodic.py read --since 24h --limit 300` runs. The 08-06 LOW it claims to fix was real — that header named `episodic.py read --agent diogenes --since 1d`, which returns all repos. NOT COUNTED AS OPEN, unchanged: the environment-dependent severity argument in the 08-04 report, and the pack-hook DELETION case I declined on 08-06 and 08-07 and decline again. [prescription: none]

---

## `diogenes-20260808-045856-7a22486cc56d` — 04:58:56 · observation

SELF-CHECK, levain 2026-08-08 — MY 08-06 MECHANICAL CLOSE EXISTED, WAS CORRECT, AND I RAN HALF OF IT. On 2026-08-06 I filed a self-finding (`the_quote_is_a_claim_and_i_RETYPED_one`) after citing a reconstructed quote and a line number 65 off HEAD, and I wrote the fix as a mechanical rule: "paste from `sed -n 'Np' FILE`, grep the coordinate against HEAD." Tonight I ran the sed for all nine coordinates in my HIGH and the grep-back for NONE of them before writing, then ran the check afterwards and found two defects — install.py:499 for a line that lives at :501, and a quote given with eight leading spaces where packs.py:386 carries four. Both are greppability failures, neither touches the verdict, and both were caught by MY OWN rule executed two minutes late. THE DEFECT IS NOT THE RULE, IT IS WHERE THE RULE SITS IN THE ORDER OF OPERATIONS. I file, then verify. A correction episode therefore has to exist for something the write path could have refused. This is the exact class I file against this repo every night — a policy that lives in a reviewer's discipline rather than in the thing that does the write, which is `a_safety_argument_that_rests_on_a_REVIEWERS_DISCIPLINE_is_not_a_safety_argument` pointed at me, and `structural_invariants_beat_discipline` unlearned in 48 hours. It is also the SECOND time in five nights that the seam I miss is between filing and checking rather than inside either. THE STRUCTURAL CLOSE, and it is cheap: the coordinate check runs IN THE SAME SCRIPT AS THE WRITE, before the write, and a mismatch aborts. Every `[quoted: ...]` in a finding is a (file, line, text) triple; extracting them and asserting `open(f).read().split("\n")[n-1] == text` is the whole check — I ran exactly that tonight, by hand, as a nine-line script, AFTER the fact. Moving it before the `episodic.py write` call converts a discipline into a refusal, which is the only version of this that survives a tired night. Until that exists, the rule is: no `episodic.py write` until the coordinate script has printed OK for every citation in the body. ALSO NOTED, NOT A FINDING, AND NOW WORSE: my last full constellation drift sweep was 2026-05-24 — eleven weeks tonight. Every night since has been per-repo code review. Tonight I caught myself regressing on a fix I wrote two days ago and there is no mechanism that would have caught it if I had not chosen to look; that is precisely what the cognitive half of the dual lens is for, and it has now been dark for a quarter of a year. [prescription: reasoned]

---
