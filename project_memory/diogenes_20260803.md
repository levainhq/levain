# Diogenes — levain — 2026-08-03 overnight

**4 finding(s), routed UNTRIAGED.** Verbatim from the episodic store, newest review, severity-ordered. Each carries Diogenes' own `[prescription: run|reasoned|none]` tag — `run` means the fix was executed and verified, `reasoned` means it was not.

⚠ **TRIAGE AGAINST DISK BEFORE ACTING.** Reviewers hallucinate, and diff-scoping produces false positives. Nothing here has been verified by a human.

⚠ **THE ROLL-UP THAT ANNOUNCED THESE MISLABELLED ITS PER-REPO NUMBERS** (corrected 2026-08-03, after a first pass of this note overstated it). The inbox summary read *"20 finding(s) across flow(19), wisp(4), levain(2), permit-leads(1)"*. **The headline 20 is CORRECT.** The parenthetical is not findings — it is **COMMITS REVIEWED**: `diogenes_code_review_laptop.py` builds `reviewed.append((name, commits))` and rendered it with the same `f"{n}({c})"` form the stderr line uses correctly under "Reviewing N active repo(s)". Dropped into a findings sentence, the identical rendering silently changed meaning — so the list could not sum to its own headline (19+4+2+1 = 26 ≠ 20). Actual findings per repo: **flow 6, wisp 6, levain 4, permit-leads 4** (= 20). Fixed at the source; the summary now reports per-repo FINDINGS and labels the commit counts separately.

⚠ **AND THE FIRST VERSION OF THIS NOTE SAID "21 across flow(7) … every per-repo number AND THE TOTAL disagree." The total did NOT disagree.** That count folded in a 21st episode which is an `observation` (Diogenes' own self-correction), not a `finding` — the roll-up correctly counts findings only. A correction that overstates the defect is the same class as the defect.

---

## ✅ STATUS AS OF 2026-08-03 — THREE OF FOUR FIXED. ONE IS OPEN AND OWNED BY THE NEXT SESSION.

Recording state, not triaging. Commits `ab39a94` (the HIGH) and `1e44e9c` (the cement + two LOWs).

**#1 HIGH — doctor RED for pack-layered installs. ✅ FIXED.** `_check_hook_freshness` compared
against the BASE tree while `init` renders from the LAYERED stack, so a pack-owned hook was
installed correctly and then reported stale against a tree it never came from. Pack-owned hooks now
resolve against `.levain/manifest.json`, using the manifest's own `_sha256_file` rather than a
reimplementation. **Verified END TO END on a real `levain init --pack` install:** doctor exits 0
with the fix, exits 1 without, and `init --force` — the remedy doctor itself prints — still exits 1.
Plus `_pack_layered_install`, the fixture shape that had never existed.

**#2 LOW — the "LAYOUT check owns it" misattribution. ✅ FIXED**, in the comment *and* in the test
docstring that repeated it. `_check_install_layout` never inspects CLAUDE.md/AGENTS.md at all;
`run_doctor`'s adapter-detection block owns it. Verified by reproduction.

**#3 LOW — `packs.py` "~57%" vs 55%. ✅ FIXED, and the caveat was the real finding.** Corrected to
55% **with the basis named**: `check_seed_budget.py` reads SOURCE templates (55%), `doctor` reports
a RENDERED install (61% on thin answers). Legitimately different numbers for legitimately different
objects — the four figures could only coexist because none stated what it measured.
`MECHANISM_SHARE_WARN` does not move.

### 🔴 #4 LOW (latent, NOT live) — hook freshness silently skips a MISSING hook. STILL OPEN.

**This is the one for the next dedicated session.** A hook the package ships but the install lacks
is `continue`d past — precisely the upgrade-drift shape the check exists to catch, since
`update.py` never re-installs hooks. **Not live today, verified:** both shipped trees contain
exactly three `.py` hooks and `_HOOK_REQUIRED` covers all three, so the layout check catches any
absence first. **It opens the moment a fourth hook ships**, because `_HOOK_REQUIRED` is HAND-LISTED.

⚠ **The fix pattern is already in this same file, twelve hundred lines up** — `doctor.py:554`:
*"DERIVED from `_SEED_REQUIRED`, never hand-listed: the hand-written form read
`{origin,partnership,world,memory}.md` and silently went stale the moment the required set
changed."* Either report a shipped-but-absent hook as stale, or derive `_HOOK_REQUIRED` from the
shipped tree so the layout check cannot go stale. **Same class as the 08-03 cement** (derive the
coverage from the code's own declarations), so it belongs *with* that work rather than against it.

### ⚖ RELEASE DECISION — NO 0.4.2 FOR ANY OF THIS. Hold on main.

Decided 2026-08-03. Three reasons, in order of weight:

1. **Alex has still not installed 0.4.0 or 0.4.1.** He is the only known external operator, so a
   third version he also has not installed changes nothing for the one person it is for.
2. **The HIGH was latent — zero field impact.** It needs a pack that overrides a hook, and no pack
   anywhere does.
3. **Cutting a release immediately on a `doctor.py` fix is the pattern that produced this bug
   TWICE.** 0.4.0 was cut fast to carry Alex's `install_root` fix and shipped the codex defect
   inside it; 0.4.1 was cut fast to fix that and shipped the pack defect inside it. A third fast cut
   on the same file is the same move a third time. *The Levain clock is RETIRED — done RIGHT, not
   fast.*

**FALSIFIABLE TRIGGER — cut 0.4.2 if either fires:** an operator authors a pack that overrides a
hook, or an independent reason to release arrives (at which point all of this rides along free).
**Holding is safe precisely BECAUSE of the cement** — the derived per-adapter coverage means the
next release, whenever it comes, cannot reintroduce this class silently.

---

## 1. `diogenes-20260803-042435-d0820c0e3303`

*2026-08-03T04:24:35.176538-04:00 · finding*

HIGH: levain/doctor.py:1055 — 0.4.1's hook-freshness fix leaves doctor PERMANENTLY RED (exit 1) for any install built with a pack that overrides a hook, and doctor's own printed remedy does not clear it. This is the SAME failure shape as the 0.4.0 codex bug this release exists to fix, for a different population, shipped inside the fix. doctor compares installed hooks against the BASE tree only: [quoted: `shipped_root = _base_activation_root(adapter, templates_root) / "hooks"`]. But init renders the tree from the LAYERED stack including packs — install.py:387-389 [quoted: `order_activation_roots(templates_root, _base_activation_root(chosen, templates_root), pack_dirs)`] — and pack layers WIN per relative path, documented at install.py:1620-1621 [quoted: `a pack's ``hooks/x.py`` replaces base's ``hooks/x.py```]. So a pack-owned hook is installed correctly and then reported stale against base. The docstring's central claim is therefore false: it asserts doctor 'CANNOT disagree with install about lineage, because there is no second copy of the rule left to drift' — install's rule is order_activation_roots(base, pack_dirs), doctor's is base alone; doctor took half the helper chain. REPRODUCED end-to-end in /tmp (levain 0.4.1, .venv): a pack shipping activation/hooks/session_start.py, installed via 'levain init --adapter claude-code --pack ./mypack' → 'levain doctor' EXIT 1, '[FAIL] hook freshness: installed hook script(s) differ from the package: session_start.py'. Applied doctor's own printed remedy ('levain init --force') → still EXIT 1, identical FAIL. Same-answers control WITHOUT the pack → EXIT 0, '[OK] hook freshness: hook scripts match the package'. Latency, stated honestly: NO pack shipped in this repo overrides hooks (only levain/templates/pack.toml exists), so field impact today is zero — it bites the first operator who uses a supported feature the code explicitly documents. Severity is HIGH not MEDIUM because the false confidence is the load-bearing part, by this release's own argument about _base_seed_root ('the previous docstring's confidence is what kept anyone from looking'). Scope checked, not assumed: hook freshness is the ONLY doctor check that compares installed bytes against the shipped package (_check_seed_content checks placeholders, _check_recorded_answers compares against answers.json), so this is not a systemic pack-blindness — it is this one check. THE FIX: the data is ALREADY on disk. .levain/manifest.json records each pack's contributed files with SHA256 — verified in the repro: recorded 'activation/hooks/session_start.py' hash e60b47dd... MATCHES the installed file byte-for-byte. So _check_hook_freshness should read the manifest and, for any hook path a pack contributed, compare against the recorded pack hash rather than base (or skip it). Add a pack-layered fixture to test_doctor_content.py — the 0.4.1 tests parameterize by ADAPTER but no test constructs a pack-layered install, which is the same 'the branch was never executed by the suite' root cause named twice already in this repo. WHAT WAS EXECUTED: the reproduction, the remedy retry, the no-pack control, and the manifest-hash verification. The fix itself was NOT implemented or run. [prescription: run]

---

## 2. `diogenes-20260803-042446-701b7d51eab3`

*2026-08-03T04:24:46.309815-04:00 · finding*

LOW: levain/doctor.py:1046 — the new early-return's comment defers to a check that does not own the case, which is the exact defect class ('a comment naming a world it is not in') this release shipped to fix. The comment reads [quoted: `# A hooks/ dir with no coherent adapter identity is an INCOHERENT install, and the`] / [quoted: `# LAYOUT check owns that. Comparing it against an arbitrarily chosen tree is exactly`], and the commit message repeats it ('left to the LAYOUT check, which owns it'). But _check_install_layout (doctor.py:529) never inspects CLAUDE.md or AGENTS.md — it checks install root, seed/, _ACTIVATION_FILES, the hooks dir, _HOOK_REQUIRED presence, and hook syntax. The case is actually owned by run_doctor's adapter-detection block at doctor.py:103-112 [quoted: `\"no adapter detected (no CLAUDE.md or AGENTS.md at install root)\"`]. REPRODUCED in /tmp: copied a healthy install, deleted CLAUDE.md, ran doctor → EXIT 1, but the LAYOUT check printed '[OK] activation/: 2 activation files present' and '[OK] hook scripts: 3 scripts present and parse OK', while the FAIL came from '[FAIL] adapter: no adapter detected'. So the operator IS protected and there is no behavioural bug — the defect is that the comment sends the next reader to the wrong function, and the test at test_doctor_content.py asserting this path repeats the same misattribution in its docstring ('belongs to the LAYOUT check'). Fix: name run_doctor's adapter-detection block instead of 'the LAYOUT check', in both the comment and the test docstring. WHAT WAS EXECUTED: the incoherent-install reproduction confirming which check fires. The one-line comment correction was NOT applied. [prescription: run]

---

## 3. `diogenes-20260803-042459-83cd7bde3ba8`

*2026-08-03T04:24:59.449165-04:00 · finding*

LOW: levain/packs.py:514-515 — the four-numbers-for-one-quantity fix was applied to THREE of the four sites; the packs.py prose copy survives, so the commit's claim that 'there is now exactly one authority and it is measure()' is not true on disk. The surviving line reads [quoted: `# report; after F2's first cut the same install is 54%; the base install this ships`] / [quoted: `# with is ~57%. 50% is the first round number below all three — deliberately still`]. Running the claimed single authority: '.venv/bin/python scripts/check_seed_budget.py' prints 'check_seed_budget: clean — 38,759 B eager, 55% mechanism (limits 40,000 B / 62%; target 50% once memory.md moves)'. My own 2026-08-02 finding, routed into this repo at project_memory/diogenes_20260802.md:56, explicitly enumerated all four sites including 'packs.py:514 says ~57%' — so this is a fix that consumed its finding's headline and left an enumerated instance alive. HONEST CAVEAT, and it is why the prescription is not mechanical: ~57% and 55% may not be the same quantity. measure() reads the SOURCE templates; doctor reports a RENDERED install (my repro printed '61% mechanism' for an install with mostly-blank answers, because thin identity text raises the mechanism share). packs.py does not state which basis '~57%' uses, and that unstated basis is precisely what let four numbers coexist without anyone noticing they disagreed. The comment's ARGUMENT still holds either way — 50% remains below 54%, so MECHANISM_SHARE_WARN = 0.50 is unaffected and there is no behavioural bug. Fix: either delete the number and point at measure() as was done in check_seed_budget.py, or state the basis explicitly ('source composition' vs 'a rendered install') so the two figures are legibly different quantities rather than a contradiction. Which one is correct depends on authorial intent I cannot read off disk. WHAT WAS EXECUTED: check_seed_budget.py was run and its output quoted above; the prescription was not applied. [prescription: reasoned]

---

## 4. `diogenes-20260803-042513-c93660f2bd27`

*2026-08-03T04:25:13.935575-04:00 · finding*

LOW (latent, NOT live today): levain/doctor.py:1060-1061 — hook freshness silently skips a hook the package ships but the install lacks, which is the one drift shape its own docstring says it exists to catch. The skip is [quoted: `                installed = hooks_dir / shipped.name`] / [quoted: `                if not installed.is_file():`] / [quoted: `                    continue`]. The docstring's premise is that levain 'has NO upgrade path that refreshes them: update.py never re-installs... So pip install -U levain upgrades the package and leaves the operator running the old copies' — and an operator whose install predates a NEW hook file has exactly that: a missing file, silently continued past. WHY IT IS NOT LIVE, verified on disk: both shipped trees contain exactly three .py files (levain/templates/activation/hooks and levain/templates/adapters/codex/activation/hooks each hold _levain_hook.py, session_start.py, user_prompt_submit.py), and _check_install_layout enforces presence of exactly that set via doctor.py:179 [quoted: `_HOOK_REQUIRED = (\"session_start.py\", \"user_prompt_submit.py\", \"_levain_hook.py\")`]. So today every shippable hook is covered by the layout check and nothing escapes. The gap opens the moment a fourth hook ships, because _HOOK_REQUIRED is HAND-LISTED and would have to be remembered. That failure mode is already known IN THIS FILE and already fixed once, twelve hundred lines up — doctor.py:554-556 [quoted: `# DERIVED from _SEED_REQUIRED, never hand-listed: the hand-written`] / [quoted: `# form read \"{origin,partnership,world,memory}.md\" and silently went`] / [quoted: `# stale the moment the required set changed.`]. Fix: report a shipped-but-absent hook as stale here rather than 'continue' (the remedy text, re-render with init --force, is already the right one), or derive _HOOK_REQUIRED from the shipped tree so the layout check cannot go stale. Filed as latent rather than live on purpose — no operator can hit this today. [prescription: reasoned]

---
