# Python build artifacts
*.egg-info/
__pycache__/
*.pyc
*.pyo
build/
dist/
.eggs/

# Editable install metadata
*.egg-link

# venv
.venv/
venv/

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
.worktrees/

# The lane identity slot — per-clone, never shared (FLOW_DEV_PROTOCOL: it MUST be a file,
# not an env var, because an agent every Bash call is a fresh shell).
.levain-lane
